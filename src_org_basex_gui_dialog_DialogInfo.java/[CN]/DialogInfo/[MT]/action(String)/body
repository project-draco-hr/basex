{
  if (BUTTONOPT.equals(cmd)) {
    opt=true;
    close();
  }
  if (!edit)   return;
  final boolean ftx=indexes[2].isSelected();
  for (int f=0; f < ft.length; f++) {
    ft[f].setEnabled(ftx);
    fl[f].setEnabled(ftx);
  }
}
