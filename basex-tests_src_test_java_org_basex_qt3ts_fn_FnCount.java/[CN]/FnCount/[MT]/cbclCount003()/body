{
  final XQuery query=new XQuery("let $x := year-from-date(current-date()) return count( 1 to $x ) = $x",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
