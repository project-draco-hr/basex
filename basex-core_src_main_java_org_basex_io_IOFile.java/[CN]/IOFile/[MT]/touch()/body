{
  for (int i=0; i < 5; i++) {
    try {
      if (file.createNewFile())       return true;
    }
 catch (    final IOException ex) {
      Performance.sleep(i * 10L);
      Util.debug(ex);
    }
  }
  return false;
}
