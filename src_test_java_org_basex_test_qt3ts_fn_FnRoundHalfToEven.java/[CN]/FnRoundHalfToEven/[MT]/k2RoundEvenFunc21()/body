{
  final XQuery query=new XQuery("round-half-to-even(xs:positiveInteger(\"1\")) instance of xs:positiveInteger",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
