{
  final XQuery query=new XQuery("round-half-to-even(xs:negativeInteger(\"-1\")) instance of xs:negativeInteger",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
