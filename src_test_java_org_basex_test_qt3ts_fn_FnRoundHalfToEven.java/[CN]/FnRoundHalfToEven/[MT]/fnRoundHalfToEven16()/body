{
  final XQuery query=new XQuery("round-half-to-even(xs:float('3.75'), 1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("3.8") && assertType("xs:float")));
}
