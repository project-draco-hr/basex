{
  final XQuery query=new XQuery("round-half-to-even(xs:short(\"0\")) instance of xs:short",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
