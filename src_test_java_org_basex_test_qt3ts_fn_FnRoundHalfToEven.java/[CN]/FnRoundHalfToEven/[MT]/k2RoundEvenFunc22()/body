{
  final XQuery query=new XQuery("round-half-to-even(xs:nonPositiveInteger(\"0\")) instance of xs:nonPositiveInteger",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
