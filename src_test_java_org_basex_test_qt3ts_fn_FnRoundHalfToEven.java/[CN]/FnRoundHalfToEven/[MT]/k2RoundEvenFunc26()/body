{
  final XQuery query=new XQuery("round-half-to-even(xs:int(\"0\")) instance of xs:int",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
