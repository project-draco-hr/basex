{
  final XQuery query=new XQuery("round-half-to-even(xs:byte(\"0\")) instance of xs:byte",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
