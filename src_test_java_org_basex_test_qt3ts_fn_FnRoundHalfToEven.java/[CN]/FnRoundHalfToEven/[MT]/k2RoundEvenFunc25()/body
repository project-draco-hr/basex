{
  final XQuery query=new XQuery("round-half-to-even(xs:long(\"0\")) instance of xs:long",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
