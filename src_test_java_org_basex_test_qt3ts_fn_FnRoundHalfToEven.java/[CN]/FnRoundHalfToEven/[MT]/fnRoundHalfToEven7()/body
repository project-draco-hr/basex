{
  final XQuery query=new XQuery("round-half-to-even(4561.000005e0, -2)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("4600") && assertType("xs:double")));
}
