{
  final XQuery query=new XQuery("round-half-to-even(xs:unsignedShort(\"0\")) instance of xs:unsignedShort",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
