{
  final XQuery query=new XQuery("round-half-to-even(xs:unsignedLong(\"0\")) instance of xs:unsignedLong",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
