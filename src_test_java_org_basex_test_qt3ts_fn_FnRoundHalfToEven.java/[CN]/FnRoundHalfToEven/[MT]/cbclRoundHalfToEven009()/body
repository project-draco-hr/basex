{
  final XQuery query=new XQuery("fn:round-half-to-even(xs:float(12345.6), -2)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"12300"));
}
