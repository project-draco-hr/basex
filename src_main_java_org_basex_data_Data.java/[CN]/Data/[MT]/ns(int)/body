{
  final Atts as=new Atts();
  if (nsFlag(pre)) {
    final int[] nsp=nspaces.get(pre);
    for (int n=0; n < nsp.length; n+=2)     as.add(nspaces.prefix(nsp[n]),nspaces.uri(nsp[n + 1]));
  }
  return as;
}
