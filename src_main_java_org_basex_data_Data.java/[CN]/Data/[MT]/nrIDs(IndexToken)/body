{
switch (token.type()) {
case TEXT:
    return txtindex.nrIDs(token);
case ATTRIBUTE:
  return atvindex.nrIDs(token);
case FULLTEXT:
return ftxindex.nrIDs(token);
default :
return Integer.MAX_VALUE;
}
}
