{
  final TokenBuilder tb=new TokenBuilder();
  final InputStream bis=i instanceof BufferedInputStream || i instanceof BufferInput ? i : new BufferedInputStream(i);
  int b;
  while ((b=bis.read()) != -1)   tb.add((byte)b);
  bis.close();
  cont=tb.finish();
  return cont;
}
