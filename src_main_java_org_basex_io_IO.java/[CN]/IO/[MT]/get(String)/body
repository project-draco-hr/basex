{
  if (source == null)   return new IOContent("");
  final String s=source.trim();
  return s.indexOf('<') == 0 ? new IOContent(s) : IOUrl.isFileURL(s) ? IOFile.get(s) : IOFile.isValid(s) ? new IOFile(s) : IOUrl.isValid(s) ? new IOUrl(s) : new IOContent(s);
}
