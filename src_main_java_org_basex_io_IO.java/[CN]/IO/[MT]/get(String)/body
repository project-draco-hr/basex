{
  if (source == null)   return new IOContent(Token.EMPTY);
  final String s=source.trim();
  return s.indexOf('<') == 0 ? new IOContent(s) : s.startsWith(FILEPREF) ? new IOFile(IOUrl.file(s)) : IOFile.isValid(s) ? new IOFile(s) : IOUrl.isValid(s) ? new IOUrl(s) : new IOContent(s);
}
