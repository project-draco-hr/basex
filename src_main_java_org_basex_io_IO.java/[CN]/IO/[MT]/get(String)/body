{
  if (s == null)   return new IOFile("");
  if (s.startsWith("<"))   return new IOContent(Token.token(s));
  if (!s.contains(":") || s.startsWith("file:") || s.length() > 2 && s.charAt(1) == ':')   return new IOFile(s);
  return new IOUrl(s);
}
