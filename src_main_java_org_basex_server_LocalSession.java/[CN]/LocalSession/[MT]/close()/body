{
  try {
    final ListIterator<LocalQuery> i=queries.listIterator();
    while (i.hasNext()) {
      i.next().closeListener();
      i.remove();
    }
    execute(new Exit());
  }
 catch (  final IOException ex) {
    Util.debug(ex);
  }
}
