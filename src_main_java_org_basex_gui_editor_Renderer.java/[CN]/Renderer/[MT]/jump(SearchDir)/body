{
  if (!text.jump(dir))   return -1;
  final int hh=h;
  h=Integer.MAX_VALUE;
  final Graphics g=getGraphics();
  for (init(g); more(g) && text.pos() < text.cursor(); next())   ;
  h=hh;
  return y;
}
