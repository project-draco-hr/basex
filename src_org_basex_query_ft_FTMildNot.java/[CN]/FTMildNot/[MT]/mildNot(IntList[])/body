{
  boolean f=true;
  for (int i=1; i < pos.length; i++) {
    for (int j=0; j < pos[i].size; j++) {
      if (pos[0].contains(pos[i].list[j]))       f&=pos[0].list[pos[0].size - 1] > pos[i].list[j];
    }
  }
  return f;
}
