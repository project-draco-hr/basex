{
  final Item it=expr[0].iter(ctx).next();
  if (it.score() == 0)   return score(0);
  boolean f=false;
  for (int i=1; i < expr.length; i++)   f|=expr[1].iter(ctx).next().score() != 0;
  return score(!f || ctx.ftselect.mildNot() ? it.score() : 0);
}
