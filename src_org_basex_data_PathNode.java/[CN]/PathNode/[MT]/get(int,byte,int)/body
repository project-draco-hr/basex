{
  for (  final PathNode c : ch) {
    if (c.kind == k && c.name == t) {
      c.count++;
      c.tl+=l;
      return c;
    }
  }
  final PathNode n=new PathNode(t,k,l,this);
  ch=Array.add(ch,n);
  return n;
}
