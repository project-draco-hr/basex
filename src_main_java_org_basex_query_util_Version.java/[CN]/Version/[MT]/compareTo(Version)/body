{
  if (major == v.major) {
    if (v.minor != -1) {
      if (v.patch != -1) {
        if (minor == v.minor)         return patch > v.patch ? 1 : patch < v.patch ? -1 : 0;
        return minor > v.minor ? 1 : -1;
      }
      return minor > v.minor ? 1 : minor < v.minor ? -1 : 0;
    }
    return minor == -1 ? 0 : 1;
  }
  return major > v.major ? 1 : -1;
}
