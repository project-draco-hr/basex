{
  try {
    return new ClientSession("localhost",Integer.parseInt(System.getProperty("org.jaxrx.serverport")),System.getProperty("org.jaxrx.user"),System.getProperty("org.jaxrx.password"));
  }
 catch (  final IOException ex) {
    error(ex);
    return null;
  }
}
