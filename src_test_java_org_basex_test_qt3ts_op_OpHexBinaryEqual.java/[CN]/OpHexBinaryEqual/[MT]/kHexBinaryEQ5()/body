{
  final XQuery query=new XQuery("xs:hexBinary(xs:base64Binary(xs:hexBinary(\"03\"))) ne xs:hexBinary(\"13\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
