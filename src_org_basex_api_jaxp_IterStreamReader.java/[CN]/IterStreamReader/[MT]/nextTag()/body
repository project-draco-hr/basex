{
  next();
  while ((type == CHARACTERS && isWhiteSpace()) || (type == CDATA && isWhiteSpace()) || type == SPACE || type == PROCESSING_INSTRUCTION || type == COMMENT) {
    next();
  }
  checkType(START_ELEMENT,END_ELEMENT);
  return type;
}
