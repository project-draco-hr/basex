{
  next();
  while ((kind == CHARACTERS && isWhiteSpace()) || (kind == CDATA && isWhiteSpace()) || kind == SPACE || kind == PROCESSING_INSTRUCTION || kind == COMMENT) {
    next();
  }
  checkType(START_ELEMENT,END_ELEMENT);
  return kind;
}
