{
  getAttributes();
  for (int a=0; a < atts.size; a++) {
    if (!s1.equals(getAttributeLocalName(a)))     continue;
    if (s == null || s.equals(getAttributeNamespace(a)))     return getAttributeValue(a);
  }
  return null;
}
