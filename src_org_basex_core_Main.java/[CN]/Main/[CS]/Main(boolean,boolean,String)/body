{
  context.server=sv;
  if (sa) {
    launcher=new Launcher(context);
  }
 else {
    try {
      launcher=new ClientLauncher(context);
    }
 catch (    final Exception ex) {
      Main.error(ex,true);
      ok=false;
      return;
    }
  }
  parseArguments(args);
}
