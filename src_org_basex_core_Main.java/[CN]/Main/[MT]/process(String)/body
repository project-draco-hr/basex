{
  try {
    for (    final Process p : new CommandParser(in,context,false,context.server).parse()) {
      if (p instanceof Exit)       return false;
      if (!process(p,true))       break;
    }
  }
 catch (  final QueryException ex) {
    error(ex,ex.getMessage());
  }
  return true;
}
