{
  if (msg) {
    debug(ex);
    if (ex instanceof BindException) {
      errln(SERVERBIND);
    }
 else     if (ex instanceof IOException) {
      errln(SERVERERR);
    }
 else {
      errln(ex.getMessage());
    }
  }
 else {
    ex.printStackTrace();
  }
}
