{
  set(Prop.INFO,ON);
  while (console) {
    Main.out("> ");
    final String in=System.console().readLine();
    if (in == null)     break;
    if (!process(in))     return true;
  }
  return false;
}
