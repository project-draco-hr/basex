{
  final XQuery query=new XQuery("string-to-codepoints('b??????????')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("98, 223, 1682, 12365, 63744"));
}
