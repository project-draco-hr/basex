{
  values=v.nextValues();
  if (values.length != 0) {
    key=di.readBytes();
  }
 else {
    v.close();
    di.close();
    DropDB.drop(data.meta.name,pref + '.' + IO.BASEXSUFFIX,data.meta.prop);
  }
}
