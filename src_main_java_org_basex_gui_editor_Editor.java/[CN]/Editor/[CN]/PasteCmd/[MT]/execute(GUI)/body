{
  if (!hist.active())   return;
  final int tc=text.getCaret();
  text.pos(tc);
  final String txt=clip();
  if (txt == null)   return;
  if (text.selected())   text.delete();
  text.add(txt);
  finish(tc);
}
