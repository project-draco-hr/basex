{
  try {
    rend.search(sc);
    gui.status.setText(sc.search.isEmpty() ? OK : Util.info(Util.info(STRINGS_FOUND_X,sc.nr())));
    jump(SearchDir.SAME);
  }
 catch (  final Exception ex) {
    final String msg=ex.getMessage().replaceAll(Prop.NL + ".*","");
    gui.status.setError(REGULAR_EXPR + COLS + msg);
  }
}
