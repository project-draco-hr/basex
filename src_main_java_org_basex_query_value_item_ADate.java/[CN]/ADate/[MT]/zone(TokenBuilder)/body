{
  if (zon == Short.MAX_VALUE)   return;
  if (zon == 0) {
    tb.add('Z');
  }
 else {
    tb.add(zon > 0 ? '+' : '-');
    prefix(tb,Math.abs(zon) / 60,2);
    tb.add(':');
    prefix(tb,Math.abs(zon) % 60,2);
  }
}
