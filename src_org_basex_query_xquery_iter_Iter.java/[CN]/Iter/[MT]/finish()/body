{
  Item[] item=new Item[1];
  Item i;
  int s=0;
  while ((i=next()) != null) {
    if (s == item.length) {
      final Item[] tmp=new Item[s << 2];
      System.arraycopy(item,0,tmp,0,s);
      item=tmp;
    }
    item[s++]=i;
  }
  return Seq.get(item,s);
}
