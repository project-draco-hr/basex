{
  new Thread(){
    @Override public void run(){
      if (!checkRes(new XQuery(QUERYN),s).equals("192000"))       Main.errln("efficiency test failed");
      closeSession(s);
      tdone++;
    }
  }
.start();
}
