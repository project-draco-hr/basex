{
  final XQuery query=new XQuery("fn:boolean(fn:string(xs:dayTimeDuration(\"P05DT09H08M\") div 2.0))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
