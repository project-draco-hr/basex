{
  if (ret == null) {
    Type t=items[0].type;
    for (int s=1; s < size; s++) {
      if (t != items[s].type) {
        t=AtomType.ITEM;
        break;
      }
    }
    ret=t;
    type=t;
  }
  return SeqType.get(ret,Occ.ONE_MORE);
}
