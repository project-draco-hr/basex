{
  final boolean ok=super.exec();
  if (ok && !(result instanceof Nodes))   return error("Result must be a NodeSet.");
  if (result.size() != 0)   context.current((Nodes)result);
  if (!Prop.allInfo)   error("");
  return ok;
}
