{
  final TokenBuilder tb=new TokenBuilder("Possible bug? Please send your feedback to " + MAIL);
  if (ext.length != 0) {
    tb.add(NL + NAME + ' '+ VERSION+ COLS+ NL);
    for (    final Object e : ext)     tb.add(e + NL);
  }
  return tb.toString();
}
