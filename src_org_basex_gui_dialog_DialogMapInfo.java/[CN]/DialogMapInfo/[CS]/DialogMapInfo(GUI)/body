{
  super(main,MAPINFOTITLE,false);
  final BaseXBack p=new BaseXBack();
  p.setLayout(new TableLayout(6,1,0,5));
  info=new BaseXCheckBox(MAPINFO,HELPMAPINFO,GUIProp.mapinfo,0,this);
  p.add(info);
  finish(GUIProp.mapinfoloc);
  action(null);
}
