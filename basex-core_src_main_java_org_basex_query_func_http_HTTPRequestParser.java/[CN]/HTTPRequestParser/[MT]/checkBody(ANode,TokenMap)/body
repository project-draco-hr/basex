{
  if (bodyAttrs.get(MEDIA_TYPE) == null)   throw HC_REQ_X.get(info,"Attribute media-type of http:body is mandatory");
  if (bodyAttrs.get(SRC) != null && (bodyAttrs.size() > 2 || body.children().more()))   throw HC_ATTR.get(info);
}
