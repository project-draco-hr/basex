{
  final XQuery query=new XQuery("(hours-from-duration( xs:dayTimeDuration('PT1H90M')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("2"));
}
