{
  final XQuery query=new XQuery("string-join(tokenize( 'a    b c', '\\s'), '|')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"a||||b|c"));
}
