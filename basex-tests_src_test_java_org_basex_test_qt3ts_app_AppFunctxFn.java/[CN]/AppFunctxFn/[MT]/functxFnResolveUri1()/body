{
  final XQuery query=new XQuery("(resolve-uri('prod', 'http://datypic.com/'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"http://datypic.com/prod"));
}
