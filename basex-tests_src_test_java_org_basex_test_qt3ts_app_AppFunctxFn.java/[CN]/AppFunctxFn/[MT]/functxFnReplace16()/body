{
  final XQuery query=new XQuery("(replace('abc123', '([a-z])', '$1x'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"axbxcx123"));
}
