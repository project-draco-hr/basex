{
  final XQuery query=new XQuery("(years-from-duration( xs:yearMonthDuration('P3Y')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("3"));
}
