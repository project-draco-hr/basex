{
  final XQuery query=new XQuery("(substring-before('queryquery', 'ery'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"qu"));
}
