{
  final XQuery query=new XQuery("(months-from-duration( xs:yearMonthDuration('P1Y')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("0"));
}
