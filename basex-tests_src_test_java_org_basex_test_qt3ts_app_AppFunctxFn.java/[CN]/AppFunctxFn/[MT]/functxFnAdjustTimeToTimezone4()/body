{
  final XQuery query=new XQuery("(adjust-time-to-timezone( xs:time('17:00:00-03:00'), ()))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"17:00:00"));
}
