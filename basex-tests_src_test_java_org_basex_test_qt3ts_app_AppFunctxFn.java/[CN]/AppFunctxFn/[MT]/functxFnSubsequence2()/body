{
  final XQuery query=new XQuery("(subsequence( ('a', 'b', 'c', 'd', 'e'), 3, 2))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"c d"));
}
