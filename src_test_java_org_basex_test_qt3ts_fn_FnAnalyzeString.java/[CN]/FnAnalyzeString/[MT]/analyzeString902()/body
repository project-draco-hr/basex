{
  final XQuery query=new XQuery("analyze-string(\"abc\", \"abc\", \"w\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORX0001"));
}
