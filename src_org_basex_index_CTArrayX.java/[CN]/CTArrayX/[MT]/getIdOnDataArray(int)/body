{
  byte[] bId;
  bId=new byte[nodes[currentNode].length - nodes[currentNode][0] - 2];
  System.arraycopy(nodes[currentNode],nodes[currentNode].length - bId.length,bId,0,bId.length);
  return Array.byteToIntNN(bId);
}
