{
  int[][] maxResult=null;
  int[][] tmpResult;
  int k=1;
  while (k < data.length) {
    tmpResult=ftUnaryNot(getDataFromDataArray(k),resultFTContent);
    maxResult=ftOR(maxResult,tmpResult);
    k++;
  }
  return maxResult;
}
