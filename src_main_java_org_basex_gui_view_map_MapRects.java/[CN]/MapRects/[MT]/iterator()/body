{
  return new Iterator<MapRect>(){
    private int c=-1;
    @Override public boolean hasNext(){
      return ++c < size;
    }
    @Override public MapRect next(){
      return list[c];
    }
    @Override public void remove(){
      Main.notexpected();
    }
  }
;
}
