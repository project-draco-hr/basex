{
  final GregorianCalendar gc=new GregorianCalendar();
  gc.setTime(date);
  final XMLGregorianCalendar xgc=factory == null ? null : factory.newXMLGregorianCalendar(gc);
  return token(xgc.toXMLFormat());
}
