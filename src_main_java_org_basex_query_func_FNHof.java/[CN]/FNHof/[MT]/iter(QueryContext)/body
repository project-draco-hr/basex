{
switch (def) {
case SORTWITH:
    return sortWith(ctx);
case HOFID:
case CONST:
  return expr[0].iter(ctx);
case UNTIL:
return until(ctx).iter();
case ITERATE:
return iterate(ctx);
default :
return super.iter(ctx);
}
}
