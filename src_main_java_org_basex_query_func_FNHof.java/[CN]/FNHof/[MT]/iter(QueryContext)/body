{
switch (def) {
case HOFSORTWITH:
    return sortWith(ctx);
case HOFID:
case HOFCONST:
  return expr[0].iter(ctx);
case HOFUNTIL:
return until(ctx).iter();
case HOFITERATE:
return iterate(ctx);
default :
return super.iter(ctx);
}
}
