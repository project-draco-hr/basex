{
  final XQuery query=new XQuery("declare function xsi:foo() { 1 }; xsi:foo()",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0045"));
}
