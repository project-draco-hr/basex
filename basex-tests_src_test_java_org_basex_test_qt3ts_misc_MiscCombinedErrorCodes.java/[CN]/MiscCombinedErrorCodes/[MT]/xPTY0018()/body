{
  final XQuery query=new XQuery("(<a/>, <b/>)/(if (position() mod 2 = 1) then position() else .)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0018"));
}
