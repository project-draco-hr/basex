{
  final XQuery query=new XQuery("for $x in (1, 'hello', xs:date('2007-11-28')) order by $x return $x",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
