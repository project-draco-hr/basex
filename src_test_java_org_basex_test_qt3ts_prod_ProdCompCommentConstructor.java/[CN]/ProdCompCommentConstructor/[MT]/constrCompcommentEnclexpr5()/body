{
  final XQuery query=new XQuery("comment {/root}",ctx);
  query.context(node(file("prod/CompAttrConstructor/DupNode.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<!--texttext-->",false));
}
