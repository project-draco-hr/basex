{
  final XQuery query=new XQuery("comment {'com','-','-','ment'}",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<!--com - - ment-->",false));
}
