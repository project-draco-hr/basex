{
  final XQuery query=new XQuery("comment {comment {'one', comment {'two'}}, 'three', comment {'four'}}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<!--one two three four-->",false));
}
