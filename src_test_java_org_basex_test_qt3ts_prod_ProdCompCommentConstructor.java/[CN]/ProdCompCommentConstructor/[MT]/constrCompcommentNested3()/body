{
  final XQuery query=new XQuery("document {comment {'one'}, <a/>, comment {'two'}, <b/>, comment {'three'}}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<!--one--><a/><!--two--><b/><!--three-->",false));
}
