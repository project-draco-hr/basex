{
  if (!(v instanceof ItemCache))   return false;
  final ItemCache ic=(ItemCache)v;
  if (size != ic.size)   return false;
  for (int i=0; i < size; ++i) {
    if (item[i].type != ic.item[i].type || !item[i].sameAs(ic.item[i]))     return false;
  }
  return true;
}
