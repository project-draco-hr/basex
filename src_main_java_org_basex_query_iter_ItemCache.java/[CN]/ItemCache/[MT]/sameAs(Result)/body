{
  if (!(v instanceof ItemCache))   return false;
  final ItemCache sb=(ItemCache)v;
  if (size != sb.size)   return false;
  for (int i=0; i < size; ++i) {
    if (item[i].type != sb.item[i].type || !item[i].sameAs(sb.item[i]))     return false;
  }
  return true;
}
