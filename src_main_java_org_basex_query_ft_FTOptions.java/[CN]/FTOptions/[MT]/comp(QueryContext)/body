{
  final FTOpt tmp=ctx.ftopt;
  opt.init(tmp);
  if (opt.sw != null && ctx.resource.value instanceof DBNode)   opt.sw.comp(((DBNode)ctx.resource.value).data);
  ctx.ftopt=opt;
  expr[0]=expr[0].comp(ctx);
  tmp.mark=ctx.ftopt.mark;
  ctx.ftopt=tmp;
  return expr[0];
}
