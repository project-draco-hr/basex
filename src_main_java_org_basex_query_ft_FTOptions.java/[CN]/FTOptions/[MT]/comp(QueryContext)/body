{
  final FTOpt tmp=ctx.ftopt;
  ctx.ftopt=opt.init(tmp);
  if (opt.sw != null && ctx.value instanceof DBNode)   opt.sw.comp(((DBNode)ctx.value).data);
  expr[0]=expr[0].comp(ctx);
  ctx.ftopt=tmp;
  return expr[0];
}
