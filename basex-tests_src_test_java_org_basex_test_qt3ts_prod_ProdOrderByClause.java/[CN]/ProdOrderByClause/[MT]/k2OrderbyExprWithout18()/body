{
  final XQuery query=new XQuery("(for $i in current-time() order by $i return $i) eq current-time()",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
