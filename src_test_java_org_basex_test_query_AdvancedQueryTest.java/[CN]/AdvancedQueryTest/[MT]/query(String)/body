{
  final QueryProcessor qp=new QueryProcessor(qu,CTX);
  try {
    return qp.execute().toString().replaceAll("(\\r|\\n) *","");
  }
  finally {
    try {
      qp.close();
    }
 catch (    IOException e) {
    }
  }
}
