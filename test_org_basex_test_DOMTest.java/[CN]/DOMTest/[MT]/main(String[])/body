{
  Prop.read();
  System.out.println("=== BaseX DOM ===========================");
  test(eval(new XPathImpl()));
  System.out.println("\n=== Java DOM ============================");
  test(eval(XPathFactory.newInstance().newXPath()));
}
