{
  for (int i=0; i < exprs.length - 1; i++)   if (exprs[i].ebv(ctx,info).bool(info))   return Bln.TRUE;
  final Expr last=exprs[exprs.length - 1];
  return tailCall ? last.item(ctx,ii) : last.ebv(ctx,ii).bool(ii) ? Bln.TRUE : Bln.FALSE;
}
