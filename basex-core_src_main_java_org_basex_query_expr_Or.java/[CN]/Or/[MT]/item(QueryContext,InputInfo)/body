{
  for (int i=0; i < expr.length - 1; i++)   if (expr[i].ebv(ctx,info).bool(info))   return Bln.TRUE;
  final Expr last=expr[expr.length - 1];
  return tailCall ? last.item(ctx,ii) : last.ebv(ctx,ii).bool(ii) ? Bln.TRUE : Bln.FALSE;
}
