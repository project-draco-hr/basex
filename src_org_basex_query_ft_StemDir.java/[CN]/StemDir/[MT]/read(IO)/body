{
  try {
    for (    final byte[] sl : split(fl.content(),'\n')) {
      byte[] val=null;
      for (      final byte[] st : split(norm(sl),' ')) {
        if (val == null)         val=st;
 else         put(st,val);
      }
      add(sl);
    }
    return true;
  }
 catch (  final IOException ex) {
    return false;
  }
}
