{
  final XQuery query=new XQuery("declare namespace test = 'http://www.example.com'; \n" + "        <a/> instance of element(*, test:unknownType)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
