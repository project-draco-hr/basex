{
  try {
    cs=new ClientSession(Text.LOCALHOST,Integer.parseInt(System.getProperty("org.basex.serverport")),System.getProperty("org.basex.user"),System.getProperty("org.basex.password"));
  }
 catch (  final Exception ex) {
    Util.stack(ex);
    throw new JaxRxException(ex);
  }
}
