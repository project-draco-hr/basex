{
  try {
    cs=new ClientSession("localhost",Integer.parseInt(System.getProperty("org.basex.serverport")),System.getProperty("org.basex.user"),System.getProperty("org.basex.password"));
  }
 catch (  final Exception ex) {
    ex.printStackTrace();
    throw new JaxRxException(ex);
  }
}
