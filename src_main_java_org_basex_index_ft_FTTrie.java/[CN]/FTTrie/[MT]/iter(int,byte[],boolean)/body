{
  if (token == null || token.length == 0)   return FTIndexIterator.FTEMPTY;
  final int[] node=node(token,id);
  return node == null ? FTIndexIterator.FTEMPTY : iter(currID,node[node.length - 1],inB,fast);
}
