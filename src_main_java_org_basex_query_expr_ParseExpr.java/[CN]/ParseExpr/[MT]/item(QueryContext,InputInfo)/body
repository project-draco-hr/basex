{
  final Iter ir=iter(ctx);
  final Item it=ir.next();
  if (it == null || ir.size() == 1)   return it;
  final Item n=ir.next();
  if (n != null)   XPSEQ.thrw(input,"(" + it + ", "+ n+ (ir.next() != null ? ", ..." : "")+ ")");
  return it;
}
