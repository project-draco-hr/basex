{
  Item it=null;
  if (type().zeroOrOne()) {
    it=item(ctx,input);
  }
 else {
    final Iter ir=iter(ctx);
    it=ir.next();
    if (it != null && !it.isNode() && ir.next() != null)     CONDTYPE.thrw(input,this);
  }
  return it == null ? Bln.FALSE : it;
}
