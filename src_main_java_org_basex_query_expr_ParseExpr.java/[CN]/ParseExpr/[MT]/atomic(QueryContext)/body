{
  final Iter ir=iter(ctx);
  final long s=ir.size();
  if (s == 1)   return ir.next();
  final Item it=ir.next();
  if (it == null)   return null;
  final Item n=ir.next();
  if (n != null)   Err.or(input,XPSEQ,"(" + it + ", "+ n+ (ir.next() != null ? ", ..." : "")+ ")");
  return it;
}
