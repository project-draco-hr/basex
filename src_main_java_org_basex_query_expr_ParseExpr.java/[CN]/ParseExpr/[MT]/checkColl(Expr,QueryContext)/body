{
  final byte[] u=checkStr(e,ctx);
  if (eq(URLCOLL,u))   return;
  final Uri uri=Uri.uri(u);
  if (uri.isAbsolute() || !eq(ctx.baseURI().resolve(uri).string(),QueryText.URLCOLL))   IMPLCOL.thrw(input,e);
}
