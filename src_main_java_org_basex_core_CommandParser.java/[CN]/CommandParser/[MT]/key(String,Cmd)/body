{
  consumeWS();
  final int p=qp;
  final boolean ok=(consume(key) || consume(key.toLowerCase(Locale.ENGLISH))) && (curr(0) || ws(curr()));
  if (!ok) {
    qp=p;
    if (cmd != null)     throw help(null,cmd);
  }
  return ok;
}
