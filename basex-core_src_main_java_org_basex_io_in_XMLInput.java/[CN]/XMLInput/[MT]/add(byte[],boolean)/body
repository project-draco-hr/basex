{
  if (spaces)   add(new NewlineInput(new ArrayInput(Token.SPACE)));
  add(new NewlineInput(new ArrayInput(value)));
  if (spaces)   add(new NewlineInput(new ArrayInput(Token.SPACE)));
  return ip < 32;
}
