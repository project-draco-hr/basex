{
  finishOpen();
  byte[] type=types.get(level);
  if (eq(type,ARR)) {
    print(']');
  }
 else   if (eq(type,OBJ)) {
    print('}');
  }
 else   if (eq(type,STR)) {
    print("\"\"");
  }
 else   if (eq(type,NULL)) {
    print(NULL);
  }
 else {
    error("Value expected for type \"%\"",type);
  }
  finishClose();
}
