{
  final String query=getText();
  if (cmdMode()) {
    cmdPopup(query);
  }
 else   if (GUIProp.searchmode == 1 || GUIProp.searchmode == 0 && query.startsWith("/")) {
    queryPopup(query);
  }
 else {
    pop.setVisible(false);
  }
}
