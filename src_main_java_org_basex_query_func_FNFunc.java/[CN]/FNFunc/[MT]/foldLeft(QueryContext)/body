{
  final FunItem f=withArity(0,2,ctx);
  final Iter xs=expr[2].iter(ctx);
  Iter res=expr[1].iter(ctx);
  for (Item x; (x=xs.next()) != null; )   res=f.invIter(ctx,input,res.finish(),x);
  return res;
}
