{
  final FunItem f=withArity(0,2,ctx);
  final Iter xs=expr[2].iter(ctx);
  ItemCache res=ItemCache.get(expr[1].iter(ctx));
  for (Item x; (x=xs.next()) != null; )   res=f.invIter(ctx,input,Seq.get(res.item,res.item.length),x);
  return res;
}
