{
switch (def) {
case MAP:
    return map(ctx);
case FILTER:
  return filter(ctx);
case MAPPAIRS:
return zip(ctx);
case FOLDLEFT:
return foldLeft(ctx);
case FOLDLEFT1:
return foldLeft1(ctx);
case FOLDRIGHT:
return foldRight(ctx);
case SORTWITH:
return sortWith(ctx);
case HOFID:
return expr[0].iter(ctx);
case CONST:
return expr[0].iter(ctx);
case UNTIL:
return until(ctx);
default :
return super.iter(ctx);
}
}
