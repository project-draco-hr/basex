{
switch (def) {
case MAP:
    return map(ctx);
case FILTER:
  return filter(ctx);
case MAPPAIRS:
return zip(ctx);
case FOLDLEFT:
return foldLeft(ctx);
case FOLDRIGHT:
return foldRight(ctx);
case SORTWITH:
return sortWith(ctx);
default :
return super.iter(ctx);
}
}
