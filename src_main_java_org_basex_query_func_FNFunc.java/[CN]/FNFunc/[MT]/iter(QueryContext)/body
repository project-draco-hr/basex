{
switch (sig) {
case MAP:
    return forEach(ctx);
case FOR_EACH:
  return forEach(ctx);
case FILTER:
return filter(ctx);
case MAP_PAIRS:
return forEachPair(ctx);
case FOR_EACH_PAIR:
return forEachPair(ctx);
case FOLD_LEFT:
return foldLeft(ctx);
case FOLD_RIGHT:
return foldRight(ctx);
default :
return super.iter(ctx);
}
}
