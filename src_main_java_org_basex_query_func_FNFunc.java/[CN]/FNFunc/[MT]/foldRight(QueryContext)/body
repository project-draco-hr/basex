{
  final FunItem f=withArity(0,2,ctx);
  Iter res=expr[1].iter(ctx);
  final Item[] xs=ItemCache.get(expr[2].iter(ctx)).item;
  for (int i=xs.length; i-- > 0; )   res=f.invIter(ctx,input,xs[i],res.finish());
  return res;
}
