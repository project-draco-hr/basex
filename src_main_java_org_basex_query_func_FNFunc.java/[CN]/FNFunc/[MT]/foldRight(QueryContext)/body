{
  final FunItem f=withArity(0,2,ctx);
  Iter res=expr[1].iter(ctx);
  final ItemCache xs=ItemCache.get(expr[2].iter(ctx));
  for (int i=(int)xs.size(); i-- != 0; )   res=f.invIter(ctx,input,xs.item[i],res.finish());
  return res;
}
