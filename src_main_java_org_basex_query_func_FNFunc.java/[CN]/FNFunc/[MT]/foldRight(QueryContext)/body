{
  final FItem f=withArity(0,2,ctx);
  final Value xs=expr[2].value(ctx);
  if (xs.empty())   return expr[1].iter(ctx);
  Value res=expr[1].value(ctx);
  for (long i=xs.size(); --i >= 0; )   res=f.invValue(ctx,input,xs.itemAt(i),res);
  return res.iter();
}
