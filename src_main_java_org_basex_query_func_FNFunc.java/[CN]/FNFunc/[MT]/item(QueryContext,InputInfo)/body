{
switch (def) {
case FUNCARITY:
    return Itr.get(getFun(0,FunType.ANY_FUN,ctx).arity());
case FUNCNAME:
  return getFun(0,FunType.ANY_FUN,ctx).fName();
case PARTAPP:
return partApp(ctx,ii);
default :
return super.item(ctx,ii);
}
}
