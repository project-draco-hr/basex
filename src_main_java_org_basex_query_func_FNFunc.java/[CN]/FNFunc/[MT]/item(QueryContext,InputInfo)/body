{
switch (def) {
case FUNCARITY:
    return Itr.get(getFun(0,FunType.ANY,ctx).arity());
case FUNCNAME:
case PARTAPP:
  NOTIMPL.thrw(input,def.desc);
return null;
default :
return super.item(ctx,ii);
}
}
