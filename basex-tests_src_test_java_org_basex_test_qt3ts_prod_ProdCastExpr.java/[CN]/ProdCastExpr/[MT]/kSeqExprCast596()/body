{
  final XQuery query=new XQuery("xs:integer(\"6789\") cast as xs:float ne xs:float(\"3.4e5\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
