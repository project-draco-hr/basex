{
  final XQuery query=new XQuery("(xs:decimal(remove((3e3, 1.1), 1))) eq 1.1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertBoolean(true) || error("XPTY0004")));
}
