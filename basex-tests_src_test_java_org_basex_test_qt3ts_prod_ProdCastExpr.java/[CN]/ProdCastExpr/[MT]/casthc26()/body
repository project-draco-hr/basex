{
  final XQuery query=new XQuery("xs:integer(12678967543233) cast as xs:integer",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("12678967543233"));
}
