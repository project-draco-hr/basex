{
  final XQuery query=new XQuery("let $var := xs:byte(120) cast as xs:unsignedByte return $var instance of xs:unsignedByte",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
