{
  if (!gui)   return true;
  if (osize != size)   return false;
  for (int i=0; i < osize; i++) {
    if (!Array.eq(prepos[i],oprepos[i]) || Array.eq(poi[i],opoi[i]))     return false;
  }
  return true;
}
