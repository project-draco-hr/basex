{
  if (!gui)   return;
  if (bu) {
    osize=size;
    oprepos=prepos;
    opoi=poi;
    tpos=new IntList();
    tcol=new IntList();
    tc=0;
  }
  size=0;
  prepos=new int[1][];
  poi=new int[1][];
}
