{
  if (item == null) {
    if (expr == null)     Err.or(VAREMPTY,this);
    final Item it=ctx.item;
    ctx.item=null;
    item=ctx.iter(expr).finish();
    ctx.item=it;
    check();
  }
  return item.iter();
}
