{
  cs=session;
  try {
    cs.out.write(0);
    cs.send(query);
    id=cs.receive();
    if (!cs.ok())     throw new BaseXException(cs.receive());
  }
 catch (  final IOException ex) {
    throw new BaseXException(ex);
  }
}
