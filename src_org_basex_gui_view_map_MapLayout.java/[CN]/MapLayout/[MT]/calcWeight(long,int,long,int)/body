{
  double sizeP=data.fs != null ? (double)GUIProp.sizep : 0d;
  if (sSize == 0)   sizeP=0d;
  long dadSize=(size == 0 && sSize == 0) ? 1 : sSize;
  return ((sizeP / 100) * ((double)size / dadSize)) + ((1 - sizeP / 100) * ((double)children / sChildren));
}
