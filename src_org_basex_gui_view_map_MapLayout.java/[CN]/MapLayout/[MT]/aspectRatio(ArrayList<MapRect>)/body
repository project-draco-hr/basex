{
  double aar=0.0;
  for (int i=0; i < rects.size(); i++) {
    aar+=rects.get(i).x < rects.get(i).y ? rects.get(i).y / rects.get(i).x : rects.get(i).x / rects.get(i).y;
  }
  return aar / rects.size();
}
