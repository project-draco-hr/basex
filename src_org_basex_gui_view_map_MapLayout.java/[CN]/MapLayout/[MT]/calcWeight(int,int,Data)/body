{
  double weight;
  long size=Token.toLong(data.attValue(data.sizeID,rect));
  long sSize=Token.toLong(data.attValue(data.sizeID,par));
  weight=calcWeight((int)size,children(data,rect).size,(int)sSize,children(data,par).size);
  return weight;
}
