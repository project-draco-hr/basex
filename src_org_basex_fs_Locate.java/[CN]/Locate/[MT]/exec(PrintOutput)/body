{
switch (version) {
case '1':
    path=fs.regex(path);
  locateTable(DataFS.ROOTDIR,out);
break;
case '2':
locateXQuery(out);
break;
default :
path=fs.regex(path);
locateTable(DataFS.ROOTDIR,out);
break;
}
if (cFlag) printCount(out);
}
