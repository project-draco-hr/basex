{
  final XQuery query=new XQuery("(3,4,5)[(xs:integer(5) - xs:integer(.)) gt 1]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("3"));
}
