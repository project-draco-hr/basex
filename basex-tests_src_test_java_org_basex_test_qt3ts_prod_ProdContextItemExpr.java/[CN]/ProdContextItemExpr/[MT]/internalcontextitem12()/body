{
  final XQuery query=new XQuery("(3,4,5)[(xs:integer(.) * xs:integer(.)) gt 2]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"3 4 5"));
}
