{
  final TokenBuilder tb=new TokenBuilder(Util.name(this) + "[");
  for (int i=0; i < size; ++i) {
    tb.add(key[i]);
    tb.add("=\"");
    tb.add(val[i]);
    tb.add("\"");
  }
  return tb.add("]").toString();
}
