{
  final TokenBuilder sb=new TokenBuilder(Main.name(this) + "[");
  for (int i=0; i < size; ++i) {
    sb.add(key[i]);
    sb.add("=\"");
    sb.add(val[i]);
    sb.add("\"");
  }
  return sb.add("]").toString();
}
