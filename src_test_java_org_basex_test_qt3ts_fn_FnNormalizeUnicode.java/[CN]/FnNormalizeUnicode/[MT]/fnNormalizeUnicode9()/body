{
  final XQuery query=new XQuery("string-to-codepoints(fn:normalize-unicode('e?????e?????e??'))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("234, 64976, 234, 64976, 234"));
}
