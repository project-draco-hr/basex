{
  final XQuery query=new XQuery("fn:upper-case(fn:normalize-unicode(\"normalizedstring\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"NORMALIZEDSTRING"));
}
