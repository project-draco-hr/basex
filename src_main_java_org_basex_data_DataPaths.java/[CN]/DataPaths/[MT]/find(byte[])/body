{
  int l=0, h=order.length - 1;
  while (l <= h) {
    final int m=l + h >>> 1;
    final int c=Token.diff(paths[order[m]],v);
    if (c == 0)     return m;
    if (c < 0)     l=m + 1;
 else     h=m - 1;
  }
  return l;
}
