{
  int l=0, h=order.length - 1;
  while (l <= h) {
    int m=l + h >>> 1;
    final int c=diff(paths[m],v);
    if (c == 0) {
      while (m > 0 && eq(paths[m - 1],v))       --m;
      return m;
    }
    if (c < 0)     l=m + 1;
 else     h=m - 1;
  }
  return l;
}
