{
  double d=twoDigits();
  if (d == -1)   return false;
  if (consume('.')) {
    if (!digit(curr()))     throw error("digit");
    long f=10;
    while (digit(curr())) {
      d+=number() / f;
      f*=10;
    }
  }
  seconds=d;
  return true;
}
