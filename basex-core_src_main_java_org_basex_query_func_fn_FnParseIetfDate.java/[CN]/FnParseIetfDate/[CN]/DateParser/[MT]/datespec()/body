{
  if (!daynum())   return false;
  if (!dsep())   throw error("'-'");
  if (!monthname())   throw error("name of month");
  if (!dsep())   throw error("'-'");
  if (!year())   throw error("year");
  return true;
}
