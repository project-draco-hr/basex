{
  final GetOpts g=new GetOpts(args,"Rh");
  while (g.more()) {
    final int ch=checkOpt(g);
switch (ch) {
case 'R':
      fRecursive=true;
    break;
}
}
path=g.getPath();
if (path == null) error("",100);
}
