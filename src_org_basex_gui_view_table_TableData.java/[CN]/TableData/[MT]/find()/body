{
  final Data data=GUI.context.data();
  final String xpath=Find.findTable(filter,colNames,data.tags.key(rootTag),data,GUIProp.filterrt);
  GUI.get().execute(Commands.XPATH,xpath.length() != 0 ? xpath : "/");
}
