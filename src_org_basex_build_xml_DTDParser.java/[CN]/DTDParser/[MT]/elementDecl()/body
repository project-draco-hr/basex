{
  if (!consumeWS())   error();
  element=consumeSpecName();
  tags.add(element);
  BaseX.debug("----------------------");
  BaseX.debug("- Element: '%'",element);
  contentSpec();
  consumeWS();
  if (!consume(GREAT))   error();
}
