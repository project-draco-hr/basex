{
  int p=pos;
  byte c=next();
  if (!XMLToken.isFirstLetter(c) && c != '%') {
    error();
  }
  do {
    c=next();
  }
 while (XMLToken.isLetter(c) || c == '%' || c == ';' || quantity(c));
  prev();
  return substring(content,p,pos);
}
