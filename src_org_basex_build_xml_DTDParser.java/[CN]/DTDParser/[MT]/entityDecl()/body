{
  if (!consumeWS())   error();
  if (consume(PERCENT)) {
    if (!consumeWS())     error();
    byte[] name=consumeSpecName();
    BaseX.debug("----------------------");
    BaseX.debug("- Entity: '%'",name);
    final byte[] val=entDef();
    ents.add(name,val);
  }
 else {
    byte[] name=consumeSpecName();
    BaseX.debug("----------------------");
    BaseX.debug("- Entity: '%'",name);
    final byte[] val=entDef();
    ents.add(name,val);
  }
}
