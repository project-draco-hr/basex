{
  if (!consumeWS())   error();
  if (percentage(next())) {
    if (!consumeWS())     error();
    byte[] name=consumeName2();
    BaseX.debug("----------------------");
    BaseX.debug("- Entity: '%'",name);
    if (!consumeWS())     error();
    final byte[] val=entDef();
    ents.add(name,val);
  }
 else {
    prev();
    byte[] name=consumeName2();
    BaseX.debug("----------------------");
    BaseX.debug("- Entity: '%'",name);
    final byte[] val=entDef();
    ents.add(name,val);
  }
}
