{
  int p=pos;
  byte c=next();
  if (!XMLToken.isFirstLetter(c) && !percentage(c)) {
    error();
  }
  do {
    c=next();
  }
 while (XMLToken.isLetter(c) || percentage(c) || semicolon(c)|| quantity(c));
  prev();
  return substring(content,p,pos);
}
