{
  int p=pos;
  byte c=next();
  if (!XMLToken.isFirstLetter(c))   error(NAMEERROR);
  do {
    c=next();
    if (c == ')') {
      prev();
      return substring(content,p,pos);
    }
  }
 while (XMLToken.isLetter(c));
  return substring(content,p,pos);
}
