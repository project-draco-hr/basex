{
  if (!consumeWS())   error();
  if (consume(REQ)) {
    BaseX.debug(REQ);
  }
 else   if (consume(IMP)) {
    BaseX.debug(IMP);
  }
 else   if (consume(FIX)) {
    BaseX.debug(FIX);
    consumeWS();
    BaseX.debug(consumeQuoted());
  }
}
