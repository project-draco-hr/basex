{
  if (!consumeWS())   error(WSERROR);
  attl=consumeSpecName();
  atts.add(attl);
  BaseX.debug("----------------------");
  BaseX.debug("- ATTLIST: '%'",attl);
  consumeWS();
  if (!consume(GREAT)) {
    attDef();
  }
 else {
    prev();
  }
}
