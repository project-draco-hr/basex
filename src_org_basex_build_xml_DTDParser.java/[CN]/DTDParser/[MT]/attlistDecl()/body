{
  if (!consumeWS())   error();
  attl=consumeName2();
  atts.add(attl);
  BaseX.debug("----------------------");
  BaseX.debug("- ATTLIST: '%'",attl);
  consumeWS1();
  if (!consume(GREAT)) {
    attType();
    dDecl();
  }
}
