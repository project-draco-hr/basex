{
  byte quote=next();
  if (quote != '\'' && quote != '"')   error();
  int p=pos;
  byte c;
  while ((c=next()) != quote) {
    if (c == 0)     error();
  }
  return substring(content,p,pos - 1);
}
