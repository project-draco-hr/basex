{
  byte quote=next();
  if (quote != '\'' && quote != '"')   error(QUOTEERROR);
  int p=pos;
  byte c;
  while ((c=next()) != quote) {
    if (c == 0)     error();
  }
  return substring(content,p,pos - 1);
}
