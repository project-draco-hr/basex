{
  checkT=consumeName();
  if (checkT.equals(ID) || checkT.equals(IDR) || checkT.equals(IDRS)|| checkT.equals(ENT)|| checkT.equals(ENTS)|| checkT.equals(NMT)|| checkT.equals(NMTS)) {
    return true;
  }
  return false;
}
