{
  int p=pos;
  checkT=consumeName();
  String help=string(checkT);
  if (help.equals(string(ID)) || help.equals(string(IDR)) || help.equals(string(IDRS))|| help.equals(string(ENT1))|| help.equals(string(ENTS))|| help.equals(string(NMT))|| help.equals(string(NMTS))) {
    return true;
  }
  pos=p;
  return false;
}
