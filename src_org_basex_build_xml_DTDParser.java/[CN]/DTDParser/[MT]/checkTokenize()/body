{
  int p=pos;
  checkT=consumeName();
  byte[] help=checkT;
  if (eq(help,ID) || eq(help,IDR) || eq(help,IDRS)|| eq(help,ENT1)|| eq(help,ENTS)|| eq(help,NMT)|| eq(help,NMTS)) {
    return true;
  }
  pos=p;
  return false;
}
