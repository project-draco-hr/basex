{
  consumeWS();
  if (consume(BRACKETC) || consume(DASH)) {
    if (!consume(STAR)) {
      mixedElement();
    }
 else {
      BaseX.debug("*");
    }
  }
 else {
    BaseX.debug(consumeSpecName());
    mixedElement();
  }
}
