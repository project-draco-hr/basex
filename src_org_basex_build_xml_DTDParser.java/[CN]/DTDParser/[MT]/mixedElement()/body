{
  consumeWS();
  if (consume(BRACKETC) || consume(DASH)) {
    consumeWS();
    if (consume(STAR)) {
      BaseX.debug("*");
    }
 else     if (consume(GREAT)) {
      prev();
    }
 else {
      mixedElement();
    }
  }
 else {
    BaseX.debug(consumeSpecName());
    mixedElement();
  }
}
