{
  consumeWS();
  if (consume(BRACKETC)) {
    if (consume(PLUS) || consume(QUESTION) || consume(STAR)) {
      consumeWS();
      if (consume(DASH)) {
        consumeWS();
        BaseX.debug(consumeName());
        enumeratedAttlist();
      }
 else       if (consume(DASH)) {
        consumeWS();
        if (consume(BRACKETO)) {
          enumeratedAttlist();
        }
 else {
          BaseX.debug(consumeName());
          enumeratedAttlist();
        }
      }
    }
  }
 else   if (consume(DASH)) {
    consumeWS();
    BaseX.debug(consumeName());
    enumeratedAttlist();
  }
 else {
    BaseX.debug(consumeName());
    enumeratedAttlist();
  }
}
