{
  if (!consumeWS())   error();
  if (consume(EMP)) {
    BaseX.debug(EMP);
  }
 else   if (consume(ANY)) {
    BaseX.debug(ANY);
  }
 else   if (consume(BRACKETO)) {
    consumeWS();
    if (consume(PC)) {
      BaseX.debug(PC);
      consumeMixed();
    }
 else {
      consumeChildren();
    }
  }
 else   error();
}
