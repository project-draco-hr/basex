{
  final XQuery query=new XQuery("empty(compare(\"a string\", \"a string\", \"http://www.example.com/COLLATION/NOT/SUPPORTED\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || error("FOCH0002")));
}
