{
switch (func) {
case _ARRAY_HEAD:
    return head(qc).iter();
case _ARRAY_TAIL:
  return tail(qc).iter();
case _ARRAY_FOLD_LEFT:
return foldLeft(qc).iter();
case _ARRAY_FOLD_RIGHT:
return foldRight(qc).iter();
default :
return super.iter(qc);
}
}
