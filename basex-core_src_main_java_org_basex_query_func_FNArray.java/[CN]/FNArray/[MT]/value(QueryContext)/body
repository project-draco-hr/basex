{
switch (func) {
case _ARRAY_HEAD:
    return head(qc);
case _ARRAY_TAIL:
  return tail(qc);
case _ARRAY_FOLD_LEFT:
return foldLeft(qc);
case _ARRAY_FOLD_RIGHT:
return foldRight(qc);
default :
return super.value(qc);
}
}
