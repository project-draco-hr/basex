{
  if ((ntok & 0xFFFF) == 0 && scm == 0 && rt.totalMemory() - rt.freeMemory() > maxMem) {
    writeIndex(Integer.toString(cf));
    Performance.gc(1);
    cf++;
  }
  tree.index(tok,pre,wp.pos,cf);
  ntok++;
}
