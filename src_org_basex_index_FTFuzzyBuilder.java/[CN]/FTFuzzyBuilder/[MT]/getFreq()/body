{
  byte j=1;
  maxfreq=new int[nodes.size()];
  nmbdocwt=new int[ntok];
  c=0;
  for (; j < tree.length; j++) {
    final FTHash tre=tree[j];
    if (tre != null) {
      tre.init();
      while (tre.more()) {
        getFreq(tre.pre[tre.next()]);
      }
    }
  }
}
