{
  try {
    while (true) {
      ss.accept().close();
      if (stop.exists()) {
        stop.delete();
        if (jetty != null)         jetty.stop();
        ss.close();
        break;
      }
    }
  }
 catch (  final Exception ex) {
    Util.errln(ex);
  }
}
