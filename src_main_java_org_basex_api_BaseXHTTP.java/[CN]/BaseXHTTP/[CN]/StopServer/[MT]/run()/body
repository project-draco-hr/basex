{
  try {
    while (true) {
      ss.accept().close();
      if (!stop.exists())       continue;
      stop.delete();
      BaseXHTTP.this.stop();
      ss.close();
      break;
    }
  }
 catch (  final Exception ex) {
    Util.errln(ex);
  }
}
