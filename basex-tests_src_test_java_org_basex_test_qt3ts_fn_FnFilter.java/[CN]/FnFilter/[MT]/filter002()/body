{
  final XQuery query=new XQuery("filter(function($x){$x gt 10}, (12, 4, 46, 23, -8))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("12, 46, 23"));
}
