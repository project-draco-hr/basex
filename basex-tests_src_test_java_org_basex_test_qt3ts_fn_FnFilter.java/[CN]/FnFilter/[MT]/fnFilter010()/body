{
  final XQuery query=new XQuery("fn:filter( function($a as item()) as xs:boolean* { fn:boolean($a), fn:boolean($a) }, () )",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((error("XPST0005") || assertEmpty()));
}
