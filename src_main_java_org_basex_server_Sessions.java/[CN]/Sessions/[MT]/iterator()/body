{
  return new Iterator<ServerProcess>(){
    private int c=-1;
    @Override public boolean hasNext(){
      return ++c < size;
    }
    @Override public ServerProcess next(){
      return list[c];
    }
    @Override public void remove(){
      Main.notexpected();
    }
  }
;
}
