{
  final XQuery query=new XQuery("fn:seconds-from-duration(xs:duration(\"P1Y2M3DT10H30M911S\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("11"));
}
