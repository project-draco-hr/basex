{
  final XQuery query=new XQuery("fn:seconds-from-duration(xs:dayTimeDuration(\"P3DT10H12.5S\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("12.5"));
}
