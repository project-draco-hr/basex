{
  check("let $seq := ('a', 'b', 'c') " + "for $i in 1 to count($seq) " + "for $j in $i + 1 to count($seq) "+ "let $b := $seq[$j] "+ "return concat($i, $j, $b)","12b 13c 23c","every $for in //For satisfies $for << //Let[@var eq '$b']");
}
