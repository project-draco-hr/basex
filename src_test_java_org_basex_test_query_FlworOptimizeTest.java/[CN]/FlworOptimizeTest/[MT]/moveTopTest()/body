{
  check("let $seq := ('a', 'b', 'c') " + "for $i in 1 to count($seq) " + "for $j in $i + 1 to count($seq) "+ "let $m := $seq[(count($seq) + 1) idiv 2] "+ "return concat($i, $j, $m)","12b 13b 23b","every $for in //For satisfies //Let[@var eq '$m'] << $for");
}
