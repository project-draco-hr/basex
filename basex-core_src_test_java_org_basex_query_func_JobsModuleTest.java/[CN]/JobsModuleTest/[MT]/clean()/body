{
  while (!query(_JOBS_LIST.args() + "[. != " + _JOBS_CURRENT.args()+ "]").isEmpty()) {
    Performance.sleep(10);
  }
  query("for $id in " + _JOBS_RESULTS.args() + " return try { "+ _JOBS_RESULT.args(" $id")+ " } catch * { }");
}
