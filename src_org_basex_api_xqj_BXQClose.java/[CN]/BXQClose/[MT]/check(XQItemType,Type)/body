{
  check();
  final BXQItemType bit=(BXQItemType)it;
  if (it != null && !t.instance(bit.type))   throw new BXQException(BaseX.info("Wrong data type; % expected, % found",bit.type,t));
  return it != null ? bit.type : t;
}
