{
  check();
  final BXQItemType bit=(BXQItemType)it;
  if (it != null && !bit.type.instance(t) && !t.instance(bit.type))   throw new BXQException(WRONG,t,bit.type);
  return it != null ? bit.type : t;
}
