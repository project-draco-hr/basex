{
switch (func) {
case _ARCHIVE_ENTRIES:
    return entries(qc);
case _ARCHIVE_EXTRACT_TEXT:
  return extractText(qc);
case _ARCHIVE_EXTRACT_BINARY:
return extractBinary(qc);
default :
return super.iter(qc);
}
}
