{
  TokenSet hs=null;
  if (e < expr.length) {
    hs=new TokenSet();
    final Iter names=ctx.iter(expr[e]);
    for (Item en; (en=names.next()) != null; ) {
      hs.add(checkElmStr(en).string(info));
    }
  }
  return hs;
}
