{
  TokenSet hs=null;
  if (e < exprs.length) {
    hs=new TokenSet();
    final Iter names=ctx.iter(exprs[e]);
    for (Item en; (en=names.next()) != null; ) {
      hs.add(checkElmStr(en).string(info));
    }
  }
  return hs;
}
