{
  final XQuery query=new XQuery("xs:double(\"2.2250738585072012e-308\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertType("xs:double"));
}
