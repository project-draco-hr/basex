{
  final int p=pre + c;
  final int affectedPre=p;
  boolean merged=mergeTexts(data,affectedPre - 1,affectedPre);
  merged|=mergeTexts(data,affectedPre + md.meta.size - 1,affectedPre + md.meta.size);
  return merged;
}
