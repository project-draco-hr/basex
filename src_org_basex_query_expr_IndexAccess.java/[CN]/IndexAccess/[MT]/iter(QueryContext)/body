{
  return new Iter(){
    IndexIterator it;
    @Override public Item next(){
      if (it == null)       it=data.ids(ind);
      if (it.more()) {
        ctx.item=new DBNode(data,it.next());
      }
 else {
        ctx.item=null;
      }
      return ctx.item;
    }
  }
;
}
