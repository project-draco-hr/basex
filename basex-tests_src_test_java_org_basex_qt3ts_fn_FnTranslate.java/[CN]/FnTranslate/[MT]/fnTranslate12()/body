{
  final XQuery query=new XQuery("fn:string-length(fn:translate(\"ABC\",\"ABC\",\"ABC\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"3"));
}
