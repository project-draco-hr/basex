{
  final XQuery query=new XQuery("compare(xs:untypedAtomic('http://www.example.com/'), 'http://www.example.com/')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("0"));
}
