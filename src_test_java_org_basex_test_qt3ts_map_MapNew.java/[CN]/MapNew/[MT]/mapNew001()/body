{
  final XQuery query=new XQuery("map:new()",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertCount(1) && assertType("map(*)") && assertType("map(xs:date, element()+)")&& assertType("function(xs:QName) as item()*")));
}
