{
  final XQuery query=new XQuery("deep-equal(map:new(for $i in 1 to 1000 return map:entry($i, $i+1)),\n" + "                         map:new(for $i in 2 to 1000 return map:entry($i, $i+1)))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
