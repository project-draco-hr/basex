{
  int p=0, s=0;
  boolean f=true;
  for (  final FTStringMatch sm : mtc) {
    if (sm.not)     continue;
    if (f) {
      if (p == sm.queryPos)       continue;
      p=sm.queryPos;
      f=false;
    }
    f=s <= sm.start;
    if (f)     s=sm.start;
  }
  return f;
}
