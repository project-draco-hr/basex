{
  if (var.name == null) {
    super.comp(ctx);
  }
 else {
    final int s=ctx.vars.size();
    ctx.vars.add(v == null ? var : var.bind(v,ctx).copy());
    super.comp(ctx);
    ctx.vars.reset(s);
  }
  type=expr.type();
  return this;
}
