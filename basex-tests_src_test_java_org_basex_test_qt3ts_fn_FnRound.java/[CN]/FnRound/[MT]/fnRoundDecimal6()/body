{
  final XQuery query=new XQuery("fn:round(xs:decimal(\"-12.1\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-12") && assertType("xs:decimal")));
}
