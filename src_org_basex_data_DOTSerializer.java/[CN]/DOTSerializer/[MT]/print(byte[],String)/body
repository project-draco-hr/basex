{
  final byte[] text=t.length > 60 ? Token.concat(Token.substring(t,0,60),Token.token("...")) : t;
  out.println(BaseX.info(NODE,count,text,col));
  nodes.set(count,tags.size);
  if (tags.size > 0)   children[tags.size - 1].add(count);
  color=null;
  count++;
}
