{
  final byte[] text=t.length > 60 ? Token.concat(Token.substring(t,0,60),Token.DOTS) : t;
  out.println(BaseX.info(NODE,count,text,col));
  nodes.set(count,level);
  if (level - 1 >= 0)   children[level - 1].add(count);
  color=null;
  count++;
}
