{
  final XQuery query=new XQuery("fn:count((<elem xmlns=\"http://ns.example.com/uri\"/>)/@*)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("0"));
}
