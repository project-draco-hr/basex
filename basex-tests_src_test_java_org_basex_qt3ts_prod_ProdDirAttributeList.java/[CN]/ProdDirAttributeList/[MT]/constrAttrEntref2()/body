{
  final XQuery query=new XQuery("fn:string-length(string((<elem attr=\"&amp;&lt;&gt;\"/>)/@attr))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("3"));
}
