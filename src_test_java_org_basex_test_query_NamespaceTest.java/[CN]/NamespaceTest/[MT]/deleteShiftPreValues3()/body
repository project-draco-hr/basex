{
  create(15);
  query("delete node doc('d15')/*:a/*:c","");
  new Open("d15").execute(CONTEXT);
  assertEquals(NL + "  Pre[1] xmlns=\"A\" " + NL+ "    Pre[2] xmlns=\"B\" "+ NL+ "    Pre[3] xmlns=\"E\" ",CONTEXT.data.ns.toString());
}
