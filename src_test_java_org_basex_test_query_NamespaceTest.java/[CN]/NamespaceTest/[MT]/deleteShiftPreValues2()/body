{
  create(14);
  query("delete node doc('d14')/*:a/*:b","");
  new Open("d14").execute(CONTEXT);
  assertEquals(NL + "  Pre[1] xmlns=\"A\" " + NL+ "    Pre[2] xmlns=\"C\" ",CONTEXT.data.ns.toString());
}
