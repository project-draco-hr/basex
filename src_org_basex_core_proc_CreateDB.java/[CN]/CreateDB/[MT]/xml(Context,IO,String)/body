{
  if (!ctx.user.perm(User.CREATE))   throw new IOException(Main.info(PERMNO,CmdPerm.READ));
  if (!io.exists())   throw new BuildException(FILEWHICH,io);
  return xml(ctx,new DirParser(io,ctx.prop),name);
}
