{
  final XQuery query=new XQuery("fold-left(function($a, $b) { $a and $b }, false(), (true(), false(), false()))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
