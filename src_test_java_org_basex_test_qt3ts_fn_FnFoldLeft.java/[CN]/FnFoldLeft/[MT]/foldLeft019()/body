{
  final XQuery query=new XQuery("fold-left(function($a, $b){ if(empty($a)) then $b else ($a + $b) div 2 }, (), (13, 14, 9, 6))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"8.625"));
}
