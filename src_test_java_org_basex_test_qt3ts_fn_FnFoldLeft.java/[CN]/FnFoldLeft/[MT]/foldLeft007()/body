{
  final XQuery query=new XQuery("fold-left(fn:concat(\"$f(\", ?, \", \", ?, \")\"), \"$zero\", 1 to 5)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"$f($f($f($f($f($zero, 1), 2), 3), 4), 5)"));
}
