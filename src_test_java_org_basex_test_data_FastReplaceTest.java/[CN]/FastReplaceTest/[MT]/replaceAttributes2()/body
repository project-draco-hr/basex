{
  try {
    new XQuery("for $i in //item return replace node $i/attribute() with " + "(attribute att1 {'0'}, attribute att2 {'1'})").execute(CONTEXT);
    new XQuery("//item/attribute()").execute(CONTEXT);
  }
 catch (  final BaseXException e) {
    fail(e.getMessage());
  }
}
