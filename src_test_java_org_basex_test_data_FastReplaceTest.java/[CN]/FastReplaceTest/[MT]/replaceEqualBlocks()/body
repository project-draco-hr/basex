{
  try {
    new XQuery("for $i in //item/location/text() " + "return replace node $i with $i").execute(CONTEXT);
    new XQuery("count(//item)").execute(CONTEXT);
  }
 catch (  final BaseXException e) {
    fail(e.getMessage());
  }
}
