{
  try {
    new XQuery("for $i in //item return replace node $i with $i").execute(CONTEXT);
    new XQuery("count(//item)").execute(CONTEXT);
  }
 catch (  BaseXException e) {
    fail(e.getMessage());
  }
}
