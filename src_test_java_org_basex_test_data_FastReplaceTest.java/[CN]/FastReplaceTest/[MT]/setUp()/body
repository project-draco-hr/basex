{
  new CreateDB(DBNAME,DOC).execute(CONTEXT);
  new XQuery("let $items := /site/regions//item " + "for $i in 1 to 5 " + "return (insert node $items into /site/regions, "+ "insert node $items before /site/regions, "+ "insert node $items after /site/closed_auctions)").execute(CONTEXT);
}
