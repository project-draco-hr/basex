{
  int l=1, c=1;
  for (int i=0, ch; i < pos; i+=Character.charCount(ch)) {
    ch=query.codePointAt(i);
    if (ch == '\n') {
      l++;
      c=1;
    }
 else     if (ch != '\r') {
      c++;
    }
  }
  return new int[]{l,c};
}
