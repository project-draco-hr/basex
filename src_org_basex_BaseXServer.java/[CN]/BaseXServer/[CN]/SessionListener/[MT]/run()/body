{
  while (running) {
    try {
      final Socket s=socket.accept();
      if (!running) {
        close();
      }
 else {
        sessions.add(new Session(s,verbose,bx));
      }
    }
 catch (    final IOException ex) {
      error(ex,false);
    }
  }
}
