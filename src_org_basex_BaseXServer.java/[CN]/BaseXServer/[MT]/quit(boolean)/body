{
  if (!running)   return;
  running=false;
  super.quit(user);
  try {
    if (console)     System.in.close();
    server.close();
    log.write("Server stopped.");
    log.closeLog();
  }
 catch (  final IOException ex) {
    error(ex,false);
  }
  console=false;
  context.close();
}
