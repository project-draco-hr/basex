{
  try {
    running=false;
    console=false;
    for (    final Session s : context.sessions)     s.close();
    new ClientLauncher(context);
  }
 catch (  final IOException ex) {
    Main.error(ex,false);
  }
}
