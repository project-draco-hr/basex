{
  if (!running)   return;
  running=false;
  super.quit(user);
  try {
    if (console)     System.in.close();
    server.close();
  }
 catch (  final IOException ex) {
    error(ex,false);
  }
  console=false;
  context.close();
}
