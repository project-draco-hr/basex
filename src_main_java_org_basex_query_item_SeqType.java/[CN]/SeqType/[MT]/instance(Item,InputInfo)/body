{
  final boolean ins=it.type.instance(type);
  if (!ins && !it.unt() && !it.func()&& (!it.num() || type != AtomType.FLT && type != AtomType.DBL)&& (it.type != AtomType.URI || type != AtomType.STR))   Err.cast(ii,type,it);
  return ins;
}
