{
  final boolean ins=it.type.instance(type);
  if (!it.unt() && !ins && (!it.num() || type != SimpleType.FLT && type != SimpleType.DBL)&& (it.type != SimpleType.URI || type != SimpleType.STR))   Err.cast(ii,type,it);
  return ins;
}
