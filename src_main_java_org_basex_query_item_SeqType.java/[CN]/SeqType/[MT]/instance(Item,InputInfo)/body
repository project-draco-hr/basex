{
  final boolean ins=it.type.instance(type);
  if (!it.unt() && !ins && (!it.num() || type != Type.FLT && type != Type.DBL)&& (it.type != Type.URI || type != Type.STR))   Err.cast(ii,type,it);
  return ins;
}
