{
  final boolean ins=it.type.instance(type);
  if (!it.unt() && !ins && (!it.num() || type != AtomType.FLT && type != AtomType.DBL)&& (it.type != AtomType.URI || type != AtomType.STR))   Err.cast(ii,type,it);
  return ins;
}
