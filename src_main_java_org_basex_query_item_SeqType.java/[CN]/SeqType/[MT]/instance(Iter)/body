{
  Item it=iter.next();
  if (it == null)   return mayBeZero();
  if (zeroOrOne())   return iter.next() == null && it.type.instance(type) && checkExtension(it);
  do {
    if (!it.type.instance(type) || !checkExtension(it))     return false;
  }
 while ((it=iter.next()) != null);
  return true;
}
