{
  Item it=iter.next();
  if (it == null)   return mayBeZero();
  if (zeroOrOne())   return iter.next() == null && it.type.instance(type) && checkExt(it);
  do {
    if (!it.type.instance(type) || !checkExt(it))     return false;
  }
 while ((it=iter.next()) != null);
  return true;
}
