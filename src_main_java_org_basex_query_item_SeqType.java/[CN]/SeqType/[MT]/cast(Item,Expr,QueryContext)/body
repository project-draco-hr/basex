{
  if (it == null) {
    if (occ == 0)     Err.empty(expr);
    return null;
  }
  if (it.type == type) {
    if (it.type == Type.STR)     ((Str)it).direct=false;
    return it;
  }
  return check(type.e(it,ctx));
}
