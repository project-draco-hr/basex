{
  check("function($f) {" + "  let $loop := function($x) { $f(function() { $x($x) }) }" + "  return $loop($loop)"+ "}(function($f) { 42 })","42","exists(//" + Util.className(DynFuncCall.class) + ")","every $f in outermost(//" + Util.className(DynFuncCall.class) + ")/* satisfies"+ "  $f instance of element("+ Util.className(FuncItem.class)+ ")");
}
