{
  check("function($f) {" + "  let $loop := function($x) { $f(function() { $x($x) }) }" + "  return $loop($loop)"+ "}(function($f) { 42 })","42","exists(//" + Util.name(DynFuncCall.class) + ")","every $f in outermost(//" + Util.name(DynFuncCall.class) + ")/* satisfies"+ "  $f instance of element("+ Util.name(FuncItem.class)+ ")");
}
