{
  check("let $fold-left :=" + "  function($f, $start, $seq) {" + "    let $go :="+ "      function($go, $acc, $xs) {"+ "        if(empty($xs)) then $acc"+ "        else $go($go, $f($acc, head($xs)), tail($xs))"+ "      }"+ "    return $go($go, $start, $seq)"+ "  }"+ "return $fold-left(function($a,$b) {$a + $b}, 0, 1 to 100000)","5000050000","empty(//" + Util.className(InlineFunc.class) + ')',"every $f in //" + Util.className(FuncItem.class) + " satisfies $f/*[1]/@name = '$go'","count(//" + Util.className(DynFuncCall.class) + ") = 3","count(distinct-values(//" + Util.className(Var.class) + "/@id)) = 3");
}
