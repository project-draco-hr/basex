{
  final int e=s + l;
  for (int i=s; i < e; ++i) {
    final char c=ch[i];
    if (sb.length() != 0 || Character.isHighSurrogate(c)) {
      sb.append(c);
    }
 else {
      tb.add(c);
    }
  }
}
