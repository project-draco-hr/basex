{
  file=new RandomAccessFile(f,"rw");
  len=file.length();
  fc=file.getChannel();
  tmpblock=ByteBuffer.allocateDirect(BLOCKSIZE);
  mbytebuffer=fc.map(FileChannel.MapMode.READ_WRITE,0,BLOCKSIZE);
  for (int i=1; i < BUFFERS; i++)   pos[i]=-1;
}
