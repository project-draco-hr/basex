{
  if (pos[c] + BLOCKSIZE == len - 1) {
    try {
      fc.position(pos[c] + BLOCKSIZE);
      tmpblock.clear();
      fc.write(tmpblock);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
  cursor(pos[c] + BLOCKSIZE);
}
