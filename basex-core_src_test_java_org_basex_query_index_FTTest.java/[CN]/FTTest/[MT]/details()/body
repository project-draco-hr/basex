{
  final MainOptions opts=context.options;
  final String sb=toString(opts,MainOptions.FTINDEX) + ';' + toString(opts,MainOptions.STEMMING)+ ';'+ toString(opts,MainOptions.DIACRITICS)+ ';'+ toString(opts,MainOptions.CASESENS);
  return sb;
}
