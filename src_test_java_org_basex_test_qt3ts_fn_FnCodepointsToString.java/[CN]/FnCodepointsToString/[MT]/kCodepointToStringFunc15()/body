{
  final XQuery query=new XQuery("string-to-codepoints(codepoints-to-string(31))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("31") || error("FOCH0001")));
}
