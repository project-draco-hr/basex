{
  final XQuery query=new XQuery("fn:string-join((fn:codepoints-to-string((49,97)),'ab'),'')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1aab"));
}
