{
  final XQuery query=new XQuery("codepoints-to-string(8)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertStringValue(false,"&#x8;") || error("FOCH0001")));
}
