{
  Prop.read();
  final String[] fn=new String[]{"/home/dbis/xml/11mb.xml"};
  int runs=2;
  run(runs,fn,false);
  run(runs,fn,true);
  runs=10;
  run(runs,fn,false);
  run(runs,fn,true);
  run(runs,fn,false);
  run(runs,fn,true);
  runs=30;
  run(runs,fn,false);
  run(runs,fn,true);
}
