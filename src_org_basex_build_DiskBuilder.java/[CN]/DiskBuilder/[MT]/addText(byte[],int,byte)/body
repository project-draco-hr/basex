{
  long v=Token.toSimpleInt(txt);
  if (v != Integer.MIN_VALUE) {
    v|=0x8000000000L;
  }
 else {
    v=txtlen;
    txtlen+=xout.writeToken(txt);
  }
  tout.write(kind);
  tout.write(0);
  tout.write(0);
  tout.write5(v);
  tout.writeInt(par);
  tout.writeInt(size++);
}
