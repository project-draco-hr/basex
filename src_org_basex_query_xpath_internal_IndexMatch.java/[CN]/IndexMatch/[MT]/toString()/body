{
  final TokenBuilder sb=new TokenBuilder();
  sb.add(name());
  sb.add("(");
  if (path.steps.size() != 0)   sb.add(path + ", ");
  sb.add(expr + "[");
  sb.add(match.toString() + "])");
  return sb.toString();
}
