{
  final XQuery query=new XQuery("count(<a><b/><b/></a>/b[2]/preceding-sibling::b)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1"));
}
