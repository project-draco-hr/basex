{
  return new Iterator<FTMatch>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public FTMatch next(){
      return match[c];
    }
    public void remove(){
      BaseX.notexpected();
    }
  }
;
}
