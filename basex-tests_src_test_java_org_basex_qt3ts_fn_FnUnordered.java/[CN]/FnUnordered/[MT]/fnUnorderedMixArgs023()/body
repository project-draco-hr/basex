{
  final XQuery query=new XQuery("fn:unordered ( (\"a\", xs:time(\"12:30:00\"), \"b\", \"c\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertPermutation("\"a\", xs:time('12:30:00'), \"b\", \"c\""));
}
