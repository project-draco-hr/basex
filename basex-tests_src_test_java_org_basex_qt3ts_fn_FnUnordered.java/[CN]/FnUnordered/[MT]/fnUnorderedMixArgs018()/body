{
  final XQuery query=new XQuery("fn:unordered ( (\"a\", xs:boolean(\"0\"), \"b\", \"c\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertPermutation("\"a\", false(), \"b\", \"c\""));
}
