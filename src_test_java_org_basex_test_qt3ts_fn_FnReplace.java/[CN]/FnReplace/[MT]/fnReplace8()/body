{
  final XQuery query=new XQuery("replace(\"AAAA\", \"A+?\", \"b\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"bbbb"));
}
