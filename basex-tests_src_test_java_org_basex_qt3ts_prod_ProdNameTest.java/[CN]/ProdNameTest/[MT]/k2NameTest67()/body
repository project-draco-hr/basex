{
  final XQuery query=new XQuery("<e>{<e foo=\"asd\"/>/attribute(foo, notBound:untypedAtomic)}</e>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0081"));
}
