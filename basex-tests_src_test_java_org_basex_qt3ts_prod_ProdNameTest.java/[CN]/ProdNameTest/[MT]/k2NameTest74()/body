{
  final XQuery query=new XQuery("<e>{<e><b/></e>/element(foo, xs:doesNotExist)}</e>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0008"));
}
