{
  if (level != 0 || docsys == null)   return false;
  if (sep)   indent();
  print(DOCTYPE);
  if (dt == null)   print(M_HTML);
 else   print(dt);
  if (docpub != null) {
    print(' ' + PUBLIC + " \""+ docpub+ '"');
  }
 else {
    print(' ' + SYSTEM);
  }
  print(" \"" + docsys + '"');
  print(ELEM_C);
  sep=true;
  return true;
}
