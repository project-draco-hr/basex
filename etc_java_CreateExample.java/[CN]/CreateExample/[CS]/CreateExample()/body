{
  try {
    BaseXClient session=new BaseXClient("localhost",1984,"admin","admin");
    try {
      InputStream bais=new ByteArrayInputStream("<xml>Hello World!</xml>".getBytes());
      session.create("database",bais);
      System.out.println(session.info());
      System.out.println(session.execute("xquery doc('database')"));
      session.execute("drop db database");
    }
 catch (    IOException ex) {
      ex.printStackTrace();
    }
    session.close();
  }
 catch (  IOException ex) {
    ex.printStackTrace();
  }
}
