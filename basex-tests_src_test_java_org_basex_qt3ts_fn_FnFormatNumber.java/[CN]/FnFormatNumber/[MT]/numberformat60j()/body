{
  final XQuery query=new XQuery("format-number(1E18,'#####################')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1000000000000000000"));
}
