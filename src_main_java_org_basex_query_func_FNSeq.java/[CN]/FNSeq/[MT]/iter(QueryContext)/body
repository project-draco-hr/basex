{
switch (func) {
case INDEXOF:
    return indexOf(ctx);
case DISTINCT:
  return distinctValues(ctx);
case INSBEF:
return insertBefore(ctx);
case REVERSE:
return reverse(ctx);
case REMOVE:
return remove(ctx);
case SUBSEQ:
return subsequence(ctx);
case TAIL:
return tail(ctx);
default :
return super.iter(ctx);
}
}
