{
switch (func) {
case REVERSE:
case HEAD:
    return expr[0].returned(ctx).zeroOrOne() ? expr[0] : this;
case TAIL:
  return expr[0].returned(ctx).zeroOrOne() ? Seq.EMPTY : this;
default :
return this;
}
}
