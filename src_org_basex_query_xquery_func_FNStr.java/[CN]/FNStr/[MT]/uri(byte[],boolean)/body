{
  final int tl=tok.length;
  final TokenBuilder tb=new TokenBuilder();
  for (int t=0; t < tl; t++) {
    final byte b=tok[t];
    if (letterOrDigit(b) || !iri && contains(RES,b) || iri && contains(IRIRES,b))     tb.add(b);
 else     hex(tb,b);
  }
  return tb.finish();
}
