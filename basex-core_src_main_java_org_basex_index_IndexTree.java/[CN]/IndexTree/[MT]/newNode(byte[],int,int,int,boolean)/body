{
  tree.add(-1);
  tree.add(-1);
  tree.add(par);
  mod.add(false);
  keys.add(key);
  addNewValues(value,pos);
  if (!exist)   maps.put(Num.num(keys.size() - 1),values.size() - 1);
  return mod.size() - 1;
}
