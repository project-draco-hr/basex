{
  tree.add(-1);
  tree.add(-1);
  tree.add(par);
  mod.add(false);
  keys.add(key);
  addNewIds(id,pos);
  if (!exist)   maps.put(Num.num(keys.size() - 1),ids.size() - 1);
  return mod.size() - 1;
}
