{
  final int ln=cn;
  if (r(cn) == -1) {
    int t=cn;
    cn=p(cn);
    while (cn != -1 && t == r(cn)) {
      t=cn;
      cn=p(cn);
    }
  }
 else {
    cn=r(cn);
    while (l(cn) != -1)     cn=l(cn);
  }
  return ln;
}
