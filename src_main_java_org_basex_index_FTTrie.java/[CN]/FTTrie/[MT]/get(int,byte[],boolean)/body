{
  if (searchNode == null || searchNode.length == 0)   return FTIndexIterator.EMP;
  final int[] ne=nodeId(id,searchNode);
  return ne == null ? FTIndexIterator.EMP : iter(did,ne[ne.length - 1],inD,f);
}
