{
  if (token == null || token.length == 0)   return FTIndexIterator.EMP;
  final int[] node=node(token,id);
  return node == null ? FTIndexIterator.EMP : iter(currID,node[node.length - 1],inB,fast);
}
