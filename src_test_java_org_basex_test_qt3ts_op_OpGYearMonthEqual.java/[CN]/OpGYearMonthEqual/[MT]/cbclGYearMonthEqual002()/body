{
  final XQuery query=new XQuery("xs:gYearMonth(\"2008-01\") eq xs:gYearMonth(\"2008-01+09:00\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(false));
}
