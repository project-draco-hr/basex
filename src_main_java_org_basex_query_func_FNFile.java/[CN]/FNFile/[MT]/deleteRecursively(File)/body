{
  if (!file.canWrite())   FILEDEL.thrw(input,file.getPath());
  final File[] children=file.listFiles();
  if (children != null) {
    for (    final File ch : children)     deleteRecursively(ch);
  }
  if (!file.delete())   CANNOTDEL.thrw(input,file.getPath());
  return null;
}
