{
  if (!path.exists())   PATHNOTEXISTS.thrw(input,path.getAbsolutePath());
  if (path.isDirectory())   PATHISDIR.thrw(input,path.getAbsolutePath());
  return new B64Stream(new IOFile(path),FILEERROR);
}
