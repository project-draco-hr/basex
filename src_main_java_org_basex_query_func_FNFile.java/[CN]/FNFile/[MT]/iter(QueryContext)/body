{
  checkAdmin(ctx);
  final File path=new File(string(checkStr(expr[0],ctx)));
switch (def) {
case _FILE_LIST:
    return list(path,ctx);
case _FILE_READ_TEXT_LINES:
  return readTextLines(path,ctx);
default :
return super.iter(ctx);
}
}
