{
  checkCreate(ctx);
switch (sig) {
case _FILE_LIST:
    return list(checkFile(0,ctx),ctx);
case _FILE_READ_TEXT_LINES:
  return readTextLines(checkFile(0,ctx),ctx);
default :
return super.iter(ctx);
}
}
