{
  final XQuery query=new XQuery("(<a>1</a>,<b>2</b>)/(if(position() eq 1) then . else data(.))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0018"));
}
