{
  final XQuery query=new XQuery("let $a := . return fn:count(.[/ is $a])",ctx);
  query.context(node(file("prod/PathExpr/OneTopElement.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
