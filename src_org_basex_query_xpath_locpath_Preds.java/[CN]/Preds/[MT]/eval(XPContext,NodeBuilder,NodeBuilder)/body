{
  final NodeSet ns=ctx.item;
  NodeBuilder n=nodes;
  ctx.item=tmp;
  for (int s=0; s < size; s++) {
    if (n.size == 0)     break;
    n=preds[s].eval(ctx,n);
  }
  result.add(n);
  ctx.item=ns;
  nodes.reset();
}
