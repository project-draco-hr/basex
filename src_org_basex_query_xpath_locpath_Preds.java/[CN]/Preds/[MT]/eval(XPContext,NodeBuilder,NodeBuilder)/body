{
  final NodeSet ns=ctx.local;
  NodeBuilder n=nodes;
  ctx.local=tmp;
  for (int s=0; s < size; s++) {
    if (n.size == 0)     break;
    n=preds[s].eval(ctx,n);
  }
  result.add(n);
  ctx.local=ns;
  nodes.reset();
}
