{
  final Nod ns=ctx.item;
  ctx.item=tmp;
  tmp.set(pre);
  tmp.currSize=1;
  for (int j=1; j != size; j++) {
    if (!preds[j].eval(ctx,tmp,1)) {
      ctx.item=ns;
      return;
    }
  }
  ctx.item=ns;
  result.add(pre);
}
