{
  final NodeSet ns=ctx.local;
  ctx.local=tmp;
  tmp.set(pre);
  tmp.currSize=1;
  for (int j=1; j != size; j++) {
    if (!preds[j].eval(ctx,tmp,1)) {
      ctx.local=ns;
      return;
    }
  }
  ctx.local=ns;
  result.add(pre);
}
