{
  final byte[] nm=Token.token(name);
  final int s=getLength();
  for (int i=0; i < s; ++i) {
    final byte[] n=xquery != null ? xquery.get(i).nname() : nodes.data.name(nodes.nodes[i],Data.ELEM);
    if (Token.eq(n,nm))     return item(i);
  }
  return null;
}
