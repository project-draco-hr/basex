{
  final XQuery query=new XQuery("\n" + "        declare variable $global := count(*); \n" + "        <e/>/($global, $global)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPDY0002"));
}
