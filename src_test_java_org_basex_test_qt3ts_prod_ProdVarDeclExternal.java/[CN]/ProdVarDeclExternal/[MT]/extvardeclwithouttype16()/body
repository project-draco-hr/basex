{
  final XQuery query=new XQuery("declare variable $x external; $x and fn:false()",ctx);
  query.bind("x",new XQuery("let $var := true() and true() return $var",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || error("FORG0006")));
}
