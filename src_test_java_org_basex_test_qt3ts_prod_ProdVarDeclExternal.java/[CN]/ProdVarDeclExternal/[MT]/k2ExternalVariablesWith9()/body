{
  final XQuery query=new XQuery("declare variable $exampleComThisVarIsNotRecognized as processing-instruction()? external; 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("1") || error("XPDY0002")));
}
