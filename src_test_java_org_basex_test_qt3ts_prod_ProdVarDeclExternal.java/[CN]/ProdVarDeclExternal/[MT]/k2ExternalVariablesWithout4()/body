{
  final XQuery query=new XQuery("\n" + "        \n" + "        declare variable $exampleComThisVarIsNotRecognized external; $exampleComThisVarIsNotRecognized",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPDY0002"));
}
