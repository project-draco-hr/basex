{
  final XQuery query=new XQuery("\n" + "        declare default element namespace \"\"; \n" + "        declare namespace prefix = \"\"; \n"+ "        declare variable $prefix:input-context external; 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0081"));
}
