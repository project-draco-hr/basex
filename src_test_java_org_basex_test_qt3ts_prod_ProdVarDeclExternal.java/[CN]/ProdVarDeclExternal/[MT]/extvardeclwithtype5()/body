{
  final XQuery query=new XQuery("declare variable $x as xs:boolean external; fn:string($x)",ctx);
  query.bind("x",new XQuery("true()",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"true"));
}
