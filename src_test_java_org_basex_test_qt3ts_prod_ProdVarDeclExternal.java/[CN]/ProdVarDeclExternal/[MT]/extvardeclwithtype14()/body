{
  final XQuery query=new XQuery("declare variable $x as xs:integer external; $x div xs:integer(10)",ctx);
  query.bind("x",new XQuery("let $var := 40 div 2 return xs:integer($var)",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(assertEq("2"));
}
