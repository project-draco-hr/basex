{
  final XQuery query=new XQuery("declare variable $x external; $x mod 2",ctx);
  query.bind("x",new XQuery("let $var := 55 mod 3 return $var",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test((assertEq("1") || error("XPTY0004")));
}
