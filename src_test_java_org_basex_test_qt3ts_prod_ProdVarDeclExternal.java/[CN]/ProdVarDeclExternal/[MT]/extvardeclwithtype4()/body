{
  final XQuery query=new XQuery("declare variable $x as xs:double external; $x",ctx);
  query.bind("x",new XQuery("xs:double(1.2E2)",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(assertEq("120"));
}
