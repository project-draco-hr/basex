{
  final XQuery query=new XQuery("declare variable $x as xs:integer external; $x * $x",ctx);
  query.bind("x",new XQuery("let $var := 2 * 2 return $var",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(assertEq("16"));
}
