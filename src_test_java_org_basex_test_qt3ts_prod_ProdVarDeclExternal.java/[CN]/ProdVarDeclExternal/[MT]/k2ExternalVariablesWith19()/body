{
  final XQuery query=new XQuery("declare variable $i as xs:string := xs:anyURI(\"http://www.example.com/\") ; $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
