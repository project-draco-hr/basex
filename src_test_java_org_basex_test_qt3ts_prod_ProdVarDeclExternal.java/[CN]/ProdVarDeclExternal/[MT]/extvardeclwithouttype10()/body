{
  final XQuery query=new XQuery("declare variable $x external; $x + $x",ctx);
  query.bind("x",new XQuery("let $var := 1 return $var",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test((assertEq("2") || error("XPTY0004")));
}
