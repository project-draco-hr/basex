{
  final XQuery query=new XQuery("declare variable $x external; xs:float($x)",ctx);
  query.bind("x",new XQuery("12.5E10",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"1.25E11") || error("XPTY0004")));
}
