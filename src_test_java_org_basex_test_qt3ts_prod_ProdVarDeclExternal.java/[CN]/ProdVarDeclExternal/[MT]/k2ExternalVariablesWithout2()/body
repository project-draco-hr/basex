{
  final XQuery query=new XQuery("declare variable $input-context external; declare variable $input-context external; $input-context",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0049"));
}
