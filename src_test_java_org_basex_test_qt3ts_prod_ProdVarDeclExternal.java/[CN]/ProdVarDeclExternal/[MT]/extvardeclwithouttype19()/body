{
  final XQuery query=new XQuery("declare variable $x external; xs:dateTime($x)",ctx);
  query.bind("x",new XQuery("let $var := avg((1,2,4)) return $var",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
