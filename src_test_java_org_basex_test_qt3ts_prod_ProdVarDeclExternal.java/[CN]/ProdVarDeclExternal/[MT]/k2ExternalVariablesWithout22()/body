{
  final XQuery query=new XQuery("declare variable $global := count(*); <wrongFocus> <e1/> <e2/> </wrongFocus>/$global",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
