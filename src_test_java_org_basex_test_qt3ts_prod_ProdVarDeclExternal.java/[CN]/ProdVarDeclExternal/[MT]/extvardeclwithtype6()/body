{
  final XQuery query=new XQuery("declare variable $x as xs:float external; $x",ctx);
  try {
    query.bind("x",new XQuery("xs:float(1267.43233E12)",ctx).value());
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertStringValue(false,"1.2674323E15") || assertStringValue(false,"1.2674324E15")));
}
