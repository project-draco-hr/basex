{
  final XQuery query=new XQuery("declare variable $x external; $x",ctx);
  query.bind("x",new XQuery("1.2E2",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test((assertEq("120") || error("XPTY0004")));
}
