{
  final XQuery query=new XQuery("declare variable $input-context as item()* external; declare variable $input-context external; 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0049"));
}
