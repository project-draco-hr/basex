{
  final XQuery query=new XQuery("declare variable $x external; $x idiv 2",ctx);
  query.bind("x",new XQuery("let $var := 20 idiv 2 return $var",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test((assertEq("5") || error("XPTY0004")));
}
