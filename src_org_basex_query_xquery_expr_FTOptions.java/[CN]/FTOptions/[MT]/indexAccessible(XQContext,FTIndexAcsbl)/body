{
  final MetaData meta=ia.data.meta;
  ia.io=meta.ftcs == opt.is(FTOpt.CS) && meta.ftdc == opt.is(FTOpt.DC) && meta.ftst == opt.is(FTOpt.ST) && opt.sw == null && (!opt.is(FTOpt.WC) || !meta.ftfz);
  final FTOpt tmp=ctx.ftopt;
  ctx.ftopt=opt;
  expr[0].indexAccessible(ctx,ia);
  ctx.ftopt=tmp;
}
