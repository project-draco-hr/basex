{
  if (mth == M_TEXT)   return;
  finishElement();
  if (ind)   indent(true);
  if (mth == M_HTML && contains(v,'>'))   error(SERPI);
  print(PI1);
  print(n);
  print(' ');
  print(v);
  print(mth == M_HTML ? ELEM2 : PI2);
}
