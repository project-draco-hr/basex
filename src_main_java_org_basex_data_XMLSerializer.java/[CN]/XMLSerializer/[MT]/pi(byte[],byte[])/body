{
  if (method == M_TEXT)   return;
  finishElement();
  if (ind)   indent(true);
  if (method == M_HTML && contains(v,'>'))   error(SERPI);
  print(PI1);
  print(n);
  print(' ');
  print(v);
  print(method == M_HTML ? ELEM2 : PI2);
}
