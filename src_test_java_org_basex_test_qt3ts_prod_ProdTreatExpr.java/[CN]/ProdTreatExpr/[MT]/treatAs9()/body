{
  final XQuery query=new XQuery("let $var := xs:date(\"2000-01-01+05:00\") return fn:year-from-date($var treat as xs:date)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("2000"));
}
