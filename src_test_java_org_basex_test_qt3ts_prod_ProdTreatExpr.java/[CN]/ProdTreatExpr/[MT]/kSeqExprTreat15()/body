{
  final XQuery query=new XQuery("(4 treat as item() + - 5) = ((4 treat as item()+) - 5)",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPTY0004")));
}
