{
  final XQuery query=new XQuery("let $var := (100+200) div 10 return ($var cast as xs:integer treat as xs:integer) + 10",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("40"));
}
