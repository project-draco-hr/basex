{
  final XQuery query=new XQuery("let $var := (100+200) div 2 return fn:concat($var treat as xs:string,\"a string\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPDY0050"));
}
