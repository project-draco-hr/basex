{
  final XQuery query=new XQuery("let $var := xs:double(-100) return fn:abs($var cast as xs:integer treat as xs:integer)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("100"));
}
