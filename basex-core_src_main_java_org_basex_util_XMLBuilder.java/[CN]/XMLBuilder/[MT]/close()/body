{
  final TokenBuilder cch=cache;
  final byte[] name=open.pop();
  if (opening) {
    cch.add('/');
    opening=false;
  }
 else {
    ws();
    cch.add('<').add('/').add(name);
  }
  cch.add('>');
  indenting=indent;
  return this;
}
