{
  final int vl=value.length;
  for (int k=0; k < vl; k+=cl(value,k)) {
    final int ch=cp(value,k);
    if (ch == '"') {
      cache.add(E_QUOT);
    }
 else     if (ch == 0x9 || ch == 0xA) {
      addHex(ch);
    }
 else {
      add(ch);
    }
  }
}
