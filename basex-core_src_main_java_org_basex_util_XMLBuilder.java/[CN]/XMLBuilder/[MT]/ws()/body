{
  if (indenting) {
    final int os=open.size();
    if (os >= 0) {
      final TokenBuilder cch=cache;
      cch.add('\n');
      for (int o=0; o < os; o++)       cch.add(' ').add(' ');
    }
  }
}
