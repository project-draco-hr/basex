{
  try {
    final ClientSession cs=login(u,p);
    try {
      return listDatabases(cs).contains(childName) ? new BXDatabase(childName,u,p) : null;
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
