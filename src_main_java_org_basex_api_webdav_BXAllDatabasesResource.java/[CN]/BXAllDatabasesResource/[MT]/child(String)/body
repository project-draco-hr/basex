{
  try {
    final ClientSession cs=login(user,pass);
    try {
      return new BXDocumentDatabase(childName,user,pass);
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    return null;
  }
}
