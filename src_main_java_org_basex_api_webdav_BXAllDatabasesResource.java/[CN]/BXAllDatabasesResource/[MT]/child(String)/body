{
  try {
    final Session cs=session.login();
    try {
      return listDbs(cs).contains(childName) ? new BXDatabase(childName,session) : null;
    }
  finally {
      cs.close();
    }
  }
 catch (  final Exception ex) {
    handle(ex);
  }
  return null;
}
