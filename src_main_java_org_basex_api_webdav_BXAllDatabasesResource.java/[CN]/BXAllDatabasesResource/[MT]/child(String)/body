{
  try {
    final ClientSession cs=login(user,pass);
    try {
      return listDatabases(cs).contains(childName) ? new BXCollectionDatabase(childName,user,pass) : null;
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
