{
  try {
    final Session cs=factory.login(user,pass);
    try {
      return listDbs(cs).contains(childName) ? new BXDatabase(childName,factory,user,pass) : null;
    }
  finally {
      cs.close();
    }
  }
 catch (  final Exception ex) {
    handle(ex);
  }
  return null;
}
