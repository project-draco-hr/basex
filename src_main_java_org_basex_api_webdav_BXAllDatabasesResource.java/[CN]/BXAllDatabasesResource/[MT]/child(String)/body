{
  try {
    final ClientSession cs=login(user,pass);
    try {
      return new BXDocumentResource(childName,user,pass);
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    return null;
  }
}
