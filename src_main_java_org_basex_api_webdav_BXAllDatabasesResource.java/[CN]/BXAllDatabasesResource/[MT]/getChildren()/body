{
  try {
    final List<BXResource> dbs=new ArrayList<BXResource>();
    final ClientSession cs=login(u,p);
    try {
      for (      final String d : listDatabases(cs))       dbs.add(new BXDatabase(d,u,p));
      return dbs;
    }
  finally {
      cs.close();
    }
  }
 catch (  final Exception e) {
    e.printStackTrace();
  }
  return null;
}
