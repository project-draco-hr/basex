{
  final List<BXResource> dbs=new ArrayList<BXResource>();
  for (  final String d : listDatabases(ctx))   dbs.add(isCollection(ctx,d) ? new BXCollectionDatabaseResource(ctx,d) : new BXDocumentResource(ctx,d));
  return dbs;
}
