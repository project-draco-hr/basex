{
  final XQuery query=new XQuery("ceiling(-0.1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("0.0") && assertType("xs:decimal")));
}
