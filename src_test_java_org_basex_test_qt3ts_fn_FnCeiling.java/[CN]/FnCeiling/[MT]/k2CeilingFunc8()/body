{
  final XQuery query=new XQuery("ceiling(xs:negativeInteger(.)) instance of xs:integer",ctx);
  query.context(node(file("fn/abs/e-1.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
