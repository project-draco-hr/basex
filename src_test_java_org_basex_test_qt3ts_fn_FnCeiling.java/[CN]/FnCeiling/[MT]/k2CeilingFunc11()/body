{
  final XQuery query=new XQuery("ceiling(xs:short(.)) instance of xs:integer",ctx);
  query.context(node(file("fn/abs/e0.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
