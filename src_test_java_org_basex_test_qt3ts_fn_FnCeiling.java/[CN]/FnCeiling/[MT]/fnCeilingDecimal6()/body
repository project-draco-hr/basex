{
  final XQuery query=new XQuery("ceiling(-12345678.567890)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-12345678") && assertType("xs:decimal")));
}
