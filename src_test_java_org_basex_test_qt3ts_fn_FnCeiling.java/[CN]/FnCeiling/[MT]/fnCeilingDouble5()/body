{
  final XQuery query=new XQuery("ceiling(-0.1e0)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-0.0e0") && assertEq("-0") && assertType("xs:double")));
}
