{
  final XQuery query=new XQuery("fn:years-from-duration(xs:yearMonthDuration(\"P2030Y12M\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("2031"));
}
