{
  final Item it=checkEmptyType((expr.length == 2 ? expr[1] : checkCtx(ctx)).atomic(ctx,input),Type.NOD);
switch (def) {
case LANG:
    return lang(lc(checkEStr(expr[0],ctx)),checkNode(it));
default :
  return super.atomic(ctx,ii);
}
}
