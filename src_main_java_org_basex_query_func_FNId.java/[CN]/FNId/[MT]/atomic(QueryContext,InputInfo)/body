{
  final Item it=(expr.length == 2 ? expr[1] : checkCtx(ctx)).atomic(ctx,input);
  if (it == null)   Err.or(input,XPEMPTYPE,desc(),Type.NOD);
switch (func) {
case LANG:
    return lang(lc(checkEStr(expr[0],ctx)),checkNode(it));
default :
  return super.atomic(ctx,ii);
}
}
