{
  final Item it=checkEmpty((expr.length == 2 ? expr[1] : checkCtx(ctx)).item(ctx,input));
switch (def) {
case LANG:
    return lang(lc(checkEStr(expr[0],ctx)),checkNode(it));
default :
  return super.item(ctx,ii);
}
}
