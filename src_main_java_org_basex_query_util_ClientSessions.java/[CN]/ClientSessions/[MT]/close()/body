{
  for (  final byte[] c : conns) {
    final ClientSession cs=conns.get(c);
    if (cs == null)     continue;
    try {
      cs.close();
    }
 catch (    final IOException ex) {
      Util.debug(ex);
    }
  }
}
