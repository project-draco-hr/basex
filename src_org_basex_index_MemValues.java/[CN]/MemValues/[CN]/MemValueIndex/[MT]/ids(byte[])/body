{
  final int i=id(key);
  if (i == 0)   return IndexIterator.EMPTY;
  return new IndexIterator(){
    int p=-1;
    public int next(){
      return ++p < len[i] ? ids[i][p] : null;
    }
    public int size(){
      return len[i];
    }
  }
;
}
