{
  final int i=id(key);
  if (i == 0)   return IndexIterator.EMPTY;
  return new IndexIterator(){
    int p=-1;
    public boolean more(){
      return ++p < len[i];
    }
    public int next(){
      return ids[i][p];
    }
  }
;
}
