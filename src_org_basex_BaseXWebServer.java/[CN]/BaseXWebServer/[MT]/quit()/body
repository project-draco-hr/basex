{
  try {
    final Socket s=new Socket(context.prop.get(Prop.HOST),context.prop.num(Prop.WEBPORT));
    s.getOutputStream().write(Token.token("STOP" + NL + NL));
    s.close();
  }
 catch (  final Exception ex) {
    BaseXServer.error(ex,false);
  }
}
