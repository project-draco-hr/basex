{
  final XQuery query=new XQuery("ceiling(12.5)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("13.0") && assertType("xs:decimal")));
}
