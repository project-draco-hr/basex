{
  final String queryStr="import module namespace w = 'http://basex.org/webdav';" + "w:delete-lock($lock-token)";
  final Query q=http.session().query(queryStr);
  q.bind("lock-token",token);
  q.execute();
}
