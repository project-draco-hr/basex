{
  RandomAccessFile f=null;
  try {
    f=new RandomAccessFile(fl.file(),"rw");
    len=f.length();
  }
 catch (  final IOException ex) {
    if (f != null)     f.close();
    throw ex;
  }
  file=f;
  cursor(0);
}
