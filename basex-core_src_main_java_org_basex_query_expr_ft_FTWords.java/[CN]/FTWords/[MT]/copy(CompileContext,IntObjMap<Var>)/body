{
  final FTWords ftw=new FTWords(info,query.copy(cc,vm),mode,occ == null ? null : Arr.copyAll(cc,vm,occ));
  ftw.simple=simple;
  ftw.compiled=compiled;
  ftw.data=data;
  ftw.tokens=tokens;
  ftw.ftOpt=ftOpt;
  return ftw;
}
