{
  final FTWords ftw=new FTWords(info,query.copy(cc,vs),mode,occ == null ? null : Arr.copyAll(cc,vs,occ));
  if (ftt != null)   ftw.ftt=ftt.copy(ftw);
  ftw.tokens=tokens;
  ftw.data=data;
  ftw.first=first;
  ftw.pos=pos;
  ftw.fast=fast;
  return ftw;
}
