{
  final XQuery query=new XQuery("(4 to remove((2e0, 4), 1) treat as xs:integer)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"4"));
}
