{
  final int k=data.kind(p);
  final TokenList sl=new TokenList();
  sl.add(p);
  sl.add(p - data.parent(p,k));
  sl.add(data.size(p,k));
  sl.add(data.attSize(p,k));
  final int u=data.uri(p,k);
  if (data.nsFlag(p))   sl.add("+" + u);
 else   sl.add(u);
  sl.add(TABLEKINDS[k]);
  byte[] cont=null;
  if (k == Data.ELEM) {
    cont=data.name(p,k);
  }
 else   if (k == Data.ATTR) {
    cont=new TokenBuilder(data.name(p,k)).add(ATT1).add(data.text(p,false)).add(ATT2).finish();
  }
 else {
    cont=data.text(p,true);
  }
  sl.add(replace(chop(cont,64),'\n',' '));
  t.contents.add(sl);
}
