{
  final XQuery query=new XQuery("round-half-to-even(4561234567, -2)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("4561234600") && assertType("xs:integer")));
}
