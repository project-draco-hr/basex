{
  final XQuery query=new XQuery("timezone-from-time(adjust-time-to-timezone(xs:time(\"00:00:00\"))) eq implicit-timezone()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
