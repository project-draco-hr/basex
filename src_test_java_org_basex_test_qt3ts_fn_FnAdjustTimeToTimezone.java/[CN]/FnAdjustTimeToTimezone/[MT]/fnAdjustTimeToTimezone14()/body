{
  final XQuery query=new XQuery("fn:string(fn:adjust-time-to-timezone(xs:time(\"10:00:00Z\"),())) or fn:true()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
