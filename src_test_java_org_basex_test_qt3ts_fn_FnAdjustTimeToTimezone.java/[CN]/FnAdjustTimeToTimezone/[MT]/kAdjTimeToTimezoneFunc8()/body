{
  final XQuery query=new XQuery("adjust-time-to-timezone(xs:time(\"08:02:00\"), xs:dayTimeDuration(\"PT14H0M0.001S\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("FODT0003"));
}
