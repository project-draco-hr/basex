{
  final XQuery query=new XQuery("import schema namespace ns1 = \"\";\n" + "             \"abc\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0057"));
}
