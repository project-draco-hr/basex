{
  final int name=data.name(pre);
  return data.kind(pre) == Data.ELEM && (name == data.tags.id(token(DeepFile.FILE_NS)) || name == data.tags.id(token(DeepFile.DIR_NS)) || name == data.tags.id(token(FSImporter.DOC_NODE)) || name == data.tags.id(token(FSImporter.ROOT_NODE)));
}
