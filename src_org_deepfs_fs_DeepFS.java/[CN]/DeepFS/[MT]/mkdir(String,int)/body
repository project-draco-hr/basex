{
  final String method="[-basex_mkdir] ";
  final int n=createNode(path,S_IFDIR.getNativeValue() | mode);
  BaseX.debug(method + "path: " + path+ " mode: "+ Integer.toOctalString(mode)+ " id : ("+ n+ ")");
  refresh();
  return n;
}
