{
  final int name=data.name(pre);
  return data.kind(pre) == Data.ELEM && (data.name(pre) == fileID || data.name(pre) == dirID || name == data.tags.id(DataText.FSML) || name == data.tags.id(DataText.DEEPFS));
}
