{
  ctx=new Context();
  if (!new Open(name).execute(ctx))   new CreateDB("<" + string(DEEPFS) + " "+ S_XMLNSC+ S_FS+ "=\""+ S_FSURL+ "\"/>",name).execute(ctx);
  data=ctx.data();
  initNames();
  initRootStat();
}
