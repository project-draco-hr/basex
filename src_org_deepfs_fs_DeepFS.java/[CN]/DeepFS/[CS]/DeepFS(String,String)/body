{
  ctx=new Context();
  if (!new Open(dbname).execute(ctx))   new CreateDB("<" + string(DEEPFS) + " "+ "mountpoint=\""+ mountpoint+ "\"/>",dbname).execute(ctx);
  data=ctx.data;
  initNames();
  initRootStat();
}
