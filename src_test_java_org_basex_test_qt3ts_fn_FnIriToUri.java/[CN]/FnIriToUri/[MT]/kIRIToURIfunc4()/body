{
  final XQuery query=new XQuery("normalize-space(iri-to-uri((\"somestring\", current-time())[1])) eq \"somestring\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPTY0004")));
}
