{
  final XQuery query=new XQuery("iri-to-uri(\"<> \"\"{}|\\^`\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"%3C%3E%20%22%7B%7D%7C%5C%5E%60"));
}
