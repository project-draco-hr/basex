{
  if (query == null) {
    final IO io=IO.get(args[0]);
    if (!io.exists() || io.isDir())     throw new BaseXException(RES_NOT_FOUND_X,ctx.user.has(Perm.CREATE) ? io : args[0]);
    query=Token.string(io.read());
    ctx.prop.set(Prop.QUERYPATH,io.path());
  }
  return query;
}
