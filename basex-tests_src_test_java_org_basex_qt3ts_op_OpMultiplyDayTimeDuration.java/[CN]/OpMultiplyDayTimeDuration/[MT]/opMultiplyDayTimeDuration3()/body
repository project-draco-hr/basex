{
  final XQuery query=new XQuery("fn:string((xs:dayTimeDuration(\"P20DT20H10M\") * 2.0)) or fn:false()",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
