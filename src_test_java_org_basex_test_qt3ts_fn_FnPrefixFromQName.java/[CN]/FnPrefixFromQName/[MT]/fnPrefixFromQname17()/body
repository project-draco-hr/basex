{
  final XQuery query=new XQuery("fn:starts-with(fn:prefix-from-QName(xs:QName(\"foo:bar\")),\"f\")",ctx);
  query.namespace("foo","http://example.org");
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
