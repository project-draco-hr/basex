{
switch (version) {
case '1':
    path=FSUtils.transformToRegex(path);
  locateTable(FSUtils.getROOTDIR(),out);
break;
case '2':
locateXQuery(out);
break;
default :
path=FSUtils.transformToRegex(path);
locateTable(FSUtils.getROOTDIR(),out);
break;
}
if (cFlag) {
printCount(out);
}
}
