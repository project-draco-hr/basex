{
  final XQuery query=new XQuery("for $x in 9 to 9 return codepoints-to-string($x to $x+1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("'\t\n'"));
}
