{
  final XQuery query=new XQuery("xs:yearMonthDuration(\"P1M\") ne xs:dayTimeDuration(\"P31D\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
