{
  final XQuery query=new XQuery("xs:yearMonthDuration(\"P1Y12M\") eq xs:duration(\"P1Y12M0D\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
