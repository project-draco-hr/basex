{
  final XQuery query=new XQuery("xs:yearMonthDuration('P0Y') eq xs:dayTimeDuration('P0D')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
