{
  final boolean ge=n >= f;
  if (ge) {
    word(tb,n / f,false);
    long r=n % f;
    tb.add(' ').add(unit);
    if (ord)     tb.add(TH);
    if (r > 0) {
      tb.add(' ');
      if (r < 100)       tb.add(AND).add(' ');
    }
    word(tb,r,ord);
  }
  return ge;
}
