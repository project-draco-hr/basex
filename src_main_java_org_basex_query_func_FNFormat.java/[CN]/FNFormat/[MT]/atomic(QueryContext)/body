{
switch (func) {
case FORMINT:
    return formatInt(ctx);
case FORMNUM:
  return formatNum(ctx);
case FORMDTM:
case FORMDAT:
case FORMTIM:
Err.or(NOTIMPL,func.desc);
return null;
default :
return super.atomic(ctx);
}
}
