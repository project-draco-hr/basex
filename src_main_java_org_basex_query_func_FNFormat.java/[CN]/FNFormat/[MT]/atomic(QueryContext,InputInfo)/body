{
switch (def) {
case FORMINT:
    return formatInteger(ctx);
case FORMNUM:
  return formatNumber(ctx);
case FORMDTM:
return formatDate(Type.DTM,ctx);
case FORMDAT:
return formatDate(Type.DAT,ctx);
case FORMTIM:
return formatDate(Type.TIM,ctx);
default :
return super.atomic(ctx,ii);
}
}
