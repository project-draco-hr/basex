{
switch (def) {
case FORMINT:
    return formatInteger(ctx);
case FORMNUM:
  return formatNumber(ctx);
case FORMDTM:
return formatDate(AtomType.DTM,ctx);
case FORMDAT:
return formatDate(AtomType.DAT,ctx);
case FORMTIM:
return formatDate(AtomType.TIM,ctx);
default :
return super.item(ctx,ii);
}
}
