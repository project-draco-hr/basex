{
  for (  final Field f : props.getFields()) {
    final Object obj=f.get(null);
    if (!(obj instanceof Object[]))     continue;
    final Object[] o=(Object[])obj;
    if (o.length == 2)     Collections.addAll(FUNC,o[0].toString().toLowerCase().split("-"));
  }
}
