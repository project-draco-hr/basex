{
  final ByteList tb=new ByteList();
  tb.add('"');
  for (  final byte v : val) {
switch (v) {
case '&':
      tb.add(E_AMP);
    break;
case '>':
  tb.add(E_GT);
break;
case '<':
tb.add(E_LT);
break;
default :
tb.add(v);
}
}
return tb.add('"').toString();
}
