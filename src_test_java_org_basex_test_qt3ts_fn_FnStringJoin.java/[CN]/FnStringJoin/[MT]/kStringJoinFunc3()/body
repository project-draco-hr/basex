{
  final XQuery query=new XQuery("string-join(('Now', 'is', 'the', 'time', '...'), ' ') eq \"Now is the time ...\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
