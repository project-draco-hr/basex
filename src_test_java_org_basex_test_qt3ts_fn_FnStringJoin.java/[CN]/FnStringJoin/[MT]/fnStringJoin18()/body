{
  final XQuery query=new XQuery("fn:string-join((\"string-joinstring-join\"),\"string-join\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"string-joinstring-join"));
}
