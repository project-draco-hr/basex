{
  final XQuery query=new XQuery("string-join(('Blow, ', 'blow, ', 'thou ', 'winter ', 'wind!'), '') eq \"Blow, blow, thou winter wind!\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
