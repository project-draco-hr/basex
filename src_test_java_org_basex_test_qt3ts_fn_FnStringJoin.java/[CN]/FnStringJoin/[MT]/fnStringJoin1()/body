{
  final XQuery query=new XQuery("fn:string-join(('Now', 'is', 'the', 'time', '...'), ' ')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"Now is the time ..."));
}
