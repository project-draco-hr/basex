{
  if (u == FTUnit.WORD)   return w;
  final IntList il=u == FTUnit.SENTENCE ? sen : par;
  if (sen.size == 0) {
    init();
    while (more()) {
      sen.add(sent);
      par.add(para);
    }
  }
  return il.list[w];
}
