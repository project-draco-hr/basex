{
  final int l=text.length;
  pa=false;
  lp=p;
  for (; p < l; p+=cl(text[p])) {
    final int c=cp(text,p);
    if (c == '\n') {
      pa=true;
      p+=cl((byte)' ');
      break;
    }
 else     if (ftChar(c)) {
      break;
    }
  }
  if (lp < p)   return true;
  pos++;
  s=p;
  if (p == l)   return false;
  for (; p < l; p+=cl(text[p])) {
    int c=cp(text,p);
    if (!ftChar(c)) {
      s=p - 1;
      break;
    }
  }
  return true;
}
