{
  for (  final Step s : steps) {
    if (s.preds.size() != 0 || s.axis != Axis.ANC && s.axis != Axis.ANCORSELF && s.axis != Axis.DESC && s.axis != Axis.SELF && s.axis != Axis.DESCORSELF && s.axis != Axis.CHILD && s.axis != Axis.PARENT)     return false;
  }
  return true;
}
