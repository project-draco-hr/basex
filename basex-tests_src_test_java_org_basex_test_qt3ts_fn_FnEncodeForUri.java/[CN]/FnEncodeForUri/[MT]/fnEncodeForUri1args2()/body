{
  final XQuery query=new XQuery("encode-for-uri(\"~b??b??\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"~b%C3%A9b%C3%A9"));
}
