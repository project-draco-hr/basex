{
  final Object entry=props.get(key[0].toString());
  if (entry == null)   Main.notexpected("Property " + key[0] + " not defined.");
  final Class<?> cc=entry.getClass();
  if (c != cc)   Main.notexpected("Property '" + key[0] + "' is a "+ cc.getSimpleName());
  return entry;
}
