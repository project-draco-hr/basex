{
  final Object entry=props.get(key[0].toString());
  if (entry == null)   throw Util.notexpected("Property " + key[0] + " not defined.");
  final Class<?> cc=entry.getClass();
  if (c != cc)   Util.notexpected("Property '" + key[0] + "' is a "+ Util.name(cc));
  return entry;
}
