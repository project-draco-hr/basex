{
  tba.delete(0,3);
  assertEquals("Three nodes deleted => size-3",size - 3,tba.size());
  assertEntrysEqual(3,0,size - 3);
  closeAndReload();
  assertEquals("Three nodes deleted => size-3",size - 3,tba.size());
  assertEntrysEqual(3,0,size - 3);
}
