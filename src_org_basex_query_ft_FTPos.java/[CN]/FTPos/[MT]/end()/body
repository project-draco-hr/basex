{
  if (!end)   return true;
  final int s=ft.count() - 1;
  for (int i=0; i < size; i++) {
    for (int j=0; j < pos[i].size; j++)     if (pos[i].list[j] == s)     return true;
  }
  return false;
}
