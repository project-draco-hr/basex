{
  if (n == size)   return true;
  for (int i=0; i < pos[n].size; i++) {
    final int p=calcPosition(pos[n].list[i],sdunit);
    if (p < bl.size && bl.get(p))     continue;
    bl.set(true,p);
    if (diff(bl,n + 1))     return true;
    bl.set(false,p);
  }
  return false;
}
