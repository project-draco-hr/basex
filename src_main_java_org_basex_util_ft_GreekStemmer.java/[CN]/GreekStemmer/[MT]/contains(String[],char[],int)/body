{
  for (  final String e : strings) {
    final int el=e.length();
    if (l != el)     continue;
    int i=-1;
    while (++i < l && e.charAt(i) == s[i])     ;
    if (i == l)     return true;
  }
  return false;
}
