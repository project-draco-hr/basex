{
  check(_ZIP2_ENTRIES);
  query(COUNT.args(_ZIP2_ENTRIES.args(_FILE_READ_BINARY.args(ZIP))),"5");
  query(COUNT.args(_ZIP2_ENTRIES.args(_FILE_READ_BINARY.args(ZIP)) + "[@size][@last-modified][@compressed-size]"),"5");
}
