{
  check(_ZIP2_EXTRACT_TEXTS);
  query(COUNT.args(_ZIP2_EXTRACT_TEXTS.args(_FILE_READ_BINARY.args(ZIP))),"5");
  query("let $a := " + _FILE_READ_BINARY.args(ZIP) + "let $b := "+ _ZIP2_ENTRIES.args("$a")+ "return "+ COUNT.args(_ZIP2_EXTRACT_TEXTS.args("$a","$b")),5);
  query("let $a := " + _FILE_READ_BINARY.args(ZIP) + "let $b := "+ _ZIP2_EXTRACT_TEXTS.args("$a","test/input.xml")+ "let $c := "+ PARSE_XML.args("$b")+ "return $c//title/text()","XML");
}
