{
  final boolean active=r.w >= 16 && r.h >= 16 && my - r.y < 16 && mx - r.x < 16 && fs.isFile(r.p) && GUIFS.mime(fs.name(r.p)) != GUIFS.Type.IMAGE;
  if (active && click)   fs.launch(View.focused);
  return active;
}
