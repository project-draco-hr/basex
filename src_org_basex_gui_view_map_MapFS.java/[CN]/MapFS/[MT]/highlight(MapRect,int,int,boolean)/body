{
  final Data data=GUI.context.data();
  final boolean active=r.w >= 16 && r.h >= 16 && my - r.y < 16 && mx - r.x < 16 && FSUtils.isFile(data,r.p) && GUIFS.mime(FSUtils.getName(data,r.p)) != GUIFS.Type.IMAGE;
  if (active && click)   FSUtils.launch(data,ViewData.focusedPre);
  return active;
}
