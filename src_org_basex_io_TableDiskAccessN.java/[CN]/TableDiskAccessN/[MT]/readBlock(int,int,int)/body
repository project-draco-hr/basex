{
  try {
    final int b=blocks[ind];
    mbb=fc.map(FileChannel.MapMode.READ_WRITE,b * IO.BLOCKSIZE,IO.BLOCKSIZE);
    block=b;
    index=ind;
    firstPre=first;
    nextPre=next;
  }
 catch (  final IOException ex) {
    ex.printStackTrace();
  }
}
