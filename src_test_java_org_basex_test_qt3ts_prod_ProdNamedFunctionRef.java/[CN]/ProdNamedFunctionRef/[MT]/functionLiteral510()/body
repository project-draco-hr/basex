{
  final XQuery query=new XQuery("Q{http://www.w3.org/2001/XMLSchema}ENTITY#1('ENTITY')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"ENTITY"));
}
