{
  final XQuery query=new XQuery("Q{http://www.w3.org/2001/XMLSchema}hexBinary#1('0fb7')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"0FB7"));
}
