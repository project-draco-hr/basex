{
  final XQuery query=new XQuery("<out>{fn:count(//center/processing-instruction('a-pi'))}</out>",ctx);
  try {
    query.context(node(file("prod/AxisStep/TreeCompass.xml")));
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<out>1</out>",false));
}
