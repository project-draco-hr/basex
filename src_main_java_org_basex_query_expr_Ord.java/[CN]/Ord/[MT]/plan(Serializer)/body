{
  ser.openElement(ORDER_BY);
  ser.attribute(DIR,token(desc ? DESCENDING : ASCENDING));
  ser.attribute(token(EMPTYORD),token(lst ? LEAST : GREATEST));
  ser.finished();
  expr.plan(ser);
  ser.closeElement();
}
