{
  final URL url=new URL(dest);
  final String protocol=url.getProtocol();
  if (!protocol.equalsIgnoreCase("HTTP") && !protocol.equalsIgnoreCase("HTTPS"))   HTTPERR.thrw(ii,"Invalid URL");
  return (HttpURLConnection)url.openConnection();
}
