{
  final XQuery query=new XQuery("<e xmlns:d=\"http://www.example.com/\"/>, d:d",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0081"));
}
