{
  final XQuery query=new XQuery("namespace-uri(<e xmlns=\"http://example.com/{{1}}\"/>)",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"http://example.com/{1}") || error("XQST0046") || error("XQST0022")));
}
