{
  final XQuery query=new XQuery("count(<e xmlns=\"http://example.com/\"><a xmlns=\"\"/></e>/namespace-uri(exactly-one(*)))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
