{
  final XQuery query=new XQuery("let $i := <e xml:space=\"preserve\"/> return <a>{$i/@*}</a>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a xml:space=\"preserve\"/>",false));
}
