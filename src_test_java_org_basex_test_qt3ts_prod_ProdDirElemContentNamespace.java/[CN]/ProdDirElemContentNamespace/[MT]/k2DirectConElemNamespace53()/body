{
  final XQuery query=new XQuery("count(in-scope-prefixes(<a xmlns=\"http://example.com/\"> <e xmlns=\"\"/> </a>/e))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
