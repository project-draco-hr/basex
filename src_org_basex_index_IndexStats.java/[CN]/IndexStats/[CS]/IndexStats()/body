{
  nr=Prop.indexocc;
  occMin=new int[nr];
  occMax=new int[nr];
  txtMin=new byte[nr][];
  txtMax=new byte[nr][];
  for (int o=0; o < txtMin.length; o++) {
    txtMin[o]=Token.EMPTY;
    txtMax[o]=Token.EMPTY;
    occMin[o]=Integer.MAX_VALUE;
  }
}
