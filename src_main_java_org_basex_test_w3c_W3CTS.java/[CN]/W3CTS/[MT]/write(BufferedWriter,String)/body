{
  final BufferedReader br=new BufferedReader(new InputStreamReader(new FileInputStream(f),UTF8));
  for (String line; (line=br.readLine()) != null; ) {
    bw.write(line);
    bw.write(NL);
  }
  br.close();
}
