{
  final XQuery query=new XQuery("fn:normalize-space(\"This\ttext should contains no tabs\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"This text should contains no tabs"));
}
