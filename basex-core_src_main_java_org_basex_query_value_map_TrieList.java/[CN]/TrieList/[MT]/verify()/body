{
  try {
    for (int i=1; i < size; i++) {
      for (int j=i; j-- > 0; ) {
        if (keys[i].sameKey(keys[j],null))         return false;
      }
    }
  }
 catch (  final QueryException ex) {
    return false;
  }
  return true;
}
