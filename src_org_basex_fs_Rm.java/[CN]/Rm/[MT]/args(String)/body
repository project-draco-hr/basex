{
  final FSGetOpts g=new FSGetOpts(args,"R");
  while (g.more()) {
    final int ch=checkOpt(g);
switch (ch) {
case 'R':
      fRecursive=true;
    break;
}
}
path=g.getPath();
if (path == null) error("",100);
}
