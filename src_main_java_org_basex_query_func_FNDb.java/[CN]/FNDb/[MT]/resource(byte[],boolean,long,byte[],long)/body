{
  final String tstamp=formatDate(new Date(mdate),Dtm.FORMAT);
  final FElem res=new FElem(Q_RESOURCE).add(path).add(Q_RAW,token(raw)).add(Q_CTYPE,ctype).add(Q_MDATE,token(tstamp));
  return raw ? res.add(Q_SIZE,token(size)) : res;
}
