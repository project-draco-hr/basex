{
  final String tstamp=formatDate(new Date(mdate),Dtm.FORMAT);
  final FElem res=new FElem(RESOURCE).add(path).add(new FAttr(RAW,token(raw))).add(new FAttr(CTYPE,ctype)).add(new FAttr(MDATE,token(tstamp)));
  return raw ? res.add(new FAttr(SIZE,token(size))) : res;
}
