{
  final String tstamp=Dtm.FORMAT.format(new Date(mdate));
  final FElem res=new FElem(RESOURCE).add(new FTxt(path)).add(new FAttr(RAW,token(raw))).add(new FAttr(CTYPE,ctype)).add(new FAttr(MDATE,token(tstamp)));
  return raw ? res.add(new FAttr(SIZE,token(size))) : res;
}
