{
switch (def) {
case OPENID:
    return open(ctx,true);
case OPENPRE:
  return open(ctx,false);
case SYSTEM:
return system(ctx);
case INFO:
return info(ctx);
case IDXINFO:
return indexInfo(ctx);
default :
return super.item(ctx,ii);
}
}
