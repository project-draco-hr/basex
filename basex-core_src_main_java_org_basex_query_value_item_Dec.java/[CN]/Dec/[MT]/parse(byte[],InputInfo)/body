{
  try {
    if (!contains(val,'e') && !contains(val,'E'))     return new BigDecimal(Token.string(val).trim());
  }
 catch (  final NumberFormatException ignored) {
  }
  throw FUNCAST.get(ii,AtomType.DEC,chop(val));
}
