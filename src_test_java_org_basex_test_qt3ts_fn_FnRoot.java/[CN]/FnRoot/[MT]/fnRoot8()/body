{
  final XQuery query=new XQuery("let $var := <?format role=\"output\" ?> return fn:root($var)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<?format role=\"output\" ?>",false));
}
