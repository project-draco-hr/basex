{
  final XQuery query=new XQuery("let $var := <!-- A Comment Node --> return fn:root($var)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<!-- A Comment Node -->",false));
}
