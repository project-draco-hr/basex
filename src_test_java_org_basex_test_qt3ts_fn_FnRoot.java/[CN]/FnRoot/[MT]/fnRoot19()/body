{
  final XQuery query=new XQuery("let $var := element anElement {\"Element Content\"} return fn:root($var) is fn:root($var)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
