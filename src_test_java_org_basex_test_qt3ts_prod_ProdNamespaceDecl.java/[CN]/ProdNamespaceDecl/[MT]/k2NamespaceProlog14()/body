{
  final XQuery query=new XQuery("declare variable $inputDoc := 2; declare namespace x = \"http://example.com/\"; 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
