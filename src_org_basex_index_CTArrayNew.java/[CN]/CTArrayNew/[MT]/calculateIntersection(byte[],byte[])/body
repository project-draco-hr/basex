{
  int i=0;
  if (b1 != null && b2 != null) {
    final int minLength=Math.min(b1.length,b2.length);
    for (; i < minLength; i++) {
      if (b1[i] != b2[i]) {
        return getBytes(b1,0,i);
      }
    }
    return getBytes(b1,0,i);
  }
  return null;
}
