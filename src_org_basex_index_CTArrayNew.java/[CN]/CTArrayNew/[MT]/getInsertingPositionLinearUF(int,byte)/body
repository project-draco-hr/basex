{
  found=false;
  int i=1;
  int s=nextL.list[cn].length - 2;
  if (s == i)   return i;
  while (i < s && tokensL.list[nextL.list[nextL.list[cn][i]][0]][0] < toInsert)   i++;
  if (i < s && tokensL.list[nextL.list[nextL.list[cn][i]][0]][0] == toInsert) {
    found=true;
  }
  return i;
}
