{
  if (type.unt())   check(NodeType.DEL,NodeType.ELM,NodeType.ATT,AtomType.ATM);
  if (base != -1)   return base;
  for (int b=0; b < BASE.length; ++b)   if (BASE[b] == type)   return b;
  throw new BXQException(NOBASE);
}
