{
  if (type != Type.ELM && type != Type.ATT && type.unt)   throw new BXQException("Node must be element, attribute or atomic.");
  for (int b=0; b < BASE.length; b++) {
    if (BASE[b] == type)     return b;
  }
  throw new BXQException("Item has no base type.");
}
