{
  if (type != Type.ELM && type != Type.ATT && type.unt)   throw new BXQException(ELMATT);
  for (int b=0; b < BASE.length; b++) {
    if (BASE[b] == type)     return b;
  }
  throw new BXQException(NOBASE);
}
