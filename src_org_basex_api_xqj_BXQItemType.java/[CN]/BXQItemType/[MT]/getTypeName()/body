{
  if (type != Type.ELM && type != Type.ATT && type != Type.ATM && type.unt)   throw new BXQException(ELMATT);
  final Type t=base != -1 ? BASE[base] : type;
  return new QName(Token.string(XQTokens.XSURI),Token.string(t.name));
}
