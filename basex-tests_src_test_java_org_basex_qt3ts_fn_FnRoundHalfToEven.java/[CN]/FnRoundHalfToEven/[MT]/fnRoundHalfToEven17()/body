{
  final XQuery query=new XQuery("round-half-to-even(123.355, 2)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("123.36") && assertType("xs:decimal")));
}
