{
  final XQuery query=new XQuery("round-half-to-even(xs:float('0.05'), 1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("0.1") && assertType("xs:float")));
}
