{
synchronized (list) {
    for (    final PData d : list) {
      if (d.data == data) {
        final boolean close=--d.pins == 0;
        if (close) {
          list.remove(d);
        }
        return close;
      }
    }
    return false;
  }
}
