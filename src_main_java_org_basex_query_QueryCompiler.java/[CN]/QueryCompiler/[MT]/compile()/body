{
  for (  final Scope[] comp : components(0))   circCheck(comp).compile(ctx);
  for (  final StaticVar v : ctx.vars)   if (id(v) == -1)   for (  final Scope[] comp : components(add(v)))   circCheck(comp);
}
