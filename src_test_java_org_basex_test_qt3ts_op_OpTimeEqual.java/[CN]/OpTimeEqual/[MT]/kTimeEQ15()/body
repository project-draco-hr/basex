{
  final XQuery query=new XQuery("xs:date(\"1999-12-04\") eq xs:time(\"12:12:23\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
