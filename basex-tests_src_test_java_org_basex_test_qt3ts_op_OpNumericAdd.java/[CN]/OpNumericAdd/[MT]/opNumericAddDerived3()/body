{
  final XQuery query=new XQuery("xs:nonNegativeInteger(10) + xs:short(15)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("25"));
}
