{
  final XQuery query=new XQuery("\n" + "        declare construction strip; \n" + "        declare construction strip; \n"+ "        \"abc\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0067"));
}
