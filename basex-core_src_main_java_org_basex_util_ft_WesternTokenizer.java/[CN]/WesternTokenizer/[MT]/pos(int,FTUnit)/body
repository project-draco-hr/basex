{
  if (u == FTUnit.WORDS)   return w;
  final IntList il=u == FTUnit.SENTENCES ? sen : par;
  if (sen.isEmpty()) {
    init();
    while (more()) {
      sen.add(sent);
      par.add(para);
    }
  }
  return il.get(w);
}
