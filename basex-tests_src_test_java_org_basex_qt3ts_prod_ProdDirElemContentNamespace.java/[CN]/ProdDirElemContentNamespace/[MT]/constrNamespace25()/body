{
  final XQuery query=new XQuery("<elem xmlns=\"http://www.example.com{'/namespace'}\"/>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0022"));
}
