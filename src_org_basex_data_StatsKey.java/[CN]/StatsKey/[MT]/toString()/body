{
switch (kind) {
case CAT:
    return ", " + cats.size() + " values";
case DBL:
  return ", numeric(" + min + " - "+ max+ ")";
case INT:
return ", numeric(" + (int)min + " - "+ (int)max+ ")";
case TEXT:
return ", strings";
default :
return "";
}
}
