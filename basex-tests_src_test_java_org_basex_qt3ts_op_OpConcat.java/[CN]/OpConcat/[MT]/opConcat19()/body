{
  final XQuery query=new XQuery("12 || 34 - 50",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("\"12-16\"") && assertType("xs:string")));
}
