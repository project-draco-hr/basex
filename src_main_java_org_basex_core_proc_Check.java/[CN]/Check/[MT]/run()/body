{
  new Close().run(context);
  final String path=args[0];
  final String db=IO.get(path).dbname();
  final Proc p=MetaData.found(path,db,context.prop) ? new Open(db) : new CreateDB(path);
  final boolean ok=p.run(context);
  info(p.info());
  return ok;
}
