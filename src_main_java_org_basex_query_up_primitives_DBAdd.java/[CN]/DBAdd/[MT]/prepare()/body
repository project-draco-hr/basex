{
  md=new MemData(data);
  for (int i=0; i < docs.size(); i++) {
    md.insert(md.meta.size,-1,docData(docs.get(i),paths.get(i)));
    docs.set(i,null);
    paths.set(i,null);
    size++;
  }
  docs=null;
  paths=null;
}
