{
  md=new MemData(tmp);
  for (int i=0; i < docs.size(); i++) {
    md.insert(md.meta.size,-1,new DataClip(docData(docs.get(i),paths.get(i),ctx,data.meta.name)));
    docs.set(i,null);
    paths.set(i,null);
    size++;
  }
  docs=null;
  paths=null;
}
