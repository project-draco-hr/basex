{
  final int attl=attRef != null ? attRef.length >> 1 : 0;
  data.addElem(tag,tns,par,attl + 1,attl + 1,kind);
  for (int a=0; a < attl; a++) {
    data.addAtt(attRef[a << 1],attRef[(a << 1) + 1],atr[(a << 1) + 1],a + 1);
  }
  size=data.size;
}
