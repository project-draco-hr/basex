{
  meta=new MetaData(db);
  meta.txtindex=true;
  meta.atvindex=true;
  meta.ftxindex=false;
  meta.file=parser.file;
  stats=new Stats(db);
  data=new MemData(64,tags,atts);
  data.meta=meta;
  return this;
}
