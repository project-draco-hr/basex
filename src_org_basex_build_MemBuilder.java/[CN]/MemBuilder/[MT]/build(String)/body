{
  data=new MemData(tags,atts,ns,path,parser.prop);
  meta=data.meta;
  meta.name=db;
  meta.txtindex=true;
  meta.atvindex=true;
  meta.ftxindex=false;
  meta.file=parser.io;
  parse(db);
  data.init();
  return data;
}
