{
  final byte[] ln=type.ln();
  for (  final Type t : Type.values()) {
    if (t.par != null && Levenshtein.similar(lc(ln),lc(t.name)))     Err.or(FUNSIMILAR,ln,t.name);
  }
  Err.or(FUNCUNKNOWN,type.str());
}
