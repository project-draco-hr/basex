{
  final byte[] nm=Token.lc(name.ln());
  FNIndex.get().error(nm);
  for (int n=0; n < size; n++) {
    if (Levenshtein.similar(nm,Token.lc(func[n].var.name.ln())))     Err.or(FUNSIMILAR,name.str(),func[n].var.name.str());
  }
}
