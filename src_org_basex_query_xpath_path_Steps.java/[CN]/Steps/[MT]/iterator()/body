{
  return new Iterator<Step>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public Step next(){
      return steps[c];
    }
    public void remove(){
      BaseX.notimplemented();
    }
  }
;
}
