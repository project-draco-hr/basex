{
  final Geometry geom=gmlReader(node,Q_GML_POLYGON);
  if (geom == null && gmlReader(node) != null)   throw GeoErrors.polygonNeeded(node.qname().local());
  return gmlWriter(((Polygon)geom).getExteriorRing());
}
