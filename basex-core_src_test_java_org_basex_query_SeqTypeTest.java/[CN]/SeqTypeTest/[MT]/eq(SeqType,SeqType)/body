{
  if (r == null) {
    assertNull("\nExpected: null\nReturned: " + s,s);
  }
 else {
    assertNotNull("\nExpected: " + r + "\nReturned: "+ s,s);
    assertTrue("\nExpected: " + r + "\nReturned: "+ s,s.eq(r));
  }
}
