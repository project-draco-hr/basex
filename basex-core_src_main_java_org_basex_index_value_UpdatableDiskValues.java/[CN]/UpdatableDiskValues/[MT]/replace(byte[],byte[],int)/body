{
  final int p=get(old);
  if (p >= 0) {
    final int[] tmp={id};
    if (deleteIds(p,old,tmp) == 0) {
      cache.delete(old);
      tmp[0]=p;
      deleteKeys(tmp);
    }
  }
  insertId(key,id);
}
