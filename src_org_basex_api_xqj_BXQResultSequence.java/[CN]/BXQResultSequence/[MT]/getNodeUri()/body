{
  if (item.node()) {
    Node node=(Node)item;
    try {
      return new URI(node.getBaseURI());
    }
 catch (    URISyntaxException ex) {
      throw new BXQException(ex.toString());
    }
  }
  return null;
}
