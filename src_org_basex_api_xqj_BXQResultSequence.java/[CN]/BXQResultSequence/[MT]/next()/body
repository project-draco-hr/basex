{
  checkIfClosed();
  if (pos < 0)   return false;
  try {
    item=result.next();
    if (item == null) {
      pos=-1;
      return false;
    }
 else {
      pos++;
      return true;
    }
  }
 catch (  XQException ex) {
    throw new BXQException(ex);
  }
}
