{
  final XQuery query=new XQuery("declare variable $e := <e> <a>3</a> <a>2</a> <a>1</a> </e>; exactly-one(for $i in $e/a order by $i return $i)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("FORG0005"));
}
