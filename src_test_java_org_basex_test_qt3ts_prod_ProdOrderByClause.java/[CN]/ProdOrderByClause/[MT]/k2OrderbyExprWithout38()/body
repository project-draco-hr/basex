{
  final XQuery query=new XQuery("for $i in (1, 2, 3) stable order by 1 return reverse(($i, \"FO\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"FO 1 FO 2 FO 3"));
}
