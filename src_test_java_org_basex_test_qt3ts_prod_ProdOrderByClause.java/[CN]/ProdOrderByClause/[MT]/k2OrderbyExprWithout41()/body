{
  final XQuery query=new XQuery("for $a in (3, 2, 1) let $b := (2, 1), $c := (2, 1), $d := (2, 1), $e := (2, 1) order by $a return $a",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"1 2 3"));
}
