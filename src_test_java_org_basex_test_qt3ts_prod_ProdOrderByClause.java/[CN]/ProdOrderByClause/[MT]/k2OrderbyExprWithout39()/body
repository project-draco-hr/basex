{
  final XQuery query=new XQuery("for $a in (1, 4, 2) let $i := (1, 3, 2) order by $i return 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"1 1 1") || error("XPTY0004")));
}
