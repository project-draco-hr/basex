{
  final XQuery query=new XQuery("for $x in (\"A\",\"B\",\"C\") order by string($x) ascending collation \"http://nonexistentcollition.org/ifsupportedwoooayouarethebestQueryimplementation/makeitharder\" return string($x)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0076"));
}
