{
  final XQuery query=new XQuery("let $i := (xs:hexBinary(\"FF\"), xs:hexBinary(\"FF\")) stable order by $i return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"FF FF") || error("XPTY0004")));
}
