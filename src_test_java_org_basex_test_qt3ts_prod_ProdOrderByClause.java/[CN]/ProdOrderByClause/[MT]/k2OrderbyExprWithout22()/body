{
  final XQuery query=new XQuery("for $i in (1, 3, 2) let $c := 3 stable order by () return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"1 3 2") || assertStringValue(false,"1 2 3") || error("XPST0005")));
}
