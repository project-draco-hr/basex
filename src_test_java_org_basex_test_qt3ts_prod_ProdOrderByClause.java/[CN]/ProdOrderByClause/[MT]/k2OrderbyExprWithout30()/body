{
  final XQuery query=new XQuery("<r> { for $i in 1 order by () return () } </r>",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertSerialization("<r/>",false) || error("XPST0005")));
}
