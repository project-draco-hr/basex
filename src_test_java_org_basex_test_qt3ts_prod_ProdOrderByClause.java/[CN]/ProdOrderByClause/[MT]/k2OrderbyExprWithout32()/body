{
  final XQuery query=new XQuery("let $i := (1, 2, 3) stable order by $i return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"1 2 3") || error("XPTY0004")));
}
