{
  final XQuery query=new XQuery("for $a in (3, 2, 1), $b in (6, 5, 4) order by $a return $a",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"1 1 1 2 2 2 3 3 3"));
}
