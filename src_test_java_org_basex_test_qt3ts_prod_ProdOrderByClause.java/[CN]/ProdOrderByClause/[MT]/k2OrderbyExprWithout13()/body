{
  final XQuery query=new XQuery("for $a in (3, 2, 1), $b in (6, 5, 4) stable order by $b return $a",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"3 2 1 3 2 1 3 2 1"));
}
