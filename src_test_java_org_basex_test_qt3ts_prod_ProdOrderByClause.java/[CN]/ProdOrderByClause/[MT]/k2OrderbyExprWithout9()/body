{
  final XQuery query=new XQuery("let $i := (1, 3, 2), $b := (4, 6, 5) order by $b return $b",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"4 6 5") || error("XPTY0004")));
}
