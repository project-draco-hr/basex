{
  final XQuery query=new XQuery("for $i in (false(), true(), true(), false(), true(), false()) order by $i return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"false false false true true true"));
}
