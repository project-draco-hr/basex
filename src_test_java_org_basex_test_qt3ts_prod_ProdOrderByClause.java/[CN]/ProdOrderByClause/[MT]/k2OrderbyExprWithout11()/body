{
  final XQuery query=new XQuery("for $a in (1, 4, 2) let $i := (1, 3, 2) order by $a return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"1 3 2 1 3 2 1 3 2"));
}
