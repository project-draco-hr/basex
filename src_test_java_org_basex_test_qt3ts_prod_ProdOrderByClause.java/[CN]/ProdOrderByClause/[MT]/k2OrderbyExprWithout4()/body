{
  final XQuery query=new XQuery("let $i as xs:integer* := (1, 2, 3) order by 1 collation \"http:\\\\invalid%>URI\\someURI\" return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0046"));
}
