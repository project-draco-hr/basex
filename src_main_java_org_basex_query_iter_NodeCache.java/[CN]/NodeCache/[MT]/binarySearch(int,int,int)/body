{
  int l=start, r=start + length - 1;
  while (l <= r) {
    final int m=l + r >>> 1;
    final int npre=((DBNode)item[m]).pre;
    if (npre == pre)     return m;
    if (npre < pre)     l=m + 1;
 else     r=m - 1;
  }
  return -(l + 1);
}
