{
  file=new File(Prop.HOME + ".basexevents");
  try {
    if (file.exists()) {
      final DataInput in=new DataInput(file);
      read(in);
      in.close();
    }
 else {
      write();
    }
  }
 catch (  final IOException ex) {
    Util.debug(ex);
  }
}
