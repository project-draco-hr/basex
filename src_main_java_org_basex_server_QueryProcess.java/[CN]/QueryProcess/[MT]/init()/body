{
  qp.parse();
  if (!qp.ctx.updating)   startTimeout(ctx.main.prop.num(Prop.TIMEOUT));
  monitored=true;
  ctx.lock.before(qp.ctx.updating);
  iter=qp.iter();
  item=iter.next();
  xml=qp.getSerializer(out);
}
