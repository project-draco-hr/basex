{
  final long to=ctx.prop.num(Prop.TIMEOUT);
  if (to == 0)   return;
  timeout=new Thread(){
    @Override public void run(){
      Performance.sleep(10);
      running=false;
    }
  }
;
  timeout.start();
}
