{
  Item item;
  try {
    if ((item=iter.next()) != null) {
      send(true);
      item.serialize(serializer);
      send(true);
    }
 else {
      send(false);
      close();
    }
  }
 catch (  Exception ex) {
    send(false);
    close();
  }
}
