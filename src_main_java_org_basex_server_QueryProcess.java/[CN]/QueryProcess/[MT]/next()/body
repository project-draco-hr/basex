{
  if (xml == null)   init();
  if (stopped)   SERVERTIMEOUT.thrw(null);
  item=iter.next();
  if (item != null) {
    xml.init();
    xml.openResult();
    item.serialize(xml);
    xml.closeResult();
  }
}
