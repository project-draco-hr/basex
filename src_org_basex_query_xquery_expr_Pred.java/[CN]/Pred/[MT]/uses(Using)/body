{
switch (u) {
case POS:
    for (    final Expr e : expr) {
      final Type t=e.returned();
      if (t == null || t.num || e.uses(u))       return false;
    }
  return super.uses(u);
default :
return super.uses(u);
}
}
