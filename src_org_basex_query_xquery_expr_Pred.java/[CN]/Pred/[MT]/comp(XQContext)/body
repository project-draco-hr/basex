{
  root=root.comp(ctx);
  super.comp(ctx);
  if (expr.length > 1)   return this;
  final boolean last=expr[0] instanceof Fun && ((Fun)expr[0]).func == FunDef.LAST;
  final boolean num=expr[0].n();
  return !last && !num && uses(Using.POS) ? this : new PredIter(root,expr,last,num);
}
