{
  root=ctx.comp(root);
  super.comp(ctx);
  final boolean last=expr[0] instanceof Fun && ((Fun)expr[0]).func == FunDef.LAST;
  final boolean num=expr[0].i() && ((Item)expr[0]).n();
  return expr.length > 1 || (!last && !num && uses(Using.POS)) ? this : new PredIter(root,expr,last,num);
}
