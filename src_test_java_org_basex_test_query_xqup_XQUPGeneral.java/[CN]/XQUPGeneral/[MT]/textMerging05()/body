{
  createDB();
  new XQuery("let $i := //item[@id='item0']/location return " + "(insert node 'foo' into $i, delete node $i/text())").execute(CONTEXT);
  final String r=new XQuery("//item[@id='item0']/location/text()").execute(CONTEXT);
  assertEquals("foo",n(r));
}
