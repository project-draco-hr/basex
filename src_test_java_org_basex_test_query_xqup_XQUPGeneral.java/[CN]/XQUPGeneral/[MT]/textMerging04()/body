{
  createDB();
  new XQuery("let $i := //item[@id='item0'] return insert node 'foo' " + "before $i/location/text()").execute(CONTEXT);
  final String r=new XQuery("let $i := //item[@id='item0'] return " + "($i/location/text())[1]").execute(CONTEXT);
  assertEquals("fooUnited States",n(r));
}
