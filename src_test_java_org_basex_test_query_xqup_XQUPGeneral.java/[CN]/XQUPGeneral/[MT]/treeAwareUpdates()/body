{
  final String r=new XQuery("copy $n := <a><b><c/></b></a> " + "modify (replace value of node $n/b with (), " + "insert node <d/> into $n/b, insert node <d/> after $n/b) return $n").execute(CONTEXT);
  assertEquals("<a>\n  <b/>\n  <d/>\n</a>",r);
}
