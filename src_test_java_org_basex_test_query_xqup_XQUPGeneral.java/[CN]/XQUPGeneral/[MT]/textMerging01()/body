{
  createDB();
  new XQuery("insert node 'foo' into //item[@id='item0']/location").execute(CONTEXT);
  final String r=new XQuery("(//item[@id='item0']/location/text())[1]").execute(CONTEXT);
  assertEquals("United Statesfoo",r);
}
