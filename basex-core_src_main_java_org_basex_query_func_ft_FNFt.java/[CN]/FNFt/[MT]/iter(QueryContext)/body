{
switch (func) {
case _FT_SEARCH:
    return search(qc);
case _FT_SCORE:
  return score(qc);
case _FT_MARK:
return mark(qc,false);
case _FT_EXTRACT:
return mark(qc,true);
case _FT_TOKENS:
return tokens(qc);
case _FT_TOKENIZE:
return tokenize(qc);
default :
return super.iter(qc);
}
}
