{
  final XQuery query=new XQuery("declare boundary-space preserve; declare boundary-space strip; \"abc\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0068"));
}
