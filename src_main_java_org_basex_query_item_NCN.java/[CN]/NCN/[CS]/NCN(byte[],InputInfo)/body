{
  super(Token.norm(v),AtomType.NCN);
  if (v.length == 0)   XPNAME.thrw(ii);
  int i=-1;
  while (++i != v.length) {
    final byte c=v[i];
    if (Token.letter(c))     continue;
    if (i == 0 || !Token.digit(c) && c != '-' && c != '_' && c != '.')     XPINVNAME.thrw(ii,v);
  }
}
