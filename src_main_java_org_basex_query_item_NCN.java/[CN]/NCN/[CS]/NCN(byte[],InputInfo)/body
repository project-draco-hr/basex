{
  super(Token.norm(v),Type.NCN);
  if (v.length == 0)   Err.or(ii,XPNAME);
  int i=-1;
  while (++i != v.length) {
    final byte c=v[i];
    if (Token.letter(c))     continue;
    if (i == 0 || !Token.digit(c) && c != '-' && c != '_' && c != '.')     Err.or(ii,XPINVNAME,v);
  }
}
