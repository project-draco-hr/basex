{
  final XQuery query=new XQuery("count((1, 2, 3, timezone-from-time(current-time()), 4)) gt 0",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
