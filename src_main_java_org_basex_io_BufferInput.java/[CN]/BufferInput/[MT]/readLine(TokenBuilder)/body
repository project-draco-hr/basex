{
  tb.reset();
  while (true) {
    final int ch=readChar();
    if (ch == 0x0D)     continue;
    if (ch == 0x00)     return tb.size() != 0;
    if (ch == 0x0A)     return true;
    tb.addUTF(ch);
  }
}
