{
  final XQuery query=new XQuery("format-number(123456789.34, '#,###.##')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"123,456,789.34"));
}
