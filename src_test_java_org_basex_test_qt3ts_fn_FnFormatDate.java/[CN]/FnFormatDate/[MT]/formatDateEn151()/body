{
  final XQuery query=new XQuery("format-date($b, '[MNn]', 'xib', (), ())",ctx);
  query.bind("b",new XQuery("xs:date('2006-03-01')",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"[Language: en]March"));
}
