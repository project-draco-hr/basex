{
  final XQuery query=new XQuery("format-date($t, '[M,*-2]')",ctx);
  query.bind("t",new XQuery("xs:date('0985-03-01')",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"3"));
}
