{
  final XQuery query=new XQuery("format-date(current-date(), '[yY]', 'en', (), ())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOFD1340"));
}
