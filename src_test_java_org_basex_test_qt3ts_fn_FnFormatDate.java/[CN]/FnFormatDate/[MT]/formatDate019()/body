{
  final XQuery query=new XQuery("format-date(xs:date('1987-12-13+05:30'), '[ZZ]')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"+05:30"));
}
