{
  final XQuery query=new XQuery("deep-equal((attribute name {\"content\"}, attribute name {\"content\"}), (attribute name {\"content\"}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
