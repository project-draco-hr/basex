{
  final XQuery query=new XQuery("deep-equal((current-time(), current-time(), current-time()), (current-time(), current-time()))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
