{
  final XQuery query=new XQuery("deep-equal(attribute name {\"content \"}, attribute name2 {\"content \"})",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(false));
}
