{
  final XQuery query=new XQuery("declare variable $d1 := document {()}; declare variable $d2 := document {<?target data?>}; deep-equal($d1, $d2)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
