{
  return new Iterator<byte[]>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public byte[] next(){
      return list[c];
    }
    public void remove(){
      BaseX.notexpected();
    }
  }
;
}
