{
  DataInput di=null;
  try {
    di=new DataInput(dbfile(DATAINF));
    read(di);
  }
  finally {
    if (di != null)     try {
      di.close();
    }
 catch (    final IOException ignored) {
    }
  }
}
