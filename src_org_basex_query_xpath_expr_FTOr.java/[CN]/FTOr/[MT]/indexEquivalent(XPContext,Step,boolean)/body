{
  if (pex.length == 2 && seq)   exprs[pex[0]].indexEquivalent(ctx,curr,seq);
  for (int i=0; i < exprs.length; i++) {
    exprs[i]=exprs[i].indexEquivalent(ctx,curr,seq);
  }
  if (pex.length == 1) {
    return exprs[pex[0]];
  }
  return new FTUnion(exprs,pex);
}
