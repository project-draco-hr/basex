{
  mouseX=e.getX();
  mouseY=e.getY();
  if (mouseX < XMARGIN || mouseX > getWidth() - XMARGIN + ITEMSIZE / 2 || mouseY < YMARGIN - ITEMSIZE / 2 || mouseY > getHeight() - YMARGIN) {
    valueFocused=false;
    focusedItem=-1;
    repaint();
  }
 else {
    focus();
    repaint();
  }
}
