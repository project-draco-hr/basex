{
  super.paintComponent(g);
  final int w=getWidth();
  final int h=getHeight();
  if (w + h != viewDimension) {
    viewDimension=w + h;
    plotChanged=true;
  }
  drawBox(g);
  if (scatterData.size == 0)   return;
  if (plotImg == null || plotChanged)   plotImg=createPlotImage();
  g.drawImage(plotImg,0,0,this);
  if (focusedItem != -1)   drawItem(g,scatterData.x[focusedItem],scatterData.y[focusedItem],true);
  plotChanged=false;
}
