{
  final ScatterAxis axis=drawX ? scatterData.xAxis : scatterData.yAxis;
  final byte[] val=axis.getValue(focused);
  if (val.length == 0)   return "";
  return axis.numType == Kind.TEXT ? string(val) : formatString(toDouble(val),drawX);
}
