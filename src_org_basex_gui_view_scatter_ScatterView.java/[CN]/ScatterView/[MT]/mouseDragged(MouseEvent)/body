{
  if (!dragging) {
    dragging=true;
    selectionBox.setStart(mouseX,mouseY);
    focusedItem=-1;
  }
  mouseX=e.getX();
  mouseY=e.getY();
  selectionBox.setEnd(mouseX,mouseY);
  focus();
  repaint();
}
