{
  final XQuery query=new XQuery("<elem attr=\"val1\" attr2=\"val2\" attr=\"val3\"/>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0040"));
}
