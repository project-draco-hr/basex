{
  final XQuery query=new XQuery("namespace-uri-from-QName(node-name(exactly-one(<e xmlns=\"http://example.com/\">{attribute attr {()} }/</e>/@attr))) eq \"\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
