{
  final XQuery query=new XQuery("<foo attr=\"{<foo attr=\"foo\"/>}\"/>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<foo attr=\"\"/>",false));
}
