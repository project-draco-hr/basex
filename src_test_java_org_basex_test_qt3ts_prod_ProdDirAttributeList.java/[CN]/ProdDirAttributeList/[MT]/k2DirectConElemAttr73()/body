{
  final XQuery query=new XQuery("declare default element namespace \"http://example.com/\"; namespace-uri-from-QName(node-name(attribute e {()})) eq \"\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
