{
  final XQuery query=new XQuery("count((<a>0</a>,<a>1</a>) intersect (<a>3</a>,<a>4</a>))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("0"));
}
