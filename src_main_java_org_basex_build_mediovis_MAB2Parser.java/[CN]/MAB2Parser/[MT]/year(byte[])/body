{
  final byte[] n=new byte[4];
  final int l=line.length;
  int c=0;
  for (int i=4; i < l; i++) {
    final byte b=line[i];
    if (b >= '0' && b <= '9') {
      n[c++]=b;
      if (c == 4)       return n;
    }
  }
  return c != 0 ? Arrays.copyOf(n,c) : null;
}
