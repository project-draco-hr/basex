{
  if (kind == Kind.ALL)   return "*";
  if (kind == Kind.NAME)   return "*:" + string(name.str());
  final String uri=name.uri == Uri.EMPTY || name.ns() ? "" : "{" + string(name.uri.str()) + "}";
  return uri + (kind == Kind.NS ? "*" : string(name.str()));
}
