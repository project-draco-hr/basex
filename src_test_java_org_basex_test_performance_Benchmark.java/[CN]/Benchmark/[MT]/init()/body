{
  server=!local && !BaseXServer.ping(LOCALHOST,CONTEXT.mprop.num(MainProp.SERVERPORT)) ? new BaseXServer("-z") : null;
  session=local ? new LocalSession(CONTEXT) : new ClientSession(CONTEXT,Text.ADMIN,Text.ADMIN);
  session.execute(new Set(Prop.QUERYINFO,true));
}
