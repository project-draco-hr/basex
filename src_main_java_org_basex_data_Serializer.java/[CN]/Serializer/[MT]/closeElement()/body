{
  if (tags.size() == 0)   throw new IOException("All elements closed.");
  final byte[] open=tags.pop();
  ns.size=nsl.pop();
  if (inTag) {
    inTag=false;
    empty();
  }
 else {
    close(open);
  }
}
