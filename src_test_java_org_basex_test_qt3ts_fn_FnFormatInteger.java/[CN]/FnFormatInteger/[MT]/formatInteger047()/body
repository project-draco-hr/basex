{
  final XQuery query=new XQuery("string-join(for $i in 1 to 5 return format-integer($i, '???'), '|')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("'???|???|???|???|???'"));
}
