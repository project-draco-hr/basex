{
  final XQuery query=new XQuery("format-integer((), 'Ww')",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("''") && assertType("xs:string") && assertQuery("count($result) = 1")));
}
