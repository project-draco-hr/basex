{
  final XQuery query=new XQuery("format-integer(1500000, '#,###,000')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("'1,500,000'"));
}
