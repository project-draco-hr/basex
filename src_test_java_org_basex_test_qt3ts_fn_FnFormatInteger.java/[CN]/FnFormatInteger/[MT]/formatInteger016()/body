{
  final XQuery query=new XQuery("string-join(for $i in 1 to 10 return format-integer($i, 'Ww'), '|')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("'One|Two|Three|Four|Five|Six|Seven|Eight|Nine|Ten'"));
}
