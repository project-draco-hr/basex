{
  final long[] t=new long[n];
  t[0]=nextClearBit(i);
  for (int k=0; k < n; k++)   if ((t[k]=nextClearBit(t[k - 1] + 1L)) >= last) {
    final long[] r=new long[k];
    System.arraycopy(t,0,r,0,k);
    return r;
  }
  return t;
}
