{
  final int[] t=new int[n];
  t[0]=nextClearBit(i);
  for (int k=1; k < n; k++)   if ((t[k]=nextClearBit(t[k - 1] + 1)) >= last) {
    final int[] r=new int[k];
    System.arraycopy(t,0,r,0,k);
    return r;
  }
  return t;
}
