{
  final XQuery query=new XQuery("for $i in 1 where count(($i, 2, timezone-from-time(current-time()))) return true()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
