{
  final XQuery query=new XQuery("let $var := <anElement>Some content</anElement> where $var is $var return $var is $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
