{
  final XQuery query=new XQuery("for $i in (1, 2, current-time())[1] where fn:boolean($i treat as xs:integer) return true()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
