{
  final XQuery query=new XQuery("\n" + "         let $doc := (/)\n" + "         return (11 to 15)!\n"+ "            (for $file at $c in $doc//File\n"+ "            where $c = last()\n"+ "            return string($file/FileName))\n"+ "      ",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"File00000000004 File00000000004 File00000000004 File00000000004 File00000000004"));
}
