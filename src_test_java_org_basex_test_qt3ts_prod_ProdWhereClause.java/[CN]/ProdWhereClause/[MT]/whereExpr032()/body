{
  final XQuery query=new XQuery("\n" + "         for $x allowing empty in 1 to 5\n" + "         where $x lt 0\n"+ "         return empty($x)\n"+ "      ",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEmpty());
}
