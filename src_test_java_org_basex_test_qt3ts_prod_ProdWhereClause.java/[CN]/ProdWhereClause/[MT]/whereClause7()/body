{
  final XQuery query=new XQuery("let $var := \"String\" where fn:string-length($var) = 6 return fn:string-length($var)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("6"));
}
