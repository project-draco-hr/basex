{
  final XQuery query=new XQuery("let $var := (fn:false()) where fn:not($var and fn:false()) return $var and fn:false()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
