{
  final XQuery query=new XQuery("for $folder in //Folder where $folde/@name = \"ABC\" return <folder/>",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
