{
  final XQuery query=new XQuery("string(exactly-one((for $fo in (1, 2, 3) where $fo eq 3 return $fo)))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"3"));
}
