{
  try {
    final Iter iter=iter(root);
    finish();
    return iter;
  }
 catch (  final StackOverflowError e) {
    if (Prop.debug)     e.printStackTrace();
    Err.or(XPSTACK);
    return null;
  }
}
