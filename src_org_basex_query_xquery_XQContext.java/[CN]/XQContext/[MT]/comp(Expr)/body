{
  final Expr ex=e.comp(this);
  if (inf && ex != e && !(e instanceof VarCall) && !(e instanceof Cast) && !(e instanceof List) && !(ex.getClass().getName().contains("Iter"))) {
    compInfo(OPTSIMPLE,e,ex);
  }
  return ex;
}
