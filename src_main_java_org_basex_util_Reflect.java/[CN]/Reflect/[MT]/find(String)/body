{
  for (  final String n : names) {
    try {
      Class<?> c=classes.get(n);
      if (c == null) {
        c=Class.forName(n);
        classes.put(n,c);
      }
      return c;
    }
 catch (    final Exception ex) {
    }
  }
  return null;
}
