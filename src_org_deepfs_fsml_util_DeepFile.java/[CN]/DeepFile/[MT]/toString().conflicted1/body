{
  final StringBuilder str=new StringBuilder("xmlns");
  if (prefix.length > 0) {
    str.append(':');
    str.append(string(prefix));
  }
  str.append("=\"");
  str.append(string(uri));
  str.append("\"");
  return str.toString();
}
