{
  final ArrayList<byte[]> vals=metaElements.get(elem);
  if (vals == null)   return null;
  final int max=vals.size();
  final String[] strings=new String[max];
  for (int i=0; i < max; i++)   strings[i]=Token.string(vals.get(0));
  return strings;
}
