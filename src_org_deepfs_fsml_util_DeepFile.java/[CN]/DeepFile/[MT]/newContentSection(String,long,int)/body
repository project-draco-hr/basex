{
  bfc.position();
  final BufferedFileChannel sub=bfc.subChannel(title,contentSize);
  final DeepFile subFile=new DeepFile(parser,sub,fsmeta,fsxml,fscont,position,contentSize,fstextmax);
  subFile.addMeta(MetaElem.TITLE,title);
  return subFile;
}
