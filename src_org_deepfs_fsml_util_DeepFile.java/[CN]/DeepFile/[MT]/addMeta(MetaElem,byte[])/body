{
  if (!elem.getType().instance(Type.STR))   Main.debug("Invalid data type for " + "metadata element " + elem + " (string - as byte array).");
 else   addMeta(elem,ParserUtil.checkUTF(value),null);
}
