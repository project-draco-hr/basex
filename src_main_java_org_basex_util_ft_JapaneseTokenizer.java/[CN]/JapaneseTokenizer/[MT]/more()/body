{
  if (special) {
    return tokens.hasNext();
  }
  while (true) {
    if (!tokens.hasNext())     break;
    currToken=tokens.next();
    if (currToken.isMark())     continue;
    if (currToken.isAttachedWord())     continue;
    return true;
  }
  return false;
}
