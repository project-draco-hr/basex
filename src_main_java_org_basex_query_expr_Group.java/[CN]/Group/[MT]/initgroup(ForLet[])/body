{
  Var[] vs=new Var[groupby.length];
  Var[] fs=new Var[fl.length];
  for (int i=0; i < groupby.length; i++)   vs[i]=((VarCall)groupby[i].expr).var;
  for (int i=0; i < fl.length; i++)   fs[i]=fl[i].var;
  gp=new GroupPartition(vs,fs);
}
