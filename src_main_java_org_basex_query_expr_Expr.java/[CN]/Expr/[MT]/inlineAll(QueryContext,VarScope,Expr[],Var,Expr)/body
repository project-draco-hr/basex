{
  boolean change=false;
  for (int i=0; i < expr.length; i++) {
    final Expr nw=expr[i].inline(ctx,scp,v,e);
    if (nw != null) {
      expr[i]=nw;
      change=true;
    }
  }
  return change;
}
