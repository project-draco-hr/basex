{
  final Item it=ctx.item;
  if (it == null)   Err.or(XPNOCTX,this);
  if (it instanceof DBNode) {
    if (!ctx.context.perm(User.READ,((DBNode)it).data.meta))     throw new QueryException(PERMNO,CmdPerm.READ);
  }
  return it;
}
