{
  final Item it=ctx.item;
  if (!ctx.context.perm(User.READ,ctx.context.data.meta))   throw new QueryException(Main.info(PERMNO,CmdPerm.READ));
  if (it == null)   Err.or(XPNOCTX,this);
  return it;
}
