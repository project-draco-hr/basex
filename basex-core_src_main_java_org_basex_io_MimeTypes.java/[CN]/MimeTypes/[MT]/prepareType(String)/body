{
  final String[] t={type,""};
  if (type.equals("*")) {
    t[1]=type;
  }
 else {
    final int i=type.indexOf('/');
    if (i != -1) {
      t[0]=type.substring(i);
      t[1]=type.substring(i + 1);
    }
  }
  return t;
}
