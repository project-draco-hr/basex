{
switch (type) {
case ATT:
case PI:
    return type.name + " " + string(nname())+ " { \""+ string(atom())+ "\" }";
case ELM:
  return type.name + " " + string(nname())+ " { ... }";
case DOC:
return type.name + " { \"" + string(data.text(pre,true))+ "\" }";
default :
return type.name + " { \"" + string(atom())+ "\" }";
}
}
