{
switch (type) {
case ATT:
case PI:
    return type.name + " " + string(nname())+ " { \""+ string(str())+ "\" }";
case ELM:
  return type.name + " " + string(nname())+ " { ... }";
case DOC:
return type.name + " { \"" + string(data.text(pre,true))+ "\" }";
default :
return type.name + " { \"" + Err.chop(str())+ "\" }";
}
}
