{
  final XQuery query=new XQuery("declare variable $ext external := 0; $ext",ctx);
  query.bind("ext",new XQuery("5",ctx).value());
  final QT3Result res=result(query);
  result=res;
  test(assertEq("5"));
}
