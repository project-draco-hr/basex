{
  final XQuery query=new XQuery("declare variable $x external; \"result\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"result") || error("XPDY0002")));
}
