{
  final XQuery query=new XQuery("\n" + "        declare variable $x external := $a + 10;\n" + "        $x\n"+ "      ",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
