{
  print(", ");
  if (!att) {
    print("{");
    att=true;
  }
  print('"');
  for (  final byte ch : name)   code(ch);
  print("\":\"");
  for (  final byte ch : value)   code(ch);
  print("\"");
}
