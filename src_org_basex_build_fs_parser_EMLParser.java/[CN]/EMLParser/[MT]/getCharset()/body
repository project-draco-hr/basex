{
  if (mCurrLine.contains("charset=")) {
    mBodyCharset=mCurrLine.split("charset=")[1].split(";")[0].replace("\"","").trim();
    fsparser.metaEvent(meta.setString(StringField.ENCODING,mBodyCharset));
  }
}
