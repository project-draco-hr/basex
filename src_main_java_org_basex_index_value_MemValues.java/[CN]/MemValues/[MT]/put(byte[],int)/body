{
  final int i=put(key);
  int[] tmp=ids[i];
  if (tmp == null) {
    tmp=new int[]{id};
  }
 else {
    final int l=len[i];
    if (l == tmp.length)     tmp=Arrays.copyOf(tmp,l << 1);
    tmp[l]=id;
  }
  ids[i]=tmp;
  len[i]++;
  return i;
}
