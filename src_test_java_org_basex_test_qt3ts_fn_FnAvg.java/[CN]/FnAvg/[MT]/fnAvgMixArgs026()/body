{
  final XQuery query=new XQuery("fn:avg(( (xs:double(\"1.34\"), xs:float(\"INF\"))))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"INF"));
}
