{
  final XQuery query=new XQuery("avg(for $x in 1 to 9 return xs:yearMonthDuration(concat(\"P\",$x,\"M\")))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"P5M"));
}
