{
  final XQuery query=new XQuery("fn:avg(( fn:empty(\"Hello\")) or fn:boolean(fn:count(\"Hello\")))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
