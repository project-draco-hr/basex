{
  final XQuery query=new XQuery("fn:avg(( (xs:date(\"1993-03-31\"), xs:string(\"xyz\"), (), (), \"a\" , \"b\")))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("FORG0006"));
}
