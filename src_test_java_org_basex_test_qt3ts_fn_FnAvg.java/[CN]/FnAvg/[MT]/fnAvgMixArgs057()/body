{
  final XQuery query=new XQuery("fn:avg(( (xs:double(\"1.34\"), xs:float(\"INF\"), true())))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
