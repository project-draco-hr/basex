{
  final XQuery query=new XQuery("avg((xs:double(\"NaN\"), current-date() - xs:date(\"1997-01-01\") ))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
