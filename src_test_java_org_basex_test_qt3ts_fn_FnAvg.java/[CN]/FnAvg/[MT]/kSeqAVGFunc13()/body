{
  final XQuery query=new XQuery("avg((xs:untypedAtomic(3), xs:integer(0), xs:decimal(1))) instance of xs:double",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
