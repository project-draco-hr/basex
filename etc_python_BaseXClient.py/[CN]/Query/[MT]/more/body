def more(self):
    self.__session.send(('\x01' + self.__id))
    self.__next = self.__session.receive()
    if (not self.__session.ok()):
        raise IOError(self.__session.readString())
    return (len(self.__next) != 0)
