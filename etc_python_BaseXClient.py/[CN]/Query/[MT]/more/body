def more(self):
    if self.__open:
        self.__session.send(((('\x01' + self.__id) + '\x00') + '\x00'))
        if self.__session.check():
            self.__part = self.__session.res()
            return True
        else:
            if self.__session.check():
                print 'Query ', self.__id, ':', self.__session.res()
            self.__open = False
            return False
