def run(self):
    self.__id = self.__session.executeIter(self.__query)
    if (self.__id != '0'):
        return True
    else:
        self.__open = False
        self.__info = self.__session.res()
        return False
