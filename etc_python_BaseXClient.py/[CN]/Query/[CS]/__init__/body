def __init__(self, session, q):
    self.__session = session
    self.__session.send(('\x00' + q))
    self.__id = self.__session.receive()
    if (not self.__session.ok()):
        raise IOError(self.__session.readString())
