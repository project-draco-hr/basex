{
  if (quit)   bx.sessions.remove(this);
  try {
    context.close();
    BaseX.outln("Client " + clientId + " has logged out.");
    socket.close();
  }
 catch (  final IOException e) {
    e.printStackTrace();
  }
  timeout.interrupt();
  session=null;
}
