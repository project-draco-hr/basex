{
  if (quit)   bx.sessions.remove(this);
  try {
    context.close();
    BaseX.outln("Client % has logged out.",clientId);
    socket.close();
  }
 catch (  final IOException e) {
    e.printStackTrace();
  }
  timeout.interrupt();
  session=null;
}
