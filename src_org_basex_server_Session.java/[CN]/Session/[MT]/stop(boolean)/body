{
  running=false;
  if (s)   bxs.sessions.remove(this);
  new Close().execute(context);
  BaseX.outln("Client % has logged out.",clientId);
  timeout=null;
  session=null;
  try {
    socket.close();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
