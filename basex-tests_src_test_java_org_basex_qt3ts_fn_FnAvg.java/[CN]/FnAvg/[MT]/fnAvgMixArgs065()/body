{
  final XQuery query=new XQuery("fn:avg(( (xs:time(\"12:30:00\"), xs:decimal(\"2.000003\"), 2)))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
