{
  final int size=1000;
  final ClientQuery[] cqs=new ClientQuery[size];
  for (int q=0; q < size; q++)   cqs[q]=cs.query(String.valueOf(q));
  for (int q=0; q < size; q++)   assertEquals(q,Integer.parseInt(cqs[q].next()));
  for (  final ClientQuery cq : cqs)   cq.close();
}
