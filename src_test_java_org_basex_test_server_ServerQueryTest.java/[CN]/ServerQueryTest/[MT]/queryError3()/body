{
  try {
    final ClientQuery cq=cs.query("(1,'a')[. eq 1]");
    cq.init();
    assertEquals("1",cq.next());
    cq.next();
    fail("Error expected.");
  }
 catch (  final BaseXException ex) {
  }
}
