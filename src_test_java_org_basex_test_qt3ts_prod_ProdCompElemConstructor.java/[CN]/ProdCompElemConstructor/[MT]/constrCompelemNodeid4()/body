{
  final XQuery query=new XQuery("for $x in <?pi content?>, $y in element elem {$x} return exactly-one($y/processing-instruction()) is $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
