{
  final XQuery query=new XQuery("element elem {1, //west/@mark}",ctx);
  query.context(node(file("prod/AxisStep/TopMany.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XQTY0024"));
}
