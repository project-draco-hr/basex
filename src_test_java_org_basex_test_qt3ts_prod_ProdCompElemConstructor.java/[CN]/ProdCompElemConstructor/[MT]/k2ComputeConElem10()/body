{
  final XQuery query=new XQuery("declare construction strip; element e {\"content\"} instance of element(*, xs:untyped)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
