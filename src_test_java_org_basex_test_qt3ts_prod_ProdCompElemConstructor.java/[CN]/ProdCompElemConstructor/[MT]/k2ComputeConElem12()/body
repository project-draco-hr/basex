{
  final XQuery query=new XQuery("declare construction strip; element e {\"content\"} instance of element(b, xs:untyped)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
