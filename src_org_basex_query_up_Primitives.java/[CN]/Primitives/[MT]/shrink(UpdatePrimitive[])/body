{
  int l=0;
  for (  final UpdatePrimitive element : up)   if (element != null)   l++;
  final UpdatePrimitive[] t=new UpdatePrimitive[l];
  int m=0;
  for (  final UpdatePrimitive element : up)   if (element != null)   t[m++]=element;
  return t;
}
