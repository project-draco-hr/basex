{
  if (open) {
    out.write(1);
    client.send(id);
    out.write(0);
    if (client.check()) {
      next=client.readString();
      return true;
    }
    if (client.check()) {
      System.out.println(client.readString());
    }
    open=false;
    return false;
  }
  return true;
}
