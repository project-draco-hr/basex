{
  if (token.get().length > Token.MAXLEN)   return null;
switch (token.type) {
case TXT:
    return txtindex.ids(token);
case ATV:
  return atvindex.ids(token);
case FTX:
return ftxindex.ids(token);
default :
return null;
}
}
