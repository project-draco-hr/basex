{
switch (kind(pre)) {
case TEXT:
case COMM:
    return textNum(pre);
case ATTR:
  return attNum(pre);
case PI:
final byte[] txt=text(pre);
return toDouble(substring(txt,indexOf(txt,' ') + 1));
default :
return toDouble(atm(pre));
}
}
