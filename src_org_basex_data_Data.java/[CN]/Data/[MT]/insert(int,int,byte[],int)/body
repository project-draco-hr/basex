{
  meta.update();
  if (kind == ELEM) {
    insertElem(pre,pre - par,val,1,1);
  }
 else   if (kind == DOC) {
    insertDoc(pre,1,val);
  }
 else {
    insertText(pre,pre - par,val,kind);
  }
  updateTable(pre,par,1);
}
