{
switch (type) {
case TXT:
    return txtindex.ids(token);
case ATV:
  return atvindex.ids(token);
case WRD:
return wrdindex.ids(token);
case FTX:
return ftxindex.ids(token);
default :
return null;
}
}
