{
  if (token.length > Token.MAXLEN)   return Integer.MAX_VALUE;
switch (type) {
case TXT:
    return txtindex.nrIDs(token);
case ATV:
  return atvindex.nrIDs(token);
case WRD:
return wrdindex.nrIDs(token);
default :
return Integer.MAX_VALUE;
}
}
