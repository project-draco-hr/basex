{
  meta.update();
  byte[] v=val;
  if (k == PI) {
    final byte[] o=text(pre,true);
    final int i=indexOf(o,' ');
    v=concat(i == -1 ? o : substring(o,0,i),SPACE,v);
  }
  text(pre,v,k != ATTR);
}
