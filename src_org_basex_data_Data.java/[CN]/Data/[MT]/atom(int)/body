{
switch (kind(pre)) {
case TEXT:
case COMM:
    return text(pre);
case ATTR:
  return attValue(pre);
case PI:
final byte[] txt=text(pre);
return substring(txt,indexOf(txt,' ') + 1);
default :
return atm(pre);
}
}
