{
  try {
    if (dirt)     flush();
    pos[c]=n;
    fc.position(n);
    bytebuffernio.clear();
    fc.read(bytebuffernio);
    bytebuffernio.clear();
    bytebuffernio.get(buffer[c]);
  }
 catch (  final IOException ex) {
    ex.printStackTrace();
  }
}
