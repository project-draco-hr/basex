{
  file=new RandomAccessFile(f,"rw");
  fc=file.getChannel();
  bytebuffernio=ByteBuffer.allocateDirect(IO.BLOCKSIZE);
  fc.read(bytebuffernio);
  bytebuffernio.clear();
  bytebuffernio.get(buffer[0]);
  for (int i=1; i < BUFFERS; i++)   pos[i]=-1;
  len=file.length();
}
