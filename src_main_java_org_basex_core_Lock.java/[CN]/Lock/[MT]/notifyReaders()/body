{
  int c=1;
  do {
    c++;
    final Resource l=list.remove(0);
synchronized (l) {
      l.locked=false;
      l.notify();
    }
  }
 while (list.size() > 0 && list.get(0).reader && c < ctx.prop.num(Prop.PARALLEL));
}
