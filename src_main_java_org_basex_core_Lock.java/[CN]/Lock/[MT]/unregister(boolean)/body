{
  if (SKIP)   return;
  if (!w)   --activeR;
  if (list.size() > 0) {
    if (list.get(0).reader) {
      notifyReaders();
    }
 else {
      notifyNext();
    }
  }
 else {
    if (activeR == 0)     state=State.IDLE;
  }
}
