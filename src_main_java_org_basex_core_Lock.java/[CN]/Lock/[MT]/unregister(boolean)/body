{
  if (SKIP)   return;
  if (!w)   --activeR;
  if (list.size() > 0) {
    if (list.get(0).reader) {
      notifyReaders();
    }
 else {
      notifyNext();
    }
  }
 else {
    state=State.IDLE;
  }
}
