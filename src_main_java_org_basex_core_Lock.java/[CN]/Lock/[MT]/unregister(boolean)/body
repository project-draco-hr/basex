{
  if (SKIP)   return;
  if (w) {
    if (list.size() > 0 && list.get(0).reader) {
      notifyReaders();
    }
 else {
      notifyWriter();
    }
  }
 else {
    if (--activeR == 0) {
      notifyWriter();
    }
  }
}
