{
  if (SKIP)   return;
  if (w) {
    if (waiting.size() > 0 && waiting.getFirst().reader) {
      notifyReaders();
    }
 else {
      notifyWriter();
    }
  }
 else {
    if (--activeR == 0) {
      notifyWriter();
    }
  }
}
