{
  super(os,props);
  props.set(S_METHOD,M_JSON);
  jprop=new JsonProp(props.get(SerializerProp.S_JSON));
  if (jprop.spec() == null)   BXJS_CONFIG.thrwSerial("Unknown spec '" + jprop.get(JsonProp.SPEC) + "'");
}
