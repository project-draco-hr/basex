{
  final XQuery query=new XQuery("fn:count(fn:trace(() ,\"The value of the empty sequence is:\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"0") || error("XPST0005")));
}
