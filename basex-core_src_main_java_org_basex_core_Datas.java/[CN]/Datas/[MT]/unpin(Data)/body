{
  final Integer pins=list.get(data);
  if (pins == null)   return;
  data.unpin();
  final int p=pins;
  if (p == 1) {
    data.close();
    list.remove(data);
  }
 else {
    list.put(data,p - 1);
  }
}
