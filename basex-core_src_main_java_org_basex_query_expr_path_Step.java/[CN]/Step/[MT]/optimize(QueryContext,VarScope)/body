{
  if (!test.optimize(qc))   return Empty.SEQ;
  final Expr e=super.optimize(qc,scp);
  if (e != this)   return e;
  return has(Flag.FCS) ? this instanceof IterPosStep || !posIterator() ? this : new IterPosStep(this) : new IterStep(info,axis,test,preds);
}
