{
  return u == Use.X30 && (def == Function.ENVIRONMENT_VARIABLE || def == Function.AVAILABLE_ENVIRONMENT_VARIABLES) || u == Use.NDT && (def == Function.ERROR || def == Function.TRACE) || super.uses(u);
}
