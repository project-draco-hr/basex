{
  final XQuery query=new XQuery("fn:boolean(xs:anyURI(\"http://www.example.org/examples\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
