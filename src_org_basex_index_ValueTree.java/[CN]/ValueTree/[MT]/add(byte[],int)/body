{
  if (size == pre.length) {
    final int s=size << 1;
    pre=Arrays.copyOf(pre,s);
    ns=Arrays.copyOf(ns,s);
    t=Arrays.copyOf(t,s);
  }
  t[size]=tok;
  pre[size]=Num.newNum(id);
  ns[size]=1;
}
