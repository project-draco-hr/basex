{
  tree.add(-1);
  tree.add(-1);
  tree.add(pa);
  mod.add(false);
  tokens.add(tok);
  pres.add(Num.newNum(pre));
  if (!f) {
    maps.addNN(Num.num(tokens.size() - 1),pres.size() - 1);
  }
  return mod.size() - 1;
}
