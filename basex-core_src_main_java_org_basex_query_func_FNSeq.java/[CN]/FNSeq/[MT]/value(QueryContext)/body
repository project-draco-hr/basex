{
switch (func) {
case DISTINCT_VALUES:
    return distinctValues(qc);
case SUBSEQUENCE:
  return subseqValue(qc);
case TAIL:
final Value v=qc.value(exprs[0]);
return SubSeq.get(v,1,v.size() - 1);
default :
return super.value(qc);
}
}
