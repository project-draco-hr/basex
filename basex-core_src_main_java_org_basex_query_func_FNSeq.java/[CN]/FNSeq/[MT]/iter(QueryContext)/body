{
switch (func) {
case INDEX_OF:
    return indexOf(qc);
case DISTINCT_VALUES:
  return distinctValuesIter(qc);
case INSERT_BEFORE:
return insertBefore(qc);
case REVERSE:
return reverse(qc);
case REMOVE:
return remove(qc);
case SUBSEQUENCE:
return subseqIter(qc);
case TAIL:
return tail(qc);
case OUTERMOST:
return most(qc,true);
case INNERMOST:
return most(qc,false);
default :
return super.iter(qc);
}
}
