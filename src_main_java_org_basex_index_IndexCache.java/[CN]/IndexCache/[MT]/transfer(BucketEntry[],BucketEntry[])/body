{
  for (int j=0; j < src.length; ++j) {
    BucketEntry e=src[j];
    src[j]=null;
    while (e != null) {
      BucketEntry next=e.next;
      int i=indexFor(e.hash,dest.length);
      e.next=dest[i];
      dest[i]=e;
      e=next;
    }
  }
}
