{
synchronized (this) {
    if (!activeWriter && waitingWriters.size() == 0) {
      ++activeReaders;
      return;
    }
    ++waitingReaders;
    try {
      wait();
    }
 catch (    final InterruptedException ex) {
      ex.printStackTrace();
    }
  }
}
