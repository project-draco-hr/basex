{
  for (  final OrderBy l : ob) {
    final Item m=l.get(o[a]);
    final Item n=l.get(o[b]);
    final int c=m == null ? n == null ? 0 : l.lst ? -1 : 1 : n == null ? l.lst ? 1 : -1 : m.diff(input,n);
    if (c != 0)     return l.desc ? -c : c;
  }
  return 0;
}
