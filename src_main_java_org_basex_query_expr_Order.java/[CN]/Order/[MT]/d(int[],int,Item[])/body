{
  for (int k=0; k < ord.length; k++) {
    final OrderBy or=ord[k];
    final Item m=or.item(o[a]);
    final Item n=it[k];
    final boolean x=m == null;
    final boolean y=n == null;
    final int c=x ? y ? 0 : or.lst ? -1 : 1 : y ? or.lst ? 1 : -1 : m.diff(this,n);
    if (c != 0)     return or.desc ? -c : c;
  }
  return 0;
}
