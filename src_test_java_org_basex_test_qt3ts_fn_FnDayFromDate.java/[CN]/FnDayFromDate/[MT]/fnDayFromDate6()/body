{
  final XQuery query=new XQuery("fn:day-from-date(xs:date(\"1999-05-31Z\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"31"));
}
