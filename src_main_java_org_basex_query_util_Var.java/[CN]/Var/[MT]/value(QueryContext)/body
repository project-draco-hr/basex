{
  if (value == null) {
    if (expr == null)     VAREMPTY.thrw(input,this);
    final Value v=ctx.value;
    ctx.value=null;
    try {
      value=cast(ctx.value(expr.comp(ctx)),ctx);
    }
  finally {
      ctx.value=v;
    }
  }
  return value;
}
