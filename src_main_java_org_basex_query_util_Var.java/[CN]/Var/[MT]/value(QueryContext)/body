{
  if (value == null) {
    if (expr == null)     Err.or(input,VAREMPTY,this);
    final Value v=ctx.value;
    ctx.value=null;
    value=cast(ctx.iter(expr).finish(),ctx);
    ctx.value=v;
  }
  return value;
}
