{
  if (value == null) {
    if (expr == null)     VAREMPTY.thrw(input,this);
    final Value v=ctx.value;
    ctx.value=null;
    value=cast(expr.comp(ctx).value(ctx),ctx);
    ctx.value=v;
  }
  return value;
}
