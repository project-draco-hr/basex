{
  if (item == null) {
    if (expr == null)     Err.or(VAREMPTY,this);
    final Item it=ctx.item;
    ctx.item=null;
    item=cast(ctx.iter(expr).finish(),ctx);
    ctx.item=it;
  }
  return item;
}
