{
  if (item == null) {
    if (expr == null)     error(VAREMPTY,this);
    final Item it=ctx.item;
    ctx.item=null;
    item=cast(ctx.iter(expr).finish(),ctx);
    ctx.item=it;
  }
  return item;
}
