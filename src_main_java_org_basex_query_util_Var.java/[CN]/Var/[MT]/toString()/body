{
  final TokenBuilder tb=new TokenBuilder();
  if (name != null) {
    tb.add(DOLLAR);
    tb.add(name.str());
    if (type != null)     tb.add(" " + AS + " ");
  }
  if (type != null)   tb.add(type);
  return tb.toString();
}
