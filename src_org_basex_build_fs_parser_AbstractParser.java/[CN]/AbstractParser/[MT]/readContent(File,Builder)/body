{
  FileChannel fc=null;
  try {
    fc=new RandomAccessFile(f,"r").getChannel();
    readContent(f,fc,fc.size(),b);
  }
 catch (  IOException e) {
  }
 finally {
    try {
      if (fc != null)       fc.close();
    }
 catch (    IOException e) {
    }
  }
}
