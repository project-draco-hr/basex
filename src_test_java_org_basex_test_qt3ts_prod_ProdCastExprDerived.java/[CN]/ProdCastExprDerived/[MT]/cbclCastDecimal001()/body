{
  final XQuery query=new XQuery("1.7976931348623157E+308 cast as xs:decimal",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((error("FOCA0001") || assertQuery("string-length(string($result)) gt 300")));
}
