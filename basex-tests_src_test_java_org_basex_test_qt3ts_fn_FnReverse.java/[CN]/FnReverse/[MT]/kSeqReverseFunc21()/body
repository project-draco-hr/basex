{
  final XQuery query=new XQuery("reverse((1, 2, current-time(), 3))[last() - 0]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("1"));
}
