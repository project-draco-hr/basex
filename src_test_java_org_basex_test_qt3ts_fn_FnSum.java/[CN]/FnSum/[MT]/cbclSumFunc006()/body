{
  final XQuery query=new XQuery("sum((1,exactly-one((1 to 10)[. div 2 = 2])))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"5"));
}
