{
  final BaseXServer server=createServer();
  insert(clients,runs);
  delete(clients,runs);
  final ClientSession s=createClient();
  s.execute(new DropDB(NAME));
  s.close();
  stopServer(server);
}
