{
  final int tl=tok.length;
  int p=0;
  int s=0;
  boolean l=false;
  for (int t=0; t < tl && p < pos; t++) {
    final byte c=tok[t];
    final boolean ld=letterOrDigit(c);
    if (!l && ld)     p++;
    if (c == '\n')     s++;
    l=ld;
  }
  return s;
}
