{
  try {
    if (dirt)     flush();
    pos[c]=n;
    fc.position(n);
    fc.read(bbuffer);
    bbuffer.clear();
    buffer[c]=bbuffer.array();
  }
 catch (  final IOException ex) {
    ex.printStackTrace();
  }
}
