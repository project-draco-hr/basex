{
  file=new RandomAccessFile(f,"rw");
  fc=file.getChannel();
  fc.position(0);
  bbuffer=ByteBuffer.allocate(IO.BLOCKSIZE);
  fc.read(bbuffer);
  buffer[0]=bbuffer.array();
  bbuffer.clear();
  for (int i=1; i < BUFFERS; i++)   pos[i]=-1;
  len=file.length();
}
