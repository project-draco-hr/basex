{
  final XQuery query=new XQuery("1 eq subsequence((\"1\", 2, 3.1, \"four\"), 1, 1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
