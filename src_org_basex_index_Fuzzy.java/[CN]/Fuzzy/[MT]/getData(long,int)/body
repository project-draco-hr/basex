{
  int[][] d=new int[2][s];
  if (data.meta.fcompress) {
    d[0]=dat.readNums(p,s);
    d[1]=dat.readNums(s);
  }
 else {
    d[0]=dat.readInts(p,p + s * 4L);
    d[1]=dat.readInts(p + s * 4L,p + 2 * s * 4L);
  }
  return d;
}
