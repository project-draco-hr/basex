{
  final int df=Math.abs(tok1.length - tok2.length);
  if (df > e)   return false;
  int d=(tok1.length > tok2.length) ? Levenshtein.ls(tok1,sp,tok1.length,tok2,e) : Levenshtein.ls(tok2,sp,tok2.length,tok1,e);
  return d <= e;
}
