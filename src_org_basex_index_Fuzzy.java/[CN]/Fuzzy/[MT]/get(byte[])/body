{
  final int p=getPointerOnToken(tok);
  if (p == -1)   return IndexIterator.EMPTY;
  int[][] res=getData(getPointerOnData(p,tok.length),getDataSize(p,tok.length));
  return new IndexArrayIterator(res[0],res[1]);
}
