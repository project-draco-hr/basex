{
  if (session == null) {
    while (user == null) {
      Util.out(USERNAME + COLS);
      user=Util.input();
    }
    while (pass == null) {
      Util.out(PASSWORD + COLS);
      pass=Util.password();
    }
    session=new ClientSession(context,user,pass,out);
  }
  return session;
}
