{
  if (session == null) {
    while (user == null) {
      Util.out(SERVERUSER + COLS);
      user=input().get(0);
    }
    while (pass == null) {
      Util.out(SERVERPW + COLS);
      pass=password();
    }
    session=new ClientSession(context,user,pass);
  }
  return session;
}
