{
  if (session == null) {
    while (user == null) {
      Util.out(SERVERUSER + COLS);
      user=Util.input();
    }
    while (pass == null) {
      Util.out(SERVERPW + COLS);
      pass=Util.password();
    }
    session=new ClientSession(context,user,pass,out);
  }
  return session;
}
