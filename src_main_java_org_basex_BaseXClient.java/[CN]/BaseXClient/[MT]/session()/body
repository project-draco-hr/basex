{
  if (session == null) {
    while (user == null) {
      Main.out(SERVERUSER + COLS);
      user=input();
    }
    while (pass == null) {
      Main.out(SERVERPW + COLS);
      pass=password();
    }
    session=new ClientSession(context,user,pass);
  }
  return session;
}
