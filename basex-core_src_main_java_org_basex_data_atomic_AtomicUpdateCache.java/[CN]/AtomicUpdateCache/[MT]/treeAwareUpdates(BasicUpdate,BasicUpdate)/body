{
  if (a.destructive()) {
    final int pre=a.location;
    final int fol=pre + data.size(pre,data.kind(pre));
    if (b.location <= fol && (b instanceof Insert || b instanceof InsertAttr) && b.parent >= pre && b.parent < fol || b.location < fol) {
      return true;
    }
  }
  return false;
}
