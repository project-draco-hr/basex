{
  final XQuery query=new XQuery("xs:dayTimeDuration(\"P9223372036854775807D\") div 0.5",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FODT0002"));
}
