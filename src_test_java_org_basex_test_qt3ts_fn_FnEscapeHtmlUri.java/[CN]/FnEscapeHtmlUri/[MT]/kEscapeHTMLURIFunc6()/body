{
  final XQuery query=new XQuery("normalize-space(iri-to-uri((\"example.com\", current-time())[1] treat as xs:string))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"example.com"));
}
