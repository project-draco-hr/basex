{
  try {
    final Context ctx=new Context();
    ctx.data(CreateDB.xml(new XMLParser(IO.get("test/org/basex/test/fuse/getattrtest.xml")),"DBNAME"));
    dbfs.data=ctx.data();
  }
 catch (  final IOException e) {
    e.printStackTrace();
    fail("Problem loading test database.");
  }
}
