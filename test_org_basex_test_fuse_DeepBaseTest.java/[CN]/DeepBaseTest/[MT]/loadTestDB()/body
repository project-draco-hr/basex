{
  try {
    final Data data=CreateDB.xml(ctx,new XMLParser(IO.get("test/org/basex/test/fuse/getattrtest.xml")),DBNAME);
    ctx.openDB(data);
    dbfs.data=data;
  }
 catch (  final IOException e) {
    e.printStackTrace();
    fail("Problem loading test database.");
  }
}
