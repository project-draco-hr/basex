{
  final File[] files=d.listFiles();
  if (files == null)   return;
  for (  final File f : files) {
    if (!valid(f)) {
      visitSymLink(f);
    }
 else     if (f.isDirectory()) {
      visitDirectory(f);
    }
 else {
      visitFile(f);
    }
  }
}
