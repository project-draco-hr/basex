{
  for (  final Clause cl : clauses)   if (!cl.visitVars(visitor))   return false;
  if (!ret.visitVars(visitor))   return false;
  for (int i=clauses.size(); --i >= 0; )   if (!clauses.get(i).undeclare(visitor))   return false;
  return true;
}
