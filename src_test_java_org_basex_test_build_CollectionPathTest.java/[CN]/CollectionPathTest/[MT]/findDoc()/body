{
  final String find="for $x in collection('" + NAME + '/'+ DIR+ "xmark.xml') "+ "where $x//location contains text 'uzbekistan' "+ "return $x";
  final QueryProcessor qp=new QueryProcessor(find,context);
  assertEquals(1,qp.execute().size());
  qp.close();
}
