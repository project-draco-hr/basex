{
  final String find="for $x in collection('" + DBNAME + "/etc/xml/xmark.xml') "+ "where $x//location contains text 'uzbekistan' "+ "return $x";
  final QueryProcessor qp=new QueryProcessor(find,CONTEXT);
  assertEquals(1,qp.execute().size());
  qp.close();
}
