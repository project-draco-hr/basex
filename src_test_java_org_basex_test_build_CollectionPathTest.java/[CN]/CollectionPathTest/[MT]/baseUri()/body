{
  final String find="for $x in collection('" + DB + "/etc/test/xmark.xml') "+ "return base-uri($x)";
  final QueryProcessor qp=new QueryProcessor(find,CONTEXT);
  assertEquals(FILES[1],qp.iter().next().toJava());
  qp.close();
}
