{
  final String fun=check(FunDef.BENTRY);
  query(fun + "('" + ZIP+ "', '"+ ENTRY1+ "')");
  contains("xs:hexBinary(" + fun + "('"+ ZIP+ "', '"+ ENTRY1+ "'))","610A61626F");
  error(fun + "('abc', 'xyz')",Err.ZIPNOTFOUND);
  error(fun + "('" + ZIP+ "', '')",Err.ZIPNOTFOUND);
}
