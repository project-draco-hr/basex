{
  return RuntimeExpr.funcItem(new RuntimeExpr(info){
    @Override public Map item(    final QueryContext qc,    final InputInfo ii) throws QueryException {
      return result(seed,qc);
    }
    @Override public Value value(    final QueryContext qc) throws QueryException {
      return result(seed,qc);
    }
  }
,0,sc,qctx,info);
}
