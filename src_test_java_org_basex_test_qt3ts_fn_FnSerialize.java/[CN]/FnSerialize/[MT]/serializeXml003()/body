{
  final XQuery query=new XQuery("serialize(doc('../docs/atomic.xml'),\n" + "            doc('serialize/serialize-003.xml')/params/*)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertQuery("contains($result,'atomic')"));
}
