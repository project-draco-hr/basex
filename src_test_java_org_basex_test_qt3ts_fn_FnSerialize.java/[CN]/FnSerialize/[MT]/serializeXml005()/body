{
  final XQuery query=new XQuery("serialize(doc('../docs/atomic.xml'),\n" + "            doc('serialize/serialize-005.xml')/params/*)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("SEPM0016"));
}
