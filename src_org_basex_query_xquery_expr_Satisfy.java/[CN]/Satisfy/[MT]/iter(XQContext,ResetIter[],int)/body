{
  final boolean last=p + 1 == fl.length;
  while (it[p].next().bool()) {
    final boolean res=last ? ctx.iter(expr).ebv().bool() : iter(ctx,it,p + 1);
    if (res ^ every) {
      for (      final ResetIter ri : it)       ri.reset();
      return res;
    }
  }
  return every;
}
