{
  final XQuery query=new XQuery("fn:string(fn:current-date()) eq fn:string(fn:current-date())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
