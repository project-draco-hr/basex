{
  ArrayList<Object[]> list=getList();
  if (list != null) {
    String[] t=new String[list.size()];
    int i=0;
    for (    Object[] s : list) {
      t[i]=(String)s[0];
      i++;
    }
    return t;
  }
  return new String[]{};
}
