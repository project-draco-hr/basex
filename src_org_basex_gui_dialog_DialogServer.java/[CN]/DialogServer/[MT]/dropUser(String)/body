{
  ArrayList<Object[]> list=getList();
  if (list != null) {
    int i=0;
    int t=0;
    for (    Object[] s : list) {
      if (s[0].equals(usern))       t=i;
      i++;
    }
    list.remove(t);
  }
  writeList(list);
}
