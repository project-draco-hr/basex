{
  opened();
  valid(it,XQItemType.class);
  if (it.getItemKind() != XQItemType.XQITEMKIND_ELEMENT)   throw new BXQException(ELM);
  return new BXQItemType(Type.DEL,it.getNodeName(),it.getBaseType());
}
