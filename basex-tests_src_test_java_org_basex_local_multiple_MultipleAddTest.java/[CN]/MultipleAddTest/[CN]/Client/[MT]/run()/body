{
  try {
    new Open(NAME).execute(ctx);
    for (int i=0; i < runs; ++i) {
      new Add("",INPUT).execute(ctx);
    }
  }
 catch (  BaseXException ex) {
    ex.printStackTrace();
  }
 finally {
    ctx.close();
  }
}
