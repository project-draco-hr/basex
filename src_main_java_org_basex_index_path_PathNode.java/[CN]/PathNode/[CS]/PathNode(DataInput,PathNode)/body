{
  name=(short)in.readNum();
  kind=in.readByte();
  size=in.readNum();
  ch=new PathNode[in.readNum()];
  in.readDouble();
  par=p;
  for (int i=0; i < ch.length; ++i)   ch[i]=new PathNode(in,this);
}
