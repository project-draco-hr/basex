{
  out.writeBytesArray(keys);
  out.writeNums(next);
  out.writeNums(bucket);
  out.writeNum(size);
  for (int s=1; s < size; s++) {
    if (stat[s] == null)     stat[s]=new StatsKey();
    stat[s].finish(out);
  }
}
