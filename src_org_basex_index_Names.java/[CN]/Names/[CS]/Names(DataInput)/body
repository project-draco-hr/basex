{
  keys=in.readBytesArray();
  next=in.readNums();
  bucket=in.readNums();
  counter=in.readNums();
  noleaf=in.readBools();
  size=in.readNum();
  stat=new StatsKey[next.length];
  for (int s=1; s < size; s++)   stat[s]=new StatsKey(in);
}
