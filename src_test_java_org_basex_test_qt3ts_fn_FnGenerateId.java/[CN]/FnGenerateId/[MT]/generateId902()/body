{
  final XQuery query=new XQuery("let $f := function($x as item()) {generate-id($x)} return $f(3)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
