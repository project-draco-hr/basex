{
  final XQuery query=new XQuery("xs:unsignedLong(\"18446744073709551615\") lt xs:unsignedLong(\"100\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(false));
}
