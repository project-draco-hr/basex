{
  return new StreamingOutput(){
    @Override public void write(    final OutputStream out){
      BXUtil.query(resource,out,params.get(EURLParameter.QUERY),params.get(EURLParameter.WRAP),params.get(EURLParameter.OUTPUT),params.get(EURLParameter.START),params.get(EURLParameter.COUNT));
    }
  }
;
}
