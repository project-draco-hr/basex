{
  atts.reset();
  atts.add(NAME,token(f.getName()));
  builder.startElem(DIR,atts);
  sizeStack[++lvl]=0;
  parse(f);
  final long size=sizeStack[lvl];
  addFSAtts(f,size);
  builder.endElem(DIR);
  sizeStack[--lvl]+=size;
}
