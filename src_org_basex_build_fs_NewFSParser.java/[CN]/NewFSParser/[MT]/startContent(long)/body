{
  if (!prop.is(Prop.FSCONT))   return;
  atts.reset();
  atts.add(OFFSET,offset >= 0 ? token(offset) : UNKNOWN);
  atts.add(SIZE,UNKNOWN);
  contentSizeIdStack[contentOpenedCounter++]=builder.startElem(CONTENT_NS,atts) + 2;
  return;
}
