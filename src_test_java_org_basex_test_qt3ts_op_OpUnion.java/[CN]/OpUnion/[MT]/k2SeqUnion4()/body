{
  final XQuery query=new XQuery("let $i := <e> <a/> <b/> <c/> </e>/a , $t := $i/following-sibling::b return (($i union ($i, $i)), (($t, $t) union $t))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a/><b/>",false));
}
