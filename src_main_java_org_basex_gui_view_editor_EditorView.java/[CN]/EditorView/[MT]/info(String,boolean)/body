{
  ++threadID;
  errPos=-1;
  errFile=null;
  info.setCursor(!ok && error(msg) ? GUIConstants.CURSORHAND : GUIConstants.CURSORARROW);
  info.setText(msg.replaceAll(STOPPED_AT + ".*\\r?\\n\\[.*?\\] ",""),ok ? Msg.SUCCESS : Msg.ERROR);
  info.setToolTipText(ok ? null : msg);
  stop.setEnabled(false);
  go.setEnabled(true);
}
