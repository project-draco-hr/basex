{
  final int thread=++threadID;
  new Thread(){
    @Override public void run(){
      if (gui.updating)       go.setEnabled(false);
      Performance.sleep(200);
      if (thread == threadID) {
        info.setToolTipText(null);
        info.setText(INFOWAIT,Msg.SUCCESS);
        if (!gui.updating)         stop.setEnabled(true);
      }
    }
  }
.start();
}
