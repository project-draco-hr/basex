{
  final BaseXFileChooser fc=new BaseXFileChooser(OPEN,gui.gprop.get(GUIProp.XQPATH),gui);
  fc.addFilter(XQUERY_FILES,IO.XQSUFFIXES);
  final IOFile file=fc.select(Mode.FOPEN);
  if (file != null)   open(file);
}
