{
  final XQuery query=new XQuery("fold-right(function($a, $b){ if(exists($b[. eq $a])) then $b else ($a, $b) }, (), (1, 2, 2, 3, 3, 3, 4, 5, 5))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("1, 2, 3, 4, 5"));
}
