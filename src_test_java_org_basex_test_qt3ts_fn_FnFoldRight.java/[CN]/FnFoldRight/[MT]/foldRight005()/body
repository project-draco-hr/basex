{
  final XQuery query=new XQuery("let $f := function($a, $b){($b, $a)} return fold-right($f, (), 1 to 5)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("5,4,3,2,1"));
}
