{
  final XQuery query=new XQuery("fold-right(function($a, $b){ string-length($a) + $b }, 0, (\"Hello\", \"World\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"10"));
}
