{
  final XQuery query=new XQuery("fold-right(function($a, $b){ if(empty($b)) then $a else if($a lt $b) then $b else $a }, (), 1 to 10)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"10"));
}
