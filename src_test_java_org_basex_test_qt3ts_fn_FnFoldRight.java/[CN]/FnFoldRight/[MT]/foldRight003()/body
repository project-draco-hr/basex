{
  final XQuery query=new XQuery("fold-right(function($a, $b) { $a or $b }, false(), (true(), false(), false()))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
