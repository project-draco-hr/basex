{
  final XQuery query=new XQuery("fold-right(function($a , $b){ ($b, $a) }, (), (1 to 5))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("5, 4, 3, 2, 1"));
}
