{
  while (running) {
    try {
      final ServerProcess s=new ServerProcess(server.accept(),this);
      if (STOP.exists()) {
        STOP.delete();
        quit(false);
      }
 else       if (s.init()) {
        context.add(s);
      }
    }
 catch (    final IOException ex) {
      break;
    }
  }
}
