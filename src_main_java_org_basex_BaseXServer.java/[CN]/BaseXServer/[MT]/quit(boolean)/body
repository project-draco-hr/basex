{
  if (!running)   return;
  running=false;
  super.quit(user);
  try {
    if (console)     System.in.close();
    tserver.close();
    server.close();
  }
 catch (  final IOException ex) {
    log.write(ex.getMessage());
    Util.stack(ex);
  }
  console=false;
  context.close();
}
