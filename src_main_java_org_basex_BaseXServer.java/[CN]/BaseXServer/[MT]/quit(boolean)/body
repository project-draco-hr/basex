{
  if (!running)   return;
  running=false;
  super.quit(user);
  try {
    if (console)     System.in.close();
    server.close();
  }
 catch (  final IOException ex) {
    context.log.write(ex.getMessage());
    ex.printStackTrace();
  }
  console=false;
  context.close();
}
