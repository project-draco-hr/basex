{
  try {
    STOP.write(Token.EMPTY);
    new Socket("localhost",ctx.prop.num(Prop.SERVERPORT));
    outln(SERVERSTOPPED);
  }
 catch (  final IOException ex) {
    errln(server(ex));
  }
  return;
}
