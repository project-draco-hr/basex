{
  final XQuery query=new XQuery("fn:count(fn:document-uri(/works[1]/employee[1]))",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
