{
  final XQuery query=new XQuery("fn:document-uri(<element1>contenty</element1>,\"Argument 2\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0017"));
}
