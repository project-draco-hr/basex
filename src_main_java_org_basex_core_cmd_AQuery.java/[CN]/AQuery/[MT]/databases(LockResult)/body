{
  if (null == qp)   lr.writeAll=true;
 else {
    qp.databases(lr);
    if (qp != null) {
      qi.readLocked=lr.readAll ? null : lr.read;
      qi.writeLocked=lr.writeAll ? null : lr.write;
    }
 else     if (qp.updating)     lr.writeAll=true;
 else     lr.readAll=true;
  }
}
