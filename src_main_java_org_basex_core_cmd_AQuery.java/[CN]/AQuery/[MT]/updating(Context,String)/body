{
  try {
    final Performance p=new Performance();
    qp=queryProcessor(qu,ctx);
    qp.parse();
    qi.pars=p.time();
    return qp.updating;
  }
 catch (  final QueryException ex) {
    Util.debug(ex);
    qe=ex;
    if (qp != null)     qp.close();
    return false;
  }
}
