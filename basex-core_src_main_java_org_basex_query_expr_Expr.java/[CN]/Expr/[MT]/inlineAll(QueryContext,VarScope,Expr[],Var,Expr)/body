{
  boolean change=false;
  for (int i=0; i < arr.length; i++) {
    final Expr nw=arr[i].inline(qc,scp,var,ex);
    if (nw != null) {
      arr[i]=nw;
      change=true;
    }
  }
  return change;
}
