{
switch (u) {
case POS:
    for (int e=0; e != expr.length; e++) {
      final Type t=expr[e].returned();
      if (t == null || t.num || expr[e].uses(u))       return true;
    }
  return super.uses(u);
default :
return super.uses(u);
}
}
