{
  check("function($f) {" + "  let $loop := function($x) { $f(function() { $x($x) }) }" + "  return $loop($loop)"+ "}(function($f) { 42 })","42","exists(//" + Util.name(DynamicFunc.class) + ")","every $f in outermost(//" + Util.name(DynamicFunc.class) + ")/* satisfies"+ "  $f instance of element("+ Util.name(FuncItem.class)+ ")");
}
