{
  check("function($f) {" + "  let $loop := function($x) { $f(function() { $x($x) }) }" + "  return $loop($loop)"+ "}(function($f) { 42 })","42","exists(/DynFuncCall)","every $f in /DynFuncCall/* satisfies $f instance of element(FuncItem)");
}
