{
  final TokenBuilder tb=new TokenBuilder();
  return tb.addExt((index.type() == IndexType.TEXT ? Function._DB_TEXT_RANGE : Function._DB_ATTRIBUTE_RANGE).get(null,info,Str.get(ictx.data.meta.name),Dbl.get(index.min),Dbl.get(index.max))).toString();
}
