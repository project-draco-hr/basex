{
  final XQuery query=new XQuery("<result> {//text() and fn:true()} </result>",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<result>true</result>",false));
}
