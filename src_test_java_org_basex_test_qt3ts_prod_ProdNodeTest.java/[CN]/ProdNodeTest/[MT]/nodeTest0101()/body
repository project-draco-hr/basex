{
  final XQuery query=new XQuery("<out>{fn:count(//comment())}</out>",ctx);
  query.context(node(file("prod/AxisStep/TreeEmpty.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<out>0</out>",false));
}
