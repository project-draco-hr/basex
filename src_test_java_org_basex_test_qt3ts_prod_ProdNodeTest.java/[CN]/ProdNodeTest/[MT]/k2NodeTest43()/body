{
  final XQuery query=new XQuery("(4, 5)[position() = 2][last() = 1][last() = 1][last()]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("5"));
}
