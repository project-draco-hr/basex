{
  final XQuery query=new XQuery("declare function local:aFunction() { (<e/>/., <e/>/((<e/>, 2), 1, 2)) }; 1, local:aFunction()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0018"));
}
