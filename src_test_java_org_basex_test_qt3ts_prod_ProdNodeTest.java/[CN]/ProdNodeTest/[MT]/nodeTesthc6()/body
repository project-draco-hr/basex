{
  final XQuery query=new XQuery("<result> {//overtime/node() or fn:true()} </result>",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<result>true</result>",false));
}
