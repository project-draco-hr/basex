{
  new File(dir).mkdirs();
  final String file=dir + DATE.format(d) + ".log";
  start=d;
  try {
    fw=new OutputStreamWriter(new FileOutputStream(file,true),Token.UTF8);
  }
 catch (  final IOException ex) {
    Util.stack(ex);
  }
}
