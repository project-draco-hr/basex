{
  new File(dir).mkdirs();
  final String file=dir + DATE.format(d) + ".log";
  start=d;
  try {
    fos=new FileOutputStream(file,true);
  }
 catch (  final IOException ex) {
    Util.stack(ex);
  }
}
