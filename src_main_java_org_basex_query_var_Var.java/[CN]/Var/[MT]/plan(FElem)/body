{
  final FElem e=planElem(QueryText.NAM,'$' + Token.string(name.string()),QueryText.ID,Token.token(id));
  if (declared != null)   e.add(planAttr(QueryText.AS,declared.toString()));
  addPlan(plan,e);
}
