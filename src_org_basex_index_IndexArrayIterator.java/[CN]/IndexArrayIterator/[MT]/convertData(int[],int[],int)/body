{
  final IntArrayList ia=new IntArrayList(siz);
  int i=0, s;
  int[] t;
  while (i < siz) {
    s=i;
    while (i < siz && r1[s] == r1[i])     i++;
    t=new int[i - s + 1];
    t[0]=r1[s];
    System.arraycopy(r2,s,t,1,i - s);
    ia.add(t);
  }
  ftdata=ia.list;
  size=ia.size;
}
