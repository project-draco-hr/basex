{
  final AxisIter parts=multipart.children();
  ANode part=null;
  while ((part=parts.next()) != null) {
    final RequestHandler r=new RequestHandler(part,ii);
    writePart(r.getHeaders(),r.getPayload(),out,boundary);
  }
  out.write(new TokenBuilder().add("--".getBytes()).add(boundary).add("--").add(CRLF).finish());
}
