{
  byte[] statusOnly=req.getAttrs().get(STATUSONLY);
  boolean s=(statusOnly == null || !Bln.parse(statusOnly,info)) ? false : true;
  return ResponseHandler.getResponse(conn,s,req.getAttrs().get(OVERMEDIATYPE),ctx.context.prop,info);
}
