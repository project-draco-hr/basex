{
  final IOContent io=new IOContent(readHttpContent(conn));
  final Parser parser=new HTMLParser(io,null,ctx.resource.context.prop);
  return new DBNode(MemBuilder.build(parser,ctx.resource.context.prop,""),0);
}
