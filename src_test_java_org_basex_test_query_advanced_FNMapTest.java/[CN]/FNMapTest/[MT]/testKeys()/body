{
  final String fun=check(FunDef.MAPKEYS);
  query(fun + "(map:new(for $i in 1 to 3 return map:entry($i,$i+1)))","1 2 3");
  query("let $map := map:new(for $i in 1 to 3 return map:entry($i, $i + 1))" + "for $k in " + fun + "($map) return map:get($map, $k)","2 3 4");
}
