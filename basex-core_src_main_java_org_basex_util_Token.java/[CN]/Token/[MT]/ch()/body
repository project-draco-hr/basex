{
  if (norm == null) {
    final int nl=0x400;
    final char[] nrm=new char[nl];
    for (int n=0; n < nl; ++n)     nrm[n]=(char)n;
    for (    final char[] aNC : NC)     nrm[aNC[0]]=aNC[1];
    norm=nrm;
  }
  return norm;
}
