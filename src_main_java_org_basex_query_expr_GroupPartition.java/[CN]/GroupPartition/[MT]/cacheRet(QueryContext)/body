{
  pgvars=new Var[gv.length];
  pgngvars=new Var[ngv.length];
  for (int j=0; j < gv.length; ++j)   pgvars[j]=ctx.vars.get(gv[j]);
  for (int j=0; j < ngv.length; ++j)   pgngvars[j]=ctx.vars.get(ngv[j]);
  cachedRet=true;
}
