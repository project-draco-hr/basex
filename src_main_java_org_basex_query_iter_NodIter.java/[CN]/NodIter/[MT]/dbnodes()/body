{
  if (dupl)   sort(sort);
  if (size == 0 || !(item[0] instanceof DBNode))   return false;
  final DBNode n=(DBNode)item[0];
  for (int s=1; s < size; s++) {
    if (!(item[s] instanceof DBNode) || n.data != ((DBNode)item[s]).data)     return false;
  }
  return true;
}
