{
  if (mth == M_TEXT)   return;
  if (ind)   indent(true);
  if (mth == M_HTML && contains(v,'>'))   SERPI.thrwSerial();
  print(PI_O);
  print(n);
  print(' ');
  print(v);
  print(mth == M_HTML ? ELEM_C : PI_C);
}
