{
  final long s=size();
  if (s == 1)   return next();
  final Item it=next();
  if (it == null)   return null;
  final Item n=next();
  if (n != null)   Err.or(XPSEQ,"(" + it + ","+ n+ (next() != null ? ",..." : "")+ ")");
  return it;
}
