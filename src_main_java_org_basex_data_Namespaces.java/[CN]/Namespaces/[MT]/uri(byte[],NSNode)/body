{
  if (Token.eq(Token.XML,pr))   return 0;
  final int id=pref.id(pr);
  if (id == 0)   return 0;
  NSNode n=nd;
  while (n != null) {
    final int u=n.uri(id);
    if (u != 0)     return u;
    n=n.par;
  }
  return 0;
}
