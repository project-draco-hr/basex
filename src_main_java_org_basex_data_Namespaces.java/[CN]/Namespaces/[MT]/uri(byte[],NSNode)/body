{
  if (Token.eq(Token.XML,pr))   return 0;
  final int id=prefs.id(pr);
  if (id == 0)   return 0;
  NSNode n=nd;
  while (n != null) {
    final int u=n.uri(id);
    if (u != 0)     return u;
    n=n.parent;
  }
  return 0;
}
