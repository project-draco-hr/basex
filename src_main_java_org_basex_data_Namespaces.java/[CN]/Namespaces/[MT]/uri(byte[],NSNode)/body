{
  if (Token.eq(Token.XML,pref))   return 0;
  final int id=prefs.id(pref);
  if (id == 0)   return 0;
  NSNode n=node;
  while (n != null) {
    final int u=n.uri(id);
    if (u != 0)     return u;
    n=n.parent;
  }
  return 0;
}
