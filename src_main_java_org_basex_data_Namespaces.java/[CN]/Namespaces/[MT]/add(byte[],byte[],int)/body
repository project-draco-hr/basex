{
  NSNode node=null;
  if (!newns) {
    node=new NSNode(pre);
    current.add(node);
    current=node;
    newns=true;
  }
  final int k=prefs.put(pref);
  final int v=uris.put(uri);
  current.add(k,v);
  if (pref.length == 0)   defaults.set(level,v);
  return node;
}
