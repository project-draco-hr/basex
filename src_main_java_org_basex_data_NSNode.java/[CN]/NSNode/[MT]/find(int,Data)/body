{
  final int s=find(p);
  if (s == -1)   return this;
  final NSNode ch=children[s];
  final int cp=ch.pre;
  if (cp == p)   return ch;
  if (cp + d.size(cp,Data.ELEM) <= p)   return this;
  return children[s].find(p,d);
}
