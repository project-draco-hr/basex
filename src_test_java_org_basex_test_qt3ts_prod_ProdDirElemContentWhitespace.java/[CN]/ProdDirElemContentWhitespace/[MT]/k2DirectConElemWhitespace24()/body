{
  final XQuery query=new XQuery("string(<elem> content <![CDATA[ content ]]> content </elem>)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false," content  content  content "));
}
