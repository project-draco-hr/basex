{
  final XQuery query=new XQuery("declare boundary-space strip; <elem><![CDATA[ ]]></elem>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem> </elem>",false));
}
