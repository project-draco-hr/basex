{
  final XQuery query=new XQuery("declare boundary-space strip; string(<e xml:space=\"preserve\"> </e>) eq \"\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
