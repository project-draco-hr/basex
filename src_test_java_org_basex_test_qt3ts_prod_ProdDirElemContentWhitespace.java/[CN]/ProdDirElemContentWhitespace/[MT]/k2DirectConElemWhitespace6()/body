{
  final XQuery query=new XQuery("declare boundary-space strip; <e xml:space=\"preserve\"> </e>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<e xml:space=\"preserve\"/>",false));
}
