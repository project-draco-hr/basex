{
  final XQuery query=new XQuery("declare boundary-space strip; <elem> &#x30; </elem>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem> 0 </elem>",false));
}
