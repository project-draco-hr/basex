{
  final XQuery query=new XQuery("declare boundary-space strip; <elem>&#xA;</elem>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertQuery("deep-equal(string-to-codepoints(string($result)), (10))"));
}
