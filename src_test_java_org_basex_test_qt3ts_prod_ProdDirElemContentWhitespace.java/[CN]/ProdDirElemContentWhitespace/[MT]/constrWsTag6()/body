{
  final XQuery query=new XQuery("declare boundary-space preserve; <elem>   </elem>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem>   </elem>",false));
}
