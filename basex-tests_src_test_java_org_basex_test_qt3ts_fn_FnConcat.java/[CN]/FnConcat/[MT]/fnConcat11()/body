{
  final XQuery query=new XQuery("fn:concat(fn:concat(\"zzz\",\"zz\"),\"123\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"zzzzz123"));
}
