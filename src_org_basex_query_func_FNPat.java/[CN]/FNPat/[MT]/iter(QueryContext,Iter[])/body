{
  final byte[] val=checkStr(arg[0]);
switch (func) {
case MATCH:
    return match(val,arg);
case REPLACE:
  return replace(val,arg);
case TOKEN:
return token(val,arg);
default :
BaseX.notexpected(func);
return null;
}
}
