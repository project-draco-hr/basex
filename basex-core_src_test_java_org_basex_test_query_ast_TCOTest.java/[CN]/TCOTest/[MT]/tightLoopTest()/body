{
  check("declare function local:foo() { local:foo() }; local:foo()",null,"exists(//" + Util.name(StaticFunc.class) + "/"+ Util.name(StaticFuncCall.class)+ "[@tailCall eq 'true'])","exists(//" + Util.name(StaticFuncCall.class) + "[@tailCall eq 'false'])");
}
