{
  final XQuery query=new XQuery("declare option Q{http://www.example.com/ns}option \"ignore me\"; <a/>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a/>",false));
}
