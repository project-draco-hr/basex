{
  if (!minus)   return it;
switch (it.type) {
case DBL:
    return Dbl.get(-it.dbl());
case FLT:
  return Flt.get(-it.flt());
case DEC:
return Dec.get(it.dec().negate());
default :
return Itr.get(-it.itr());
}
}
