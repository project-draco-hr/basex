{
  final XQuery query=new XQuery("for $h in ./works[1]/employee[2] return\n" + "         fn:string-length(fn:name($h/child::text()[last()]))",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
