{
  try {
    final File in=pr.dbpath(db);
    final IOFile file=new IOFile(pr.dbpath(db + "-" + DATE.format(new Date())+ IO.ZIPSUFFIX));
    final Zip zip=progress(new Zip(file));
    zip.zip(in);
    return true;
  }
 catch (  final IOException ex) {
    Util.debug(ex);
    return false;
  }
}
