{
  try {
    final File in=mprop.dbpath(db);
    final IOFile file=new IOFile(mprop.dbpath(db + "-" + DATE.format(new Date())+ IO.ZIPSUFFIX));
    final Zip zip=progress(new Zip(file));
    zip.zip(in);
    return true;
  }
 catch (  final IOException ex) {
    Util.debug(ex);
    return false;
  }
}
