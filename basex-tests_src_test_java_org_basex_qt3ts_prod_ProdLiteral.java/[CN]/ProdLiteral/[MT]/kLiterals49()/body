{
  final XQuery query=new XQuery("\"I love brownies&amp;cookies.\" eq \"I love brownies&amp;cookies.\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
