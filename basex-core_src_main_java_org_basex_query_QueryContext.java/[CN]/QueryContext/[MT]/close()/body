{
  if (closed)   return;
  if (parent == null) {
    closed=true;
    resources.close();
  }
  for (  final Entry<Option<?>,Object> e : staticOpts.entrySet()) {
    context.options.put(e.getKey(),e.getValue());
  }
}
