{
  if (closed)   return;
  closed=true;
  for (  final Entry<Option<?>,Object> e : staticOpts.entrySet())   context.options.put(e.getKey(),e.getValue());
  resource.close();
  if (jdbc != null)   jdbc.close();
  if (sessions != null)   sessions.close();
  modLoader.close();
}
