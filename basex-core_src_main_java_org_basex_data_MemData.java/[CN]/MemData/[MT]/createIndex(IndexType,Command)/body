{
  final IndexBuilder ib;
switch (type) {
case TEXT:
    ib=new MemValuesBuilder(this,true);
  break;
case ATTRIBUTE:
ib=new MemValuesBuilder(this,false);
break;
case FULLTEXT:
throw new BaseXException(NO_MAINMEM);
default :
throw Util.notExpected();
}
if (cmd != null) cmd.proc(ib);
set(type,ib.build());
}
