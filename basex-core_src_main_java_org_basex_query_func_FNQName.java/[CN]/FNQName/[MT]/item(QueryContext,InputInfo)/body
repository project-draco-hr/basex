{
  final Item it=exprs[0].item(qc,info);
  final Item it2=exprs.length == 2 ? exprs[1].item(qc,info) : null;
switch (func) {
case RESOLVE_QNAME:
    return resolveQName(it,it2);
case QNAME:
  return qName(it,it2);
case LOCAL_NAME_FROM_QNAME:
return lnFromQName(qc,it);
case PREFIX_FROM_QNAME:
return prefixFromQName(qc,it);
case NAMESPACE_URI_FOR_PREFIX:
return nsUriForPrefix(it,it2);
case RESOLVE_URI:
return resolveUri(it,it2);
default :
return super.item(qc,ii);
}
}
