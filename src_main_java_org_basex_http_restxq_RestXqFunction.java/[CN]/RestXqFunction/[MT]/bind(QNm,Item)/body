{
  Item it=item;
  for (  final Var var : function.args) {
    if (var.name.eq(name)) {
      if (var.type != null)       it=var.type.type.cast(item,context,null);
      var.bind(it,context);
      return;
    }
  }
}
