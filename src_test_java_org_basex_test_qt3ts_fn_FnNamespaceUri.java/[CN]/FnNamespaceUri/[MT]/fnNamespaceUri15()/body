{
  final XQuery query=new XQuery("fn:namespace-uri(/works[1]/employee[1]/@name)",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("\"\""));
}
