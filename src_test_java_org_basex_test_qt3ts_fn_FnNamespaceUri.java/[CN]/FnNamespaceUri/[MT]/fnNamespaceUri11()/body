{
  final XQuery query=new XQuery("fn:namespace-uri(element elementNode {\"with no namespace\"})",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("\"\""));
}
