{
  final XQuery query=new XQuery("format-integer(12345678901,'# 000')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("'12 345 678 901'"));
}
