{
  final XQuery query=new XQuery("format-integer(602347826, '#(000)000-000')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("'(602)347-826'"));
}
