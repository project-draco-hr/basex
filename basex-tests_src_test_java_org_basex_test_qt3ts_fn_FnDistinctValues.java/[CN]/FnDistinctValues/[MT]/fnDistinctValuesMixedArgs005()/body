{
  final XQuery query=new XQuery("fn:distinct-values(( 0e0, -0, 0, 1 ))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertPermutation("1, 0"));
}
