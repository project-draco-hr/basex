{
  final XQuery query=new XQuery("empty(insert-before((1, current-time(), 3), 0, (4, 5, 6))[last() - 10])",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
