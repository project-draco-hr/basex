{
  init();
  tok.init();
  boolean m1=more();
  boolean m2=tok.more();
  while (m1 && m2 && Token.cmp(get(),tok.get()) != 0) {
    m2=tok.more();
  }
  while (m1 && m2 && Token.cmp(get(),tok.get()) == 0) {
    m1=more();
    m2=tok.more();
  }
  return !m1;
}
