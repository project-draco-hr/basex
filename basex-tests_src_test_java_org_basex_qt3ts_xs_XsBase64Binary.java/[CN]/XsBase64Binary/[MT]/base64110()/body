{
  final XQuery query=new XQuery("xs:hexBinary(xs:base64Binary(\"AQIDBA==\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"01020304"));
}
