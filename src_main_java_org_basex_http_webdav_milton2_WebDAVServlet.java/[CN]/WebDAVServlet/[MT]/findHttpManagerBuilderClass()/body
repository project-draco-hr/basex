{
  try {
    enableLocking();
    return (Class<? extends HttpManagerBuilder>)WebDAVServlet.class.getClassLoader().loadClass(HTTP_MANAGER_BUILDER_ENT);
  }
 catch (  Throwable e) {
    Util.debug("milton2 server enterprise is not available");
    return null;
  }
}
