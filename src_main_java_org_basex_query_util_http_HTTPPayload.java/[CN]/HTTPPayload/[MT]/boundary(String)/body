{
  int i=ct.toLowerCase(Locale.ENGLISH).indexOf(BOUNDARY_IS);
  if (i == -1)   return null;
  String b=ct.substring(i + BOUNDARY_IS.length());
  if (b.charAt(0) == '"') {
    i=b.lastIndexOf('"');
    b=b.substring(1,i);
  }
  return token(b);
}
