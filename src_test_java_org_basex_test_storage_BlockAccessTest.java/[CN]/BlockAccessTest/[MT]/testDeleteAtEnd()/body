{
  tda.delete(size - 3,3);
  assertEquals("Three nodes deleted => size-3",size - 3,tda.size());
  assertEntrysEqual(0,0,size - 3);
  closeAndReload();
  assertEquals("Three nodes deleted => size-3",size - 3,tda.size());
  assertEntrysEqual(0,0,size - 3);
}
