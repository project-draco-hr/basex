{
  tda.delete(0,nodes);
  assertEquals(blocks - 1,tda.blocks());
  assertEntrysEqual(nodes,0,size - nodes);
  closeAndReload();
  assertEquals(blocks - 1,tda.blocks());
  assertEntrysEqual(nodes,0,size - nodes);
}
