{
  final File[] files=d.listFiles();
  if (files == null)   return;
  for (  final File f : files) {
    if (IO.isSymlink(f))     continue;
    if (f.isDirectory()) {
      dir(f);
    }
 else {
      file(f);
    }
  }
}
