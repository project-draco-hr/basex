{
  final File[] files=d.listFiles();
  if (files == null)   return;
  final boolean fuse=prop.is(Prop.FUSE);
  for (  final File f : files) {
    if (!valid(f))     continue;
    if (f.isDirectory()) {
      if (fuse)       new File(backingpath + f.getAbsolutePath().substring(importRootLength)).mkdir();
      dir(f);
    }
 else {
      if (fuse)       copy(f.getAbsoluteFile(),new File(backingpath + f.getAbsolutePath().substring(importRootLength)));
      file(f);
    }
  }
}
