{
  try {
    final Field f=tda.getClass().getDeclaredField("blocks");
    f.setAccessible(true);
    return f.getInt(tda);
  }
 catch (  final Exception ex) {
    Util.stack(ex);
    return 0;
  }
}
