{
  try {
    return new Window(sliding,cc.copy(var,vm),expr.copy(cc,vm),start.copy(cc,vm),only,end != null ? end.copy(cc,vm) : null);
  }
 catch (  final QueryException e) {
    throw Util.notExpected(e);
  }
}
