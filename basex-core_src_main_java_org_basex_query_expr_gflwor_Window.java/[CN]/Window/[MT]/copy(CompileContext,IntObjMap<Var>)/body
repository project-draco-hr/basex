{
  try {
    return new Window(sliding,cc.copy(var,vs),expr.copy(cc,vs),start.copy(cc,vs),only,end != null ? end.copy(cc,vs) : null);
  }
 catch (  final QueryException e) {
    throw Util.notExpected(e);
  }
}
