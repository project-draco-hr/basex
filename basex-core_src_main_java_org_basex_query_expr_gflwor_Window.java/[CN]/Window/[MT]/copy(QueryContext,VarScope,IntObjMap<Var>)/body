{
  final Var v=scp.newCopyOf(qc,var);
  vs.put(var.id,v);
  try {
    return new Window(info,sliding,v,expr.copy(qc,scp,vs),start.copy(qc,scp,vs),only,end != null ? end.copy(qc,scp,vs) : null);
  }
 catch (  final QueryException e) {
    throw Util.notExpected(e);
  }
}
