{
  no(new Rename(FILE,"xxx"));
  ok(new CreateDB(NAME,FILE));
  no(new Rename(FN,"/"));
  no(new Rename(FN,""));
  ok(new Rename(FILE,FILE));
  ok(new Rename(FILE,"xxx"));
  ok(new Rename(FILE,"xxx"));
}
