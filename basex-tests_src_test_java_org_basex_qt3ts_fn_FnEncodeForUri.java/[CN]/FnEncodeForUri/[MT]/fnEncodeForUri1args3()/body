{
  final XQuery query=new XQuery("encode-for-uri(\"100% organic\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"100%25%20organic"));
}
