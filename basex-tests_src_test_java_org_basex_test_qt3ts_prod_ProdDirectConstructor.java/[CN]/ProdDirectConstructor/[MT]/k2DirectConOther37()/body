{
  final XQuery query=new XQuery("<?target {1 + 1}?>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<?target {1 + 1}?>",false));
}
