{
  final XQuery query=new XQuery("string-join((<a>X</a>, <a>Y</a>, <a>Z</a>), '')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"XYZ"));
}
