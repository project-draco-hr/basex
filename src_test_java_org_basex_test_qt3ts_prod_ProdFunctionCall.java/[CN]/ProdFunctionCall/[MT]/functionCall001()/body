{
  final XQuery query=new XQuery("concat(<a>X</a>, <a>Y</a>)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"XY"));
}
