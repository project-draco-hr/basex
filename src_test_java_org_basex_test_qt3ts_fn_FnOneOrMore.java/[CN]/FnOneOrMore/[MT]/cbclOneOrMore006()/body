{
  final XQuery query=new XQuery("one-or-more((1 to 10)[. div 2 = 0])",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0004"));
}
