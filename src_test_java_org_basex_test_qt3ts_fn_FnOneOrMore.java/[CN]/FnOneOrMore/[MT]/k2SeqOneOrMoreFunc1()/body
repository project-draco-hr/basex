{
  final XQuery query=new XQuery("codepoints-to-string(one-or-more(string-to-codepoints(\"foo\")))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"foo"));
}
