{
switch (func) {
case _HASH_MD5:
    return hash("MD5",qc);
case _HASH_SHA1:
  return hash("SHA",qc);
case _HASH_SHA256:
return hash("SHA-256",qc);
case _HASH_HASH:
return hash(qc);
default :
return super.item(qc,ii);
}
}
