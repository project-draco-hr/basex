{
  final XQuery query=new XQuery(queryFile(file("misc/XMLEdition/XML10-4ed-Excluded-char-2.xq")),ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0003"));
}
