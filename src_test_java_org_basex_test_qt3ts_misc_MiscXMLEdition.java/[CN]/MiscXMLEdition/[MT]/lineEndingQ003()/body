{
  final XQuery query=new XQuery("deep-equal(string-to-codepoints('\r \r\n" + " \n" + "\r'), (10, 32, 10, 32, 10, 10))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
