{
  final XQuery query=new XQuery("<egg??/>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertSerialization("<egg??/>",false) || serializationMatches("<egg??/>","")));
}
