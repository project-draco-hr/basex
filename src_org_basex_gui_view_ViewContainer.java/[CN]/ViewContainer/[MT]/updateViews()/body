{
  removeAll();
  layout.setVisibility();
  layout.createView(this);
  validate();
  repaint();
  final String lay=layout.layoutString();
  gui.prop.set(views == panels[0] ? GUIProp.LAYOUTCLOSED : GUIProp.LAYOUTOPENED,lay);
}
