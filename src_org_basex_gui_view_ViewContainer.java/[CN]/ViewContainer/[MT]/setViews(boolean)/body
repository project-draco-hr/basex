{
  views=panels[db ? 1 : 0];
  final String lo1=gui.prop.get(db ? GUIProp.LAYOUTOPENED : GUIProp.LAYOUTCLOSED);
  final String lo2=db ? LAYOUTOPEN : LAYOUTCLOSE;
  if (!buildLayout(lo1) && !buildLayout(lo2)) {
    Main.errln("Could not build layout \"%\"",lo2);
  }
}
