{
  views=panels[db ? 1 : 0];
  if (db) {
    if (!buildLayout(GUIProp.layoutopened))     buildLayout(GUIConstants.LAYOUTOPENED);
  }
 else {
    if (!buildLayout(GUIProp.layoutclosed))     buildLayout(GUIConstants.LAYOUTCLOSED);
  }
}
