{
  found=false;
  int i=cne[0] + 1;
  int s=cne.length - 2;
  if (s == i)   return i;
  while (i < s && cne[i + 1] < toInsert)   i+=2;
  if (i < s && cne[i + 1] == toInsert) {
    found=true;
  }
  return i;
}
