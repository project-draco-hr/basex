{
  if (searchNode == null || searchNode.length == 0)   return null;
  int[] ne=getNodeIdFromTrieRecursive(cNode,getNodeEntry(cNode),searchNode);
  if (ne == null) {
    return null;
  }
  return getDataFromDataArray(ne[ne.length - 2],ne[ne.length - 1]);
}
