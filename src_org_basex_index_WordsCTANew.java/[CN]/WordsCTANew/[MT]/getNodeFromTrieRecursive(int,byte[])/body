{
  if (searchNode == null || searchNode.length == 0)   return null;
  int[] ne=getNodeIdFromTrieRecursive(cNode,searchNode);
  if (ne == null) {
    return null;
  }
  long ldid=did;
  return getDataFromDataArray(ne[ne.length - 1],ldid);
}
