{
  final XQuery query=new XQuery("sum((1, 2, 3, xs:anyURI(\"a string\"), xs:double(\"NaN\")), 3)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
