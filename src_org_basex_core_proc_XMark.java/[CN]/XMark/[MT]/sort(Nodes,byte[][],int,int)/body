{
  if (e <= s)   return;
  int i=s - 1;
  int j=e;
  while (true) {
    while (diff(sort[++i],sort[e]) < 0)     ;
    while (i != j && diff(sort[--j],sort[e]) > 0)     ;
    if (i >= j)     break;
    swap(in,sort,i,j);
  }
  swap(in,sort,i,e);
  sort(in,sort,s,i - 1);
  sort(in,sort,i + 1,e);
}
