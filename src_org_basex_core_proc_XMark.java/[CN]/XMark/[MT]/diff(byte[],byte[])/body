{
  final int tl=to.length;
  final int ql=qu.length;
  final int l=tl < ql ? tl : ql;
  int i=-1;
  while (++i < l) {
    final int c=to[i] - qu[i];
    if (c != 0)     return c;
  }
  return tl - ql;
}
