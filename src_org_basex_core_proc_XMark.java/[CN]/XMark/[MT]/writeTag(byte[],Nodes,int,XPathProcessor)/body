{
  final int[] tmp=eval(steps,in,pre).nodes;
  out.startElement(tag);
  if (tmp.length == 0) {
    out.emptyElement();
  }
 else {
    out.finishElement();
    for (    final int n : tmp)     out.text(data.atom(n));
    out.closeElement();
  }
}
