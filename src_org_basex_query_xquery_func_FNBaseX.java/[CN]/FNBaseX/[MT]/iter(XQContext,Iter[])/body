{
switch (func) {
case EVAL:
    return eval(arg);
case RANDOM:
  return random(arg);
case CONTAINSLC:
return contains(arg);
case FILENAME:
return filename(ctx);
default :
throw new RuntimeException("Not defined: " + func);
}
}
