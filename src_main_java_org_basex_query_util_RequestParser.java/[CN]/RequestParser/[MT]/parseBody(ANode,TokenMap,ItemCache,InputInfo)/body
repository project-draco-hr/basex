{
  parseAttrs(body,attrs);
  checkBody(body,attrs,ii);
  if (attrs.get(SRC) == null) {
    ANode n;
    final NodeMore i=body.children();
    while ((n=i.next()) != null) {
      bodyContent.add(n);
    }
  }
}
