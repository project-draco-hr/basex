{
  super.comp(ctx);
  if (uses(Use.CTX,ctx))   return cmp(ctx);
  for (  final Expr e : expr)   if (!e.value())   return cmp(ctx);
  return optPre(func.ret.zeroOrOne() ? atomic(ctx,input) : iter(ctx).finish(),ctx);
}
