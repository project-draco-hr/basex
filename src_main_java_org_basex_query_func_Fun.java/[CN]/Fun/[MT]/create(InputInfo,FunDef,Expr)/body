{
  try {
    return f.func.getDeclaredConstructor(InputInfo.class,FunDef.class,Expr[].class).newInstance(ii,f,e);
  }
 catch (  final Exception ex) {
    ex.printStackTrace();
    Main.debug(ex);
    return null;
  }
}
