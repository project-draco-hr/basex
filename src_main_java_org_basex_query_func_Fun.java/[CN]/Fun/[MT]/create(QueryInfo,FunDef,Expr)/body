{
  try {
    return f.func.getDeclaredConstructor(QueryInfo.class,FunDef.class,Expr[].class).newInstance(i,f,e);
  }
 catch (  final Exception ex) {
    ex.printStackTrace();
    Main.debug(ex);
    return null;
  }
}
