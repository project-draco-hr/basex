{
  final Query q=s.query("for $d in db:list($db, $path) " + "return db:add($trgdb, collection($db || '/' || $d), " + "$trgdir || '/' || substring-after($d, $path))");
  q.bind("$db",db);
  q.bind("$path",path);
  q.bind("$trgdb",trgdb);
  q.bind("$trgdir",trgdir);
  q.execute();
}
