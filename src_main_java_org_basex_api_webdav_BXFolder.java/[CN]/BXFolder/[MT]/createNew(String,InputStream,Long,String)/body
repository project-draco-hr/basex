{
  try {
    ClientSession cs=login(user,pass);
    try {
      cs.execute(new Open(db));
      cs.add(doc,path,io);
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
