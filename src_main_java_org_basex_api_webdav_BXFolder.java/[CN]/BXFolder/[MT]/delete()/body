{
  try {
    final ClientSession cs=login(user,pass);
    try {
      cs.execute(new Open(db));
      cs.execute(new Delete(path));
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
