{
  try {
    ClientSession cs=login(user,pass);
    try {
      cs.execute(new Open(db));
      cs.query("db:delete('" + path + "')").execute();
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
