{
  try {
    final Session s=factory.login(user,pass);
    try {
      return factory.resource(s,db,path + SEP + childName,user,pass);
    }
  finally {
      s.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
