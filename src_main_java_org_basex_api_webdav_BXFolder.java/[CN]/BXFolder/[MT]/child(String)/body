{
  try {
    final Session s=BXResourceFactory.login(user,pass);
    try {
      return resource(s,db,path + DIRSEP + childName);
    }
  finally {
      s.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
