{
  try {
    final ClientSession cs=login(user,pass);
    try {
      return resource(cs,db,path + DIRSEP + childName);
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
