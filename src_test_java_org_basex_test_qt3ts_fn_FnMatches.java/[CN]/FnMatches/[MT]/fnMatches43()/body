{
  final XQuery query=new XQuery("fn:matches(concat('Mary', codepoints-to-string(10), 'Jones'), 'Mary.Jones')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
