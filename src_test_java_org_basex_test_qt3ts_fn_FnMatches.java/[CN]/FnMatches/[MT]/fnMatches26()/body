{
  final XQuery query=new XQuery("fn:matches(\"concat('abcd', codepoints-to-string(10), 'defg', codepoints-to-string(10))\", \"^$\", \"m\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
