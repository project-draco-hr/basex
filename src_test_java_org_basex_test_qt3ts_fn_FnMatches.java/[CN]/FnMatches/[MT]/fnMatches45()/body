{
  final XQuery query=new XQuery("fn:matches(concat('Mary', codepoints-to-string(13), 'Jones'), 'Mary.Jones')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
