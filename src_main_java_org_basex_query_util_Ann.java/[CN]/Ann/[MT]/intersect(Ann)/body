{
  final Ann o=new Ann();
  for (int i=0; i < size; i++) {
    final QNm name=names[i];
    final Value val=values[i];
    try {
      for (int j=0; j < ann.size; j++) {
        if (name.eq(ann.names[j]) && Compare.deep(val,ann.values[j],null))         o.add(name,val);
      }
    }
 catch (    QueryException e) {
      Util.notexpected(e);
    }
  }
  return o;
}
