{
  this.max=max;
  occMin=new int[max];
  occMax=new int[max];
  txtMin=new byte[max][];
  txtMax=new byte[max][];
  for (int t=0; t < max; ++t) {
    txtMin[t]=Token.EMPTY;
    txtMax[t]=Token.EMPTY;
    occMin[t]=Integer.MAX_VALUE;
  }
}
