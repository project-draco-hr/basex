{
  final XQuery query=new XQuery("fn:min((xs:integer(5000000),xs:double(3e8)))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("5.0E6"));
}
