{
  if (txt != null) {
    final boolean db=context.db();
    if (!db && GUIProp.showstarthelp || db && GUIProp.showhelp)     help.setText(txt,true);
  }
  if (GUIProp.mousefocus && comp != null && comp.isEnabled())   comp.requestFocusInWindow();
}
