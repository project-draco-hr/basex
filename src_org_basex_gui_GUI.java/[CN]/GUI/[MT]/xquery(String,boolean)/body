{
  final Namespaces ns=context.data().ns;
  final int def=ns.get(Token.EMPTY,0);
  String in=qu;
  if (in.trim().length() == 0)   in=".";
  if (def != 0)   in="declare default element namespace \"" + Token.string(ns.key(def)) + "\"; "+ in;
  execute(new XQuery(in),main);
}
