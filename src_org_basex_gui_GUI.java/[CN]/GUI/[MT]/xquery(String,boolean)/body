{
  final Namespaces ns=context.data.ns;
  final int uri=ns.uri(Token.EMPTY,0);
  String in=qu.trim().isEmpty() ? "." : qu;
  if (uri != 0)   in=Main.info("declare default element namespace \"%\"; %",ns.key(uri),in);
  execute(new XQuery(in),main);
}
