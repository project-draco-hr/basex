{
  StringBuilder sb=new StringBuilder();
  int len=(int)(Math.random() * 10 + 1);
  for (int k=0; k < len; k++) {
    sb.append((char)((Math.random() <= 0.5) ? (Math.random() * 26 + 65) : (Math.random() * 26 + 97)));
  }
  return sb.toString();
}
