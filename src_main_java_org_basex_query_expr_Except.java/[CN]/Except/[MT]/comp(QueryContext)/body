{
  super.comp(ctx);
  if (expr[0].empty())   return optPre(Empty.SEQ,ctx);
  for (int e=1; e < expr.length; ++e) {
    if (expr[e].empty()) {
      expr=Array.delete(expr,e--);
      ctx.compInfo(OPTREMOVE,desc(),SimpleType.EMP);
    }
  }
  return expr.length == 1 && !dupl ? expr[0] : this;
}
