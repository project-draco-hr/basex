{
  if (!(node instanceof DBNode))   return;
  final DBNode n=(DBNode)node;
  final Data data=n.data;
  final int k=Nod.kind(n.type);
  if (a)   UpdateFunctions.insertAttributes(n.pre,data,r);
 else   data.insertSeq(n.pre + data.size(n.pre,k),data.parent(n.pre,k),r);
  data.delete(n.pre);
}
