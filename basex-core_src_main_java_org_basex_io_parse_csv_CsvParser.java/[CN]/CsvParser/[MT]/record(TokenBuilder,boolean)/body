{
  if (record && first && data)   conv.record();
  if (record || !first) {
    if (data) {
      conv.entry(entry.finish());
    }
 else {
      conv.header(entry.finish());
    }
    entry.reset();
  }
}
