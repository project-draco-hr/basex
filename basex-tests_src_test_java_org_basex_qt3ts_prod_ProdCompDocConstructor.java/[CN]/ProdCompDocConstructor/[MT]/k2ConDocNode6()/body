{
  final XQuery query=new XQuery("string(document{\"some text\", <e/>, attribute name {\"content\"}})",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
