{
  final XQuery query=new XQuery("(xs:dayTimeDuration(\"P10DT01H01M\") div -2.0)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"-P5DT30M30S"));
}
