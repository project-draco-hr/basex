{
  final XQuery query=new XQuery("xs:dayTimeDuration(\"P3DT4H3M3.100S\") div 0",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FODT0002"));
}
