{
  final Item i=it.u() ? Dbl.get(n) : it;
  if (n == d)   return i.iter();
switch (it.type) {
case DEC:
    return Dec.iter(d);
case DBL:
  return Dbl.iter(d);
case FLT:
return Flt.iter((float)d);
default :
return Itr.iter((long)d);
}
}
