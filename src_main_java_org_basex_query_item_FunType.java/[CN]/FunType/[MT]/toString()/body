{
  final TokenBuilder tb=new TokenBuilder(FUNCTION).add('(');
  if (this == ANY) {
    tb.add('*').add(')');
  }
 else {
    tb.addSep(args,", ").add(") as ").add(ret.toString());
  }
  return tb.toString();
}
