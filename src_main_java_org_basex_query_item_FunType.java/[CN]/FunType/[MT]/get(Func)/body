{
  final SeqType[] at=new SeqType[f.args.length];
  for (int i=0; i < at.length; i++)   at[i]=f.args[i].type == null ? SeqType.ITEM_ZM : f.args[i].type;
  return new FunType(at,f.var.type == null ? SeqType.ITEM_ZM : f.var.type);
}
