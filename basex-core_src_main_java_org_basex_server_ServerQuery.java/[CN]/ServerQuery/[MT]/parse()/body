{
  if (!parsed) {
    try {
      perf.time();
      qp().parse();
      qp.qc.info.parsing=perf.time();
      parsed=true;
    }
 catch (    final QueryException ex) {
      throw new BaseXException(ex);
    }
  }
  return qp;
}
