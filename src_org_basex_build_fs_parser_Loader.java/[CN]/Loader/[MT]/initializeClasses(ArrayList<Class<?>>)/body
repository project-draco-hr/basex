{
  int counter=0;
  for (  Class<?> c : classes) {
    try {
      Class.forName(c.getName(),true,INSTANCE);
      counter++;
    }
 catch (    Throwable t) {
      BaseX.errln("Failed to load class (%)",t.getMessage());
      break;
    }
  }
  return counter;
}
