{
  final XQuery query=new XQuery("fn:tokenize(\"The cat sat on the mat\", \"\\s+\", \"t\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORX0001"));
}
