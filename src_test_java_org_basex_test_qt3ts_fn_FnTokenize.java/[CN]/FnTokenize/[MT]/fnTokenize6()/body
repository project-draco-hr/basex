{
  final XQuery query=new XQuery("fn:tokenize(\"Some unparsed <br> HTML <BR> text\", \"\\s*<br>\\s*\", \"\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("\"Some unparsed\", \"HTML <BR> text\""));
}
