{
  final XQuery query=new XQuery("fn:tokenize(\"The cat sat on the mat\", \"\\s+\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"The cat sat on the mat"));
}
