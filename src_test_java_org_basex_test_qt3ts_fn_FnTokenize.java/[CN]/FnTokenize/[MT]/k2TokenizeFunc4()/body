{
  final XQuery query=new XQuery("fn:tokenize((\"abracadabra\", current-time())[1] treat as xs:string, \"(ab)|(a)\")[last() - 3]",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"c"));
}
