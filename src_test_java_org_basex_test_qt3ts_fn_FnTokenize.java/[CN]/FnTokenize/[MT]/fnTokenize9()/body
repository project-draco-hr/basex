{
  final XQuery query=new XQuery("string-join(fn:tokenize(\"abracadabra\", \"(ab)|(a)\"), '#')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"#r#c#d#r#"));
}
