{
  final XQuery query=new XQuery("fn:tokenize(concat('Mary', codepoints-to-string(13), 'Jones'), 'y.J')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("concat('Mary', codepoints-to-string(13), 'Jones')"));
}
