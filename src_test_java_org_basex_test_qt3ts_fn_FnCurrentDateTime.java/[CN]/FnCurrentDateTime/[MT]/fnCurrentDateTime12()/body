{
  final XQuery query=new XQuery("fn:string(fn:current-dateTime()) eq fn:string(fn:current-dateTime())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
