{
  boolean b=false;
  if (cp.size > 0) {
    for (int i=0; i < cp.size; i++) {
      mp[pex[cp.list[i]]]=exprs[pex[cp.list[i]]].more();
      if (!b)       b=mp[i];
    }
    cp.reset();
  }
  if (!b) {
    for (    boolean c : mp)     if (c)     return true;
  }
  return b;
}
