{
  boolean b=false;
  if (cp.size > 0) {
    for (int i=0; i < cp.size; i++) {
      mp[pex[cp.get(i)]]=exprs[pex[cp.get(i)]].more();
      if (!b)       b=mp[i];
    }
    cp.reset(pex.length);
  }
  if (!b) {
    for (    boolean c : mp)     if (c)     return true;
  }
  return b;
}
