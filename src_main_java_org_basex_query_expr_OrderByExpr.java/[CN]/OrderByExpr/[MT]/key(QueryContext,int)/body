{
  Item it=expr.item(ctx,input);
  if (it != null) {
    if (it.isNode())     it=Str.get(it.string(input));
 else     if (it.isNumber() && Double.isNaN(it.dbl(input)))     it=null;
  }
  return it;
}
