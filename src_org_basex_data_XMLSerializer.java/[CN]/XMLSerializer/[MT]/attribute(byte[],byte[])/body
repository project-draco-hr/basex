{
  print(' ');
  print(n);
  print(ATT1);
  for (int k=0; k < v.length; k+=cl(v[k])) {
    final int ch=cp(v,k);
switch (ch) {
case '"':
      print(E_QU);
    break;
case 0x9:
  print(E_TAB);
break;
case 0xA:
print(E_NL);
break;
default :
ch(ch);
}
}
print(ATT2);
}
