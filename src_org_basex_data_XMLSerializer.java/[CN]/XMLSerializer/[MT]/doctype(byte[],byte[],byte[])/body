{
  out.print(DT);
  out.print(' ');
  out.print(t);
  if (te != null) {
    out.print(' ');
    out.print(SYS);
    out.print(' ');
    out.print(te);
  }
  if (ti != null) {
    out.print(' ');
    out.print(ti);
  }
  out.print(ELEM2);
  out.println();
}
