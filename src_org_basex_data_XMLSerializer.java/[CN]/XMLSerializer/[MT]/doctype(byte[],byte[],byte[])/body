{
  out.print(DT);
  out.print(' ');
  out.print(t);
  out.print(' ');
  if (te != null) {
    out.print("SYSTEM".getBytes());
    out.print(' ');
    out.print(te);
    if (ti != null)     out.print(' ');
  }
  if (ti != null)   out.print(ti);
  out.print(ELEM2);
  out.println();
}
