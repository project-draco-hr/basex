{
  final XQuery query=new XQuery("fn:substring-before(\"12345\",\"2345\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("\"1\"") && assertCount(1)));
}
