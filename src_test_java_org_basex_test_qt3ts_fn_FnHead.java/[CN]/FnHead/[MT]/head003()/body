{
  final XQuery query=new XQuery("let $a := /works return head($a/*)/string(@name)",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"Jane Doe 1"));
}
