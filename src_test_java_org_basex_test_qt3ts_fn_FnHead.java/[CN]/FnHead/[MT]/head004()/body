{
  final XQuery query=new XQuery("let $a := /works return name(head($a))",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"works"));
}
