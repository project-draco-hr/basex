{
  JStruct root=object();
  if (root == null)   root=array();
  if (root == null)   error(INVALEXP,curr(),"\"{\" or \"[\"");
  skipWS();
  if (more())   error(INVALEXP,curr(),"end of file");
  return root;
}
