{
  try {
    debug("[DeepBase.unlink] " + path);
    Nodes n=queryOne(pn2xp(path));
    data.delete(n.nodes[0]);
    data.flush();
  }
 catch (  QueryException e) {
    e.printStackTrace();
    return -1;
  }
  return 0;
}
