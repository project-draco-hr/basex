{
  mountpoint=mountPoint;
  dbname=dbName;
  backingstore=backingStore;
  wgui=guitoggle;
  init();
  if (wgui) {
    final BaseXWin win=new BaseXWin(new String[]{});
    while (win.gui == null)     Performance.sleep(100);
    gui=win.gui;
    gui.context.data(data);
    gui.notify.init();
  }
}
