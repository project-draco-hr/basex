{
  found=false;
  int i=next.list[cn].length - 3;
  final int s=0;
  if (s == i)   return 1;
  while (i > s && Token.diff(tokens.list[next.list[next.list[cn][i]][0]][0],toInsert) > 0)   i--;
  if (i > s && Token.diff(tokens.list[next.list[next.list[cn][i]][0]][0],toInsert) == 0) {
    found=true;
    return i;
  }
  return i + 1;
}
