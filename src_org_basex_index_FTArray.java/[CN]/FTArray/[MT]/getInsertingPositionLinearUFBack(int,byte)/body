{
  found=false;
  int i=next.list[cn].length - 3;
  final int s=0;
  if (s == i)   return 1;
  while (i > s && tokens.list[next.list[next.list[cn][i]][0]][0] > toInsert)   i--;
  if (i > s && tokens.list[next.list[next.list[cn][i]][0]][0] == toInsert) {
    found=true;
    return i;
  }
  return i + 1;
}
