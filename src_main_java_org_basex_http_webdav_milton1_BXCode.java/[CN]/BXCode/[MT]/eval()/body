{
  try {
    final E ret=get();
    if (ret == null)     run();
    return ret;
  }
 catch (  final IOException ex) {
    Util.errln(ex);
    throw new BadRequestException(resource,ex.getMessage());
  }
}
