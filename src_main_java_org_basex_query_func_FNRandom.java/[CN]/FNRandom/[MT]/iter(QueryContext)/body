{
switch (sig) {
case _RANDOM_SEEDED_DOUBLE:
    return randomSeededDouble(ctx);
case _RANDOM_SEEDED_INTEGER:
  return randomSeededInt(ctx);
case _RANDOM_GAUSSIAN:
return randomGaussian(ctx);
default :
return super.iter(ctx);
}
}
