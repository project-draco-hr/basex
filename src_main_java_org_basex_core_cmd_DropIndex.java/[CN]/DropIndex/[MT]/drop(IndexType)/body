{
  try {
    return drop(idx,context.data()) ? info(INDEX_DROPPED_X_X,idx,perf) : error(INDEX_NOT_DROPPED_X,idx);
  }
 catch (  final IOException ex) {
    Util.debug(ex);
    return error(ex.getMessage());
  }
}
