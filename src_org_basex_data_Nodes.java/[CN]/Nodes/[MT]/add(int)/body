{
  int i=find(p);
  if (i < 0) {
    if (size == 0)     pre=new int[1];
 else     if (size == pre.length)     pre=Array.extend(pre);
    i=-i - 1;
    Array.move(pre,i,1,size++ - i);
    pre[i]=p;
  }
  return this;
}
