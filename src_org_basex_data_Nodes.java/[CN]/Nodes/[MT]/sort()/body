{
  int i=Integer.MIN_VALUE;
  sorted=nodes;
  for (  final int n : nodes) {
    if (i > n) {
      sorted=Array.finish(nodes,size);
      Arrays.sort(sorted);
      break;
    }
    i=n;
  }
  return sorted;
}
