{
  check();
  if (it != null && !next)   throw new BXQException("Current item was already requested.");
  final StringBuilder sb=new StringBuilder();
  while (next())   sb.append(item().getItemAsString(p));
  return sb.toString();
}
