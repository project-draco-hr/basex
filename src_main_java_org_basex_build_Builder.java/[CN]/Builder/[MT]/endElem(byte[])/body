{
  checkStop();
  if (--lvl == 0 || tags.id(nm) != tstack.get(lvl))   error(CLOSINGTAG,parser.detail(),nm,tags.key(tstack.get(lvl)));
  final int pre=pstack.get(lvl);
  setSize(pre,meta.size - pre);
  ns.close(pre);
}
