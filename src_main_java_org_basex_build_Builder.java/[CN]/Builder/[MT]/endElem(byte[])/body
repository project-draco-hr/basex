{
  checkStop();
  if (--lvl == 0 || tags.id(tag) != tagStack[lvl])   error(CLOSINGTAG,parser.det(),tag,tags.key(tagStack[lvl]));
  final int pre=preStack[lvl];
  setSize(pre,meta.size - pre);
  ns.close(pre);
}
