{
  checkStop();
  if (--lvl == 0 || tags.id(name) != tagStack[lvl])   error(CLOSINGTAG,parser.detail(),name,tags.key(tagStack[lvl]));
  final int pre=preStack[lvl];
  setSize(pre,meta.size - pre);
  ns.close(pre);
}
