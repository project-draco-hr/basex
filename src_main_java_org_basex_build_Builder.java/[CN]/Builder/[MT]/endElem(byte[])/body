{
  checkStop();
  if (--lvl == 0 || tags.id(name) != tstack.get(lvl))   error(CLOSINGTAG,parser.detail(),name,tags.key(tstack.get(lvl)));
  final int pre=pstack.get(lvl);
  setSize(pre,meta.size - pre);
  ns.close(pre);
}
