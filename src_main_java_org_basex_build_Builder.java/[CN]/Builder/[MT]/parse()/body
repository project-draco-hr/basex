{
  final Performance perf=Prop.debug ? new Performance() : null;
  Util.debug(tit() + DOTS);
  parser.parse(this);
  if (meta.size == 0) {
    startDoc(token(name));
    endDoc();
    setSize(0,meta.size);
  }
  meta.lastid=meta.size - 1;
  Util.memory(perf);
}
