{
  if (meta.chop)   t.trim();
  final boolean ignore=!inDoc || lvl == 1;
  if ((meta.chop && t.size() != 0 || !t.wsp()) && ignore)   error(inDoc ? AFTERROOT : BEFOREROOT,parser.det());
  if (t.size() != 0 && !ignore)   addText(t,Data.TEXT);
}
