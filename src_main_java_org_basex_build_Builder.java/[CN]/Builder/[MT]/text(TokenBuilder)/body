{
  if (meta.chop)   t.chop();
  if ((meta.chop && t.size() != 0 || !t.wsp()) && (!inDoc || lvl == 1))   error(inDoc ? AFTERROOT : BEFOREROOT,parser.det());
  if (t.size() == 0)   return;
  addText(t,Data.TEXT);
}
