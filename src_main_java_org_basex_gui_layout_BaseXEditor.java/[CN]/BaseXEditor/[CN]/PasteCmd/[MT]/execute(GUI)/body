{
  if (hist == null)   return;
  final int tc=text.cursor();
  text.pos(tc);
  final String txt=clip();
  if (txt == null)   return;
  if (text.marked())   text.delete();
  text.add(txt);
  finish(tc);
}
