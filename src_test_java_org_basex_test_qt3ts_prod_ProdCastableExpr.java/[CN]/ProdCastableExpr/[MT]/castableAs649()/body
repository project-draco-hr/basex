{
  final XQuery query=new XQuery("count(//employee[salary castable as xs:integer][xs:integer(salary) gt 65000])",ctx);
  try {
    query.context(node(file("op/union/acme_corp.xml")));
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("3"));
}
