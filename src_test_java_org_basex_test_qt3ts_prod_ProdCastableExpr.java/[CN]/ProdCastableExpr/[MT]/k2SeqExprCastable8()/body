{
  final XQuery query=new XQuery("(\"http:\\\\invalid>URI\\someURI\") castable as xs:anyURI",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
