{
  final XQuery query=new XQuery("(xs:double(1), xs:double(2), xs:double(3)) castable as xs:double*",ctx);
  final QT3Result res=result(query);
  result=res;
  test((error("XPST0003") || error("XPST0051")));
}
