{
  if (pos >= size) {
    try {
      pos=0;
      len+=size;
      size=in.read(buffer);
      if (size <= 0)       return 0;
    }
 catch (    final IOException ex) {
      ex.printStackTrace();
    }
  }
  return buffer[pos++];
}
