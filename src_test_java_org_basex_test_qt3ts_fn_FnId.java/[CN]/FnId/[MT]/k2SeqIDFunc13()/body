{
  final XQuery query=new XQuery("fn:id(//b/@ref)/data(exactly-one(@*))",ctx);
  query.context(node(file("fn/id/XMLIDMany.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"a b c d e f i"));
}
