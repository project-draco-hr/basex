{
  final XQuery query=new XQuery("fn:count(fn:id(\"nomatchingid\", /IDS[1]))",ctx);
  query.context(node(file("fn/id/iddtd.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
