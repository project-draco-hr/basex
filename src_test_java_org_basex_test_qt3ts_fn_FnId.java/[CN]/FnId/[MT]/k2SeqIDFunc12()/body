{
  final XQuery query=new XQuery("id((\".\", \"short\", \"123\"), //xs:element/@name[. = \"positiveInteger\"])/@name",ctx);
  query.context(node(file("fn/id/UsingXMLId.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"short"));
}
