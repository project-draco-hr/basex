{
  if (c.size() == 0)   return;
  final NodIter seq=new NodIter();
  for (  final NodIter ni : c) {
    Nod i;
    while ((i=ni.next()) != null)     seq.add(i);
  }
  md=UpdateFunctions.buildDB(mergeText(seq),new MemData(((DBNode)node).data));
}
