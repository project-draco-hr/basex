{
  if (!shf)   text.noMark();
  int x=text.home(shf);
  if (col == -1)   col=x;
  text.next(shf);
  text.end(Integer.MAX_VALUE,shf);
  for (int i=0; i < l; i++) {
    text.next(shf);
    text.end(col,shf);
  }
}
