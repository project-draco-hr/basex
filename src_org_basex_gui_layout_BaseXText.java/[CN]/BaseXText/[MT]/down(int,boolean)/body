{
  if (!shf)   text.noMark();
  final int x=text.home(shf);
  if (col == -1)   col=x;
  for (int i=0; i < l; i++) {
    text.end(Integer.MAX_VALUE,shf);
    text.next(shf);
  }
  text.end(col,shf);
  if (text.pos() == text.size())   col=-1;
}
