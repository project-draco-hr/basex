{
  if (!shf)   text.noMark();
  int x=text.home(shf);
  if (text.pos() == 0)   return;
  if (col == -1)   col=x;
  for (int i=0; i < l; i++) {
    text.prev(shf);
    text.home(shf);
  }
  text.end(col,shf);
}
