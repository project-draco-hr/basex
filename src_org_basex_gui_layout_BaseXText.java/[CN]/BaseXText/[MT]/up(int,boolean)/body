{
  if (!shf)   text.noMark();
  int x=text.home(shf);
  if (col == -1)   col=x;
  if (text.pos() == 0) {
    col=-1;
    return;
  }
  for (int i=0; i < l; i++) {
    text.prev(shf);
    text.home(shf);
  }
  text.end(col,shf);
}
