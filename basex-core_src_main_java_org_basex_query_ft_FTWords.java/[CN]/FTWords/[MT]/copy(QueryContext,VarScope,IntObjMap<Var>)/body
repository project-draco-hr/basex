{
  final FTWords ftw=new FTWords(info,query.copy(ctx,scp,vs),mode,occ == null ? null : Arr.copyAll(ctx,scp,vs,occ));
  if (ftt != null)   ftw.ftt=ftt.copy(ftw);
  if (txt != null)   ftw.txt=txt.copy();
  ftw.ictx=ictx;
  ftw.first=first;
  ftw.tokNum=tokNum;
  ftw.fast=fast;
  return ftw;
}
