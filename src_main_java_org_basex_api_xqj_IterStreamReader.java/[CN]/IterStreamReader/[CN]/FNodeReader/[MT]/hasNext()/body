{
  final ANode n=iter[l].next();
  if (n != null) {
    nodes[l]=n;
    node=n;
    type();
    if (kind == START_ELEMENT)     iter[++l]=n.children();
  }
 else {
    if (--l < 0)     return false;
    node=nodes[l];
    kind=END_ELEMENT;
  }
  return true;
}
