{
  if (p == s) {
    endElem();
    return;
  }
  final Data data=dbnode.data;
  final int k=data.kind(p);
  final int pa=data.parent(p,k);
  if (l > 0 && parent[l - 1] >= pa) {
    endElem();
    return;
  }
  finish(k,pa);
}
