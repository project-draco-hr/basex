{
  if (next && node == null || !next && !hasNext())   throw new NoSuchElementException();
  next=false;
  if (node.type == NodeType.ATT && read == null)   throw new XMLStreamException();
  return kind;
}
