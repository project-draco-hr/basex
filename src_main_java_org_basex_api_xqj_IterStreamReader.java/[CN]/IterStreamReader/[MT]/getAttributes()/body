{
  if (atts != null)   return;
  checkType(START_ELEMENT,ATTRIBUTE);
  atts=new NodeCache();
  final AxisIter ai=item.atts();
  while (true) {
    final ANode it=ai.next();
    if (it == null)     return;
    atts.add(it);
  }
}
