{
  if (atts == null) {
    checkType(START_ELEMENT,ATTRIBUTE);
    atts=new NodeCache();
    final AxisIter ai=node.atts();
    for (ANode n; (n=ai.next()) != null; )     atts.add(n);
  }
  return atts;
}
