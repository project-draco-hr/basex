{
  final XQuery query=new XQuery("fn:string(fn:implicit-timezone() div ( 0 div 0E0))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOCA0005"));
}
