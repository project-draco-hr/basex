{
  final XQuery query=new XQuery("(fn:encode-for-uri(\"examples0123456789example\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"examples0123456789example"));
}
