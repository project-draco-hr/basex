{
  final boolean full=rt.totalMemory() - rt.freeMemory() >= maxMem;
  if (full) {
    if (cc >= 0)     throw new IOException(PROCOUTMEM);
    merge=true;
    cc=50;
  }
 else {
    cc--;
  }
  return full;
}
