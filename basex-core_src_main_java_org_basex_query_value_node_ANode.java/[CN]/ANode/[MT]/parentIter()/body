{
  return new BasicNodeIter(){
    private boolean all;
    @Override public ANode next(){
      if (all)       return null;
      all=true;
      return parent();
    }
    @Override public ANode get(    final long i){
      return parent();
    }
    @Override public ANode value(){
      return parent();
    }
  }
;
}
