{
  final FTMatches ftm=new FTMatches(sTokenNum);
  ftm.size=size;
  ftm.match=match.clone();
  for (int i=0; i < ftm.match.length; i++)   if (ftm.match[i] != null)   ftm.match[i]=ftm.match[i].copy();
  return ftm;
}
