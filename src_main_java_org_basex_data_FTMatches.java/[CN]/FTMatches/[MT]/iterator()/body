{
  return new Iterator<FTMatch>(){
    private int c=-1;
    @Override public boolean hasNext(){
      return ++c < size;
    }
    @Override public FTMatch next(){
      return match[c];
    }
    @Override public void remove(){
      Util.notexpected();
    }
  }
;
}
