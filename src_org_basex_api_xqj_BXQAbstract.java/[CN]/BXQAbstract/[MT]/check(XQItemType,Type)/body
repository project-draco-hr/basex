{
  check();
  final BXQItemType bit=(BXQItemType)it;
  if (it != null && (t != Type.ATM || ((BXQItemType)it).type.node) && !bit.type.instance(t) && !t.instance(bit.type)) {
    throw new BXQException(WRONG,t,bit.type);
  }
  return it != null ? bit.type : t;
}
