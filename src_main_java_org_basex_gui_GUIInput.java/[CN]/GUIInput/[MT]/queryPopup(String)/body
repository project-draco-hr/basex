{
  final Data data=gui.context.data;
  if (data == null || !data.meta.pthindex)   return;
  StringList sl=null;
  try {
    final QuerySuggest qs=new QuerySuggest(query,new QueryContext(gui.context),data);
    qs.parse(null,null);
    sl=qs.complete();
    pre=query.substring(0,qs.qm);
  }
 catch (  final QueryException ex) {
    sl=ex.complete();
    boolean t=ex.col() == 1;
    pre=query.substring(0,ex.col() - (t ? 1 : 0));
  }
  if (getCaretPosition() < pre.length())   sl=null;
  createCombo(sl);
}
