{
  int i=1;
  while (true) {
    if (!running)     return;
    try {
      launch("xquery " + this.query,verbose);
      i++;
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
    Performance.sleep(r.nextInt(500));
  }
}
