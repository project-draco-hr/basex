{
  final XQuery query=new XQuery("(: Purpose: Return the IDs of those auctions that have one or more keywords in emphasis. (cf. Q15) :) \n" + "         <XMark-result-Q16> { \n" + "            let $auction := (/) return \n"+ "            for $a in $auction/site/closed_auctions/closed_auction \n"+ "            where not( empty( $a/annotation/description/parlist/listitem/parlist/listitem/text/emph/ keyword/ text() ) ) \n"+ "            return <person id=\"{$a/seller/@person}\"/> } </XMark-result-Q16>",ctx);
  query.context(node(file("app/XMark/XMarkAuction.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<XMark-result-Q16><person id=\"person362\"/><person id=\"person279\"/><person id=\"person499\"/></XMark-result-Q16>",false));
}
