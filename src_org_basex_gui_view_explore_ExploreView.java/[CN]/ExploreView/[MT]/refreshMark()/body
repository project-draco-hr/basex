{
  BaseXLayout.enable(go,!gui.prop.is(GUIProp.EXECRT));
  final Nodes marked=gui.context.marked();
  BaseXLayout.enable(filter,!gui.prop.is(GUIProp.FILTERRT) && marked != null && marked.size() != 0);
}
