{
  final XQuery query=new XQuery("/works[1]/employee[1]/empnum[1] >> <a>50000</a>",ctx);
  query.context(node(file("docs/works.xml")));
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
