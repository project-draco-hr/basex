{
  try {
    return toXML();
  }
 catch (  final IOException ex) {
    return bfc.getFileName() + "(" + metaElements.size()+ " metadata attributes, "+ textContents.size()+ " text sections, "+ xmlContents.size()+ " xml sections and "+ fileFragments.size()+ " content sections)";
  }
}
