{
  if (!AtomType.INT.instance(elem.getType())) {
    if (value <= Short.MAX_VALUE)     addMeta(elem,(short)value);
 else     metaDebug(elem,"integer");
  }
 else   addMeta(elem,String.valueOf(value),null);
}
