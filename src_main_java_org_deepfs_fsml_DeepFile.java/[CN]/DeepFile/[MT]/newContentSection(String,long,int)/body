{
  bfc.position();
  final BufferedFileChannel sub=bfc.subChannel(title,contentSize);
  final DeepFile subFile=new DeepFile(registry,sub,context,position,contentSize);
  subFile.addMeta(MetaElem.TITLE,title);
  return subFile;
}
