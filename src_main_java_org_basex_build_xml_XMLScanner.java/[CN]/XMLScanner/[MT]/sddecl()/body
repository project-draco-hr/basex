{
  if (!consume(STANDALONE))   return null;
  s();
  check('=');
  s();
  final int d=qu();
  byte[] sd=token(YES);
  if (!consume(sd)) {
    sd=token(NO);
    if (!consume(sd))     error(DECLSTANDALONE);
  }
  check((char)d);
  return sd;
}
