{
  return type instanceof MapType ? it instanceof Map && ((Map)it).hasType((MapType)type) : type instanceof ArrayType ? it instanceof Array && ((Array)it).hasType((ArrayType)type) : it.type.instanceOf(type) && (!knd || kind == null || kind.eq(it));
}
