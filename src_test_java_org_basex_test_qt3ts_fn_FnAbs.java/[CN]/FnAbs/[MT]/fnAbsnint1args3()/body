{
  final XQuery query=new XQuery("fn:abs(xs:negativeInteger(\"-1\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("1") && assertType("xs:integer")));
}
