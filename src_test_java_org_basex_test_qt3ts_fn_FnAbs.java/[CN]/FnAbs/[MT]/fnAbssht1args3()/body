{
  final XQuery query=new XQuery("fn:abs(xs:short(\"32767\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("32767") && assertType("xs:integer")));
}
