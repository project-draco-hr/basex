{
  final XQuery query=new XQuery("abs(xs:positiveInteger(.)) instance of xs:positiveInteger",ctx);
  query.context(node(file("fn/abs/e1.xml")));
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
