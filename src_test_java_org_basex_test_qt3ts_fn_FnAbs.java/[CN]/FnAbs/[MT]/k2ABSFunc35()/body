{
  final XQuery query=new XQuery("abs(xs:unsignedShort(.)) instance of xs:unsignedShort",ctx);
  query.context(node(file("fn/abs/e0.xml")));
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
