{
  if (!(node instanceof DBNode))   return;
  final DBNode n=(DBNode)node;
  final Data d=n.data;
  if (a)   UpdateFunctions.insertAttributes(n.pre + d.attSize(n.pre,d.kind(n.pre)),n.pre,d,i);
 else   d.insertSeq(n.pre + d.attSize(n.pre,Nod.kind(n.type)),n.pre,i);
}
