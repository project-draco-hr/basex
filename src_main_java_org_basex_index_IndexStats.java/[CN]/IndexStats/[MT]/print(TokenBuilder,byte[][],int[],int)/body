{
  for (int o=0; o < ocs.length; o++) {
    if (txt[o].length == 0)     continue;
    tb.add("  ");
    tb.add(txt[o]);
    for (int j=0; j < len - txt[o].length; j++)     tb.add(' ');
    tb.add(ocs[o]);
    tb.add('x');
    tb.add(NL);
  }
}
