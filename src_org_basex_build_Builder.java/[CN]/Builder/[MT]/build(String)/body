{
  init(db);
  parser.parse(this);
  if (level != 0)   error(DOCOPEN,parser.det(),tags.key(tagStack[level]));
  meta.lastid=meta.size;
  if (meta.size == 0) {
    startDoc(token(db));
    endDoc();
    setSize(0,meta.size);
  }
  return finish();
}
