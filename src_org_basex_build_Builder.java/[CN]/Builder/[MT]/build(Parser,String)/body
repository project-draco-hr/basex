{
  parser=p;
  init(db);
  final byte[] name=Token.token(db);
  dbname=name;
  addNode(name,null,true);
  parser.parse(this);
  endNode(name);
  if (level != 0)   error(CLOSEMISS,meta.dbname);
  return finish();
}
