{
  if (session == null) {
    if (sa()) {
      session=new LocalSession(context);
    }
 else {
      try {
        session=new ClientSession(context);
      }
 catch (      final Exception ex) {
        Main.error(ex,true);
        ok=false;
      }
    }
  }
  return session;
}
