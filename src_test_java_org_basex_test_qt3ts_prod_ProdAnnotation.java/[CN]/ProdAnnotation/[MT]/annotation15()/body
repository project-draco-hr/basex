{
  final XQuery query=new XQuery("\n" + "         declare %xml:x function local:foo() {\n" + "            \"bar\"\n"+ "         };\n"+ "         local:foo()\n"+ "      ",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0045"));
}
