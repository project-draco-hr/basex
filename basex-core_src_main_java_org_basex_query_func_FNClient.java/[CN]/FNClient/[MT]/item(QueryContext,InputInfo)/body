{
  checkCreate(qc);
switch (func) {
case _CLIENT_CONNECT:
    return connect(qc);
case _CLIENT_EXECUTE:
  return execute(qc);
case _CLIENT_INFO:
return info(qc);
case _CLIENT_CLOSE:
return close(qc);
default :
return super.item(qc,ii);
}
}
