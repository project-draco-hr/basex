{
  if (builder != null) {
    builder.addFront(item);
  }
 else   if (firstValue != null) {
    builder=new TreeSeqBuilder().add(firstValue).addFront(item);
    firstValue=null;
  }
 else {
    firstValue=item;
  }
  return this;
}
