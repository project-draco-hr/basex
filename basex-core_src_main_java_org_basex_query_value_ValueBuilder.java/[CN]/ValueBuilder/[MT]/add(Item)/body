{
  if (builder != null) {
    builder.add(item);
  }
 else   if (firstValue != null) {
    builder=new TreeSeqBuilder().add(firstValue).add(item);
    firstValue=null;
  }
 else {
    firstValue=item;
  }
  return this;
}
