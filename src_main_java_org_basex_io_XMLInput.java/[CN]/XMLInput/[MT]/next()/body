{
  if (pp != 0)   return last[lp + pp++ & 0x0F];
  int ch=in[ip].readChar();
  while (ch == 0 && ip != 0)   ch=in[--ip].readChar();
  last[lp++]=ch;
  lp&=0x0F;
  if (ip == 0 && ch == '\n')   line++;
  return ch;
}
