{
  final byte[] pref=ncName(NONAME);
  wsCheck(IS);
  final byte[] uri=stringLiteral();
  if (sc.ns.staticURI(pref) != null)   error(DUPLNSDECL,pref);
  sc.ns.add(pref,uri,info());
  namespaces.put(pref,uri);
}
