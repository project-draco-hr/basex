{
  final XQuery query=new XQuery("seconds-from-duration(xs:dayTimeDuration(\"P3DT8H2M1.03S\")) eq 1.03",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
