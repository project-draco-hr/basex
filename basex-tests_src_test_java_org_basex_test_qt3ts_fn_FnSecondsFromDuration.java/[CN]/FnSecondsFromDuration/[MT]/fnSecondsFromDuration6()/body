{
  final XQuery query=new XQuery("fn:seconds-from-duration(xs:dayTimeDuration(\"P01DT01H01M01S\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("1"));
}
