{
switch (def) {
case FTSEARCH:
    return search(ctx);
case FTSCORE:
  return score(ctx);
case FTMARK:
return mark(ctx,false);
case FTEXTRACT:
return mark(ctx,true);
default :
return super.iter(ctx);
}
}
