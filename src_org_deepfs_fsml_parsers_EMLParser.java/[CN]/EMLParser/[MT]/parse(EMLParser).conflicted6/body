{
  try {
    final Date d=SDF.parse(obj.mCurrLine);
    obj.meta.add(MetaElem.DATE_CREATED,d);
  }
 catch (  final ParseException ex) {
    if (NewFSParser.VERBOSE)     Main.debug("%: %",obj.bfc.getFileName(),ex.getMessage());
  }
  return true;
}
