{
  final XQuery query=new XQuery("fn:map-pairs( function($arg, $arg2) { fn:error() }, 1 to 10, 1 to 10)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOER0000"));
}
