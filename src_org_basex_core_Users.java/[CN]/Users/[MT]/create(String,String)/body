{
  for (  final User user : users) {
    if (user.name.equals(usern))     return false;
  }
  final User user=new User(usern,crypt.encrypt(Token.token(pass)),READ,WRITE,CREATE,ADMIN);
  users.add(user);
  write();
  return true;
}
