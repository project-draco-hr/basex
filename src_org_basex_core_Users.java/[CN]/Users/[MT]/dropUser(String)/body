{
  int i=0;
  int t=0;
  boolean found=false;
  for (  Object[] s : users) {
    if (s[0].equals(usern)) {
      found=true;
      t=i;
    }
    i++;
  }
  if (found) {
    users.remove(t);
  }
  return found;
}
