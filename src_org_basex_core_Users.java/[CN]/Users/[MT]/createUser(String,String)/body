{
  Object[] item={usern,false,false,false,false,crypt.encrypt(pass)};
  boolean in=false;
  for (  Object[] s : users) {
    if (s[0].equals(usern))     in=true;
  }
  if (!in) {
    users.add(item);
    return true;
  }
  return false;
}
