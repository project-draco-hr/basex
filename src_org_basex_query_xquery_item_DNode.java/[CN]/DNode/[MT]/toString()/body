{
switch (type) {
case ATT:
    return type + "(" + Token.string(data.attName(pre))+ "=\""+ Token.string(data.attValue(pre))+ "\")";
case DOC:
  return type + "(" + data.meta.filename+ ")";
case ELM:
return type + "(" + Token.string(data.tag(pre))+ "/"+ pre+ ")";
default :
return type + "(" + Token.string(str())+ ")";
}
}
