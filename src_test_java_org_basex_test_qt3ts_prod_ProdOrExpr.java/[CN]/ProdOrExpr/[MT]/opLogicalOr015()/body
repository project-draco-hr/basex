{
  final XQuery query=new XQuery("(/bib/book/price/text()) or (1)",ctx);
  query.context(node(file("docs/bib.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
