{
  final XQuery query=new XQuery("boolean((1, 2, 3, current-time())[1] treat as xs:integer) and boolean((1, 2, 3, current-time())[1] treat as xs:integer)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
