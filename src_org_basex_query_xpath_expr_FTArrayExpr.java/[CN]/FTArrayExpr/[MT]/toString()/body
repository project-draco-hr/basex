{
  final TokenBuilder sb=new TokenBuilder(name());
  sb.add('(');
  for (int i=0; i != exprs.length; i++) {
    if (i != 0)     sb.add(", ");
    sb.add(exprs[i]);
  }
  sb.add(')');
  return sb.toString();
}
