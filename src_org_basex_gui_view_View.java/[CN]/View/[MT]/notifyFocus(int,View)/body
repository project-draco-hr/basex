{
  if (focused == pre)   return;
  focused=pre;
  for (  final View v : view)   if (v != vw && v.isValid())   v.refreshFocus();
  if (pre != -1)   GUI.get().status.setPath(ViewData.path(GUI.context.data(),pre));
}
