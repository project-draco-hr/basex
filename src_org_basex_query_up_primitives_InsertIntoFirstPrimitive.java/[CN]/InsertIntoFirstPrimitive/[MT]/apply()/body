{
  if (!(node instanceof DBNode))   return;
  final MemData m=buildDB();
  if (m == null)   return;
  final DBNode n=(DBNode)node;
  final Data d=n.data;
  d.insertSeq(n.pre + d.attSize(n.pre,Nod.kind(node.type)),n.pre,m);
}
