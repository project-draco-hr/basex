{
  if (t != null && type != t) {
    if (type == null || type.instance(t)) {
      type=t;
      return true;
    }
    if (!t.instance(type))     throw XPTYPE.thrw(info,this,type,t);
  }
  return false;
}
