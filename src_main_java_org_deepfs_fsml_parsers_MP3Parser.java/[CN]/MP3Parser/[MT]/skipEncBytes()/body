{
  bfc.buffer(3);
  int skip=0;
  if ((bfc.get() & 0xFF) <= 0x04)   ++skip;
  if ((bfc.get() & 0xFF) >= 0xFE)   ++skip;
  if ((bfc.get() & 0xFF) >= 0xFE)   ++skip;
  bfc.skip(skip - 3);
  return skip;
}
