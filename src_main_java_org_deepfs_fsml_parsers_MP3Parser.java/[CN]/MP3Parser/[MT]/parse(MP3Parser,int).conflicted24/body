{
  final String encoding=obj.readEncoding();
  obj.bfc.skip(3);
  int pos=4;
  while (obj.bfc.get() != 0 && ++pos < size)   ;
  if (pos >= size)   return;
  obj.deepFile.addMeta(MetaElem.DESCRIPTION,obj.readText(size - pos,encoding));
}
