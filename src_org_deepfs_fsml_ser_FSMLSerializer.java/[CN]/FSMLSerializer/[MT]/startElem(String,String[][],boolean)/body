{
  xml.append('<').append(e);
  if (atts != null) {
    for (int i=0; i < atts.length; i++) {
      xml.append(' ').append(atts[i][0]);
      xml.append('=').append('"');
      text(atts[i][1]);
      xml.append('"');
    }
  }
  xml.append(close ? "/>" : '>');
}
