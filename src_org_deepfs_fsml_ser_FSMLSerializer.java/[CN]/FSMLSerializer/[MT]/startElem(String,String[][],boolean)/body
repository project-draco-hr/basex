{
  xml.append('<').append(e);
  if (atts != null) {
    for (    final String[] att : atts) {
      xml.append(' ').append(att[0]);
      xml.append('=').append('"');
      text(att[1]);
      xml.append('"');
    }
  }
  xml.append(close ? "/>" : '>');
}
