{
  final XQuery query=new XQuery("for $h in (/works/employee[4]) return $h/hours/string()",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"20 40"));
}
