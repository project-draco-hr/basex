{
  final XQuery query=new XQuery("for $h in (/works) return $h/employee[fn:last()]/@name",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"Jane Doe 13"));
}
