{
  final XQuery query=new XQuery("for $h in (/works/employee[10]) return $h/@name",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"John Doe 10"));
}
