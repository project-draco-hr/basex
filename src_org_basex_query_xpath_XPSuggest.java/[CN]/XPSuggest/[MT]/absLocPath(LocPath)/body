{
  final ArrayList<Node> list=new ArrayList<Node>();
  list.add(skel.root());
  stack.push(list);
  final LocPath lp=super.absLocPath(path);
  filter(false);
  if (stack.size() > 1) {
    stack.pop();
  }
  return lp;
}
