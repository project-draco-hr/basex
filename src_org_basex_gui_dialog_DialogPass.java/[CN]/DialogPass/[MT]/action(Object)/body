{
  final String nm=new String(pass.getPassword());
  ok=!nm.isEmpty() && nm.matches("[\\w]*");
  info.setText(ok || nm.isEmpty() ? null : Main.info(INVALID,SERVERPW),Icon.ERR);
  enableOK(buttons,BUTTONOK,ok);
}
