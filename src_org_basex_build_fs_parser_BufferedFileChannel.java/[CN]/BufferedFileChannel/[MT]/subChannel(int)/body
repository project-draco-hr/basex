{
  long remaining=bytesToRead - buf.remaining();
  if (remaining > rem)   throw new IllegalArgumentException("Requested number of bytes to read is too large.");
  rem-=remaining;
  return new BufferedFileChannel(f,fc,buf,remaining);
}
