{
  final XQuery query=new XQuery("count(//*[@name='John Doe 4']) eq 0",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
