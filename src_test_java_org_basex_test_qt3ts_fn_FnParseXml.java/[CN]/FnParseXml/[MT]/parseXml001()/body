{
  final XQuery query=new XQuery("parse-xml(unparsed-text(\"../docs/atomic.xml\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertType("document-node(element(*,xs:untyped))"));
}
