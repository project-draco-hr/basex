{
  last=node;
  if (getRight(node) != -1) {
    node=getRight(node);
    while (getLeft(node) != -1)     node=getLeft(node);
  }
 else {
    int tmp=node;
    node=getParent(node);
    while (node != -1 && tmp == getRight(node)) {
      tmp=node;
      node=getParent(node);
    }
  }
  return tokens.get(last);
}
