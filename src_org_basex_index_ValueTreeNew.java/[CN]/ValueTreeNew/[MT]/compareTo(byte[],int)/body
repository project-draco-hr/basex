{
  final byte[] v2=tokens.get(p2);
  int lim=Math.min(v1.length,v2.length);
  int i=-1;
  while (++i < lim) {
    int c=v1[i] - v2[i];
    if (c != 0)     return c;
  }
  return v1.length - v2.length;
}
