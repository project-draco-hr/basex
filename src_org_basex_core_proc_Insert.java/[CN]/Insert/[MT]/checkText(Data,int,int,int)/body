{
  if (kind == Data.TEXT) {
    if (data.kind(pre) == Data.TEXT)     return pre;
    if (data.parent(pre - 1,data.kind(pre - 1)) == par && data.kind(pre - 1) == Data.TEXT)     return pre - 1;
  }
  return -1;
}
