{
  final XQuery query=new XQuery("declare default order empty least;  for $i in (<a>1</a>,<a>4</a>,<a></a>,<a>7</a>) order by $i/text() ascending empty least return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a/><a>1</a><a>4</a><a>7</a>",false));
}
