{
  final XQuery query=new XQuery("declare default order empty least; declare default order empty greatest;  \"aaa\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0069"));
}
