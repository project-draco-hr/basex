{
  final XQuery query=new XQuery("declare(::)default order empty(::)greatest; declare(::)default order empty(::)least; 1 eq 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0069"));
}
