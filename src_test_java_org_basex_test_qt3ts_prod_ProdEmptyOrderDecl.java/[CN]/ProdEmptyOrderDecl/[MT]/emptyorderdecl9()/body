{
  final XQuery query=new XQuery("declare default order empty least;  for $i in (1,4,0 div 0E0,7) return $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"1 4 NaN 7"));
}
