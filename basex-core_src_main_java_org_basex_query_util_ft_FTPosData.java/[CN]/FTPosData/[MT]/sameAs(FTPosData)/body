{
  if (size != ft.size)   return false;
  for (int i=0; i < size; ++i) {
    if (pos[i].pre != ft.pos[i].pre || !Arrays.equals(pos[i].list.toArray(),ft.pos[i].list.toArray()))     return false;
  }
  return true;
}
