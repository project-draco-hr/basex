{
  final Object entry=props.get(key[0].toString());
  if (entry == null)   BaseX.notexpected("Property " + key[0] + " not defined.");
  final Class<?> cc=entry.getClass();
  if (c != cc)   BaseX.notexpected("Property '" + key[0] + "' is a "+ cc.getSimpleName());
  return entry;
}
