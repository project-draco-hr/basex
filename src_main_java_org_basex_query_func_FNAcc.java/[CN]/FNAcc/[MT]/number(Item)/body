{
  if (it.type == Type.DBL)   return it;
  double d=Double.NaN;
  try {
    if (it.type != Type.URI && (it.str() || it.num() || it.unt()))     d=it.dbl(input);
  }
 catch (  final QueryException ex) {
  }
  return Dbl.get(d);
}
