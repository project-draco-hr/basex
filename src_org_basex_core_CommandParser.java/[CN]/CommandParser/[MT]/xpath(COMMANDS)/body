{
  consumeWS();
  final StringBuilder sb=new StringBuilder();
  if (more() && !curr(';')) {
    final XPParser p=curr != null ? new XPSuggest(qu,curr) : new XPParser(qu);
    p.qp=qp;
    p.parse(false);
    sb.append(qu.substring(qp,p.qp));
    qp=p.qp;
  }
  return finish(cmd,sb);
}
