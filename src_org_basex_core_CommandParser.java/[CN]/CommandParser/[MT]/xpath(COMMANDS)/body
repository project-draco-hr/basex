{
  consumeWS();
  final StringBuilder sb=new StringBuilder();
  if (more() && !curr(';')) {
    final XPParser p=ctx != null ? new XPSuggest(qu,ctx) : new XPParser(qu);
    p.qp=qp;
    p.parse(false);
    sb.append(qu.substring(qp,p.qp));
    qp=p.qp;
  }
  return finish(cmd,sb);
}
