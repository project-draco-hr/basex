{
  final StringBuilder tb=new StringBuilder();
  consumeWS();
  boolean q=false;
  while (more()) {
    char c=curr();
    if ((c <= ' ' || c == ';') && !q)     break;
    if (c == '"')     q^=true;
 else     tb.append(c);
    consume();
  }
  return finish(cmd,tb);
}
