{
  if (!mark)   editor.noSelect();
  final int x=editor.bol(mark);
  if (lastCol == -1)   lastCol=x;
  if (editor.pos() == 0) {
    lastCol=-1;
    return;
  }
  for (int i=0; i < l; ++i) {
    editor.prev(mark);
    editor.bol(mark);
  }
  editor.forward(lastCol,mark);
}
