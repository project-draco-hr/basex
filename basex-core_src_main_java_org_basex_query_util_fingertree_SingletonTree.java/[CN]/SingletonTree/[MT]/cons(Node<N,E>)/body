{
  final long leftSize=fst.size();
  @SuppressWarnings("unchecked") final Node<N,E>[] left=new Node[]{fst}, right=new Node[]{elem};
  return DeepTree.get(left,leftSize,right,leftSize + elem.size());
}
