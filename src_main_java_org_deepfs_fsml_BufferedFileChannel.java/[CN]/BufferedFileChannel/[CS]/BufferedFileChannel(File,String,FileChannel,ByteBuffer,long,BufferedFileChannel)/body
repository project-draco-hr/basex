{
  f=file;
  name=subfilename.isEmpty() ? f.getAbsolutePath() : f.getAbsolutePath() + '/' + subfilename;
  fc=fileChannel;
  buf=buffer;
  mark=absolutePosition();
  rem=bytesToRead - buf.remaining();
  parent=p;
  size=fc.position() + rem - mark;
}
