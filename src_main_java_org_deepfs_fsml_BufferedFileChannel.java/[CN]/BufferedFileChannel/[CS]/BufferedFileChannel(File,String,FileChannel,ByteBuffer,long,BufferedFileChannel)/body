{
  f=file;
  name=subfilename.isEmpty() ? f.getAbsolutePath() : f.getAbsolutePath() + Prop.SEP + subfilename;
  fc=fileChannel;
  buf=buffer;
  mark=absolutePosition();
  rem=bytesToRead - buf.remaining();
  parent=p;
}
