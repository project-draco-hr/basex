{
  if (startsWith(n,XMLNS)) {
    if (n.length == 5) {
      nsp.add(EMPTY,v);
    }
 else     if (n[5] == ':') {
      nsp.add(substring(n,6),v);
    }
 else {
      atts.add(n,v);
    }
  }
 else {
    atts.add(n,v);
  }
}
