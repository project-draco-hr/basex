{
  long s=0;
  for (int i=0; i < size; i++) {
    final Value v=get(i);
    if (v instanceof Item) {
      s+=((Item)v).atomSize();
    }
 else {
      for (      final Item it : v)       s+=it.atomSize();
    }
  }
  return s;
}
