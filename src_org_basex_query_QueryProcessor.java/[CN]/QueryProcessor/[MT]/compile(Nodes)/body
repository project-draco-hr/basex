{
  if (context == null)   parse();
  context.compile(nodes);
  if (Prop.dotplan) {
    try {
      context.planDot();
    }
 catch (    final Exception ex) {
      ex.printStackTrace();
    }
  }
  compiled=true;
}
