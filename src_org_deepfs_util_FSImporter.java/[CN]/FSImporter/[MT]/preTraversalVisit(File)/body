{
  if (d.isDirectory())   targetNode+="/" + DataText.S_DEEPFS + "[@"+ string(DataText.BACKINGSTORE)+ " = \""+ insert(d,true)+ "\"]";
 else {
    preTraversalVisit(d.getParentFile());
    insert(d,false);
  }
}
