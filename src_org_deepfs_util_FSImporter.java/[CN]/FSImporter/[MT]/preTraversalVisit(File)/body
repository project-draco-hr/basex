{
  if (d.isDirectory())   targetNode+="/" + ROOT_NODE + "[@"+ string(DataText.BACKINGSTORE)+ " = \""+ insert(d,true)+ "\"]";
 else {
    preTraversalVisit(d.getParentFile());
    insert(d,false);
  }
}
