{
  byte[] prefix=null;
  if (startsWith(n,XMLNS)) {
    if (n.length == 5) {
      prefix=EMPTY;
    }
 else     if (n[5] == ':') {
      prefix=substring(n,6);
    }
  }
  if (prefix != null) {
    namespaces.put(prefix,v);
  }
 else {
    attributes.add(n,v);
  }
}
