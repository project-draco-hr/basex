{
  final XQuery query=new XQuery("\n" + "        declare function local:foo($arg) as xs:boolean { $arg };\n" + "        local:foo(xs:untypedAtomic(\"false\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
