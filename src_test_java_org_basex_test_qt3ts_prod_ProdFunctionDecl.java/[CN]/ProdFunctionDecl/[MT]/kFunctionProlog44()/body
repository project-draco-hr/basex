{
  final XQuery query=new XQuery("\n" + "        declare function local:myFunction($arg as item(), $arg2 as xs:integer) { $arg, $arg2 };\n" + "        local:myFunction(\"3\", ())",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
