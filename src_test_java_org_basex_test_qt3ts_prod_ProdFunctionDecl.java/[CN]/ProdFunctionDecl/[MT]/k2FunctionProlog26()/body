{
  final XQuery query=new XQuery("       declare function local:myFunc() as element(foo) { <bar/> };\n" + "        local:myFunc()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
