{
  final XQuery query=new XQuery("\n" + "        declare function local:func($a as xs:integer, $unused, $c as xs:integer) { $a + $c };\n" + "        local:func(1, 2, 3)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("4"));
}
