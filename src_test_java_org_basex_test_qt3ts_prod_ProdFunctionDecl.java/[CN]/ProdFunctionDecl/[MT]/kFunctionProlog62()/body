{
  final XQuery query=new XQuery("declare variable $my := 3;\n" + "        declare function local:myFunction($my, $arg2, $arg4) { 1 };\n" + "        $my eq 3",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
