{
  final XQuery query=new XQuery("       declare function local:foo($arg) as xs:boolean { $arg };\n" + "        local:foo(<e/>)",ctx);
  final QT3Result res=result(query);
  result=res;
  test((error("FORG0001") || error("XPTY0004")));
}
