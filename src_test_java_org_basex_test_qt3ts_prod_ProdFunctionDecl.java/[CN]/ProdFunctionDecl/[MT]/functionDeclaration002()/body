{
  final XQuery query=new XQuery("        \n" + "        declare function local:foo($n as xs:integer) { <tr> {$n} </tr> };\n" + "        local:foo(4)\n"+ "      ",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<tr>4</tr>",false));
}
