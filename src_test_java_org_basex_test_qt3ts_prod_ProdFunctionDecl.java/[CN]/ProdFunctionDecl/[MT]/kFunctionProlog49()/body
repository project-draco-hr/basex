{
  final XQuery query=new XQuery("\n" + "        declare function local:myFunction($arg as empty-sequence()) { $arg };\n" + "        empty(local:myFunction(()))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPST0005")));
}
