{
  final XQuery query=new XQuery("\n" + "        declare function local:myFunction($arg) as empty-sequence() { $arg };\n" + "        local:myFunction(1)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
