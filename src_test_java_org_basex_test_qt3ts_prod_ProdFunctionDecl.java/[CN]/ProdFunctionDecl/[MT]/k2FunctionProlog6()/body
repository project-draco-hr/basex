{
  final XQuery query=new XQuery("\n" + "        declare function local:myFunction() as xs:integer { 1.0 };\n" + "        local:myFunction()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
