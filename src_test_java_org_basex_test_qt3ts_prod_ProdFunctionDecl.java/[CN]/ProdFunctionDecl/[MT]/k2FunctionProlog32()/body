{
  final XQuery query=new XQuery("\n" + "        declare function local:foo($arg ) as xs:boolean* { $arg };\n" + "        local:foo(current-date())",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
