{
  final XQuery query=new XQuery("       declare function local:func() as xs:integer { <?target 1?> };\n" + "        local:func()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
