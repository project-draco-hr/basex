{
  final XQuery query=new XQuery("\n" + "        declare function local:myFunction($arg as xs:integer) { $arg };\n" + "        local:myFunction(1.0)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
