{
  final XQuery query=new XQuery("\n" + "        declare function local:MyFunction() { 1 };\n" + "        local:myFunctionn()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0017"));
}
