{
  final XQuery query=new XQuery("\n" + "        declare function local:myFunction($unusedArg) { true() };\n" + "        local:myFunction(1 + \"a string\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPTY0004")));
}
