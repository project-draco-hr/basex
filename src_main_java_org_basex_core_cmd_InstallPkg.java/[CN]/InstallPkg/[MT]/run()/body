{
  RepoManager rm=new RepoManager(ctx,ii);
  try {
    rm.installPackage(pkg);
  }
 catch (  QueryException e) {
    error(e.getMessage());
    e.printStackTrace();
    return false;
  }
  return true;
}
