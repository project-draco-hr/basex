{
  if (args.length == 0)   new INEXTest(null,numq,false);
 else   if (args.length == 1) {
    new INEXTest(null,Integer.parseInt(args[0]),false);
  }
 else   if (args.length == 2 && args[1].startsWith("true"))   new INEXTest(args[0],numq,true);
}
