{
  final String[][] parsers=new String[REGISTRY.size()][];
  int i=0;
  for (  final Entry<String,Class<? extends IFileParser>> parser : REGISTRY.entrySet()) {
    parsers[i++]=new String[]{parser.getKey(),parser.getValue().getName()};
  }
  return parsers;
}
