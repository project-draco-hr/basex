{
  if (it.type == Type.DBL)   return it;
  try {
    if (it.type != Type.URI && (it.s() || it.n() || it.u()))     return Dbl.get(it.dbl());
  }
 catch (  final QueryException e) {
  }
  return Dbl.get(Double.NaN);
}
