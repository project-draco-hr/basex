{
  final XQuery query=new XQuery("months-from-duration(xs:duration(\"-P3Y4M4DT1H23M2.34S\")) eq -4",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
