{
  p=v.nextPres();
  if (p.length > 0) {
    t=di.readBytes();
  }
 else {
    t=EMPTY;
    v.close();
    di.close();
    final String f=(text ? DATATXT : DATAATV) + id + '.'+ IO.BASEXSUFFIX;
    DropDB.drop(data.meta.name,f,data.meta.prop);
  }
}
