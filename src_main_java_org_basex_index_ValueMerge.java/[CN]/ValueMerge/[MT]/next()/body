{
  pre=v.nextPres();
  if (pre.length != 0) {
    token=di.readBytes();
  }
 else {
    token=EMPTY;
    v.close();
    di.close();
    DropDB.drop(data.meta.name,pref + '.' + IO.BASEXSUFFIX,data.meta.prop);
  }
}
