{
  if (c.size() == 0)   return;
  final NodIter seq=new NodIter();
  for (  final NodIter ni : c) {
    Nod i;
    while ((i=ni.next()) != null)     seq.add(i);
  }
  md=node instanceof DBNode ? buildDB(mergeText(seq),new MemData(((DBNode)node).data),null) : null;
}
