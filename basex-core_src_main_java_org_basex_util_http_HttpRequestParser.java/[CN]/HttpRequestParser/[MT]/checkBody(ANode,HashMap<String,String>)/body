{
  if (bodyAttrs.get(SerializerOptions.MEDIA_TYPE.name()) == null)   throw HC_REQ_X.get(info,"Attribute media-type of http:body is mandatory");
  if (bodyAttrs.get(SRC) != null && (bodyAttrs.size() > 2 || body.children().next() != null))   throw HC_ATTR.get(info);
}
