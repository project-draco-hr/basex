{
  parseAtts(body,atts);
  checkBody(body,atts);
  if (atts.get(SRC) == null) {
    if (contItem == null) {
      for (      final ANode n : body.children())       bodyContent.add(n);
    }
 else {
      bodyContent.add(contItem);
    }
  }
}
