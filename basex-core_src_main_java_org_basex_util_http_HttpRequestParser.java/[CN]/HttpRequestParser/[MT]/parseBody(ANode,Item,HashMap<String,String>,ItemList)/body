{
  parseAtts(body,atts);
  checkBody(body,atts);
  if (atts.get(SRC) == null) {
    if (contItem == null) {
      for (      final ANode n : body.children())       payload.add(n);
    }
 else {
      payload.add(contItem);
    }
  }
}
