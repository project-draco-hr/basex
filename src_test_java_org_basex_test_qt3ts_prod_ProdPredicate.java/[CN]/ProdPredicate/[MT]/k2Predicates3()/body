{
  final XQuery query=new XQuery("declare variable $myvar := <elem> <a/> <b/> <c/></elem>; $myvar/*[last() - 1]",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<b/>",false));
}
