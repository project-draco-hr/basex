{
  final XQuery query=new XQuery("empty((1, 2, 3)[remove((false(), \"a string\"), 2)])",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPTY0004") || error("XPST0005")));
}
