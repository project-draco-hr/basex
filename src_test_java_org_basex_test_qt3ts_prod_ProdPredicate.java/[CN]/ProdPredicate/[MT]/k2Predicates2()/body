{
  final XQuery query=new XQuery("declare variable $myvar := <elem> <a/> <b/> <c/></elem>; $myvar/*[last()]",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<c/>",false));
}
