{
  final XQuery query=new XQuery("(/works//hours[xs:integer(.) >= 80])",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<hours>80</hours><hours>80</hours><hours>80</hours>",false));
}
