{
  final XQuery query=new XQuery("((1,2,4,5,6,7,8,9,10,11)[(. idiv 2 eq 3)])",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"6 7"));
}
