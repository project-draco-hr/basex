{
  final XQuery query=new XQuery("(/works//day[xs:string(.) ne \"Monday\"])",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<day>Tuesday</day>",false));
}
