{
  final XQuery query=new XQuery("declare variable $myvar := <elem> <a/> <b/> <c/></elem>; $myvar/*[1]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<a/>",false));
}
