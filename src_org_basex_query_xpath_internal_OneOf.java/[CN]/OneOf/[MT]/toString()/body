{
  final TokenBuilder sb=new TokenBuilder();
  sb.add(name());
  sb.add("(" + path);
  for (int v=0; v < vals.length; v++) {
    sb.add((v != 0 ? ", " : " " + cmp + " ") + vals[v]);
  }
  sb.add(")");
  return sb.toString();
}
