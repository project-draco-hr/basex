{
  final XQuery query=new XQuery("map(starts-with#2, (\"aa\", \"bb\", \"cc\", \"dd\", \"ee\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
