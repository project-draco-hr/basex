{
  final XQuery query=new XQuery("map(upper-case#1, (\"aa\", \"bb\", \"cc\", \"dd\", 12))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
