{
  final XQuery query=new XQuery("<e>{for $PI in <e/>/processing-instruction() return ()}</e>",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertSerialization("<e/>",false) || error("XPST0005")));
}
