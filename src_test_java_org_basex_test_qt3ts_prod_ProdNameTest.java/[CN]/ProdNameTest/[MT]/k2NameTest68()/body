{
  final XQuery query=new XQuery("<e>{<e foo=\"1\"/>/attribute(foo, xs:integer)}</e>",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertSerialization("<e/>",false) || error("XPST0005")));
}
