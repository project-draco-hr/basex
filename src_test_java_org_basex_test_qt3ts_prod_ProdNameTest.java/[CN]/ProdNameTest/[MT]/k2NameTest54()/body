{
  final XQuery query=new XQuery("<a>{processing-instruction name {\"content\"}/*}</a>",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertSerialization("<a/>",false) || error("XPST0005")));
}
