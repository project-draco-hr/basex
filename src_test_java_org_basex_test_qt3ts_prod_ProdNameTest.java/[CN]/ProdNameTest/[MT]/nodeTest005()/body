{
  final XQuery query=new XQuery("/*/.",ctx);
  query.context(node(file("prod/AxisStep/nw_Customers.xml")));
  final QT3Result res=result(query);
  result=res;
  test((assertType("element(Root)") && assertQuery("exists($result//Country[.='Poland'])")));
}
