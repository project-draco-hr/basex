{
  final XQuery query=new XQuery("<a>{<e foo=\"content2\" bar=\"content1\"/>/attribute(foo)}</a>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a foo=\"content2\"/>",false));
}
