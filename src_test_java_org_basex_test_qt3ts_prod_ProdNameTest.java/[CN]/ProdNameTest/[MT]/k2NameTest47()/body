{
  final XQuery query=new XQuery("declare variable $i := <e > <xmlns/> <xmlns/> <xmlns/> <xmlns/> </e>; $i/xmlns",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<xmlns/><xmlns/><xmlns/><xmlns/>",false));
}
