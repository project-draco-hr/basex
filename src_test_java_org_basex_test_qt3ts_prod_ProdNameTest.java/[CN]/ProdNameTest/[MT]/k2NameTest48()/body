{
  final XQuery query=new XQuery("declare variable $i := <e xmlns=\"http://example.com/\"/>; empty($i/@xmlns)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
