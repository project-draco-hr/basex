{
  final XQuery query=new XQuery("declare variable $e := <a b =\"content\"><?b asd?><b/></a>; $e/processing-instruction(b)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<?b asd?>",false));
}
