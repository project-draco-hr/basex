{
  final XQuery query=new XQuery("declare variable $e := <a b =\"content\"><?b asd?><b/></a>; <a>{$e/@*}</a>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a b=\"content\"/>",false));
}
