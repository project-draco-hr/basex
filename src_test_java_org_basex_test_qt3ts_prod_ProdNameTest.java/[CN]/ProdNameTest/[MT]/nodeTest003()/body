{
  final XQuery query=new XQuery("/*/*[1]/name()",ctx);
  query.context(node(file("prod/AxisStep/nw_Customers.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"Customers"));
}
