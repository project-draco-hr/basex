{
  final XQuery query=new XQuery("declare variable $e := <a b =\"content\"><?b asd?><b/><c b=\"content\"/></a>; $e/b",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<b/>",false));
}
