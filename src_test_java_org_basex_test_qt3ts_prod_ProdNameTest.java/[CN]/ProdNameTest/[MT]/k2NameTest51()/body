{
  final XQuery query=new XQuery("<a>{attribute name{\"content\"}/*}</a>",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertSerialization("<a></a>",false) || error("XPST0005")));
}
