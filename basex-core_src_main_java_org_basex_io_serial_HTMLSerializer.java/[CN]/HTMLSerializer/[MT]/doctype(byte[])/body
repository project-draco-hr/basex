{
  if (lvl != 0)   return false;
  if (!super.doctype(type) && html5) {
    if (sep)     indent();
    print(DOCTYPE);
    if (type == null)     print(HTML);
 else     print(type);
    print(ELEM_C);
    if (indent)     print(nl);
  }
  return true;
}
