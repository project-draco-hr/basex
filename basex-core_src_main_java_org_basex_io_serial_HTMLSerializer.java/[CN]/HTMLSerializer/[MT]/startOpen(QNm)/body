{
  doctype(null);
  if (sep)   indent();
  out.print(ELEM_O);
  out.print(name.string());
  sep=indent;
  script=SCRIPTS.contains(lc(name.local()));
  if (content && eq(lc(elem.local()),HEAD))   ct++;
}
