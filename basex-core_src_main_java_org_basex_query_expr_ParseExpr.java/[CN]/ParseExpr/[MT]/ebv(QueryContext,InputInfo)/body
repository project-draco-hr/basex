{
  final Item it;
  if (type().zeroOrOne()) {
    it=item(qc,info);
  }
 else {
    final Iter ir=iter(qc);
    it=ir.next();
    if (!(it == null || it instanceof ANode || ir.next() == null))     throw CONDTYPE.get(info,this);
  }
  return it == null ? Bln.FALSE : it;
}
