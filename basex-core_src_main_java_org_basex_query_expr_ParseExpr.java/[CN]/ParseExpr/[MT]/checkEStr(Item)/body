{
  if (it == null)   return EMPTY;
  final Type ip=it.type;
  if (ip.isStringOrUntyped())   return it.string(info);
  throw it instanceof FItem ? FIATOM.get(info,this) : INVCAST.get(info,it.type,AtomType.STR);
}
