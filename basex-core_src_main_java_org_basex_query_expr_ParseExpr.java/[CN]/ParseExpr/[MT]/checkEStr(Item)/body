{
  if (it == null)   return EMPTY;
  final Type ip=it.type;
  if (ip.isStringOrUntyped())   return it.string(info);
  throw it instanceof FItem ? FIATOM.thrw(info,this) : Err.INVCAST.thrw(info,it.type,AtomType.STR);
}
