{
  final SAXSource source=new SAXSource(parser,is);
  final String id=is.getSystemId();
  final Data data=new MemBuilder().build(new SAXWrapper(source),id == null ? "tmp" : id);
  return new BXDoc(new DBNode(data,0));
}
