{
switch (def) {
case ONEORMORE:
    final Iter ir=expr[0].type().mayBeZero() ? expr[0].value(ctx).cache() : expr[0].iter(ctx);
  if (ir.size() < 1)   EXPECTOM.thrw(input);
return ir;
case UNORDER:
return ctx.iter(expr[0]);
default :
return super.iter(ctx);
}
}
