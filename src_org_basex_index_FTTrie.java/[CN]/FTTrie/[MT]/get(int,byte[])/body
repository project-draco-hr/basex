{
  if (searchNode == null || searchNode.length == 0)   return IndexArrayIterator.EMP;
  final int[] ne=nodeId(id,searchNode);
  return ne == null ? IndexArrayIterator.EMP : FTFuzzy.data(did,ne[ne.length - 1],inD);
}
