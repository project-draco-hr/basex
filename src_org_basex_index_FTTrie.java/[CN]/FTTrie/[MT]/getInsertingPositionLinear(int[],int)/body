{
  found=false;
  int i=cne[0] + 1;
  final int s=cne.length - 1;
  if (s == i)   return i;
  while (i < s && Token.diff((byte)cne[i + 1],(byte)toInsert) < 0)   i+=2;
  if (i < s && Token.diff((byte)cne[i + 1],(byte)toInsert) == 0) {
    found=true;
  }
  return i;
}
