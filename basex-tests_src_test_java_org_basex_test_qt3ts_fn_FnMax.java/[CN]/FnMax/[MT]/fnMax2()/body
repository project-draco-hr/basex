{
  final XQuery query=new XQuery("fn:max((xs:integer(5000000000),xs:double(3e0)))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("5.0E9"));
}
