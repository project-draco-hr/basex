{
  final Step step=ic.indexStep(expr);
  final boolean ok=step != null && ic.ictx.data.meta.ftxtindex && step.test.type == NodeType.TXT && ftexpr.indexAccessible(ic);
  ic.seq|=ic.not;
  return ok;
}
