{
  final XQuery query=new XQuery(" for $x in /langs/para[1] return $x/fn:lang(\"en\")",ctx);
  query.context(node(file("fn/lang/lang.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
