{
  final XQuery query=new XQuery("fn:lang(\"fr\",fn:exactly-one(/langs/para[1]))",ctx);
  query.context(node(file("fn/lang/lang.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
