{
  final XQuery query=new XQuery("fn:lang(\"en\",fn:exactly-one(/langs/para[2]))",ctx);
  query.context(node(file("fn/lang/lang.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
