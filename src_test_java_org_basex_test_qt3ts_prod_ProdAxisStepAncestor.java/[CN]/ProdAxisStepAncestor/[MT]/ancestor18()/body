{
  final XQuery query=new XQuery("(/works/employee[12]/overtime/day[ancestor::overtime]) or fn:true()",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
