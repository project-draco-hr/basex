{
  final XQuery query=new XQuery("let $var := <anElement>Some Content</anElement> return fn:count($var/ancestor::*)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
