{
  final Ann ann=anns.get(Annotation._BASEX_INLINE);
  final long limit=ann != null ? ann.args.length > 0 ? ((ANum)ann.args[0]).itr() : Long.MAX_VALUE : qc.context.options.get(MainOptions.INLINELIMIT);
  return expr.isValue() || expr.exprSize() < limit;
}
