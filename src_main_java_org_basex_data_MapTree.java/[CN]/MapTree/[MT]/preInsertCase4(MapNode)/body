{
  MapNode child=node;
  if (node == node.preParent.preRight && node.preParent == node.preGrandparent().preLeft) {
    rotatePreLeft(node.preParent);
    child=node.preLeft;
  }
 else   if (node == node.preParent.preLeft && node.preParent == node.preGrandparent().preRight) {
    rotatePreRight(node.preParent);
    child=node.preRight;
  }
  preInsertCase5(child);
}
