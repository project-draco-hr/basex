{
  if (nodePreColor(node.preParent) && nodePreColor(node.preSibling()) && nodePreColor(node.preSibling().preLeft)&& nodePreColor(node.preSibling().preRight)) {
    node.preSibling().preColor=false;
    preDeleteCase1(node.preParent);
  }
 else   preDeleteCase4(node);
}
