{
  current=preRoot;
  sadd=0;
  while (current != null && !(preMin() <= pre && pre <= preMax())) {
    if (pre < preMin()) {
      current.s+=mod;
      current=current.preLeft;
    }
 else {
      sadd+=current.s;
      current=current.preRight;
    }
  }
}
