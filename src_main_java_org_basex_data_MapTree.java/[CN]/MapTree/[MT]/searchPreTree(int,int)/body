{
  current=preRoot;
  sadd=0;
  while (!(preMin() <= pre && pre <= preMax())) {
    if (pre < preMin()) {
      current.s+=mod;
      current=current.preLeft;
    }
 else {
      sadd+=current.s;
      current=current.preRight;
    }
  }
}
