{
switch (func) {
case COUNT:
    return count(qc);
case MIN:
  return minmax(OpV.GT,qc);
case MAX:
return minmax(OpV.LT,qc);
case SUM:
return sum(qc,ii);
case AVG:
return avg(qc,ii);
default :
return super.item(qc,ii);
}
}
