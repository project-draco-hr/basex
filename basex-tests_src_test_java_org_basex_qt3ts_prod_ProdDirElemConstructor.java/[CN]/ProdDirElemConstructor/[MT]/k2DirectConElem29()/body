{
  final XQuery query=new XQuery("<e>a</e>, <e>b</e>, <e>c</e>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<e>a</e><e>b</e><e>c</e>",false));
}
