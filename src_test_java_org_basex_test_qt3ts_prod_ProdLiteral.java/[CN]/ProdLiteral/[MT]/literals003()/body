{
  final XQuery query=new XQuery("\"line1\n" + "line2\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"line1\nline2"));
}
