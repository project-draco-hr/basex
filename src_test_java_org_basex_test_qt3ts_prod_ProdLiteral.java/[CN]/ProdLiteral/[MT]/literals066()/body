{
  final XQuery query=new XQuery("<test>{ 'He said, \"I don''t like it.\"' }</test>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<test>He said, \"I don't like it.\"</test>",false));
}
