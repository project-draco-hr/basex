{
  final XQuery query=new XQuery("text{ 'He said, \"I don''t like it.\"' }",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"He said, \"I don't like it.\""));
}
