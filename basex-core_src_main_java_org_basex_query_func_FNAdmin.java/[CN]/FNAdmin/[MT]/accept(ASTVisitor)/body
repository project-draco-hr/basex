{
  if (oneOf(sig,_ADMIN_USERS,_ADMIN_SESSIONS) && !visitor.lock(DBLocking.ADMIN))   return false;
  if (sig == _ADMIN_USERS && expr.length > 0 && !dataLock(visitor))   return false;
  return super.accept(visitor);
}
