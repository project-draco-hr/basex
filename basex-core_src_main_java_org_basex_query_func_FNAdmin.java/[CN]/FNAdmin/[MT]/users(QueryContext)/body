{
  final ValueBuilder vb=new ValueBuilder();
  for (  final User u : exprs.length == 0 ? qc.context.users.users(null) : checkData(qc).meta.users.users(qc.context.users)) {
    vb.add(new FElem(USER).add(u.name).add(PERMISSION,u.perm.toString().toLowerCase(Locale.ENGLISH)).add(PASSWORD,u.password));
  }
  return vb;
}
