{
  int p=pos - 1;
  for (; p >= 0; p--) {
    final byte b=text[p];
    if (b == '\n')     break;
    if (!ws(b))     return;
  }
  if (++p >= pos)   return;
  selectPos=Math.max(pos - indent(),p);
  selectEnd=Math.max(pos,p);
  if (selectPos != selectEnd)   delete();
}
