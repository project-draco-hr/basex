{
  final int p=pos;
  boolean s=true;
  while (prev(select) != '\n')   s&=Character.isWhitespace(curr());
  if (pos != 0 || curr() == '\n')   next(select);
  if (p == pos || !s)   while (Character.isWhitespace(curr()) && curr() != '\n')   next(select);
}
