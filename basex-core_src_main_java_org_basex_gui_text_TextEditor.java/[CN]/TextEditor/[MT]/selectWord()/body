{
  final boolean ch=valid(curr());
  while (pos() > 0) {
    final int cp=back(true);
    if (cp == '\n' || ch != valid(cp)) {
      forward(true);
      break;
    }
  }
  startSelect();
  while (pos() < size()) {
    final int cp=curr();
    if (cp == '\n' || ch != valid(cp))     break;
    forward(true);
  }
  endSelection();
}
