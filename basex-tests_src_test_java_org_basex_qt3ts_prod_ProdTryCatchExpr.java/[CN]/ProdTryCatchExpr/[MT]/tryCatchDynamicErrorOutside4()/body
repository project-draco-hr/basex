{
  final XQuery query=new XQuery("1 + (try { \"\" } catch *:XPTY0004 { \"Invalid argument\" })",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
