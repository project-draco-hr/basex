{
  final XQuery query=new XQuery("xs:yearMonthDuration(\"P2Y\") div xs:yearMonthDuration(\"P0Y\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOAR0001"));
}
