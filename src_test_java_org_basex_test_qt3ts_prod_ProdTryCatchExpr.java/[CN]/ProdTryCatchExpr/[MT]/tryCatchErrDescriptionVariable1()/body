{
  final XQuery query=new XQuery("try { 1 div 0 } catch * { count($err:description) le 1 }",ctx);
  query.namespace("err","http://www.w3.org/2005/xqt-errors");
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
