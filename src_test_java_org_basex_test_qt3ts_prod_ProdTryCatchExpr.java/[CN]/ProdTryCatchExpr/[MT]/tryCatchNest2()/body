{
  final XQuery query=new XQuery("try { fn:error() } catch * { try { 1 div 0 } catch * { \"Division by zero\" } }",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"Division by zero"));
}
