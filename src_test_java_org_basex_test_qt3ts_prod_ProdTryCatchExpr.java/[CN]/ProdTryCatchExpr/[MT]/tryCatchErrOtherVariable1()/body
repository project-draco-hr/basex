{
  final XQuery query=new XQuery("try { fn:error() } catch * { $err:other }",ctx);
  query.namespace("err","http://www.w3.org/2005/xqt-errors");
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
