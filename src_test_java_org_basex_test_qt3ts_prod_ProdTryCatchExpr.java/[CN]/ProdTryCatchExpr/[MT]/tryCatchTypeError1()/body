{
  final XQuery query=new XQuery("try { let $i as xs:string := 1 return $i } catch * { \"Undefined variable\" }",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
