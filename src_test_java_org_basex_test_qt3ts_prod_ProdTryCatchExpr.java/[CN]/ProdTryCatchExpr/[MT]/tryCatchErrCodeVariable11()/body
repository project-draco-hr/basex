{
  final XQuery query=new XQuery("try { fn:error() } catch * { fn:local-name-from-QName($err:code) }",ctx);
  query.namespace("err","http://www.w3.org/2005/xqt-errors");
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"FOER0000"));
}
