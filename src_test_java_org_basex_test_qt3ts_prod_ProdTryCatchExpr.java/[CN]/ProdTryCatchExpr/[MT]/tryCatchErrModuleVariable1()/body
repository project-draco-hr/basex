{
  final XQuery query=new XQuery("try { fn:error() } catch * { let $n := $err:module return true() }",ctx);
  query.namespace("err","http://www.w3.org/2005/xqt-errors");
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
