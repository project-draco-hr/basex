{
  final XQuery query=new XQuery("try { $x } catch *:XPST0008 { \"Undefined variable\" }",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0008"));
}
