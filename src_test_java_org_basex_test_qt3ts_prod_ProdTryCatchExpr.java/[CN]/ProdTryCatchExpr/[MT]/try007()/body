{
  final XQuery query=new XQuery("\n" + "        let $doc := doc('rubbish.xml') \n" + "        return try { $doc } catch * {<caught-error/>}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("FODC0002"));
}
