{
  final XQuery query=new XQuery("(try { \"\" } catch err:* { \"Invalid argument\" }) + 1",ctx);
  query.namespace("err","http://www.w3.org/2005/xqt-errors");
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
