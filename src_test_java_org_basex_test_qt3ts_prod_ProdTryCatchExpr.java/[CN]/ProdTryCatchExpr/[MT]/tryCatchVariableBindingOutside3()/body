{
  final XQuery query=new XQuery("for $x as xs:integer in (0, 1, \"\") return try { $x } catch err:XPTY0004 { 0 }",ctx);
  query.namespace("err","http://www.w3.org/2005/xqt-errors");
  final QT3Result res=result(query);
  result=res;
  test((assertEq("0") || error("XPTY0004")));
}
