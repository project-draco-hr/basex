{
  final XQuery query=new XQuery("try { 1 div 0 } catch *:FOAR0001 { \"Division by zero\" }",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("\"Division by zero\""));
}
