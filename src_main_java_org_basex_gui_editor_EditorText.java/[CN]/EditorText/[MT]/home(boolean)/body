{
  int p=ps;
  boolean s=true;
  while (prev(select) != '\n')   s&=ws(curr());
  if (ps != 0 || curr() == '\n')   next(select);
  if (p == ps || !s)   while (ws(curr()) && curr() != '\n')   next(select);
}
