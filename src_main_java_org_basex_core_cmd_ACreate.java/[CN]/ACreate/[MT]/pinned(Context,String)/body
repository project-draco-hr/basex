{
  if (!MetaData.validName(name,false))   return false;
  final Data data=ctx.data();
  return data != null && data.meta.name.equals(name) ? data.pinned() : DiskData.pinned(ctx.mprop.dbpath(name),"");
}
