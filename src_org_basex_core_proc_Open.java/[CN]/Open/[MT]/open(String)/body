{
  if (db.endsWith(DataText.BDBSUF)) {
    try {
      return (Data)Class.forName("org.basex.build.BDBData").getConstructor(new Class[]{String.class}).newInstance(db);
    }
 catch (    final Exception e) {
      e.printStackTrace();
    }
  }
  return new DiskData(db);
}
