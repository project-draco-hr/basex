{
  if (Prop.usebdb) {
    try {
      return (Data)Class.forName("org.basex.data.BDBData").getConstructor(new Class[]{String.class}).newInstance(db);
    }
 catch (    final Exception e) {
      e.printStackTrace();
    }
  }
  return new DiskData(db);
}
