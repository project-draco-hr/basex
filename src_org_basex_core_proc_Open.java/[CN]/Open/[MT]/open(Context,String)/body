{
  if (!IO.dbpath(db).exists())   throw new FileNotFoundException(BaseX.info(DBNOTFOUND,db));
  if (ctx == null)   return new DiskData(db);
  Data data=ctx.pin(db);
  if (data == null) {
    data=new DiskData(db);
    ctx.addToPool(data);
  }
  return data;
}
