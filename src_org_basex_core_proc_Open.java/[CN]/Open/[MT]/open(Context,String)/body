{
  Data data=ctx.pin(db);
  if (data == null) {
    if (!ctx.prop.dbpath(db).exists())     throw new FileNotFoundException(BaseX.info(DBNOTFOUND,db));
    data=new DiskData(db,ctx.prop);
    ctx.addToPool(data);
  }
  return data;
}
