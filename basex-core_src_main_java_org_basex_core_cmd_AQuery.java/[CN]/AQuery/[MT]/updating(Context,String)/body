{
  try {
    final Performance p=new Performance();
    qp(qu,ctx).parse();
    qi.pars=p.time();
    return qp.updating;
  }
 catch (  final QueryException ex) {
    Util.debug(ex);
    cause=ex;
    qp.close();
    return false;
  }
}
