{
  try {
    final Performance p=new Performance();
    qp(qu,ctx).parse();
    info.parsing=p.time();
    return qp.updating;
  }
 catch (  final QueryException ex) {
    Util.debug(ex);
    cause=ex;
    qp.close();
    return false;
  }
}
