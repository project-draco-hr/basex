{
  final XQuery query=new XQuery("exists(function-lookup(fn:QName('http://www.w3.org/2005/xpath-functions', 'namespace-uri-for-prefix'), 2))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
