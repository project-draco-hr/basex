{
  final XQuery query=new XQuery("empty(function-lookup(fn:QName('http://www.w3.org/2005/xquery-local-functions', 'missing'), 1))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
