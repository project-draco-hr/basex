{
  final GUIProp gprop=gui.prop;
  final BaseXFileChooser fc=new BaseXFileChooser(CREATETITLE,gprop.get(GUIProp.OPENPATH),gui);
  final IO file=fc.select(BaseXFileChooser.Mode.FDOPEN);
  if (file != null) {
    ftswlpath.setText(file.path());
  }
}
