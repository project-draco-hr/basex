{
  final XQuery query=new XQuery("for $var in (/works/employee[1]) return $var/xs:boolean(exactly-one(hours) - 39)",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
