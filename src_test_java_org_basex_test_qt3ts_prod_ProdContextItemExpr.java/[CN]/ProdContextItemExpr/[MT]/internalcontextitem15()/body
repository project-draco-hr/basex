{
  final XQuery query=new XQuery("(6,10,14)[(xs:integer(.) idiv xs:integer(3)) gt 2]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"10 14"));
}
