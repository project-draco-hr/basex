{
  final XQuery query=new XQuery("(fn:true(),fn:false(),fn:true())[xs:boolean(.) or xs:boolean(.)]",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"true true"));
}
