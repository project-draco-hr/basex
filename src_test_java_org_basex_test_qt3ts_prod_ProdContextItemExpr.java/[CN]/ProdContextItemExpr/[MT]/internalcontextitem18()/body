{
  final XQuery query=new XQuery("(\"ABC\", \"DEF\",\"A\")[fn:string-length(.) gt 2]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"ABC DEF"));
}
