{
  final XQuery query=new XQuery("for $var in (/works/employee[1]) return $var/fn:sum((hours,hours))",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("80"));
}
