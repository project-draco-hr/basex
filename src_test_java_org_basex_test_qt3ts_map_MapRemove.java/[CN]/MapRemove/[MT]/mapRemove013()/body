{
  final XQuery query=new XQuery("deep-equal(map:remove(map{\"a\":=1,\"b\":=(2,3)}, \"a\"), map:entry(\"b\", (2,3)))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
