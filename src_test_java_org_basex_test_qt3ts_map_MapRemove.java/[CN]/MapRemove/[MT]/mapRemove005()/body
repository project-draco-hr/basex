{
  final XQuery query=new XQuery("map:remove(map:entry(\"a\", \"1\"), \"b\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertQuery("map:size($result) eq 1") && assertQuery("map:contains($result, \"a\")")));
}
