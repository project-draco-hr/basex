{
  final XQuery query=new XQuery("map:remove(map{}, 1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertQuery("map:size($result) eq 0"));
}
