{
  final XQuery query=new XQuery("for $file at $file in (//Folder)[1]/File return (string($file/FileName[1]), $file)",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0089"));
}
