{
  final XQuery query=new XQuery("declare function local:myFunc() { for $i at $p in (1, 2, 3) return position() }; local:myFunc()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPDY0002"));
}
