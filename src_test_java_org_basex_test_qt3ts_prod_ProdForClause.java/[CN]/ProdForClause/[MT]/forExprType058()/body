{
  final XQuery query=new XQuery("\n" + "        declare namespace fs=\"http://www.example.com/filesystem\"; \n" + "        for $test as comment() in (//fs:Folder)[1]/comment() return $test\n"+ "      ",ctx);
  query.context(node(file("prod/ForClause/fsx_NS.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<!-- This is a comment for File00000000000 --><!-- This is a comment for File00000000001 --><!-- This is a comment for File00000000002 -->",false));
}
