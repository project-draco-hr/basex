{
  final XQuery query=new XQuery("for $folder in $folder/File return <file name=\"{$folder/File/FileName}\"/>",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
