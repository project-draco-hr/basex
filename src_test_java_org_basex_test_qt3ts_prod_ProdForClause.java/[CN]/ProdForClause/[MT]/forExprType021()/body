{
  final XQuery query=new XQuery("for $doc as document-node() in (/) return count( $doc )",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
