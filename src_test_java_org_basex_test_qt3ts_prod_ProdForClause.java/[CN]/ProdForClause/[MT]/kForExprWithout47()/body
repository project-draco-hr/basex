{
  final XQuery query=new XQuery("for $foo in (\"a string\", \"another one\") return 1 + subsequence($foo, 1, 1)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
