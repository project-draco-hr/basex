{
  final XQuery query=new XQuery("deep-equal(remove(for $var in (current-time(), 2, 3, 4) return $var, 1), (2, 3, 4))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
