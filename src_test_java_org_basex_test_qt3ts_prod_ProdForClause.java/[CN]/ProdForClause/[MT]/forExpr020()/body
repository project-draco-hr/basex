{
  final XQuery query=new XQuery("for $folder in //Folder, $file in $folder/File, return $file/FileName",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
