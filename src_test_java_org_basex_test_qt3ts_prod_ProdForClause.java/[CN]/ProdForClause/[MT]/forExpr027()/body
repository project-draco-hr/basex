{
  final XQuery query=new XQuery("for $file in (//Folder)[1]/File at $pos return (string($file/FileName[1]), $pos)",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
