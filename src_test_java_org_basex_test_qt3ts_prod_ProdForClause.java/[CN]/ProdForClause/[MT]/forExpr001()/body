{
  final XQuery query=new XQuery("for $a in (\"test\", \"test1\", \"test2\") return $a",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"test test1 test2"));
}
