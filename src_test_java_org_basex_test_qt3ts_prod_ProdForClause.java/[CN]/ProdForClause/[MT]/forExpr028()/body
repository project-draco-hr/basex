{
  final XQuery query=new XQuery("for $num in ( <one/>, <two/>, <three/> ) return $num",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<one/><two/><three/>",false));
}
