{
  final XQuery query=new XQuery("for $file (//Folder)[1]/File return $file/FileName",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
