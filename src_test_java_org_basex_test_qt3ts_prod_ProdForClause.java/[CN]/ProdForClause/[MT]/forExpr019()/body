{
  final XQuery query=new XQuery("for for $folder in //Folder return $folder",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
