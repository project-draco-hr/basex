{
  final XQuery query=new XQuery("for $int as xs:string in (xs:int(1), xs:int(2)) return $int",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
