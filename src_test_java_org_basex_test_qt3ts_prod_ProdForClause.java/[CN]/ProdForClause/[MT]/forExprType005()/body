{
  final XQuery query=new XQuery("for $int as xs:int in (xs:int(1), xs:int(2)) return $int",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"1 2"));
}
