{
  final XQuery query=new XQuery("declare function local:func($arg as element()* ) as element()* { for $n in $arg/schema-element return $n }; 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
