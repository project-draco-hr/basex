{
  final int l=val.length;
  int i=0;
  if (i < l && (val[0] == '"' || val[0] == '\'')) {
    boolean d=false;
    while (++i < l) {
      if (val[i] == val[0])       d^=true;
 else       if (d)       break;
    }
    if (i == l || val[i++] != ':')     return false;
  }
  return isQName(val,i);
}
