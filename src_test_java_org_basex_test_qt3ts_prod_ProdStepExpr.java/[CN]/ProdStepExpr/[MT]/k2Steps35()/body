{
  final XQuery query=new XQuery("<e/>[1]/text{string-join(., \" \")}, 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertCount(2) && assertQuery("string($result[1]) eq \"\"") && assertQuery("$result[2] eq 1")));
}
