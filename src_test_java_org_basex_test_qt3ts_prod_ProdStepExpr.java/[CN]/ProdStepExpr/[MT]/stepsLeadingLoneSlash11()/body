{
  final XQuery query=new XQuery("declare variable $var := document {<a>123</a>}; $var[/<a/>]",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a>123</a>",false));
}
