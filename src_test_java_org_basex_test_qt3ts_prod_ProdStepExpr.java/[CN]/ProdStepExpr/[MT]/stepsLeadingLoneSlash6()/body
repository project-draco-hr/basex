{
  final XQuery query=new XQuery("declare variable $var := document {<a>123</a>}; $var[/if ($doclevel) then / else /*]",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
