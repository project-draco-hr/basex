{
  final Iter iter=arg[0];
  if (iter instanceof RangeIter) {
    ((RangeIter)iter).reverse();
    return iter;
  }
  return new Iter(){
    final SeqIter si=SeqIter.get(iter);
    int c=si.size;
    @Override public long size(){
      return si.size;
    }
    @Override public Item next(){
      return --c < 0 ? null : si.item[c];
    }
  }
;
}
