{
  final XQuery query=new XQuery("ordered {if (1 eq 1 and 2 eq 2) then (0,1,2,3,4) else (\"a\",\"b\")}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"0 1 2 3 4"));
}
