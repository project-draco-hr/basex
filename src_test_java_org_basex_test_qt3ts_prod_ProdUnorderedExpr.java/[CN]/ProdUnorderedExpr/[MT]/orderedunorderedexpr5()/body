{
  final XQuery query=new XQuery("ordered {if (every $x in (1, 2, 3) satisfies $x < 4) then (0,1,2,3,4) else (\"a\",\"b\")}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"0 1 2 3 4"));
}
