{
  final XQuery query=new XQuery("xs:float(-1.0e0) mod xs:float(-9223372036854775808)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-1") || error("FOAR0002")));
}
