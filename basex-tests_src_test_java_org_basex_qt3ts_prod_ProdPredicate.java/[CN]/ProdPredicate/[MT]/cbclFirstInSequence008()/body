{
  final XQuery query=new XQuery("( (<a><b>cheese</b></a>)/b )[1]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<b>cheese</b>",false));
}
