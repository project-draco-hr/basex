{
  final TokenList tl=new TokenList();
  final Iter ir=ctx.iter(query);
  for (byte[] qu; (qu=nextToken(ir)) != null; ) {
    if (qu.length != 0 || mode == FTMode.M_ALL || mode == FTMode.M_ALLWORDS)     tl.add(qu);
  }
  return tl;
}
