{
  if (atomic) {
    atomic=false;
  }
 else   if (indent) {
    final ArrayList<QNm> qnames=suppress();
    if (!qnames.isEmpty()) {
      for (      final QNm e : elems) {
        if (qnames.contains(e))         return;
      }
    }
    super.indent();
  }
}
