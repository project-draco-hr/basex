{
  final XQuery query=new XQuery(" (xs:double(1.2E2), 2.2E2, 3.3E2)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("120, 220, 330"));
}
