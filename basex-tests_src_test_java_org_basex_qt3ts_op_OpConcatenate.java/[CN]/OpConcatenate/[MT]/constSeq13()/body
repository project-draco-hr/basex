{
  final XQuery query=new XQuery(" (xs:string(\"ABC\"), \"D\", \"E\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("\"ABC\", \"D\", \"E\""));
}
