{
  final XQuery q=new XQuery("*:dependency[" + "@type='feature' and @value=('schemaImport','schemaValidation') or " + "@type='xml-version' and @value='1.1']",ctx).context(node);
  try {
    return q.next() == null;
  }
  finally {
    q.close();
  }
}
