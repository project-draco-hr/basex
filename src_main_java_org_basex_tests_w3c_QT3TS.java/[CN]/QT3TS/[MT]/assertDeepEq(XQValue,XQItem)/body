{
  final XQuery query=new XQuery(string("data(.)",expected),ctx);
  try {
    final XQValue exp=query.value();
    return FNSimple.deep(null,exp.internal().iter(),value.internal().iter()) ? null : Util.info(exp);
  }
  finally {
    query.close();
  }
}
