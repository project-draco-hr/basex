{
  final QueryProcessor qp=new QueryProcessor("http:send-request(" + "<http:request method='get'/>, '" + RESTURL + "unknown')[1]/@status/data()",ctx);
  assertEquals("404",qp.execute().toString());
  qp.close();
}
