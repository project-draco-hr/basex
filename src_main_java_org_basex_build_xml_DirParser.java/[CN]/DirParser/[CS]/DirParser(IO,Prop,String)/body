{
  super(f,t);
  prop=pr;
  root=f.getDir();
  if (!root.endsWith("/"))   root+="/";
  if (f.isDir()) {
    filter=pr.get(Prop.CREATEFILTER).replaceAll("\\*",".*");
    if (!filter.contains("."))     filter=".*" + filter + ".*";
  }
 else {
    filter=".*";
  }
}
