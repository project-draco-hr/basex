{
  final String rd=root.getDir().endsWith("/") ? root.getDir() : root.getDir() + "/";
  final String t2=target + io.path().replace(rd,"").replace(io.name(),"");
  return t2.startsWith("/") ? t2.substring(1) : t2;
}
