{
  final XQuery query=new XQuery("declare namespace prefix = \"http://example.com/NotRecognized\"; 1 eq (#prefix:notRecognized ##cont## # # ( \"# ) # )# )#ent #) {1}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
