{
  final XQuery query=new XQuery("(#xs:a#)(#xs:a#)(#local:a#){-5}",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("-5"));
}
