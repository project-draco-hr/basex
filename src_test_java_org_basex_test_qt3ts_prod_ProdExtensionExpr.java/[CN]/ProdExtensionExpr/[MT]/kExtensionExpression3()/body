{
  final XQuery query=new XQuery("declare namespace prefix = \"http://example.com/NotRecognized\"; (#prefix:pr content #) {1 eq 1}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
