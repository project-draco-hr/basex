{
  final XQuery query=new XQuery("(fn:concat#2)[fn:has-children()]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((error("XPTY0004")));
}
