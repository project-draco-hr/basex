{
  final XQuery query=new XQuery("fn:index-of ((10, 20, 30, 30, 20, 10), 20)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"2 5"));
}
