{
  final XQuery query=new XQuery("fn:index-of(((1),(1), (2,1), (0,1)),1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1 2 4 6"));
}
