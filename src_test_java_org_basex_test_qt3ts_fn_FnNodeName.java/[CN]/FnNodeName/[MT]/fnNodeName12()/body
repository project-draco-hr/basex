{
  final XQuery query=new XQuery("fn:local-name-from-QName(fn:node-name(attribute attributeName {\"an attribute value\"}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"attributeName"));
}
