{
  final XQuery query=new XQuery("node-name( node-name(<a/>))",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
