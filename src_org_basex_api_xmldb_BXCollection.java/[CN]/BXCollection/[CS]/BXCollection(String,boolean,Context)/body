{
  ctx=c;
  try {
    ctx.openDB(open ? Open.open(ctx,name) : CreateDB.xml(ctx,Parser.emptyParser(IO.get(name),ctx.prop),name));
  }
 catch (  final IOException ex) {
    throw new XMLDBException(ErrorCodes.VENDOR_ERROR,ex.getMessage());
  }
}
