{
  if (isOpen()) {
    return new Service[]{getService(BXQueryService.XPATH,null),getService(BXQueryService.XQUERY,null),getService(BXCollectionManagementService.MANAGEMENT,null)};
  }
  throw new XMLDBException(ErrorCodes.COLLECTION_CLOSED);
}
