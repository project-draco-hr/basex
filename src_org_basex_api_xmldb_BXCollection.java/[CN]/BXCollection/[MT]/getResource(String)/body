{
  check();
  if (id == null)   return null;
  final Data data=ctx.data;
  final byte[] idd=Token.token(id);
  for (  final int d : data.doc()) {
    if (Token.eq(data.text(d,true),idd))     return new BXXMLResource(data,d,id,this);
  }
  return null;
}
