{
  return new BXCode<BXResource>(this){
    @Override public BXResource get() throws BaseXException {
      return listDBs(s).contains(childName) ? new BXDatabase(childName,session) : null;
    }
  }
.evalNoEx();
}
