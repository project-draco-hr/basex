{
  final ClientSession session=new ClientSession("localhost",1984,"admin","admin",null);
  try {
    for (int i=0; i < NUM; ++i) {
      try {
        session.execute("drop event " + NAME + i);
      }
 catch (      BaseXException e) {
      }
    }
  }
  finally {
    session.close();
  }
}
