{
  final ClientSession session=new ClientSession("localhost",1984,Text.ADMIN,Text.ADMIN,null);
  try {
    for (int i=0; i < NUM; ++i) {
      try {
        session.execute("drop event " + NAME + i);
      }
 catch (      final BaseXException e) {
      }
    }
  }
  finally {
    session.close();
  }
}
