{
  final XQuery query=new XQuery("normalize-unicode(\"a string\", \"example.com/notSupported/\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOCH0003"));
}
