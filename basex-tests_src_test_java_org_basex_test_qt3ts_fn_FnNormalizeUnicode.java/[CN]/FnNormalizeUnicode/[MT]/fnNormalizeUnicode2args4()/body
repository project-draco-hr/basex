{
  final XQuery query=new XQuery("(normalize-unicode('??', '') eq normalize-unicode('???', ''))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(false));
}
