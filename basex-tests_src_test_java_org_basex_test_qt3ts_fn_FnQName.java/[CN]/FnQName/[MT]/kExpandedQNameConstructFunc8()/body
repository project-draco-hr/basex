{
  final XQuery query=new XQuery("QName(\"my:qName\", \"http://example.com/MyErrorNS\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOCA0002"));
}
