{
  final Expr e=ftUnaryNot();
  if (!consumeWS(NOT))   return e;
  Expr[] list={e};
  do {
    check(IN);
    list=add(list,ftUnaryNot());
  }
 while (consumeWS(NOT));
  Err.or(FTMILD);
  return new FTMildNot(list);
}
