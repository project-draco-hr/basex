{
  final FTExpr e=ftUnaryNot();
  if (!consumeWS(NOT))   return e;
  FTExpr[] list={e};
  do {
    check(IN);
    list=Array.add(list,ftUnaryNot());
  }
 while (consumeWS(NOT));
  return new FTMildNot(list);
}
