{
  consumeWS();
  int p=qp;
  final int sl=str.length();
  if (p + sl > ql)   return false;
  for (int s=0; s < sl; s++) {
    if (qu.charAt(p++) != str.charAt(s))     return false;
  }
  qp=p;
  return true;
}
