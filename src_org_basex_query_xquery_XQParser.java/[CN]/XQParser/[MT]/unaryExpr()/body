{
  boolean minus=false;
  boolean found=false;
  do {
    if (consume(MINUS)) {
      minus^=true;
      found=true;
    }
 else     if (consume(PLUS)) {
      found=true;
    }
 else {
      final Expr e=valueExpr();
      return found ? new Unary(check(e,EVALUNARY),minus) : e;
    }
  }
 while (true);
}
