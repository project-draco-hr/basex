{
  boolean minus=false;
  boolean found=false;
  do {
    skipWS();
    if (consume('-')) {
      minus^=true;
      found=true;
    }
 else     if (consume('+')) {
      found=true;
    }
 else {
      final Expr e=valueExpr();
      return found ? new Unary(check(e,EVALUNARY),minus) : e;
    }
  }
 while (true);
}
