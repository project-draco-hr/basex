{
  byte c=curr();
  if (!letter(c)) {
    if (!first)     qp--;
    return false;
  }
  if (!first)   tok.add(':');
  do {
    tok.add(consume());
    c=curr();
    if (!letterOrDigit(c) && c != '-' && c != '_' && c != '.')     break;
  }
 while (c != 0);
  return true;
}
