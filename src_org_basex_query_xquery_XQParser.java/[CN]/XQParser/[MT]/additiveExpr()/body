{
  Expr expr=multiplicativeExpr();
  while (true) {
    final Calc c=consume(PLUS) ? Calc.PLUS : consume(MINUS) ? Calc.MINUS : null;
    if (c == null)     break;
    expr=new Clc(expr,check(multiplicativeExpr(),CALCEXPR),c);
  }
  return expr;
}
