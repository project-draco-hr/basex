{
  final byte[] ln=type.ln();
  final byte[] uri=type.uri.str();
  for (  final Type t : Type.values()) {
    if (eq(t.name,ln) && eq(uri,t.uri) && (t == Type.NOD || t.par == Type.NOD))     return t;
  }
  return null;
}
