{
  final int p=qp;
  while (qp < ql) {
    final int c=curr();
    if (c == '(' && next() == ':') {
      comment();
    }
 else {
      if (c <= 0 || c > ' ')       break;
      qp++;
    }
  }
  return p != qp;
}
