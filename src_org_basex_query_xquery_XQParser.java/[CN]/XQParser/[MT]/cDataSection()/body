{
  final TokenBuilder tb=new TokenBuilder();
  while (true) {
    while (not(']'))     tb.add(consume());
    consume();
    if (curr() == ']' && next() == '>') {
      qp+=2;
      return tb.finish();
    }
    tb.add(']');
  }
}
