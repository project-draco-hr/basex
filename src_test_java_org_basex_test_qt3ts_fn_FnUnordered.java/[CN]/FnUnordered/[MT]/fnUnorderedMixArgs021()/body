{
  final XQuery query=new XQuery("fn:unordered ( (\"a\", xs:date(\"1993-03-31\"), \"b\", \"c\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertPermutation("\"a\", xs:date('1993-03-31'), \"b\", \"c\""));
}
