{
  final XQuery query=new XQuery("floor(unordered((1 to 10)[. div 2 = 0]))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,""));
}
