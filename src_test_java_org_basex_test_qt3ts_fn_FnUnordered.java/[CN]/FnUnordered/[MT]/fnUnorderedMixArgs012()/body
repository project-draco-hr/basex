{
  final XQuery query=new XQuery("fn:unordered ( (\"a\", xs:float(\"1.01\"), \"b\", \"c\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertPermutation("\"a\", 1.01, \"b\", \"c\""));
}
