{
  final XQuery query=new XQuery("fn:unordered ( (\"a\", xs:string(\"\"), \"b\", \"c\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertPermutation("\"a\", \"\",  \"b\", \"c\""));
}
