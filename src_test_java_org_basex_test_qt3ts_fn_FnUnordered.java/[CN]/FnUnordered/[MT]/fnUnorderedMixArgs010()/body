{
  final XQuery query=new XQuery("fn:unordered ( (\"a\", xs:float(\"-INF\"), \"b\", \"c\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertPermutation("\"a\", xs:float('-INF'), \"b\", \"c\""));
}
