{
  final XQuery query=new XQuery("for $i in <e/>/(., 4, 5, <e/>/((<e/>, 2))) return ($i, $i)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0018"));
}
