{
  final StringBuilder sb=new StringBuilder();
  if (s.length() != 0) {
    sb.append(' ');
    final boolean spc=s.indexOf(' ') != -1;
    if (spc)     sb.append('"');
    sb.append(s);
    if (spc)     sb.append('"');
  }
  return sb.toString();
}
