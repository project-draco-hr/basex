{
  final XQuery query=new XQuery("fn:doc-available(\"file:///a/b/c/wefdobqciyvdsoihnfcpinads.xml\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(false));
}
