{
  Item i=next();
  if (i == null)   return Seq.EMPTY;
  Item[] item={i};
  int s=1;
  while ((i=next()) != null) {
    if (s == item.length)     item=Arrays.copyOf(item,s << 1);
    item[s++]=i;
  }
  return Seq.get(item,s);
}
