{
  final XQuery query=new XQuery("<a xml:space=\"PRESERVE\"/>",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertSerialization("<a xml:space=\"PRESERVE\"/>",false) || error("XQDY0092")));
}
