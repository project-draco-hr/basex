{
  QueryProcess tmp=null;
  final int t=Integer.valueOf(in.readString());
  for (  final QueryProcess q : queries) {
    if (q.id == t) {
      tmp=q;
      break;
    }
  }
  if (tmp == null) {
    send(false);
  }
 else {
    if (in.readByte() == 1) {
      tmp.close();
    }
 else {
      tmp.more();
    }
  }
}
