{
  final XQuery query=new XQuery("fn:distinct-values(( 1, 1.0e0))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertCount(1) && assertEq("1") && (assertType("xs:integer") || assertType("xs:double"))));
}
