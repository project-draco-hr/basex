{
  final XQuery query=new XQuery("fn:distinct-values((xs:double('-INF'), xs:double('INF')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"INF -INF") || assertStringValue(false,"-INF INF")));
}
