{
  final XQuery query=new XQuery("fn:distinct-values((xs:decimal('1.2'), xs:double('1.2')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1.2"));
}
