{
  final XQuery query=new XQuery("distinct-values((-3, -3))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-3") || error("XPTY0004")));
}
