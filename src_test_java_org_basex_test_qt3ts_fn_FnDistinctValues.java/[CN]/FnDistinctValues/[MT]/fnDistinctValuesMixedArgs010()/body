{
  final XQuery query=new XQuery("fn:distinct-values((xs:decimal('1.2'), xs:decimal('1.2000000000000001')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertPermutation("1.2000000000000001, 1.2"));
}
