{
  final XQuery query=new XQuery("fn:distinct-values((1, true(), true(), ()))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertPermutation("1, true()"));
}
