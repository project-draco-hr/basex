{
  final XQuery query=new XQuery("function($a as xs:string) as item() { $a } instance of function(item()) as item()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
