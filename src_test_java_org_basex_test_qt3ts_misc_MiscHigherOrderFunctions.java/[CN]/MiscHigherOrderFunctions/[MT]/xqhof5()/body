{
  final XQuery query=new XQuery("\n" + "let $a := string-join(?, \"\")\n" + "return $a((\"foo\", \"bar\", \"baz\"))\n"+ "      ",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("\"foobarbaz\""));
}
