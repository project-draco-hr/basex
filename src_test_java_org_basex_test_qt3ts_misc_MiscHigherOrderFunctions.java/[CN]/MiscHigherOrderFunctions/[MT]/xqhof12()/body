{
  final XQuery query=new XQuery("(concat(\"one \", ?, \" three\"), substring-before(\"one two three\", ?), matches(?, \"t.*o\"), xs:NCName(?))(\"two\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
