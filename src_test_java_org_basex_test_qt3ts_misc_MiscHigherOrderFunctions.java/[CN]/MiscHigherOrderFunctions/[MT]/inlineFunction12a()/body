{
  final XQuery query=new XQuery("function($a, $a) { \"lala\", $a }(\"gibbon\", \"monkey\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0039"));
}
