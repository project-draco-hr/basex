{
  final XQuery query=new XQuery("function($a as item()) as item() { $a } instance of function(xs:string) as item()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
