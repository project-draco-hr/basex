{
  final XQuery query=new XQuery("let $f := fn:substring-before#2(?, '-') return <out>{$f('the-end-of-the-world')}</out>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<out>the</out>",false));
}
