{
  final XQuery query=new XQuery("declare function local:f($x as xs:integer) as xs:integer { $x + 1 }; local:f#1 eq 3",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("FOTY0013"));
}
