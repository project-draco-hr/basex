{
  final XQuery query=new XQuery("let $f := fn:concat#3(?, '*', ?) let $g := $f('[', ?) return <out>{$g(']')}</out>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<out>[*]</out>",false));
}
