{
  final XQuery query=new XQuery("\n" + "let $f := function($a) { node-name(.), $a }\n" + "return <a/>/$f(5)\n"+ "      \n"+ "      ",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPDY0002"));
}
