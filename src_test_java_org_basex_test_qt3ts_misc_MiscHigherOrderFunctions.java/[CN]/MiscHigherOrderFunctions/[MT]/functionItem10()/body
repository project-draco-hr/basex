{
  final XQuery query=new XQuery("(let $a := 92, $b := true() return function($c) { $a, $b, $c }) instance of function(item()*) as item()*",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
