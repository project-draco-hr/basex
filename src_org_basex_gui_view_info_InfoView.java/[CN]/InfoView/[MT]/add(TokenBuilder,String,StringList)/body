{
  if (list.size == 0)   return;
  tb.add((byte)0x02);
  tb.add(head);
  tb.add((byte)0x03);
  tb.add((byte)0x0A);
  for (int i=0; i < list.size; i++) {
    String line=list.list[i];
    if (list == strings)     line=" " + QUERYSEP + line+ ":  "+ Performance.getTimer(stat.list[i] * 10000L * Prop.runs,Prop.runs);
    tb.add(line);
    tb.add((byte)0x0A);
  }
  tb.add((byte)0x0b);
}
