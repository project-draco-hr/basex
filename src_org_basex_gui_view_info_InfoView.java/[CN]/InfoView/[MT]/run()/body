{
  while (up || down) {
    if (up && hy > h) {
      sy-=GUIProp.fontsize;
    }
 else     if (down && sy < 0) {
      sy+=GUIProp.fontsize;
    }
 else {
      GUI.get().setCursor(CURSORARROW);
    }
    repaint();
    Performance.sleep(40);
  }
}
