{
  final XQuery query=new XQuery("not(fn:boolean((1, 2, 3, current-time())[1] treat as xs:integer)) eq false()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
