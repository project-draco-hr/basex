{
  final XQuery query=new XQuery("fn:substring-before(\"substring-before\",\"substring-before\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("\"\"") && assertCount(1)));
}
