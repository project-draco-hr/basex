{
  return new BXCode<BXAbstractResource>(this){
    @Override public BXAbstractResource get() throws IOException {
      return service.dbExists(name) ? new BXDatabase(new ResourceMetaData(name,service.timestamp(name)),service) : null;
    }
  }
.evalNoEx();
}
