{
  final TokenBuilder string=new TokenBuilder();
  string.add((text ? Function._DB_TEXT : tokenize ? Function._DB_TOKEN : Function._DB_ATTRIBUTE).get(null,info,Str.get(ictx.data.meta.name),expr).toString());
  if (test != null)   string.add("/parent::").addExt(test);
  return string.toString();
}
