{
  final TokenBuilder string=new TokenBuilder();
  string.add((text ? Function._DB_TEXT : Function._DB_ATTRIBUTE).get(null,info,Str.get(ictx.data.meta.name),expr).toString());
  if (name != null)   string.add("/parent::").add(name);
  return string.toString();
}
