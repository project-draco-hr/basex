{
  return new Iterator<FTStringMatch>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public FTStringMatch next(){
      return match[c];
    }
    public void remove(){
      BaseX.notexpected();
    }
  }
;
}
