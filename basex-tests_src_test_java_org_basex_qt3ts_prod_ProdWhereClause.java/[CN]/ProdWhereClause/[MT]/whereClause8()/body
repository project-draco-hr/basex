{
  final XQuery query=new XQuery("let $var := 100 where fn:count(($var)) = 1 return fn:count(($var))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("1"));
}
