{
  if (line != 0 || parser == null)   return;
  file=parser.file;
  line=1;
  col=1;
  for (int i=0; i < parser.qm && i < parser.ql; i++) {
    final char ch=parser.qu.charAt(i);
    if (ch == 0x0A) {
      line++;
      col=1;
    }
 else     if (ch != 0x0D) {
      col++;
    }
  }
}
