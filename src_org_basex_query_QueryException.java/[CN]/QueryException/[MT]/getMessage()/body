{
  final StringBuilder sb=new StringBuilder();
  if (line != 0) {
    sb.append(file == null ? BaseX.info(POSINFO,line,col) : BaseX.info(POSFILEINFO,line,col,file));
  }
  if (code != null)   sb.append("[" + code + "] ");
  sb.append(super.getMessage());
  return sb.toString();
}
