{
  super.comp(ctx);
  final Expr e=expr[0].item() && expr[1].item() ? atomic(ctx,input) : expr[0].empty() || expr[1].empty() ? Seq.EMPTY : this;
  if (e != this)   ctx.compInfo(OPTPRE,this);
  return e;
}
