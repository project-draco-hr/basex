{
  if (reporting) {
    final File file=new File(results + path);
    if (!file.exists())     file.mkdirs();
    final BufferedWriter bw=new BufferedWriter(new OutputStreamWriter(new FileOutputStream(results + path + name),UTF8));
    bw.write(msg);
    bw.close();
  }
}
