{
  if (key == null) {
    if (CHECK && !check.isEmpty()) {
      for (      final String s : check.keySet())       Util.errln("%." + SUFFIX + ": '%' not used",Prop.language,s);
    }
    return null;
  }
  final String val=TETXTS.get(key);
  if (val == null) {
    if (!TETXTS.isEmpty())     Util.errln("%." + SUFFIX + ": '%' missing",Prop.language,key);
    return '[' + key + ']';
  }
  if (CHECK)   check.remove(key);
  return val;
}
