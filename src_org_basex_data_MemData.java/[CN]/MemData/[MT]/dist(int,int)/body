{
switch (k) {
case ELEM:
    return (int)(val1[pre] & 0xFFFF);
case TEXT:
case COMM:
case PI:
  return (int)(val2[pre] >> 32);
case ATTR:
return (int)(val2[pre] >> 32);
default :
return pre + 1;
}
}
