{
  if (!check)   return;
  final int s=size;
  if (s > 1) {
    if (sort)     sort(0,s);
    int i=1;
    final ANode[] lst=list;
    for (int j=1; j < s; ++j) {
      while (j < s && lst[i - 1].is(lst[j]))       j++;
      if (j < s)       lst[i++]=lst[j];
    }
    size=i;
  }
  check=false;
}
