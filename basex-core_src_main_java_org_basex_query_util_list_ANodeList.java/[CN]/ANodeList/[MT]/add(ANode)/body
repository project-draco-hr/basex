{
  ANode[] lst=list;
  final int s=size;
  if (s == lst.length)   lst=copyOf(newSize());
  if (s != 0 && check && !sort)   sort=lst[s - 1].diff(element) > 0;
  lst[s]=element;
  list=lst;
  size=s + 1;
}
