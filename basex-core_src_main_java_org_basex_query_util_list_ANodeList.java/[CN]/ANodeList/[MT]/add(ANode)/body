{
  ANode[] lst=list;
  final int s=size;
  if (s != 0 && check && !sort) {
    final int d=element.diff(lst[s - 1]);
    if (d == 0)     return;
    if (d < 0)     sort=true;
  }
  if (s == lst.length)   lst=copyOf(newSize());
  lst[s]=element;
  list=lst;
  size=s + 1;
}
