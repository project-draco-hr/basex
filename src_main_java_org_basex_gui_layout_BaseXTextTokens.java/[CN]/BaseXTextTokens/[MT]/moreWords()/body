{
  if (pe >= size)   return false;
  ps=pe;
  int ch=cp(text,ps);
  pe+=cl(text,ps);
  if (!ftChar(ch))   return true;
  while (pe < size) {
    ch=cp(text,pe);
    if (!ftChar(ch))     break;
    pe+=cl(text,pe);
  }
  ;
  return true;
}
