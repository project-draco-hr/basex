{
switch (kind) {
case Data.ELEM:
    return data.tags.key(name);
case Data.ATTR:
  return Token.concat(ATT,data.atts.key(name));
case Data.TEXT:
return TEXT;
case Data.COMM:
return COMM;
case Data.PI:
return PI;
default :
return Token.EMPTY;
}
}
