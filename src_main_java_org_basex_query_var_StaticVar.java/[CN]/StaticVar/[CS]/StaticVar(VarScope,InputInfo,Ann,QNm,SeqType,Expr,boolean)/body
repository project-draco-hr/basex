{
  super(ii);
  scope=scp;
  name=n;
  ann=a == null ? new Ann() : a;
  check=t;
  type=t == null ? SeqType.ITEM_ZM : t;
  expr=e;
  declared=true;
  bindable=ext || e == null;
  lazy=ann.contains(LAZY);
}
