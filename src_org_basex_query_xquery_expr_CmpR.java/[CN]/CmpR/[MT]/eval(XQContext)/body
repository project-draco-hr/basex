{
  final Iter ir=ctx.iter(expr);
  boolean mn=false;
  boolean mx=false;
  Item it;
  while ((it=ir.next()) != null) {
    final double d=it.dbl();
    mn|=mni ? d >= min : d > min;
    mx|=mxi ? d <= max : d < max;
    if (mn && mx)     return true;
  }
  return false;
}
