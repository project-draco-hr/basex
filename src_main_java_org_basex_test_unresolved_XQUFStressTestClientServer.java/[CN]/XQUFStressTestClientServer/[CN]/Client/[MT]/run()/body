{
  try {
    for (int i=0; i < NQUERIES; ++i) {
      Performance.sleep(100);
      session.execute("xquery " + q);
    }
    session.close();
  }
 catch (  final Exception ex) {
    ex.printStackTrace();
  }
}
