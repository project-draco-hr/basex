{
  while (console) {
    Util.out("> ");
    for (    final String in : inputs()) {
      if (in.isEmpty())       continue;
      final Boolean b=execute(in);
      if (b == null)       return true;
    }
  }
  return false;
}
