{
  final XQuery query=new XQuery("fn:index-of((/bib/book/publisher), \"Addison-Wesley\")",ctx);
  query.context(node(file("docs/bib.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"1 2"));
}
