{
  final XQuery query=new XQuery("fn:index-of((xs:negativeInteger('-2'), xs:negativeInteger('-1')), fn:exactly-one(xs:negativeInteger('-1')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("2"));
}
