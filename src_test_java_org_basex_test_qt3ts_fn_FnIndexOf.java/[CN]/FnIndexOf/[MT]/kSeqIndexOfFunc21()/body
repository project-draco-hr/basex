{
  final XQuery query=new XQuery("deep-equal(index-of((\"a\", \"sport\", \"and\", \"a\", \"pastime\"), \"a\"), (1, 4))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
