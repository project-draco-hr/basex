{
  this(it);
  if (minus) {
    sec=-sec;
    mil=-mil;
  }
  sec+=p ^ a.minus ? a.sec : -a.sec;
  mil+=p ^ a.minus ? a.mil : -a.mil;
  minus=sec < 0 || mil < 0;
  sec=Math.abs(sec);
  mil=Math.abs(mil);
}
