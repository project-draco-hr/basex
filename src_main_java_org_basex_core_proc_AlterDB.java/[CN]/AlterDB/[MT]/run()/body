{
  final String db=args[0];
  final String name=args[1];
  if (context.pinned(db))   return error(DBLOCKED,db);
  return !prop.dbexists(db) ? error(DBNOTFOUND,db) : alter(db,name) ? info(DBALTERED,db,name) : error(DBNOTALTERED,db);
}
