{
  final TokenBuilder tb=new TokenBuilder();
  for (  final byte[] b : header) {
    tb.add(b);
    tb.add('\t');
  }
  tb.nl();
  for (  final TokenList e : contents) {
    for (    final byte[] b : e) {
      tb.add(b);
      tb.add('\t');
    }
  }
  return tb.toString();
}
