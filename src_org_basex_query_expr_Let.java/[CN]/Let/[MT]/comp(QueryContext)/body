{
  expr=expr.comp(ctx);
  if (!score && expr.countVar(null) == 0 && !(expr instanceof CFrag || expr instanceof Path)) {
    ctx.compInfo(OPTBIND,var);
    var.bind(expr,ctx);
  }
 else {
    var.ret=expr.returned(ctx);
  }
  ctx.vars.add(var);
  return this;
}
