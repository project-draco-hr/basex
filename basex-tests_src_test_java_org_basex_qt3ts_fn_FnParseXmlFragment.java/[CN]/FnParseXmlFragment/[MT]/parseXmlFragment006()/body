{
  final XQuery query=new XQuery("parse-xml-fragment(\"vanessa\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"vanessa"));
}
