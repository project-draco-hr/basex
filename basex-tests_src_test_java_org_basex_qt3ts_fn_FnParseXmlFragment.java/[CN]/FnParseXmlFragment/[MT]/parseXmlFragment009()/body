{
  final XQuery query=new XQuery("parse-xml-fragment(\"<a> </a> <b> </b>\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<a> </a> <b> </b>",false));
}
