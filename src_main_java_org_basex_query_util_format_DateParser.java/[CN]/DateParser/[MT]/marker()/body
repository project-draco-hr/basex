{
  int p=pos;
  while (pic.charAt(pos++) != ']')   if (!more())   PICDATE.thrw(input,pic);
  final StringBuilder sb=new StringBuilder();
  for (; p < pos - 1; ++p) {
    final char ch=pic.charAt(p);
    if (!Character.isWhitespace(ch))     sb.append(ch);
  }
  return sb.toString();
}
