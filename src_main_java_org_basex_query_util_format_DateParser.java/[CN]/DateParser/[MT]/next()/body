{
  final char ch=pic.charAt(pos++);
  if (ch == '[' || ch == ']') {
    if (!more())     Err.or(PICDATE,pic);
    if (pic.charAt(pos) != ch) {
      if (ch == ']')       Err.or(PICDATE,pic);
      return 0;
    }
    pos++;
  }
  return ch;
}
