{
  final int ch=cp(pic,pos);
  pos+=cl(pic,pos);
  if (ch == '[' || ch == ']') {
    if (!more())     PICDATE.thrw(input,pic);
    if (cp(pic,pos) != ch) {
      if (ch == ']')       PICDATE.thrw(input,pic);
      return 0;
    }
    pos+=cl(pic,pos);
  }
  return ch;
}
