{
  final int ch=cp(pic,pos);
  pos+=cl(pic,pos);
  if (ch == '[' || ch == ']') {
    if (!more())     PICDATE.thrw(info,pic);
    if (cp(pic,pos) != ch) {
      if (ch == ']')       PICDATE.thrw(info,pic);
      return 0;
    }
    pos+=cl(pic,pos);
  }
  return ch;
}
