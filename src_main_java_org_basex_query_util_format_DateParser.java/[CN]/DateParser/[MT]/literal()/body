{
  final int ch=next();
  if (ch == '[') {
    if (!more())     PICDATE.thrw(info,token);
    if (!consume(ch))     return -1;
  }
 else   if (ch == ']') {
    if (!consume(ch))     PICDATE.thrw(info,token);
  }
  return ch;
}
