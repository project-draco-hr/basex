{
  final int ch=next();
  if (ch == '[' || ch == ']') {
    if (!more())     PICDATE.thrw(info,token);
    if (!consume(ch)) {
      if (ch == ']')       PICDATE.thrw(info,token);
      return -1;
    }
  }
  return ch;
}
