{
  boolean close=!open;
  if (open && tb.size() != 0) {
    open();
    close=true;
  }
  add(tb);
  if (close) {
    if (simple || row != 0) {
      builder.endElem(row == 0 ? HEADER : RECORD);
    }
    ++row;
  }
  col=0;
}
