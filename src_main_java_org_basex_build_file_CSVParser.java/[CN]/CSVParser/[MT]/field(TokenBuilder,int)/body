{
  if (attr) {
    atts.reset();
    atts.add(COLUMN,token(c));
  }
  final byte[] tag=cols ? concat(COLUMN,token(c)) : FIELD;
  builder.startElem(tag,atts);
  builder.text(tb);
  builder.endElem(tag);
  tb.reset();
}
