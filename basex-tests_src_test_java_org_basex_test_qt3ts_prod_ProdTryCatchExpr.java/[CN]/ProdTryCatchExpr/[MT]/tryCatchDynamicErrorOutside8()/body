{
  final XQuery query=new XQuery("(try { \"\" } catch *:FOAR0001 { \"Invalid argument\" }) + 1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
