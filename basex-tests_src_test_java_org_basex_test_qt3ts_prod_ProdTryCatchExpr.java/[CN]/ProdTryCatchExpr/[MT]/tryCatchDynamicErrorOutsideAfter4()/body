{
  final XQuery query=new XQuery("try { () } catch *:FOAR0001 { \"Division by zero\" }, 1 div 0",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOAR0001"));
}
