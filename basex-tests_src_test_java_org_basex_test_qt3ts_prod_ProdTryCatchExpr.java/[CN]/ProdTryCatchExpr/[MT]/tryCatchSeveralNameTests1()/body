{
  final XQuery query=new XQuery("try { 1 div 0 } catch err:XPST0008 | err:FOAR0001 { \"Division by zero\" }",ctx);
  try {
    query.namespace("err","http://www.w3.org/2005/xqt-errors");
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"Division by zero"));
}
