{
  init(pos);
  while (more()) {
    if (posW >= startW && posW <= endW || posW >= endW && posW <= startW) {
      text.mark();
    }
  }
  final String nxt=text.marked();
  return nxt.equals("\n") ? "" : nxt;
}
