{
  mark=Token.lc(m);
  startW=-1;
  endW=-1;
  if (m.length == 0)   return -1;
  final int hh=h;
  h=Integer.MAX_VALUE;
  init(0);
  while (more()) {
    if (text.contains(mark)) {
      h=hh;
      return Math.max(0,y);
    }
  }
  h=hh;
  return -1;
}
