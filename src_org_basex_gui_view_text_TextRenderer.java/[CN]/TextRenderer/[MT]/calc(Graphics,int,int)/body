{
  g=(Graphics2D)gg;
  w=ww - offset / 2;
  h=Integer.MAX_VALUE;
  init(0);
  startW=-1;
  endW=-1;
  while (more())   ;
  final int ys=GUIConstants.lfont.getSize();
  h=hh + fontH + ys;
  return y + offset;
}
