{
  FTItem it=null;
  double s=0;
  for (  final FTExpr e : expr) {
    it=e.atomic(ctx).union(it);
    final double d=s;
    s=it.score();
    if (s == 0)     break;
    s=ctx.score.and(s,d);
  }
  it.score(s);
  return it;
}
