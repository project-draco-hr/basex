{
  FTNode it=null;
  double d=0;
  for (  final FTExpr e : expr) {
    it=e.atomic(ctx).union(it);
    double s=d;
    d=it.score();
    if (d == 0)     break;
    d=ctx.score.and(d,s);
  }
  it.score(d);
  return it;
}
