{
  final FTMatches all=new FTMatches(m1.sTokenNum > m2.sTokenNum ? m1.sTokenNum : m2.sTokenNum);
  for (  final FTMatch s1 : m1) {
    for (    final FTMatch s2 : m2) {
      all.add(new FTMatch().add(s1).add(s2));
    }
  }
  return all;
}
