{
  if (data != null) {
    BaseX.debug("Warning: Database still open.");
    close();
  }
  data=d;
  copied=null;
  marked=new Nodes(d);
  current=Prop.fsmode ? new Nodes(DataFS.ROOTDIR,data) : new Nodes(d.doc(),d);
}
