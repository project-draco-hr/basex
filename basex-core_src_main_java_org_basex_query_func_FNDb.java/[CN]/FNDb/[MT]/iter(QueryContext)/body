{
switch (func) {
case _DB_OPEN:
    return open(qc).iter();
case _DB_BACKUPS:
  return backups(qc);
case _DB_TEXT:
return valueAccess(true,qc).iter(qc);
case _DB_TEXT_RANGE:
return rangeAccess(true,qc).iter(qc);
case _DB_ATTRIBUTE:
return attribute(valueAccess(false,qc),qc,2);
case _DB_ATTRIBUTE_RANGE:
return attribute(rangeAccess(false,qc),qc,3);
case _DB_LIST:
return list(qc);
case _DB_LIST_DETAILS:
return listDetails(qc);
case _DB_NODE_ID:
return node(qc,true);
case _DB_NODE_PRE:
return node(qc,false);
default :
return super.iter(qc);
}
}
