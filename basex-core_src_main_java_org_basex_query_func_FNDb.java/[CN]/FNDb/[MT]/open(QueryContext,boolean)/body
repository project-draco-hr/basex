{
  final Data data=checkData(ctx);
  final int v=(int)checkItr(exprs[1],ctx);
  final int pre=id ? data.pre(v) : v;
  if (pre < 0 || pre >= data.meta.size)   throw BXDB_RANGE.get(info,this,v);
  return new DBNode(data,pre);
}
