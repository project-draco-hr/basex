{
  if (atts == null)   return null;
  final int size=atts.size;
  final byte[][] keys=atts.key;
  final byte[][] vals=atts.val;
  final String[][] a=new String[size][];
  for (int i=0; i < size; ++i)   a[i]=new String[]{string(keys[i]),string(vals[i])};
  return a;
}
