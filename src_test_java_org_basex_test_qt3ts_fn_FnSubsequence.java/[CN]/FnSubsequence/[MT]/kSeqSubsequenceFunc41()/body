{
  final XQuery query=new XQuery("subsequence((1, 2, 3, current-time(), 5, 6, 9), 7)[last()]",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("9"));
}
