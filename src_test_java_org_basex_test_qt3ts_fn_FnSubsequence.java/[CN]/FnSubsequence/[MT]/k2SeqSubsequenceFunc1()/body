{
  final XQuery query=new XQuery("let $start := (current-time(), 2)[2] treat as xs:integer,\n" + "                $len := (current-time(), 1)[2] treat as xs:integer\n" + "                return subsequence((1, 2, 3), $start, $len)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("2"));
}
