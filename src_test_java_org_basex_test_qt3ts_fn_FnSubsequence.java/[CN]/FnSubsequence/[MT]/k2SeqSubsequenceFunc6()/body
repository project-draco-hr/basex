{
  final XQuery query=new XQuery("fn:subsequence((1,2,3), 1.4, 1.4)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("1"));
}
