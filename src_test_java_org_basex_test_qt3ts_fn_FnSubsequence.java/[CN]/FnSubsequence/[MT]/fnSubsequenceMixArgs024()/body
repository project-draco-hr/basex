{
  final XQuery query=new XQuery("fn:subsequence (1 to 10, xs:double('-INF'), xs:double('INF'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEmpty());
}
