{
  final XQuery query=new XQuery("let $f :=function() { subsequence(subsequence((1, 2, 3, 4), 3, 1), 1, 4) } return $f()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("3"));
}
