{
  String file=url;
  try {
    if (file.indexOf("%") != -1)     file=URLDecoder.decode(file,Prop.ENCODING);
  }
 catch (  final Exception ex) {
  }
  return file.replaceAll("^" + FILEPREF,"").replace("//+","/").replaceFirst("^/(\\w:)","$1");
}
