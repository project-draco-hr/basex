{
  final XQuery query=new XQuery("fn:substring-after(\"substring-after\",\"substring-after\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("\"\"") && assertCount(1)));
}
