{
  final XQuery query=new XQuery("fn:substring-after(\"substring-aftersubstring-after\",\"substring-after\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"substring-after"));
}
