{
  final XQuery query=new XQuery("fn:substring-after(\"A Character String\",())",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("\"A Character String\"") && assertCount(1)));
}
