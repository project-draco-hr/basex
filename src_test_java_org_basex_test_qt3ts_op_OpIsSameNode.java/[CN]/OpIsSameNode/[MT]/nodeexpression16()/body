{
  final XQuery query=new XQuery("(/staff[1]/employee[1]/empnum[1]) is (/staff[1]/employee[1]/empnum[1])",ctx);
  query.context(node(file("docs/staff.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
