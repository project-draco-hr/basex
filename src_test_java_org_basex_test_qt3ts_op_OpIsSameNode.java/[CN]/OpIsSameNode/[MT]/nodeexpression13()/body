{
  final XQuery query=new XQuery("count((/staff[1]/employee[1]/empnum[1]) is ())",ctx);
  query.context(node(file("docs/staff.xml")));
  final QT3Result res=result(query);
  result=res;
  test((assertEq("0") || error("XPST0005")));
}
