{
  final XQuery query=new XQuery("<a>50000</a> is /works[1]/employee[1]/empnum[1]",ctx);
  query.context(node(file("docs/works.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
