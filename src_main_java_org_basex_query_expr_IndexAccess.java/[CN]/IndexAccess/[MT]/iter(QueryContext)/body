{
  Iter[] iter={};
  final Iter ir=expr.iter(ctx);
  Item it;
  while ((it=ir.next()) != null)   iter=Array.add(iter,index(it.str()));
  return iter.length == 0 ? Iter.EMPTY : iter.length == 1 ? iter[0] : new Union(new Expr[]{expr}).eval(iter);
}
