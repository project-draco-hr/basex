{
  while (sessions.size() > 0) {
  }
  if (sessions.size() == 0) {
    try {
      sessionListener.thread=null;
      inputListener.thread=null;
      serverSocket.close();
      new Exit().execute(null);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
}
