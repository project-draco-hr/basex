{
  table=meta.prop.is(Prop.TABLEMEM) ? new TableMemAccess(meta,DATATBL) : new TableDiskAccess(meta,DATATBL);
  texts=new DataAccess(meta.file(DATATXT));
  values=new DataAccess(meta.file(DATAATV));
  super.init();
}
