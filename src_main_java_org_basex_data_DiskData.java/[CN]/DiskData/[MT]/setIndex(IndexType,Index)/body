{
switch (type) {
case TEXT:
    if (meta.textindex)     txtindex=index;
  break;
case ATTRIBUTE:
if (meta.attrindex) atvindex=index;
break;
case FULLTEXT:
if (meta.ftindex) ftxindex=index;
break;
case PATH:
if (meta.pathindex) path=(PathSummary)index;
break;
default :
break;
}
}
