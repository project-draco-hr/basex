{
switch (type) {
case TEXT:
    if (meta.txtindex)     txtindex=index;
  break;
case ATTRIBUTE:
if (meta.atvindex) atvindex=index;
break;
case FULLTEXT:
if (meta.ftxindex) ftxindex=index;
break;
case PATH:
if (meta.pthindex) path=(PathSummary)index;
break;
default :
break;
}
}
