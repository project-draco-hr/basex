{
  if (!check(o,String.class))   return;
  final String str=(String)o;
  if (EMLParser.isEmailAddress(str))   deepFile.addMeta(MetaElem.RECEIVER_EMAIL,str);
 else   deepFile.addMeta(MetaElem.RECEIVER_NAME,str);
}
