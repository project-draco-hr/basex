{
  final int id=put(n);
  if (st) {
    if (stats[id] == null)     stats[id]=new Stats();
    final Stats stat=stats[id];
    if (v != null)     stat.add(v,meta);
    stat.count++;
  }
  return id;
}
