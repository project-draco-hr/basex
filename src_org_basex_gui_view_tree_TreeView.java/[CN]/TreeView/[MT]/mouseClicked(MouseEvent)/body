{
  if (!SwingUtilities.isLeftMouseButton(e) || working || opened == null)   return;
  final Data data=GUI.context.data();
  if (getCursor() == GUIConstants.CURSORHAND && data.fs != null) {
    data.fs.launch(focused);
  }
}
