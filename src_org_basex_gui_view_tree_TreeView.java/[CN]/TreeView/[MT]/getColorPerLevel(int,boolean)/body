{
  if (fill)   return new Color(colorNode - ((l < changeColorTillLevel ? l : changeColorTillLevel) * colorDiff));
  return new Color(colorRect - ((l < changeColorTillLevel ? l : changeColorTillLevel) * colorDiff));
}
