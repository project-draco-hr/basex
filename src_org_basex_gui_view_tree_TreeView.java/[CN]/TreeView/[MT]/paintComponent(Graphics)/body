{
  super.paintComponent(g);
  g.setFont(GUIConstants.font);
  Data data=gui.context.data;
  nodeHeight=fontHeight=g.getFontMetrics().getHeight();
  setLevelDistance();
  if (windowSizeChanged()) {
    if (markedImage != null) {
      markedImage=null;
      refreshedMark=true;
    }
    focusedRect=null;
    createNewMainImage(data);
  }
  g.drawImage(treeImage,0,0,getWidth(),getHeight(),this);
  if (markedImage != null)   g.drawImage(markedImage,0,0,getWidth(),getHeight(),this);
  if (focus()) {
    highlightNode(g,focusedRect,focusedRectLevel,-1,false,SHOW_ANCESTORS,SHOW_DESCENDANTS);
  }
  if (selection)   markSelektedNodes(g);
}
