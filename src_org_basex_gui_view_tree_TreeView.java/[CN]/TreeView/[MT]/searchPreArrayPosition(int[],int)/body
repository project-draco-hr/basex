{
  int index=-1;
  int l=0;
  int r=a.length - 1;
  while (r >= l && index == -1) {
    final int m=l + (r - l) / 2;
    if (a[m] < pre) {
      l=m + 1;
    }
 else     if (a[m] > pre) {
      r=m - 1;
    }
 else {
      index=m;
    }
  }
  return index;
}
