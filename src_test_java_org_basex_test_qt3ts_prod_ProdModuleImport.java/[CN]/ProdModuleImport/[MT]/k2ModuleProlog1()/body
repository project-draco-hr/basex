{
  final XQuery query=new XQuery("module namespace example = \"http://example.com/\"; \"an expression\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0003"));
}
