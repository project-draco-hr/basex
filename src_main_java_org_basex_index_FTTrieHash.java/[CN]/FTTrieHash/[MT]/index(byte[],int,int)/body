{
  int id=add(key);
  if (id < 0) {
    id=-id;
    pre[id]=Num.add(pre[id],pr);
    pos[id]=Num.add(pos[id],po);
    ++sizes[id];
  }
 else {
    pre[id]=Num.newNum(pr);
    pos[id]=Num.newNum(po);
    sizes[id]=1;
  }
}
