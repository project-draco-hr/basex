{
  if (root == null) {
    root=new PathNode(n,k,null);
    stack.size(0);
    stack.add(root);
  }
 else   if (l == 0) {
    if (v != null)     root.stats.add(v,md);
    root.stats.count++;
  }
 else {
    stack.set(l,stack.get(l - 1).index(n,k,v,md));
  }
}
