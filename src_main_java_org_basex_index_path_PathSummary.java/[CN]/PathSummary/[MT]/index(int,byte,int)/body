{
  if (root == null) {
    root=new PathNode(n,k,null);
    stack.add(root);
  }
 else {
    final PathNode pn=stack.get(l - 1).index(n,k);
    if (l < stack.size()) {
      stack.set(l,pn);
    }
 else {
      stack.add(pn);
    }
  }
}
