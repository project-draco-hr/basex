{
  final XQuery query=new XQuery("for sliding window $w in (1, 2, 3, 4) \n" + "      start at $s when fn:true()\n" + "      only end at $e when $e - $s eq 1\n"+ "      return $w",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("(1, 2, 2, 3, 3, 4)"));
}
