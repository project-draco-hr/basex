{
  if (!(node instanceof DBNode))   return;
  final DBNode n=(DBNode)node;
  final Data d=n.data;
  final MemData m=buildDB();
  n.data.insertSeq(n.pre + d.attSize(n.pre,Nod.kind(node.type)),n.pre,m);
}
