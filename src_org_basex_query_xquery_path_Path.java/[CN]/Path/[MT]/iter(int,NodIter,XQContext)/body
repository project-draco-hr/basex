{
  final NodeIter ir=((Step)expr[l]).iter(ctx);
  final boolean more=l + 1 != expr.length;
  Node it;
  while ((it=ir.next()) != null) {
    if (more) {
      ctx.item=it;
      iter(l + 1,ni,ctx);
    }
 else {
      ctx.checkStop();
      ni.add(it);
    }
  }
}
