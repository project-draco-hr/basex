{
  if (expr.length == 1 && expr[0] instanceof Step && root instanceof SimpleIterStep) {
    final SimpleIterStep sis=(SimpleIterStep)root;
    return sis.sumUp() && sis.test.kind == Test.Kind.NAME && d.skel.desc(sis.test.name.str(),false,false).size == 0;
  }
  return false;
}
