{
  final XQuery query=new XQuery("<r> { let $i := <e> <a/> <b/> <c/> </e>/a , $t := $i/following-sibling::b return (($i intersect ($i, $i)), (($t, $t) intersect $t)) } </r>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<r><a/><b/></r>",false));
}
