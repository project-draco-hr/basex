{
  final XQuery query=new XQuery("/processing-instruction() intersect /bib/book[2]/title",ctx);
  query.context(node(file("op/union/bib2.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEmpty());
}
