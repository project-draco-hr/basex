{
  final XQuery query=new XQuery("fn:tokenize(\"1, 15, 24, 50\", \",\\s*\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1 15 24 50"));
}
