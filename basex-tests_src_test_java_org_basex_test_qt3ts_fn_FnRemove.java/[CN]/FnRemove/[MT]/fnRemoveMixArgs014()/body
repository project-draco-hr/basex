{
  final XQuery query=new XQuery("fn:remove( (xs:double(\"INF\"), 2, 3), 2)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("xs:double(\"INF\"), 3"));
}
