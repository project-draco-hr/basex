{
  if (!external || compiled)   return false;
  if (e instanceof Value) {
    Value v=(Value)e;
    if (ext && declType != null && !declType.instance(v))     v=declType.cast(v,ctx,sc,ii,e);
    bind(v);
  }
 else {
    expr=checkType(e,ii);
    value=null;
  }
  return true;
}
