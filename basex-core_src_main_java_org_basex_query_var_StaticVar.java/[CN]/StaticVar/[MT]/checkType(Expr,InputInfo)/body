{
  if (declType != null) {
    if (e instanceof Value)     declType.treat((Value)e,ii);
 else     if (e.seqType().intersect(declType) == null)     throw INVTREAT.get(ii,e.seqType(),declType);
  }
  return e;
}
