{
switch (func) {
case FUNCTION_ARITY:
    return Int.get(checkFunc(exprs[0],qc).arity());
case FUNCTION_NAME:
  return checkFunc(exprs[0],qc).funcName();
case FUNCTION_LOOKUP:
return lookup(qc,ii);
default :
return super.item(qc,ii);
}
}
