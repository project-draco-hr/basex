{
switch (func) {
case FUNCTION_ARITY:
    return Int.get(checkFunc(exprs[0],ctx).arity());
case FUNCTION_NAME:
  return checkFunc(exprs[0],ctx).funcName();
case FUNCTION_LOOKUP:
return lookup(ctx,ii);
default :
return super.item(ctx,ii);
}
}
