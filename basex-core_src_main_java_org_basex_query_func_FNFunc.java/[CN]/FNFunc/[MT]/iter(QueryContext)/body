{
switch (func) {
case FOR_EACH:
    return forEach(qc);
case FILTER:
  return filter(qc);
case FOR_EACH_PAIR:
return forEachPair(qc);
case FOLD_LEFT:
return foldLeft(qc);
case FOLD_RIGHT:
return foldRight(qc);
default :
return super.iter(qc);
}
}
