{
  final XQuery query=new XQuery("fn:remove( (xs:boolean(\"1\"), xs:double(\"-INF\"), \"s\"), 3)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("true(), xs:double(\"-INF\")"));
}
