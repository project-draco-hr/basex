{
  super();
  setFloatable(false);
  toolbar=tb;
  for (  final GUICommand cmd : toolbar) {
    if (cmd == null) {
      addSeparator();
    }
 else {
      final BaseXButton button=newButton(cmd);
      button.setFocusable(false);
      add(button);
    }
  }
  refresh();
}
