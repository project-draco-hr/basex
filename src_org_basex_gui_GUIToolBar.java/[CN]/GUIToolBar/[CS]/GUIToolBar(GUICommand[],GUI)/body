{
  setFloatable(false);
  cmd=tb;
  gui=main;
  for (  final GUICommand c : cmd) {
    if (c == null) {
      addSeparator();
    }
 else {
      final BaseXButton button=newButton(c,gui);
      button.setFocusable(false);
      add(button);
    }
  }
}
