{
  if (next && item == null || !next && !hasNext())   throw new NoSuchElementException();
  next=false;
  if (item.type == Type.ATT && read == null)   throw new XMLStreamException();
  return kind;
}
