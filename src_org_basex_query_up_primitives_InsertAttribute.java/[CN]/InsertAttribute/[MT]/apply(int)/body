{
  if (!(node instanceof DBNode))   return;
  if (m == null)   return;
  final DBNode n=(DBNode)node;
  final Data d=n.data;
  insertAttributes(n.pre + d.attSize(n.pre,Nod.kind(node.type)),n.pre,d,m);
}
