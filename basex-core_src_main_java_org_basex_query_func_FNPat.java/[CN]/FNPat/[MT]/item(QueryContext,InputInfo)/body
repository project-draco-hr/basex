{
switch (func) {
case MATCHES:
    return matches(toToken(exprs[0],qc,true),qc);
case REPLACE:
  return replace(toToken(exprs[0],qc,true),qc);
case ANALYZE_STRING:
return analyzeString(toToken(exprs[0],qc,true),qc);
default :
return super.item(qc,ii);
}
}
