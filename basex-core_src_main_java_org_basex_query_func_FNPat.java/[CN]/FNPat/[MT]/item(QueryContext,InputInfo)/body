{
switch (func) {
case MATCHES:
    return matches(checkEStr(exprs[0],ctx),ctx);
case REPLACE:
  return replace(checkEStr(exprs[0],ctx),ctx);
case ANALYZE_STRING:
return analyzeString(checkEStr(exprs[0],ctx),ctx);
default :
return super.item(ctx,ii);
}
}
