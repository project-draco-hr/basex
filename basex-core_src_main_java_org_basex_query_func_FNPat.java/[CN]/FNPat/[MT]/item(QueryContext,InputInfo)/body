{
switch (func) {
case MATCHES:
    return matches(checkEStr(exprs[0],qc),qc);
case REPLACE:
  return replace(checkEStr(exprs[0],qc),qc);
case ANALYZE_STRING:
return analyzeString(checkEStr(exprs[0],qc),qc);
default :
return super.item(qc,ii);
}
}
