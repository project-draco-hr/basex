{
  final byte[] bin=((B64)checkEmptyType(expr[0].item(ctx,input),Type.B6B)).toJava();
  return new Iter(){
    /** 
 * Position. 
 */
    int pos;
    @Override public Item next(){
      return pos < bin.length ? new Itr(bin[pos++],Type.BYT) : null;
    }
  }
;
}
