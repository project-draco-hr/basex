{
  final byte[] str=checkStr(expr[0],ctx);
  final CRC32 crc=new CRC32();
  crc.update(str);
  final byte[] res=new byte[4];
  for (int i=res.length, c=(int)crc.getValue(); i-- > 0; c>>>=8)   res[i]=(byte)(c & 0xFFL);
  return new Hex(res);
}
