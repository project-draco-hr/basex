{
  return new StreamingOutput(){
    @Override public void write(    final OutputStream output){
      final Context ctx=new Context();
      if (new Open(resource).exec(ctx)) {
        query(params.get(EURLParameter.QUERY),output,ctx,params.get(EURLParameter.WRAP),params.get(EURLParameter.START),params.get(EURLParameter.COUNT));
      }
      ctx.close();
    }
  }
;
}
