def iter_receive(self):
    self.init()
    typecode = self.read()
    while (typecode > 0):
        string = self.readString()
        yield string
        typecode = self.read()
    if (not self.ok()):
        raise IOError(self.readString())
