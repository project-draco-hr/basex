{
  for (  final Entry<String,String> op : map.entrySet()) {
    final IOFile src=data.meta.binary(op.getKey());
    if (src.exists() && !src.rename(data.meta.binary(op.getValue())))     UPDBRENAMEERR.thrw(input,src);
  }
}
