{
  final XQuery query=new XQuery("<pi>{string-to-codepoints(<?pi x ?>)}</pi>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<pi>120 32</pi>",false));
}
