{
  final Data d=nodes.data;
  if (!ctx.context.perm(User.READ,d.meta))   Err.PERMNO.thrw(null,CmdPerm.READ);
  ctx.value=DBDocSeq.get(d.empty() ? new int[0] : nodes.list,d);
  addCollection(nodes.root ? ctx.value : DBDocSeq.get(d.doc(),d),token(d.meta.name));
  globalData=true;
  addData(d);
}
