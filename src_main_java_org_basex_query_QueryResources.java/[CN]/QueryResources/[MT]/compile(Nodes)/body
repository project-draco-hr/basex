{
  final Data d=nodes.data;
  if (!ctx.context.perm(User.READ,d.meta))   PERMNO.thrw(null,CmdPerm.READ);
  ctx.value=d.empty() ? Empty.SEQ : DBNodeSeq.get(nodes.list,d,nodes.root);
  addCollection(nodes.root ? ctx.value : DBNodeSeq.get(d.doc(),d,true),token(d.meta.name));
  addData(d);
}
