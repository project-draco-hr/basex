{
  final int[] nodes=checkPre(path,FSUtils.getChildren(context.data(),curPre,path));
  if (nodes.length == 1 && FSUtils.isDir(context.data(),nodes[0])) {
    error(path,21);
  }
 else {
    cat(nodes,out);
  }
}
