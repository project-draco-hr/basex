{
  int alpha;
  int rgb;
switch (t) {
case CONNECTION:
    alpha=0x20000000;
  rgb=COLORS[4].getRGB();
return new Color(rgb + alpha,true);
default :
alpha=0x60000000;
rgb=COLORS[8].getRGB();
return new Color(rgb + alpha,true);
}
}
