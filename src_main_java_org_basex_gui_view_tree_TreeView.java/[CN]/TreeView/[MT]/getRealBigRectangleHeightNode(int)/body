{
  final int[] r=getRealBigRectangleNodeRange(px);
  final int pp=r[0];
  final int ppp=r[1];
  int maxHeight=0;
  int n=-1;
  for (int i=pp; i <= ppp; ++i) {
    final int h=sub.getSubtreeHeight(i);
    if (h >= maxHeight) {
      maxHeight=h;
      n=i;
    }
  }
  return n;
}
