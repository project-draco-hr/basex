{
  if (where != null)   return -1;
  long size=ret.size(ctx);
  if (size == -1)   return -1;
  for (  final ForLet e : fl) {
    final long s=e.size(ctx);
    if (s == -1)     return -1;
    size*=s;
  }
  return size;
}
