{
  final TokenBuilder tb=new TokenBuilder();
  tb.add("XSeq[");
  for (int i=0; i < size; i++) {
    if (i > 0)     tb.add(',');
    tb.add(val[i].toString());
  }
  tb.add(']');
  return tb.toString();
}
