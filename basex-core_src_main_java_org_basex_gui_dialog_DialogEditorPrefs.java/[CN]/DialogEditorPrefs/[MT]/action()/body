{
  margin.setEnabled(showmargin.isSelected());
  indent.setEnabled(spaces.isSelected());
  showmargin.assign();
  invisible.assign();
  shownl.assign();
  numbers.assign();
  markline.assign();
  files.assign();
  margin.assign();
  spaces.assign();
  indent.assign();
  auto.assign();
  saverun.assign();
  parseproj.assign();
  showHidden.assign();
}
