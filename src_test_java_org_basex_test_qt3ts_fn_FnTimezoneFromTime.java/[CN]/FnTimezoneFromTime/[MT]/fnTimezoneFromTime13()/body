{
  final XQuery query=new XQuery("fn:boolean(fn:string(fn:timezone-from-time(xs:time(\"10:10:20Z\"))))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
