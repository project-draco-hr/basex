{
  final XQuery query=new XQuery("matches('qwerty','[^0-9]+(?>[0-9]+)3')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORX0002"));
}
