{
  return new StreamingOutput(){
    @Override public void write(    final OutputStream out){
      final Map<String,String> p=getParams(doc);
      query(resource,out,p.get("query"),p.get("wrap"),p.get("output"),p.get("start"),p.get("count"));
    }
  }
;
}
