{
  int l=first, h=last;
synchronized (monitor) {
    while (l <= h) {
      final int m=l + h >>> 1;
      final byte[] txt=readKeyAt(m).key;
      final int d=diff(txt,key);
      if (d == 0)       return m;
      if (d < 0)       l=m + 1;
 else       h=m - 1;
    }
  }
  return -(l + 1);
}
