{
  final IntList pres=new IntList(s);
  long p=ps;
synchronized (monitor) {
    for (int i=0, id=0; i < s; i++) {
      id+=idxl.readNum(p);
      p=idxl.cursor();
      pres.add(pre(id));
    }
  }
  return iter(pres.sort());
}
