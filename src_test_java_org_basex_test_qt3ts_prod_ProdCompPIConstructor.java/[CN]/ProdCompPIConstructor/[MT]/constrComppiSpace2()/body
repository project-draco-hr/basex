{
  final XQuery query=new XQuery("processing-instruction pi {'&#x20;&#x0A;&#x0D;&#x09;text'}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<?pi text?>",false));
}
