{
  final XQuery query=new XQuery("processing-instruction {xs:untypedAtomic('one'), xs:untypedAtomic('two')} {'text'}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
