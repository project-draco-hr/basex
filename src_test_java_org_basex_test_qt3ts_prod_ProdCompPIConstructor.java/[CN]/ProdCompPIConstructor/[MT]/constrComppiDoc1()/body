{
  final XQuery query=new XQuery("processing-instruction pi {., .}",ctx);
  query.context(node(file("prod/CompAttrConstructor/DupNode.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<?pi texttext texttext?>",false));
}
