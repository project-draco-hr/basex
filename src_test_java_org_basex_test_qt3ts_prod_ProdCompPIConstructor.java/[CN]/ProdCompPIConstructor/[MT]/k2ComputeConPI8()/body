{
  final XQuery query=new XQuery("string(processing-instruction {\"thename\"} {\"asdas? >\"})",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"asdas? >"));
}
