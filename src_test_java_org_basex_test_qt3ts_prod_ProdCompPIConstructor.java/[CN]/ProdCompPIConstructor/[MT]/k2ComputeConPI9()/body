{
  final XQuery query=new XQuery("string(processing-instruction {\"thename\"} {\"content {1+ } {\"})",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"content {1+ } {"));
}
