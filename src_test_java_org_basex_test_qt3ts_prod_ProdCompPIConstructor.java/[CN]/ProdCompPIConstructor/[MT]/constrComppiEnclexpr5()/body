{
  final XQuery query=new XQuery("processing-instruction pi {/root}",ctx);
  query.context(node(file("prod/CompAttrConstructor/DupNode.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<?pi texttext?>",false));
}
