{
  final XQuery query=new XQuery("<elem xmlns:foo=\"http://www.example.com/foo\">{processing-instruction {xs:untypedAtomic('foo:pi')} {'text'}}</elem>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQDY0041"));
}
