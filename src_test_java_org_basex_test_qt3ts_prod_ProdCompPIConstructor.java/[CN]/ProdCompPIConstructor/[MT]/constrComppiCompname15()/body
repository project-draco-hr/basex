{
  final XQuery query=new XQuery("processing-instruction {xs:untypedAtomic('pi')} {'text'}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<?pi text?>",false));
}
