{
  final String user=args[0];
  final String pass=args[1];
  if (!validDB(user,false))   return error(NAMEINVALID,user);
  return !isMD5(pass) ? error(USERMD5) : context.users.alter(user,pass) ? info(USERALTER,user) : error(USERNO,user);
}
