{
  return u == Use.X30 && (sig == Function.GENERATE_ID || sig == Function.PATH || sig == Function.HAS_CHILDREN || expr.length == 0 && (sig == Function.DOCUMENT_URI || sig == Function.NODE_NAME)) || u == Use.CTX && expr.length == 0 || super.uses(u);
}
