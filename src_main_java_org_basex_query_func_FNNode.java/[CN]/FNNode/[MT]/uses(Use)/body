{
  return u == Use.X30 && (def == Function.GENERATE_ID || def == Function.PATH || def == Function.HAS_CHILDREN || expr.length == 0 && (def == Function.DOCUMENT_URI || def == Function.NODE_NAME)) || u == Use.CTX && expr.length == 0 || super.uses(u);
}
