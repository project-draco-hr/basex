{
  final int x=e.getXOnScreen();
  final int y=e.getYOnScreen();
  final Point p=getLocationOnScreen();
  if (x < p.x || y < p.y || x >= p.x + getWidth() || y >= p.y + getHeight())   close.setVisible(false);
}
