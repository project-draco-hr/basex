{
  final Query query=session.query(WRAPPER + "1 to 2");
  assertTrue("Result expected.",query.more());
  check("<db:results xmlns:db=\"ns\">  <db:result>1</db:result>" + "  <db:result>2</db:result></db:results>",query.next());
}
