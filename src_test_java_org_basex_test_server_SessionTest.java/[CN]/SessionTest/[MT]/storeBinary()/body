{
  session.execute("create db " + DB);
  session.store("X",new ArrayInput(new byte[]{-128,-2,-1,0,1,127}));
  check("-128 -2 -1 0 1 127",session.query(TO_BYTES.args(DBRETRIEVE.args(DB,"X"))).execute());
}
