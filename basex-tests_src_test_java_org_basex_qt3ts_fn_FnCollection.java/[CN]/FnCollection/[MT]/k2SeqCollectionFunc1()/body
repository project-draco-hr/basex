{
  final XQuery query=new XQuery("collection(\"http:\\\\invalidURI\\someURI%gg\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FODC0004"));
}
