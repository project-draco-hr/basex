{
  final int ch=next();
  if (ch == '[') {
    if (!more())     PICDATE.thrw(info,token);
    if (!consume('['))     return -1;
  }
 else   if (ch == ']') {
    if (!consume(']'))     PICDATE.thrw(info,token);
  }
  return ch;
}
