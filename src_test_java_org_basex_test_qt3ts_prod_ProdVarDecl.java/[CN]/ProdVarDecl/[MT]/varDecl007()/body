{
  final XQuery query=new XQuery("declare variable $x := \"a \"\" or a ' delimits a string literal\" ; $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"a \" or a ' delimits a string literal"));
}
