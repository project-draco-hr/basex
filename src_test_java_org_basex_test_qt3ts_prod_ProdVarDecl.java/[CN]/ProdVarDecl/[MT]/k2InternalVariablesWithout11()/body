{
  final XQuery query=new XQuery("declare variable $local:myVar := local:thisFunctionDoesNotExist(); 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("1") || error("XPST0017")));
}
