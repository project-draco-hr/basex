{
  final XQuery query=new XQuery("declare variable $x := '&lt;bold&gt;A sample element.&lt;/bold&gt;' ; $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"<bold>A sample element.</bold>"));
}
