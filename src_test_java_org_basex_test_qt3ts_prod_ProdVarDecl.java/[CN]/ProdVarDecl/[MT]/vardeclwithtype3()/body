{
  final XQuery query=new XQuery("declare variable $var as xs:decimal := 100; declare variable $input-context1 external; $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("100"));
}
