{
  final XQuery query=new XQuery("declare variable $local:myVar := local:thisFunctionDoesNotExist(); $local:myVar",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0017"));
}
