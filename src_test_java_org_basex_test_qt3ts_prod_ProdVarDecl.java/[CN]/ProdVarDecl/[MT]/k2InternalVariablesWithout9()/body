{
  final XQuery query=new XQuery("declare variable $local:myVar := local:myFunc(3); declare function local:myFunc($arg) { local:myFunc($local:myVar) }; $local:myVar",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQST0054"));
}
