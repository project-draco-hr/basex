{
  final XQuery query=new XQuery("declare variable $var as xs:integer := fn:count((10,2)); declare variable $input-context1 external; $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("2"));
}
