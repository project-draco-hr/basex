{
  final XQuery query=new XQuery("declare variable $var as xs:time := xs:time(\"11:12:00Z\"); declare variable $input-context1 external; $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"11:12:00Z"));
}
