{
  final XQuery query=new XQuery("declare variable $myVar as xs:integer := subsequence((1, 2, \"a string\"), 3, 1); $myVar eq 3",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
