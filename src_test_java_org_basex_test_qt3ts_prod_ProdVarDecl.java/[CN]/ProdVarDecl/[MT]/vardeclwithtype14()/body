{
  final XQuery query=new XQuery("declare variable $x as xs:integer := 10; declare variable $var as xs:integer := $x +1; declare variable $input-context1 external; $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("11"));
}
