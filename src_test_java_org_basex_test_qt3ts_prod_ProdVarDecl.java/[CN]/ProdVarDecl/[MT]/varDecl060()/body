{
  final XQuery query=new XQuery("declare variable $x := //Price/text() ; $x",ctx);
  query.context(node(file("docs/bib.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,""));
}
