{
  final XQuery query=new XQuery("declare variable $var as xs:integer := $e +1; declare variable $e as xs:integer := 10; declare variable $input-context1 external; $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
