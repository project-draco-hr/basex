{
  final XQuery query=new XQuery("declare variable $x := (xs:float(\"INF\") , xs:double(\"NaN\")) ; $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"INF NaN"));
}
