{
  final XQuery query=new XQuery("declare variable $x := \"This is a string, isn't it?\" ; $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"This is a string, isn't it?"));
}
