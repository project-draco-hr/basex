{
  final XQuery query=new XQuery("declare variable $var as xs:boolean := fn:empty((1,2,3)); declare variable $input-context1 external; $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
