{
  ns=n;
  if (!visible())   return;
  try {
    final CachedOutput co=new CachedOutput(gui.context.prop.num(Prop.MAXTEXT));
    if (n != null)     n.serialize(new XMLSerializer(co));
    setText(co,null);
    refresh=false;
  }
 catch (  final IOException ex) {
    Main.debug(ex);
  }
}
