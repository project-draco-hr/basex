{
  BufferInput bi=null;
  try {
    bi=new BufferInput(file);
    cont=bi.content().toArray();
  }
  finally {
    if (bi != null)     try {
      bi.close();
    }
 catch (    final IOException ex) {
    }
  }
}
