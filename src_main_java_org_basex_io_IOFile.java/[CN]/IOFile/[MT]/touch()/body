{
  for (int i=0; i < 10; i++) {
    try {
      if (file.createNewFile())       return true;
    }
 catch (    final IOException ex) {
      Performance.sleep(50);
      Util.debug(ex);
    }
  }
  return false;
}
