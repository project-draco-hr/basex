{
  for (int i=0; i < 3; i++) {
    try {
      if (file.createNewFile())       return true;
    }
 catch (    final IOException ex) {
      Performance.sleep(i * 10);
      Util.debug(ex);
    }
  }
  return false;
}
