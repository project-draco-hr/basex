{
  String pre=PREFILE;
  if (!path.startsWith("/")) {
    pre+="/";
    if (path.length() < 2 || path.charAt(1) != ':') {
      pre+="/" + Prop.WORK.replace('\\','/');
      if (!pre.endsWith("/"))       pre+="/";
    }
  }
  return pre + path.replace('\\','/');
}
