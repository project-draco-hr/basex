{
  while (true) {
    zip=((ZipInputStream)is).getNextEntry();
    if (zip == null)     break;
    len=zip.getSize();
    init(zip.getName());
    if (!zip.isDirectory())     return true;
  }
  return false;
}
