{
  final String s=sb.toString();
  if (s.equals("..") && size > 0) {
    if (!list[0].contains(":"))     delete(size - 1);
  }
 else   if (!s.equals(".") && !s.isEmpty()) {
    add(s.toString());
  }
  sb.reset();
}
