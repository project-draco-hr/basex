{
  final String s=tb.toString();
  if (s.equals("..") && size > 0) {
    if (!list[size - 1].contains(":"))     delete(size - 1);
  }
 else   if (!s.equals(".") && !s.isEmpty()) {
    add(s.toString());
  }
  tb.reset();
}
