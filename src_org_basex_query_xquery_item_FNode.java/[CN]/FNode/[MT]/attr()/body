{
  return new NodeIter(){
    /** 
 * Child counter. 
 */
    int c;
    @Override public Node next(){
      return FNode.this.atts != null && c != FNode.this.atts.size ? FNode.this.atts.list[c++] : null;
    }
  }
;
}
