{
  final XQuery query=new XQuery("fn:hours-from-duration(xs:dayTimeDuration(\"P3DT12H32M12S\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("12"));
}
