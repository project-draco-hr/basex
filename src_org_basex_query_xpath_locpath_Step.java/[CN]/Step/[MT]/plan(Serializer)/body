{
  ser.startElement(this);
  ser.attribute(Token.token("test"),Token.token(test.toString()));
  if (preds.size() != 0) {
    ser.finishElement();
    preds.plan(ser);
    ser.closeElement(this);
  }
 else {
    ser.emptyElement();
  }
}
