{
  query=new FSQuery(context);
  comm=cmd.arg(0).toLowerCase();
  return comm.equals(CD) || comm.equals(DU) || comm.equals(GREP)|| comm.equals(LOCATE)|| comm.equals(LS)|| comm.equals(MKDIR)|| comm.equals(PWD)|| comm.equals(RM)|| comm.equals(TOUCH);
}
