{
  query=new FSQuery(context);
  comm=cmd.arg(0).toLowerCase();
  return comm.equals(DU) || comm.equals(LS) || comm.equals(PWD)|| comm.equals(CD)|| comm.equals(LOCATE)|| comm.equals(GREP)|| comm.equals(RM);
}
