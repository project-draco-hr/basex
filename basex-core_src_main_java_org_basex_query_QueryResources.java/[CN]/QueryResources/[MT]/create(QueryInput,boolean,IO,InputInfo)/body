{
  final boolean createDB=qc.context.options.get(MainOptions.FORCECREATE);
  if (!qc.context.user.has(Perm.READ))   throw BXXQ_PERM.get(info,Util.info(Text.PERM_REQUIRED_X,Perm.READ));
  final IO source=checkPath(input,baseIO,info);
  if (single && source.isDir())   WHICHRES.get(info,baseIO);
  try {
    final Data dt=createDB ? CreateDB.create(source,qc.context) : CreateDB.mainMem(source,qc.context);
    input.path="";
    addData(dt);
    return dt;
  }
 catch (  final IOException ex) {
    throw IOERR.get(info,ex);
  }
}
