{
  final Context context=qc.context;
  if (!context.user().has(Perm.READ))   throw BXXQ_PERM_X.get(info,Util.info(Text.PERM_REQUIRED_X,Perm.READ));
  final IO source=checkPath(input,baseIO,info);
  if (single && source.isDir())   throw WHICHRES_X.get(info,baseIO);
  try {
    final boolean mem=!context.options.get(MainOptions.FORCECREATE);
    final MainOptions opts=new MainOptions(context.options,true);
    return addData(CreateDB.create(source.dbname(),new DirParser(source,opts),context,opts,mem));
  }
 catch (  final IOException ex) {
    throw IOERR_X.get(info,ex);
  }
 finally {
    input.path="";
  }
}
