{
  final ObjList<HashMap<String,String>> list=new ObjList<HashMap<String,String>>();
  final XQuery query=new XQuery("*:" + elem,ctx).context(env);
  for (  final XQItem it : query)   list.add(map(ctx,it));
  query.close();
  return list;
}
