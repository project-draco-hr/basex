{
  for (  final ForLet f : fl)   if (!f.databases(db))   return false;
  return (where == null || where.databases(db)) && (order == null || order.databases(db)) && (group == null || group.databases(db))&& ret.databases(db);
}
