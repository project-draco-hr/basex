{
  initContext(CONTEXT);
  assertTrue(new IOFile(CONTEXT.mprop.get(MainProp.WEBPATH)).md());
  root=rt;
  final StringList sl=new StringList();
  if (local)   sl.add("-l");
  sl.add("-p9996","-e9997","-h9998","-s9999","-z","-U" + Text.ADMIN,"-P" + Text.ADMIN);
  System.setOut(NULL);
  try {
    http=new BaseXHTTP(sl.toArray());
  }
  finally {
    System.setOut(OUT);
  }
}
