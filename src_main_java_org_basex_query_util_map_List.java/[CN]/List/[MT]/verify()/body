{
  try {
    for (int i=1; i < size; i++) {
      for (int j=i; j-- > 0; ) {
        if (keys[i].eq(null,keys[j]))         return false;
      }
    }
  }
 catch (  final QueryException e) {
    return false;
  }
  return true;
}
