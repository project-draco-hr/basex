{
  if (h == hash) {
    for (int i=size; i-- > 0; ) {
      if (k.eq(ii,keys[i])) {
        if (size == 2) {
          final int o=i ^ 1;
          return new Leaf(h,keys[o],values[o]);
        }
        return new List(h,Array.delete(keys,i),Array.delete(values,i));
      }
    }
  }
  return this;
}
