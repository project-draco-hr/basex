{
  final XQuery query=new XQuery("fn:string((xs:yearMonthDuration(\"P20Y10M\") div 2.0)) or fn:false()",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
