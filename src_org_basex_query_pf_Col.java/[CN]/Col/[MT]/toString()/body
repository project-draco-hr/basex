{
  final TokenBuilder tb=new TokenBuilder();
  final int m=24;
  final int s=Math.min(m,sz);
  for (int c=0; c < s; c++) {
    if (c != 0)     tb.add(" ");
    tb.add(r(c).toString());
  }
  if (sz > m)   tb.add("... ");
  return tb.toString();
}
