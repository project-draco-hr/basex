{
  if (seq == null) {
    Type t=val[0].type;
    for (int s=1; s != size && t != Type.ITEM; ++s) {
      if (t != val[s].type)       t=Type.ITEM;
    }
    seq=SeqType.get(t,SeqType.Occ.OM);
  }
  return seq;
}
