{
  final String a=p.args[arg];
  if (a != null && !a.isEmpty()) {
    if (key != null) {
      tb.add(' ');
      tb.add(key);
    }
    tb.add(' ');
    final boolean s=a.indexOf(' ') != -1;
    if (s)     tb.add('"');
    tb.add(a);
    if (s)     tb.add('"');
  }
  return this;
}
