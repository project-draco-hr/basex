{
  final String doc="<a><b/></a>";
  final AtomicUpdateList l=atomics(doc);
  l.addInsert(3,2,data("<c/>"),false);
  l.addReplace(2,data("<newb/>"));
  optimize(l,1);
  query(transform(doc,"insert node <c/> into $input/b," + "replace node $input/b with <newb/>"),"<a><newb/></a>");
}
