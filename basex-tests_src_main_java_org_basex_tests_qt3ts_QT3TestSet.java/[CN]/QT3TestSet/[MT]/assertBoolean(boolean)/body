{
  final XdmValue value=result.value;
  if (value == null)   return fail(Util.info("Boolean result: %",exp));
  return result(value.getType().eq(SeqType.BLN) && value.getBoolean() == exp,Util.info(exp));
}
