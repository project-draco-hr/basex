{
  if (gui.updating || gui.context.focused == -1)   return;
  if (e.getWheelRotation() <= 0)   gui.notify.context(new DBNodes(gui.context.data(),gui.context.focused),false,null);
 else   gui.notify.hist(false);
}
