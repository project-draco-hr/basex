{
  printIndent(level);
  final File[] ch=f.getParentFile().listFiles();
  if (ch.length == 1 || ch[ch.length - 1].equals(f)) {
    indentStrings[level]="    ";
    Util.outln("`-- " + f.getName());
  }
 else {
    indentStrings[level]="|   ";
    Util.outln("|-- " + f.getName());
  }
}
