{
  super(NodeType.ELM);
  name=nm;
  children=ch;
  atts=at;
  ns=nsp;
  if (ch != null)   for (  final ANode n : ch)   n.parent(this);
  if (at != null)   for (  final ANode n : at)   n.parent(this);
}
