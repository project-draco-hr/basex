{
  final int len=tvars.length;
  for (int i=0; i < tvars.length; i++)   if (!used.get(tvars[i].id)) {
    texpr=Array.delete(texpr,i);
    tvars=Array.delete(tvars,i--);
  }
  return tvars.length < len;
}
