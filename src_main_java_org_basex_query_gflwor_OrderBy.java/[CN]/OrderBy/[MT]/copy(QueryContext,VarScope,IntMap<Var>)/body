{
  final Var[] tv=tvars.clone();
  for (int i=0; i < tv.length; i++) {
    final Var v=tv[i], cpy=vs.get(v.id);
    if (cpy != null)     tv[i]=cpy;
  }
  return new OrderBy(tv,Arr.copyAll(ctx,scp,vs,texpr),Arr.copyAll(ctx,scp,vs,keys),stable,info);
}
