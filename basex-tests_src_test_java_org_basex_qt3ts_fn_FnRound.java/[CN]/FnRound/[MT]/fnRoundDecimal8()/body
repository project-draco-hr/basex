{
  final XQuery query=new XQuery("fn:round(-12.567, 2)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-12.57") && assertType("xs:decimal")));
}
