def console(self):
    if (self.connect() == True):
        while (self.readCommand() != 'exit'):
            self.sendCommand(self.com)
            self.sendNullbyte()
            print self.receive()
        self.sendCommand('exit')
        print 'See you.'
    else:
        print 'Access denied.'
        self.close()
