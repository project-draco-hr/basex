{
  final ANode node=checkNode(exprs.length == 2 ? exprs[1] : checkCtx(qc),qc);
switch (func) {
case ID:
    return id(qc.iter(exprs[0]),node);
case IDREF:
  return idref(qc.iter(exprs[0]),node);
case ELEMENT_WITH_ID:
return elid(qc.iter(exprs[0]),node);
default :
return super.iter(qc);
}
}
