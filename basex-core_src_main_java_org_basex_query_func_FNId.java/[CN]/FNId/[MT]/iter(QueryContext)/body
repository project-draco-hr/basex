{
switch (func) {
case ID:
    return id(exprs[0].atomIter(qc,info),toNode(arg(1,qc),qc));
case IDREF:
  return idref(exprs[0].atomIter(qc,info),toNode(arg(1,qc),qc));
case ELEMENT_WITH_ID:
return elid(exprs[0].atomIter(qc,info),toNode(arg(1,qc),qc));
default :
return super.iter(qc);
}
}
