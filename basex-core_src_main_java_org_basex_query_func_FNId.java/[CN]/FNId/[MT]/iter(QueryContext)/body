{
  final ANode node=checkNode(exprs.length == 2 ? exprs[1] : checkCtx(ctx),ctx);
switch (func) {
case ID:
    return id(ctx.iter(exprs[0]),node);
case IDREF:
  return idref(ctx.iter(exprs[0]),node);
case ELEMENT_WITH_ID:
return elid(ctx.iter(exprs[0]),node);
default :
return super.iter(ctx);
}
}
