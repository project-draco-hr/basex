{
  final ANode node=checkNode(exprs.length == 2 ? exprs[1] : checkCtx(ctx),ctx);
switch (func) {
case LANG:
    return lang(lc(checkEStr(exprs[0],ctx)),node);
default :
  return super.item(ctx,ii);
}
}
