def console(self):
    out = sys.stdout
    session.execute('SET INFO ON', out)
    while True:
        com = self.readCommand()
        if (com == 'exit'):
            break
        session.execute(com, out)
    try:
        session.close()
    except:
        print 'See you.'
        sys.exit()
    print 'See you.'
