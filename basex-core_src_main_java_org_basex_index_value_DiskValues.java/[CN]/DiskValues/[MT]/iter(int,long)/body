{
  final IntList pres=new IntList(sz);
  long p=offset;
synchronized (monitor) {
    for (int i=0, id=0; i < sz; i++) {
      id+=idxl.readNum(p);
      p=idxl.cursor();
      pres.add(pre(id));
    }
  }
  return iter(pres.sort());
}
