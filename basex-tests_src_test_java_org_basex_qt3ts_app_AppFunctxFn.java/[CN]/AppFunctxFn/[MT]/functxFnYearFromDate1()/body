{
  final XQuery query=new XQuery("(year-from-date(xs:date('2006-08-15')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("2006"));
}
