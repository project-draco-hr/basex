{
  final XQuery query=new XQuery("(translate('xml query', 'abcd', 'ABCD'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"xml query"));
}
