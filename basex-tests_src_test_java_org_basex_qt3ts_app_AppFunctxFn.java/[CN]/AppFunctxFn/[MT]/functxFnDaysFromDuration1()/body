{
  final XQuery query=new XQuery("(days-from-duration( xs:dayTimeDuration('P5D')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("5"));
}
