{
  final XQuery query=new XQuery("(round-half-to-even(9372.253, -3))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("9000"));
}
