{
  final XQuery query=new XQuery("(hours-from-duration( xs:dayTimeDuration('P1DT5H')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("5"));
}
