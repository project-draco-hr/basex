{
  super.comp(ctx);
  final SeqType[] at=new SeqType[args.length];
  for (int i=0; i < at.length; i++)   at[i]=args[i].type == null ? SeqType.ITEM_ZM : args[i].type;
  return new FunItem(args,expr,FunType.get(at,var.type()));
}
