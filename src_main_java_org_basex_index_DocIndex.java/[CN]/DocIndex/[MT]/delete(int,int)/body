{
  final IntList il=docs();
  int i=il.sortedIndexOf(pre);
  final boolean found=i >= 0;
  if (i < 0)   i=-i - 1;
 else   il.delete(i);
  il.move(-size,i);
  if (!found)   return;
  paths.delete(i);
  ordered=false;
}
