{
  final XQuery query=new XQuery("resolve-uri(\"b.html\", \"http://www.example.com/a.html#fragment\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0002"));
}
