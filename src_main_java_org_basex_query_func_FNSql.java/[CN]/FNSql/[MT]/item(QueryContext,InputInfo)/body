{
  checkAdmin(ctx);
switch (def) {
case INIT:
    return init(ctx);
case CONNECT:
  return connect(ctx);
case PREPARE:
return prepare(ctx);
case CLOSE:
return close(ctx);
case COMMIT:
return commit(ctx);
case ROLLBACK:
return rollback(ctx);
default :
return super.item(ctx,ii);
}
}
