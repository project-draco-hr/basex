{
  final NodeCache nc;
  if (node.type == NodeType.ATT) {
    if (atts == null)     atts=new NodeCache();
    nc=atts;
  }
 else {
    if (children == null)     children=new NodeCache();
    nc=children;
  }
  nc.add(node);
  node.parent(this);
  return this;
}
