{
  final XQuery query=new XQuery("fn:unordered ( (\"a\", xs:boolean(\"true\"), \"b\", \"c\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertPermutation("\"a\", true(), \"b\", \"c\""));
}
