{
  for (  final SkelNode c : ch) {
    if (c.kind == k && c.name == t) {
      c.count++;
      c.tl+=l;
      return c;
    }
  }
  final SkelNode n=new SkelNode(t,k,l,this);
  ch=Array.add(ch,n);
  return n;
}
