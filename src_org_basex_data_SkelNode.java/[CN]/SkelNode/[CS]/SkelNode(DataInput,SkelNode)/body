{
  name=(short)in.readNum();
  kind=in.readByte();
  count=in.readNum();
  ch=new SkelNode[in.readNum()];
  tl=in.readDouble();
  par=p;
  for (int i=0; i < ch.length; i++)   ch[i]=new SkelNode(in,this);
}
