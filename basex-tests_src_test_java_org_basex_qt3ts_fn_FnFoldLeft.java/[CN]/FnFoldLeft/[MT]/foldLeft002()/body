{
  final XQuery query=new XQuery("fold-left(function($a, $b) { $a * $b }, 1, (2,3,5,7))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("210"));
}
