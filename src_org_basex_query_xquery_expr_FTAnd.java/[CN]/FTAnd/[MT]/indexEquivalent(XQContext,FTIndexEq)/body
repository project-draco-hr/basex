{
  if (pex.length == 1 && nex.length == 0)   expr[pex[0]].indexEquivalent(ctx,ieq);
  for (int i=0; i < expr.length; i++) {
    expr[i]=(FTExpr)expr[i].indexEquivalent(ctx,ieq);
  }
  return new FTIntersection(pex,nex,expr);
}
