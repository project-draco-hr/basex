{
  final XQuery query=new XQuery("fn:count(/works/employee[1]/preceding::noSuchNode)",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
