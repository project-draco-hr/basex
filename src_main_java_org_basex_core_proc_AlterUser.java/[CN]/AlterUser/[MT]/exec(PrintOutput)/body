{
  final String user=args[0];
  final String pass=args[1];
  return pass == null || pass.isEmpty() ? error(PASSNO,user) : context.users.alter(user,pass) ? info(USERALTER,user) : error(USERNO,user);
}
