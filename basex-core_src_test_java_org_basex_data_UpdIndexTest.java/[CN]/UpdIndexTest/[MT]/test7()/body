{
  run(new Replace("A","<a/>"));
  run(new Replace("B","<a a='1'/>"));
  run(new Replace("C","<a a='1'/>"));
  run(new Replace("A","<a a='1'/>"));
  run(new Close());
  run(new Open(NAME));
  run(new Delete("A"));
  run(new Close());
}
