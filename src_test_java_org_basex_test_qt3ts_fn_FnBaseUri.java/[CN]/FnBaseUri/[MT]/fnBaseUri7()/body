{
  final XQuery query=new XQuery("fn:count(fn:base-uri(element anElement {\"An Element Node\"}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("1") || assertEq("0")));
}
