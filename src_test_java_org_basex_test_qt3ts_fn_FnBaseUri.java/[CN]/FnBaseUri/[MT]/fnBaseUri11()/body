{
  final XQuery query=new XQuery("fn:count(fn:base-uri(document {<aDocument>some content</aDocument>}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("0") || assertEq("1")));
}
