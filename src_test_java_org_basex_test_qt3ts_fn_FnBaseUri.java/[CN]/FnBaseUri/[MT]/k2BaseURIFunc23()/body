{
  final XQuery query=new XQuery("declare base-uri \"http://example.com/baseURI\"; empty(base-uri(processing-instruction target {\"data\"}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
