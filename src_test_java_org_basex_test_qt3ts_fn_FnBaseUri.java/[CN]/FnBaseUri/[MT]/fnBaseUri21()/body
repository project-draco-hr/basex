{
  final XQuery query=new XQuery("declare base-uri \"http://example.org\"; \n" + "        let $var := <!-- A Comment --> return fn:base-uri($var)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEmpty());
}
