{
  final XQuery query=new XQuery("declare base-uri \"http://www.example.com\"; empty(base-uri(comment {\"content\"}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
