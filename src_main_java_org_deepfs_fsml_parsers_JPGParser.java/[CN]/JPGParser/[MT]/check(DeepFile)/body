{
  final BufferedFileChannel f=df.getBufferedFileChannel();
  try {
    f.buffer(6);
  }
 catch (  final EOFException ex) {
    return false;
  }
  int b;
  return f.getShort() == 0xFFD8 && ((b=f.getShort()) == 0xFFE0 || b == 0xFFE1);
}
