{
  print(",");
  if (indent)   print(' ');
  if (!att) {
    print("{");
    att=true;
  }
  print('"');
  for (  final byte ch : name)   encode(ch);
  print("\":\"");
  for (  final byte ch : value)   encode(ch);
  print("\"");
}
