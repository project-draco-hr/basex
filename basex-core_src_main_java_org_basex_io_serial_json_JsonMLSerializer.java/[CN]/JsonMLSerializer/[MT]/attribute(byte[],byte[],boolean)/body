{
  out.print(",");
  out.print(' ');
  if (!att) {
    out.print("{");
    att=true;
  }
  out.print('"');
  for (  final byte ch : name)   encode(ch);
  out.print("\":\"");
  for (  final byte ch : norm(value))   encode(ch);
  out.print("\"");
}
