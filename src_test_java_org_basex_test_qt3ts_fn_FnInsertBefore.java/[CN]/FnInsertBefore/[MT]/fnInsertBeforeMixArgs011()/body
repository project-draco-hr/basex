{
  final XQuery query=new XQuery("fn:insert-before( (\"a\", \"b\", \"c\"),2, xs:float(\"-INF\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("\"a\", xs:float('-INF'), \"b\", \"c\""));
}
