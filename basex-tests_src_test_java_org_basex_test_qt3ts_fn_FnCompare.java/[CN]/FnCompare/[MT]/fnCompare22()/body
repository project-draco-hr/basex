{
  final XQuery query=new XQuery("fn:compare(\"a\",\"a\",\"CollationA\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOCH0002"));
}
