{
  final XQuery query=new XQuery("fn:count((xs:yearMonthDuration(\"P13Y09M\") *+0))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1"));
}
