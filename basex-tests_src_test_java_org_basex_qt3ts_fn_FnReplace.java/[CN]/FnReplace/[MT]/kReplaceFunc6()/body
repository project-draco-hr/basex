{
  final XQuery query=new XQuery("replace(\"input\", \"in\", \"thisIsInvalid\\\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORX0004"));
}
