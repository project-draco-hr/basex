{
  if (value == null) {
    if (kind == Data.ELEM) {
      if (empty == 0)       empty=1;
 else       if (empty == 1)       empty=2;
    }
  }
 else {
    stats.add(value,meta);
    if (kind == Data.TEXT && parent.empty == 1)     parent.empty=0;
  }
  stats.count++;
}
