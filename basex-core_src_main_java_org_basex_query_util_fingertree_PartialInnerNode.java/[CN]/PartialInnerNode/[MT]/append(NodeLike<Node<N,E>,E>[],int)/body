{
  if (pos == 0) {
    nodes[pos]=this;
    return 1;
  }
  final NodeLike<Node<N,E>,E>[] joined=nodes[pos - 1].concat(this);
  nodes[pos - 1]=joined[0];
  if (joined[1] != null) {
    nodes[pos]=joined[1];
    return pos + 1;
  }
  return pos;
}
