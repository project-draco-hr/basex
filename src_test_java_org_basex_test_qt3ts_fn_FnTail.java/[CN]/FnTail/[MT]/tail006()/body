{
  final XQuery query=new XQuery("declare function local:sum($n) { if (empty($n)) then 0 else head($n) + local:sum(tail($n)) }; \n" + "            local:sum(1 to 5)",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("15") && assertType("xs:integer")));
}
