{
  if (prefix.isEmpty()) {
    ctx.nsElem=token(uri);
  }
 else {
    final QNm name=new QNm(token(prefix),token(uri));
    if (!uri.isEmpty()) {
      ctx.ns.add(name,null);
    }
 else {
      ctx.ns.delete(name);
    }
  }
  return this;
}
