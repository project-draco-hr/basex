{
  final XQuery query=new XQuery("<elem> <!-- comment --> { \"a string\", 999, attribute name {\"content\"}, xs:hexBinary(\"FF\") } </elem>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQTY0024"));
}
