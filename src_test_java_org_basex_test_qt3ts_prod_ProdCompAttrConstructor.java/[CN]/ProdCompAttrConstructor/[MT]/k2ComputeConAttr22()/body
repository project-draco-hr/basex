{
  final XQuery query=new XQuery("declare variable $myVar := (<elem/>, attribute name {\"content\"}); <b> {$myVar} </b>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQTY0024"));
}
