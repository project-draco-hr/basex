{
  final XQuery query=new XQuery("element elem {attribute attr {., .}}",ctx);
  query.context(node(file("prod/CompAttrConstructor/DupNode.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem attr=\"texttext texttext\"/>",false));
}
