{
  final XQuery query=new XQuery("string-length(xs:NCName(prefix-from-QName(node-name(attribute {QName(\"http://example.com/\", \"attr\")} {()})))) > 0",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
