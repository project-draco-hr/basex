{
  final XQuery query=new XQuery("element elem {attribute {xs:untypedAtomic('attr')} {'text'}}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem attr=\"text\"/>",false));
}
