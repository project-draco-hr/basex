{
  final XQuery query=new XQuery("declare function local:myFunc() as item() { <a/> }; <b> {local:myFunc()} {attribute name {\"content\"}} </b>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQTY0024"));
}
