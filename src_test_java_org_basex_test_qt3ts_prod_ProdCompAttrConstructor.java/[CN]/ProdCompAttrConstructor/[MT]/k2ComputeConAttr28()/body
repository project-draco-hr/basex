{
  final XQuery query=new XQuery("declare variable $local:myVar := <a/>; <b> {$local:myVar} {attribute name {\"content\"}} </b>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XQTY0024"));
}
