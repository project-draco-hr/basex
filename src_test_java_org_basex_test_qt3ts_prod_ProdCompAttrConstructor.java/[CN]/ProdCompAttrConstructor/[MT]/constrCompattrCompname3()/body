{
  final XQuery query=new XQuery("element elem {attribute {xs:untypedAtomic('one'), xs:untypedAtomic('two')} {'text'}}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
