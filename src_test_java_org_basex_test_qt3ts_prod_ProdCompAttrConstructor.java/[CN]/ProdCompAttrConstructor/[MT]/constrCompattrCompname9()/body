{
  final XQuery query=new XQuery("element elem {attribute {xs:QName('aQname')} {'text'}}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem aQname=\"text\"/>",false));
}
