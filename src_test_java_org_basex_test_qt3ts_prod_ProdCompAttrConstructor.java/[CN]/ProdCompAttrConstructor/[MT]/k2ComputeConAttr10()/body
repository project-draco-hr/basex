{
  final XQuery query=new XQuery("<a> <?target content?> {<b>{attribute name{\"content\"}}</b>} </a>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a><?target content?><b name=\"content\"/></a>",false));
}
