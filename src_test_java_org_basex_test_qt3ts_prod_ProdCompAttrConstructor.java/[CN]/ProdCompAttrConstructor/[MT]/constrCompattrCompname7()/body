{
  final XQuery query=new XQuery("element elem {attribute {xs:dateTime(\"1999-05-31T13:20:00\")} {'text'}}",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
