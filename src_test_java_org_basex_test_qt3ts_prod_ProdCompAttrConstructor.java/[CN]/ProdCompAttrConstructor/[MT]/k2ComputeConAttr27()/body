{
  final XQuery query=new XQuery("declare function local:myFunc() { <elem/>, attribute name {\"content\"} }; <b> {local:myFunc()[2]} </b>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<b name=\"content\"/>",false));
}
