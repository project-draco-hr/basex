{
  final ClientSession cs=session();
  run(cs,new Code(){
    @Override public void run() throws IOException {
      if (!cs.execute(new DropDB(resource)))       throw new JAXRXException(404,cs.info());
    }
  }
);
}
