{
  final ClientSession cs=session();
  run(cs,new Code(){
    @Override public void run() throws IOException {
      if (!cs.execute(new DropDB(resource)))       throw JAXRXException.notFound(cs.info());
    }
  }
);
}
