{
  tmps=-1;
  while (++tmpe <= tmpl) {
    if (tmps == -1) {
      if (tmpe < tmpl && Token.ftChar(tmptok[tmpe]))       tmps=tmpe;
    }
 else     if (tmpe == tmpl || !Token.ftChar(tmptok[tmpe])) {
      return true;
    }
  }
  tmptok=null;
  return false;
}
