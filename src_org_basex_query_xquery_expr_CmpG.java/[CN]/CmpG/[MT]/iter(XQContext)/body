{
  return new Iter(){
    /** 
 * Iterator flag. 
 */
    private boolean more;
    @Override public Item next() throws XQException {
      return (more^=true) ? Bln.get(ev(ctx.iter(expr[0]),ctx.iter(expr[1]))) : null;
    }
    @Override public String toString(){
      return CmpG.this.toString();
    }
  }
;
}
