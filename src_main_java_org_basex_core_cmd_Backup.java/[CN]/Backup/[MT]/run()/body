{
  final String db=args[0];
  if (!validName(db))   return error(NAMEINVALID,db);
  return !prop.dbexists(db) ? error(DBNOTFOUND,db) : backup(db,prop) ? info(DBBACKUP,db,perf) : error(DBNOBACKUP,db);
}
