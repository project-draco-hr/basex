{
  if (l <= 0)   return "";
  final char[] str=new char[l];
  for (int i=0; i < l; i++) {
    if (text[s + i] < 0)     return utf8(text,s,l);
    str[i]=(char)text[s + i];
  }
  return new String(str);
}
