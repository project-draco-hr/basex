{
  final XQuery query=new XQuery("count(subsequence(1 to 3000000000, -2147483648, 2147483647))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((error("*") || assertEq("0")));
}
