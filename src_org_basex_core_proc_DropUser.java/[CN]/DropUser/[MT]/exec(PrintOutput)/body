{
  final String user=args[0];
  for (  final ServerProcess s : context.sessions) {
    if (s.context.user.name.equals(user))     return error(USERLOG,user);
  }
  return user.equals(ADMIN) ? error(USERADMIN) : context.users.drop(args[0]) ? info(USERDROP,user) : error(USERNO,user);
}
