{
  if (!parsed) {
    try {
      perf.time();
      init().parse();
      qp.ctx.info.parsing=perf.time();
      parsed=true;
    }
 catch (    final QueryException ex) {
      throw new BaseXException(ex);
    }
  }
  return qp;
}
