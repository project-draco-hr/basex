{
  final XQuery query=new XQuery("implicit-timezone() ge xs:dayTimeDuration('-PT14H') and implicit-timezone() le xs:dayTimeDuration('PT14H')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
