{
  final XQuery query=new XQuery("fn:string(fn:implicit-timezone() div xs:dayTimeDuration(\"P0DT60M00S\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertType("xs:string"));
}
