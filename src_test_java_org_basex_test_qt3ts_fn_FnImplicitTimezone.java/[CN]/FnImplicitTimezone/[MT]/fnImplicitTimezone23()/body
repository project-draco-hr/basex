{
  final XQuery query=new XQuery("(fn:adjust-dateTime-to-timezone(xs:dateTime(\"2002-03-07T10:00:00\"),fn:implicit-timezone()))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertType("xs:dateTime"));
}
