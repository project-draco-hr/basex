{
  final XQuery query=new XQuery("(fn:adjust-time-to-timezone(xs:time(\"10:00:00\"),fn:implicit-timezone()))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertType("xs:time"));
}
