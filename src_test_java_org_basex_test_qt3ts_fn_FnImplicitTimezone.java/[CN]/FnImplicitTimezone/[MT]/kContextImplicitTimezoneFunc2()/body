{
  final XQuery query=new XQuery("seconds-from-duration(implicit-timezone()) le 0 or seconds-from-duration(implicit-timezone()) gt 0",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
