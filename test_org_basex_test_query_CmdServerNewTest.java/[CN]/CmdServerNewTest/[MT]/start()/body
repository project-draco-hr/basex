{
  new Thread(){
    @Override public void run(){
      server=new BaseXServerNew();
    }
  }
.start();
  try {
    launcher=new ClientLauncherNew(CONTEXT);
  }
 catch (  final Exception ex) {
    throw new AssertionError(ex.toString());
  }
}
