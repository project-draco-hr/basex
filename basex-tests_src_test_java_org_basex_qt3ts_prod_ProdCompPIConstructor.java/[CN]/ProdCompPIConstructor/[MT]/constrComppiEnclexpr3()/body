{
  final XQuery query=new XQuery("processing-instruction pi {1,'',2}",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<?pi 1  2?>",false));
}
