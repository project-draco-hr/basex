{
  return new Iterator<MapRect>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public MapRect next(){
      return list[c];
    }
    public void remove(){
      BaseX.notexpected();
    }
  }
;
}
