{
  int i=Integer.MIN_VALUE;
  for (  final MapRect r : this) {
    if (i > r.pre) {
      sorted=Array.finish(list,size);
      Arrays.sort(sorted);
      return;
    }
    i=r.pre;
  }
  sorted=list;
}
