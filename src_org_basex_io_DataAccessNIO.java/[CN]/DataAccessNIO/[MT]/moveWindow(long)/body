{
  try {
    if ((len - p) < BUFFERSIZE) {
      mbytebuffer=fc.map(FileChannel.MapMode.READ_WRITE,p,len - p);
    }
 else {
      mbytebuffer=fc.map(FileChannel.MapMode.READ_WRITE,p,BUFFERSIZE);
    }
    off=p;
    mbytebuffer.position(0);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
