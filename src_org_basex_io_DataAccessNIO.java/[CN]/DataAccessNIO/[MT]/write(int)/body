{
  if (off == DBBUFFERSIZE) {
    try {
      rwChannel.write(dbbuffer);
      dbbuffer.clear();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    off=0;
  }
  dbbuffer.put((byte)b);
}
