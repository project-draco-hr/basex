{
  if (off == DBBUFFERSIZE) {
    try {
      fc.write(dbbuffer);
      dbbuffer.clear();
      writePosition=writePosition + DBBUFFERSIZE;
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    off=0;
  }
  dbbuffer.put((byte)b);
}
