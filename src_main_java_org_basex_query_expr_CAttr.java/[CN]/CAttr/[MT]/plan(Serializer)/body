{
  ser.openElement(this);
  ser.openElement(NAME);
  atn.plan(ser);
  ser.closeElement();
  ser.openElement(VALUE);
  for (  final Expr e : expr)   e.plan(ser);
  ser.closeElement();
  ser.closeElement();
}
