{
  final XQuery query=new XQuery("declare function foo() external; 1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((error("XQST0045") || error("XPST0017")));
}
