{
  if (memfocus) {
    Performance.gc(4);
    repaint();
    final Runtime rt=Runtime.getRuntime();
    final long occ=rt.totalMemory();
    max=rt.maxMemory();
    used=occ - rt.freeMemory();
    final String inf=MEMTOTAL + Performance.format(max,true) + Prop.NL+ MEMRESERVED+ Performance.format(occ,true)+ Prop.NL+ MEMUSED+ Performance.format(used,true)+ Prop.NL+ Prop.NL+ MEMHELP;
    JOptionPane.showMessageDialog(GUI.get(),inf,MEMTITLE,JOptionPane.INFORMATION_MESSAGE);
  }
}
