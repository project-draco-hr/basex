{
  for (int h=0; h < histsize; h++) {
    marked[h]=null;
    cont[h]=null;
    queries[h]=null;
  }
  hist=0;
  histsize=0;
  final Data data=ctx.data();
  if (data != null) {
    cont[0]=ctx.current();
    marked[0]=new Nodes(data);
    queries[0]="";
  }
  return data;
}
