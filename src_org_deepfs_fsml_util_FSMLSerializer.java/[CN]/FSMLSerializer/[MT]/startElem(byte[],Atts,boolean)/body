{
  o.write(ELEM1);
  o.write(e);
  for (int i=0; i < atts.size; i++) {
    o.write(' ');
    o.write(atts.key[i]);
    o.write('=');
    o.write('"');
    text(atts.val[i]);
    o.write('"');
  }
  o.write(close ? ELEM4 : ELEM2);
}
