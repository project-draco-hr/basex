{
  o.write(ELEM1);
  o.write(e);
  for (int i=0; i < atts.size; i++) {
    o.write(' ');
    o.write(atts.key[i]);
    o.write('=');
    o.write('"');
    text(atts.val[i]);
    o.write('"');
  }
  if (close)   o.write(ELEM4);
 else   o.write(ELEM2);
}
