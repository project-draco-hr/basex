{
  for (  final IOFile file : root.children()) {
    if (file.isDir()) {
      add(file,thread);
    }
 else {
      cache.add(file.path());
    }
    if (threadID != thread) {
      cache.reset();
      return;
    }
  }
}
