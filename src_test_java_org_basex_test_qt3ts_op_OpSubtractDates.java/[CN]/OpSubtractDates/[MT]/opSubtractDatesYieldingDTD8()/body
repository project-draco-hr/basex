{
  final XQuery query=new XQuery("xs:date(\"0001-01-01Z\") - xs:date(\"2005-07-06Z\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"-P732132D"));
}
