{
  int i=0;
  try {
    for (    final Clause c : clauses) {
      c.compile(qc,scp);
      i++;
    }
    ret=ret.compile(qc,scp);
  }
 catch (  final QueryException qe) {
    clauseError(qe,i);
  }
  return optimize(qc,scp);
}
