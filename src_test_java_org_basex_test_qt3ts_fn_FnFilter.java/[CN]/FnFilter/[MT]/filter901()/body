{
  final XQuery query=new XQuery("filter(normalize-space#1, (\"apple\", \"pear\", \"apricot\", \"advocado\", \"orange\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
