{
  if (pos == null) {
    pos=new int[1][];
    pos[0]=ps;
    poi=new int[1][];
    poi[0]=pi;
  }
 else {
    if (size - 1 == pos.length)     pos=Array.extend(pos);
    if (size - 1 == poi.length)     poi=Array.extend(poi);
    poi[size - 1]=pi;
  }
}
