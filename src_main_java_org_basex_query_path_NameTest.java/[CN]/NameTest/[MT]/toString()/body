{
  if (test == Name.ALL)   return "*";
  if (test == Name.NAME)   return "*:" + string(name.string());
  final String uri=name.uri() == Uri.EMPTY || name.ns() ? "" : "{" + string(name.uri().string()) + "}";
  return uri + (test == Name.NS ? "*" : string(name.string()));
}
