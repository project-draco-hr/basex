{
  if (test == Name.ALL)   return "*";
  if (test == Name.NAME)   return "*:" + string(name.atom());
  final String uri=name.uri() == Uri.EMPTY || name.ns() ? "" : "{" + string(name.uri().atom()) + "}";
  return uri + (test == Name.NS ? "*" : string(name.atom()));
}
