{
  new Close().execute(context,out);
  final String db=args[0];
  if (context.prop.dblocked(db))   return error(DBLOCKED,db);
  return !prop.dbexists(db) ? error(DBNOTFOUND,db) : drop(db,prop) ? info(DBDROPPED,db) : error(DBNOTDROPPED);
}
