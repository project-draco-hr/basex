{
  database.getCollection(AllTests.PATH,AllTests.LOGIN,AllTests.PW).close();
  try {
    database.getCollection("bla",AllTests.LOGIN,AllTests.PW);
    fail("URI was invalid.");
  }
 catch (  final XMLDBException e) {
    checkCode(ErrorCodes.INVALID_URI,e);
  }
  DatabaseManager.registerDatabase(database);
  final Collection coll=DatabaseManager.getCollection(AllTests.PATH,AllTests.LOGIN,AllTests.PW);
  coll.close();
}
