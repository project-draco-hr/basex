{
  QueryProcessor qp=new QueryProcessor("for $x in .//body//li return $x",CONTEXT);
  Iter iter=qp.iter();
  Item item;
  try {
    while (null != (item=iter.next())) {
      item.serialize(xmlSer);
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
    return;
  }
}
