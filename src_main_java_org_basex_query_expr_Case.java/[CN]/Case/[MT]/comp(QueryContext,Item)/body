{
  if (var.name == null) {
    super.comp(ctx);
  }
 else {
    final int s=ctx.vars.size();
    ctx.vars.add(it == null ? var : var.bind(it,ctx).copy());
    super.comp(ctx);
    ctx.vars.reset(s);
  }
  return this;
}
