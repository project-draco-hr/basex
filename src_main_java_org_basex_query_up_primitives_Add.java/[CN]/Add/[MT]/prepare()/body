{
  md=new MemData(data);
  final Iterator<Item> d=docs.iterator();
  final Iterator<byte[]> n=names.iterator();
  final Iterator<byte[]> p=paths.iterator();
  while (d.hasNext())   md.insert(md.meta.size,-1,docData(d.next(),n.next(),p.next()));
}
