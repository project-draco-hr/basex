{
  final XQuery query=new XQuery("count(<e/>/(a except element {\"name\"} {()}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertStringValue(false,"0") || error("XPST0005")));
}
