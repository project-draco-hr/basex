{
  final XQuery query=new XQuery("for $h in ( count((//hours) except (//hours))) order by number($h) return $h",ctx);
  query.context(node(file("docs/works.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
