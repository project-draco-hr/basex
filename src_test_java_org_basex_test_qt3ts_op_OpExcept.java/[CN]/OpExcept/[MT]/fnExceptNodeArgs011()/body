{
  final XQuery query=new XQuery("/bib/book except /bib/book",ctx);
  query.context(node(file("op/union/bib2.xml")));
  final QT3Result res=result(query);
  result=res;
  test((assertEmpty() || error("XPST0005")));
}
