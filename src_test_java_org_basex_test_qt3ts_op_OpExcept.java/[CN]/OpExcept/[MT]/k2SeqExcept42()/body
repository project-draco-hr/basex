{
  final XQuery query=new XQuery("<r> { let $i := <e> <a/> <b/> <c/> </e>/a , $t := $i/following-sibling::b return (($i except ($i, $i)), (($t, $t) except $t)) } </r>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<r/>",false));
}
