{
  final XQuery query=new XQuery("count(() except (<a>3</a>,<a>4</a>,<a>0</a>))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("0") || error("XPST0005")));
}
