{
  final XQuery query=new XQuery("floor(xs:float(-0.1e0))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-1.0e0") && assertType("xs:float")));
}
