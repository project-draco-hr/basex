{
  final XQuery query=new XQuery("empty(index-of(xs:anyURI(\"example.com/\"), xs:hexBinary(\"FF\")))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
