{
switch (func) {
case DATA:
    return data(qc).iter();
case COLLECTION:
  return collection(qc).iter();
case URI_COLLECTION:
return uriCollection(qc);
case UNPARSED_TEXT_LINES:
return unparsedTextLines(qc);
default :
return super.iter(qc);
}
}
