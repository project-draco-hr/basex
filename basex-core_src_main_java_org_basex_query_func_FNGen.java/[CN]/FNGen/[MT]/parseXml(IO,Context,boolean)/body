{
  final Prop prop=ctx.prop;
  final boolean chop=prop.is(Prop.CHOP);
  try {
    prop.set(Prop.CHOP,false);
    return new DBNode(frag || prop.is(Prop.INTPARSE) ? new XMLParser(input,ctx.prop,frag) : new SAXWrapper(input,ctx.prop));
  }
  finally {
    prop.set(Prop.CHOP,chop);
  }
}
