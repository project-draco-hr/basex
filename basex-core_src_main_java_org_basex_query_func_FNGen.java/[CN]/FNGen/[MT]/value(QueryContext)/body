{
switch (func) {
case DATA:
    return data(qc);
case COLLECTION:
  return collection(qc);
case URI_COLLECTION:
return uriCollection(qc).value();
case UNPARSED_TEXT_LINES:
return unparsedTextLines(qc).value();
default :
return super.value(qc);
}
}
