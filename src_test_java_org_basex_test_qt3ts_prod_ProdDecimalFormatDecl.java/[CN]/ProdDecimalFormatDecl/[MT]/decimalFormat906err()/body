{
  final XQuery query=new XQuery("format-number(931.45, '000.##0', 'foo:bar')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FODF1280"));
}
