{
switch (func) {
case MATCH:
    return match(checkStr(expr[0],ctx),ctx);
case REPLACE:
  return replace(checkStr(expr[0],ctx),ctx);
case ANALZYE:
Err.or(NOTIMPL,func.desc);
return null;
default :
return super.atomic(ctx);
}
}
