{
switch (func) {
case MATCH:
    return matches(checkStr(expr[0],ctx),ctx);
case REPLACE:
  return replace(checkStr(expr[0],ctx),ctx);
case ANALZYE:
return analyzeString(checkStr(expr[0],ctx),ctx);
default :
return super.atomic(ctx);
}
}
