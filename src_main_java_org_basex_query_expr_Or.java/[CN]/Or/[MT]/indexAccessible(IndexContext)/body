{
  int is=0;
  Expr[] exprs={};
  for (  final Expr e : expr) {
    if (!e.indexAccessible(ic) || ic.seq)     return false;
    is+=ic.is;
    if (ic.is != 0)     exprs=Array.add(exprs,e);
  }
  ic.is=is;
  expr=exprs;
  return true;
}
