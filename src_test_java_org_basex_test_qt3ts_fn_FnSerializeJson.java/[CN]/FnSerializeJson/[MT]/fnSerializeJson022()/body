{
  final XQuery query=new XQuery("let $r := serialize-json((number('NaN'), number('INF'), number('-INF')))\n" + "            return translate($r,' \t\n" + "\r', '')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"[\"NaN\",\"INF\",\"-INF\"]"));
}
