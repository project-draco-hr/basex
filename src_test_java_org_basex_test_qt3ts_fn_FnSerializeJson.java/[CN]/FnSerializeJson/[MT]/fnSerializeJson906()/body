{
  final XQuery query=new XQuery("serialize-json((1,2,3),map:entry(\"indent\",23))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOJS0002"));
}
