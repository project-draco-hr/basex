{
  final XQuery query=new XQuery("normalize-space(serialize-json(false(), map{\"spec\":=\"ECMA-262\"}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"false"));
}
