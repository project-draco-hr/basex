{
  final XQuery query=new XQuery("serialize-json(\"abcd\", map{\"spec\":=\"RFC4627\"})",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOJS0002"));
}
