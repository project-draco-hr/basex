{
  final XQuery query=new XQuery("serialize-json((1,2,3),map:entry(\"escape\",map{}))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOJS0002"));
}
