{
  final XQuery query=new XQuery("serialize-json((), map{\"spec\":=\"ECMA-262\"})",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertQuery("translate($result,' \t\n\r', '') = 'null'"));
}
