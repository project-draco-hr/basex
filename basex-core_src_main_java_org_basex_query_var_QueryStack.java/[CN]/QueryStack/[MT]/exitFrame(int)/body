{
  while (--sl >= fp)   stack[sl]=null;
  sl=fp;
  fp=fpt;
  if (stack.length > INIT && sl <= stack.length / 4) {
    int len=stack.length;
    do     len/=2;
 while (len > INIT && sl <= len / 4);
    final Value[] nst=new Value[len];
    System.arraycopy(stack,0,nst,0,sl);
    stack=nst;
  }
}
