{
  final Constr con=new Constr(ii,ctx,expr);
  if (con.errAtt || con.atts.size() != 0)   XPATT.thrw(ii);
  final FDoc doc=new FDoc(con.children,con.base);
  for (int n=0; n < con.children.size(); ++n)   con.children.get(n).parent(doc);
  return doc;
}
