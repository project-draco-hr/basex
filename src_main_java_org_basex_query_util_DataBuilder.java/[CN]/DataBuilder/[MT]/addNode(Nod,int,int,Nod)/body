{
switch (nd.ndtype) {
case DOC:
    return addDoc(nd,pre);
case ELM:
  return addElem(nd,pre,par,ndPar);
case TXT:
return pre + addText(nd,pre,par,ndPar);
case ATT:
return pre + addAttr(nd,pre,par);
case COM:
return pre + addComm(nd,pre,par);
default :
return pre + addPI(nd,pre,par);
}
}
