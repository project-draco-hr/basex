{
  if (ft == null) {
    final SeqType[] arg=new SeqType[args.length];
    for (int i=0; i < args.length; i++)     arg[i]=args[i].type == null ? SeqType.ITEM_ZM : args[i].type;
    ft=FunType.get(arg,var.type == null ? SeqType.ITEM_ZM : var.type);
  }
  return ft;
}
