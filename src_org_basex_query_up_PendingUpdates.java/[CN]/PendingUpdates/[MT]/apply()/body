{
  val.validate(frags);
  for (  final Data d : dbs.keySet().toArray(new Data[dbs.size()])) {
    val.validate(dbs.get(d));
  }
  val.status();
  frags.apply();
  for (  final Data d : dbs.keySet().toArray(new Data[dbs.size()])) {
    dbs.get(d).apply();
    d.flush();
  }
}
