{
  try {
    final int b=blocks[ind];
    writeBlock();
    file.seek((long)b << IO.BLOCKPOWER);
    file.read(buffer);
    block=b;
    index=ind;
    firstPre=first;
    nextPre=next;
  }
 catch (  final IOException ex) {
    ex.printStackTrace();
  }
}
