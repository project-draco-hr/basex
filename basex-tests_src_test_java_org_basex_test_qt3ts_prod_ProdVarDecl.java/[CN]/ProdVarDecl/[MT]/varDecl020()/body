{
  final XQuery query=new XQuery("declare variable $x := 9.999999999999999; $x",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"9.999999999999999"));
}
