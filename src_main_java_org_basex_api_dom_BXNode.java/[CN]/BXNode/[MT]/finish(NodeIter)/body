{
  final NodIter nb=new NodIter();
  try {
    Nod n=null;
    while ((n=it.next()) != null)     nb.add(n.copy());
  }
 catch (  final QueryException ex) {
    Util.notexpected();
  }
  return nb;
}
