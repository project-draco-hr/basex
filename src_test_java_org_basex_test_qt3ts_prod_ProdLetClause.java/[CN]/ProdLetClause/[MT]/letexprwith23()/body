{
  final XQuery query=new XQuery("let $var as xs:boolean := if (fn:true()) then fn:true() else fn:false() return $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
