{
  final XQuery query=new XQuery("deep-equal((<b/>, <b/>, <b/>, <b/>), (for $v1 in (1, 2, 3, 4) let $v2 := <b/> return ($v2))/.)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
