{
  final XQuery query=new XQuery("declare variable $e := <e/>; for $i in (<a/>, $e, <c/>) return $i is $e",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"false true false"));
}
