{
  final XQuery query=new XQuery("let $x := (0,0.1e-1,2.0,'a',\"cat\",'',true(), ('<a> <b> <c> 123 </c> </b> </a>')/a/b) return $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0019"));
}
