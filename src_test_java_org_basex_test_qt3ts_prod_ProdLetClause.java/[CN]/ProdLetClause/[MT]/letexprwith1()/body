{
  final XQuery query=new XQuery("let $salary as xs:decimal := \"cat\" return $salary * 2",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
