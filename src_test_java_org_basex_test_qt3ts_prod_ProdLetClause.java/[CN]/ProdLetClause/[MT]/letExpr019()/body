{
  final XQuery query=new XQuery("let $a := <elem/> let $b := <elem2/> return ($a,$b)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem/><elem2/>",false));
}
