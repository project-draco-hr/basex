{
  final XQuery query=new XQuery("let $x := (0,0.1e-1,2.0,'a',\"cat\",'',true()) return $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"0 0.01 2 a cat  true"));
}
