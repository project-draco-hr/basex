{
  return new NodeIter(){
    /** 
 * First call. 
 */
    private boolean more;
    @Override public Nod next(){
      if (more)       return null;
      more=true;
      return parent();
    }
  }
;
}
