{
  if (yAttribute == null)   return false;
  byte[] b=Token.token(yAttribute);
  final boolean tmp=!Token.contains(b,AT);
  b=Token.delete(b,AT);
  yIsTag=tmp;
  attrY=b;
  refreshCoordinates();
  return true;
}
