{
  if (xAttribute == null)   return false;
  byte[] b=Token.token(xAttribute);
  final boolean tmp=!Token.contains(b,AT);
  b=Token.delete(b,AT);
  xIsTag=tmp;
  attrX=b;
  refreshCoordinates();
  return true;
}
