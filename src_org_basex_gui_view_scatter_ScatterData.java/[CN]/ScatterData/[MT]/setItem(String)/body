{
  if (newItem == null)   return false;
  final byte[] b=Token.token(newItem);
  if (Token.eq(b,item))   return false;
  item=b;
  refreshItems();
  xAxis.refreshAxis();
  yAxis.refreshAxis();
  return true;
}
