{
  final NodeIter ni=(NodeIter)ctx.iter(step[l]);
  final boolean more=l + 1 != step.length;
  ANode node;
  while ((node=ni.next()) != null) {
    if (more) {
      ctx.value=node;
      iter(l + 1,nc,ctx);
    }
 else {
      ctx.checkStop();
      nc.add(node);
    }
  }
}
