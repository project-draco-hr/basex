{
  final NodeIter ir=(NodeIter)ctx.iter(step[l]);
  final boolean more=l + 1 != step.length;
  Nod v;
  while ((v=ir.next()) != null) {
    if (more) {
      ctx.value=v;
      iter(l + 1,ni,ctx);
    }
 else {
      ctx.checkStop();
      ni.add(v);
    }
  }
}
