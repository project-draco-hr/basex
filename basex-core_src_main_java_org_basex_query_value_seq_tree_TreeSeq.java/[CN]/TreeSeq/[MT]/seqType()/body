{
  if (ret == null) {
    final Iterator<Item> iter=iterator();
    Type t=iter.next().type;
    while (iter.hasNext()) {
      if (t != iter.next().type) {
        t=AtomType.ITEM;
        break;
      }
    }
    ret=t;
    type=t;
  }
  return SeqType.get(ret,Occ.ONE_MORE);
}
