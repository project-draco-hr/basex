{
  try {
    return expr[0].type().equals(SeqType.BLN) ? expr[0] : Fun.create(input,FunDef.BOOLEAN,expr[0]);
  }
 catch (  QueryException e) {
    e.printStackTrace();
    Main.debug(e);
    return null;
  }
}
