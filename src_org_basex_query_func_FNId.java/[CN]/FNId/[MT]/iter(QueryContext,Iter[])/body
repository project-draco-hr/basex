{
  final Item it=(arg.length == 1 ? checkCtx(ctx) : arg[1]).atomic();
  if (it == null)   Err.or(XPEMPTYPE,info(),Type.NOD);
  final Nod node=checkNode(it);
switch (func) {
case ID:
    return id(arg[0],node);
case IDREF:
  return idref(arg[0],node);
case LANG:
return lang(arg[0],node);
default :
BaseX.notexpected(func);
return null;
}
}
