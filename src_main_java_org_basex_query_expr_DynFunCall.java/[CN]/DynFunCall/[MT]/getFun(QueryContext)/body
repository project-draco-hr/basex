{
  final Item it=expr[expr.length - 1].item(ctx,input);
  if (!it.func() || ((FunType)it.type).args.length != expr.length - 1)   Err.type(this,FunType.arity(expr.length - 1),it);
  return (FItem)it;
}
