{
switch (sig) {
case _ZIP2_ENTRIES:
    return entries(ctx);
case _ZIP2_EXTRACT_TEXTS:
  return extractTexts(ctx);
case _ZIP2_EXTRACT_BINARIES:
return extractBinaries(ctx);
default :
return super.iter(ctx);
}
}
