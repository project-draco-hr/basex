{
switch (sig) {
case _ZIP2_ENTRIES:
    return entries(ctx);
case _ZIP2_EXTRACT_TEXT:
  return extractText(ctx);
case _ZIP2_EXTRACT_BINARY:
return extractBinary(ctx);
default :
return super.iter(ctx);
}
}
