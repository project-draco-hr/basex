{
  final String doc="<a><b/></a>";
  final AtomicUpdateList l=atomics(doc);
  l.addInsert(3,1,data("<d/>"),false);
  l.addReplace(2,data("<newb/>"));
  optimize(l,2);
  query(transform(doc,"insert node <d/> into $input," + "replace node $input/b with <newb/>"),"<a><newb/><d/></a>");
}
