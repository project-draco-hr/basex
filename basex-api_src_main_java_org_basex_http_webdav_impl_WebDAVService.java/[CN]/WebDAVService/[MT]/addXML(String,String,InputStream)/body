{
  final Session session=http.session();
  session.execute(new Set(MainOptions.CHOP,false));
  session.add(p,in);
  return factory.file(this,new ResourceMetaData(db,p,timestamp(db),false,APP_XML,null));
}
