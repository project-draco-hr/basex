{
  final WebDAVQuery query=new WebDAVQuery(DATA.args(_DB_INFO.args("$path") + "/descendant::" + DbFn.toName(Text.TIMESTAMP)+ "[1]")).bind("path",db);
  return DateTime.parse(execute(query).get(0)).getTime();
}
