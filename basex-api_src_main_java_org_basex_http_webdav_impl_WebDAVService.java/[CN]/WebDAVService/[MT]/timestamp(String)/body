{
  final Query q=session().query(DATA.args(_DB_INFO.args("$p") + "/descendant::" + FNDb.toName(Text.TIMESTAMP)+ "[1]"));
  q.bind("p",db);
  try {
    return DateTime.parse(q.execute(),DateTime.DATETIME).getTime();
  }
 catch (  final ParseException ex) {
    Util.errln(ex);
    return 0L;
  }
}
