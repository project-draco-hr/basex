{
  final XQuery query=new XQuery("xs:yearMonthDuration(\"P1Y\") - xs:date(\"1999-08-12\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
