{
  final Type ip=it.type;
  final boolean ins=ip.instanceOf(type);
  if (!ins && !ip.isUntyped() && !(it instanceof FItem)&& (ip != AtomType.FLT || type != AtomType.DBL)&& (ip != AtomType.URI || type != AtomType.STR)&& (type != AtomType.FLT && type != AtomType.DBL || !ip.instanceOf(AtomType.DEC)))   Err.treat(ii,this,it);
  return ins;
}
