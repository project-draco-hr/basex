{
  final String attr=(String)(drawX ? xCombo : yCombo).getSelectedItem();
  return BaseXLayout.value(value,attr.equals("@" + DataText.S_SIZE),attr.equals("@" + DataText.S_MTIME) || attr.equals("@" + DataText.S_CTIME) || attr.equals("@" + DataText.S_ATIME));
}
