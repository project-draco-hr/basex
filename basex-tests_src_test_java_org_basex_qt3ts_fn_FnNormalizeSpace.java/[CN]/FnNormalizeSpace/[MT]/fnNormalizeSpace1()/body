{
  final XQuery query=new XQuery("fn:normalize-space(\" The wealthy curled darlings of our nation. \")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"The wealthy curled darlings of our nation."));
}
