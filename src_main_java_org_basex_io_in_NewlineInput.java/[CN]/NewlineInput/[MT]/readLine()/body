{
  final TokenBuilder tb=new TokenBuilder();
  while (true) {
    final int ch=read();
    if (ch == -1)     return tb.isEmpty() ? null : tb.toString();
    if (ch == '\n')     return tb.toString();
    tb.add(ch);
  }
}
