{
  final TokenBuilder tb=new TokenBuilder();
  while (true) {
    final int ch=read();
    if (ch == -1)     return tb.isEmpty() ? null : tb;
    if (ch == '\n')     return tb;
    tb.add(ch);
  }
}
