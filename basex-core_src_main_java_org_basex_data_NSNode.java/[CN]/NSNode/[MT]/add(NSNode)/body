{
  if (size == children.length)   children=Array.copy(children,new NSNode[Array.newSize(size)]);
  int s=find(n.pre);
  if (s < 0 || n.pre != children[s].pre)   s++;
  System.arraycopy(children,s,children,s + 1,size++ - s);
  children[s]=n;
  n.parent=this;
}
