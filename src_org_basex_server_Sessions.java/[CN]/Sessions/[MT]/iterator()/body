{
  return new Iterator<ServerSession>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public ServerSession next(){
      return list[c];
    }
    public void remove(){
      Main.notexpected();
    }
  }
;
}
