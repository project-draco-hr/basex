{
  return new Iterator<Session>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public Session next(){
      return list[c];
    }
    public void remove(){
      Main.notexpected();
    }
  }
;
}
