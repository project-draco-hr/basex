{
  return new Iterator<ServerProcess>(){
    private int c=-1;
    public boolean hasNext(){
      return ++c < size;
    }
    public ServerProcess next(){
      return list[c];
    }
    public void remove(){
      Main.notexpected();
    }
  }
;
}
