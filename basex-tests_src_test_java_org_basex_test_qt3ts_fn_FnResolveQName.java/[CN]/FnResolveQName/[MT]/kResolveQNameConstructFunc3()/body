{
  final XQuery query=new XQuery("resolve-QName(\"wrongparam\", \"takes a node\", \"wrong\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0017"));
}
