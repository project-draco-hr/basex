{
  final XQuery query=new XQuery("fn:resolve-QName(\"p1:anElement\", <anElement>Some content</anElement>)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FONS0004"));
}
