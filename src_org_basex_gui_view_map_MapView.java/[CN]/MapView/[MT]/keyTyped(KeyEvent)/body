{
  if (gui.updating)   return;
  final char ch=e.getKeyChar();
  if (ch == '|') {
    gui.prop.set(GUIProp.MAPOFFSETS,(gui.prop.num(GUIProp.MAPOFFSETS) + 1) % MAPOFFSET.length);
    gui.notify.layout();
  }
}
