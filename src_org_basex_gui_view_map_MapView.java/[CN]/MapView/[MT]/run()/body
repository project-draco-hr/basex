{
  focusedRect=null;
  while (zoomStep > 1) {
    Performance.sleep(zoomSpeed);
    zoomStep--;
    repaint();
  }
  while (gui.painting)   Performance.sleep(zoomSpeed);
  zoomStep=0;
  gui.updating=false;
  focus();
  repaint();
}
