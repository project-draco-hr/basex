{
  if (!GUIProp.mapinfo || !infoinit) {
    mapInfo=new DialogMapInfo(gui);
  }
 else   if (infoinit) {
    mapInfo=new DialogMapInfo(gui,nno,nnn,recto,rectn,aaro,aarn,distance);
  }
  mapInfo.validate();
}
