{
  final XQuery query=new XQuery("declare variable $var1 := $var2; declare variable $var2 := 2; $var1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("2"));
}
