{
  final XQuery query=new XQuery("declare variable $myVar as xs:gYear := 2006; $myVar",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
