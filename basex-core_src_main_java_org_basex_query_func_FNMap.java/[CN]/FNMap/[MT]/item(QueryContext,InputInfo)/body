{
switch (func) {
case _MAP_NEW:
    return newMap(qc,ii);
case _MAP_ENTRY:
  return entry(qc,ii);
case _MAP_CONTAINS:
return Bln.get(contains(qc,ii));
case _MAP_SIZE:
return Int.get(map(qc).mapSize());
case _MAP_REMOVE:
return remove(qc,ii);
case _MAP_SERIALIZE:
return Str.get(map(qc).serialize(info));
default :
return super.item(qc,ii);
}
}
