{
  super.comp(ctx);
  Expr e=this;
  if (oneEmpty()) {
    e=Empty.SEQ;
  }
 else {
    final long[] v=range(ctx);
    if (v != null) {
      size=v[1] - v[0] + 1;
      e=size < 1 ? Empty.SEQ : size == 1 ? Itr.get(v[0]) : this;
    }
  }
  type=SeqType.ITR_OM;
  return optPre(e,ctx);
}
