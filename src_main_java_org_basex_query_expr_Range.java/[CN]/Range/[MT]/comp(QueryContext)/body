{
  super.comp(ctx);
  Expr e=this;
  if (oneEmpty()) {
    e=Empty.SEQ;
  }
 else {
    final long[] v=range(ctx);
    if (v != null) {
      if (v[0] > v[1])       e=Empty.SEQ;
      if (v[0] == v[1])       e=Itr.get(v[0]);
      size=v[0] <= v[1] ? v[1] - v[0] + 1 : 0;
    }
  }
  type=SeqType.ITR_OM;
  return optPre(e,ctx);
}
