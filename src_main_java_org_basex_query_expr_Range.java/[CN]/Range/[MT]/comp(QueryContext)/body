{
  super.comp(ctx);
  Expr e=this;
  if (expr[0].empty() || expr[1].empty()) {
    e=Empty.SEQ;
  }
 else {
    final long[] v=range(ctx);
    if (v != null) {
      if (v[0] == v[1])       e=Itr.get(v[0]);
      if (v[0] > v[1])       e=Empty.SEQ;
    }
  }
  return optPre(e,ctx);
}
