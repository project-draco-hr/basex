{
  try {
    if (value != null)     qp.context(value instanceof XQValue ? ((XQValue)value).internal() : value);
  }
 catch (  final QueryException ex) {
    throw new XQException(ex);
  }
  return this;
}
