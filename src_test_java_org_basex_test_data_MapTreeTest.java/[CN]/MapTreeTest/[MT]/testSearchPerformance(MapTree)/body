{
  final int n=BASEID + ITERATIONS;
  for (int id=BASEID + 1; id <= n; ++id)   m.insert(id,RANDOM.nextInt(id));
  final Performance p=new Performance();
  for (int i=0; i < n; ++i)   m.pre(i);
  if (VERBOSE)   Util.errln(n + " records found in: " + p);
}
