{
  check("declare function local:foo() { local:foo() }; local:foo()",null,"exists(//" + Util.name(StaticFunc.class) + "/"+ Util.name(TailFuncCall.class)+ ")","exists(//" + Util.name(BaseFuncCall.class) + ")");
}
