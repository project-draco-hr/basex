{
  check("let $a := <a/> let $b := <b/> let $c as element(a) := $a return ($c,$b)","<a/>" + Prop.NL + "<b/>",Util.info("//Let[Var/@name='$a'] << //Let[Var/@name='$b'] and " + "//Let[Var/@name='$b'] << //Let[starts-with(Var/@name, '$c')]"));
}
