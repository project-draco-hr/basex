{
  check("declare function local:id($x) { $x };" + "let $a as element(a) := local:id(<a/>) " + "let $b := <b/> "+ "let $c as element(a) := local:id($a) "+ "for $i in 1 to 2 return ($c,$b)","<a/>" + Prop.NL + "<b/>"+ Prop.NL+ "<a/>"+ Prop.NL+ "<b/>",Util.info("//Let[starts-with(Var/@name,'$a')] << //Let[Var/@name='$b'] and " + "//Let[Var/@name='$b'] << //Let[starts-with(Var/@name, '$c')]"));
}
