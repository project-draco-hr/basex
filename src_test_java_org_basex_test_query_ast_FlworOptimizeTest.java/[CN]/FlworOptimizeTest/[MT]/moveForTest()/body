{
  check("let $x := <x/> " + "for $a in 1 to 2 " + "for $b in $x "+ "return $b","<x/>" + NL + "<x/>","//For[@var eq '$b'] << //For[@var eq '$a']","every $for in //For satisfies exactly-one(//Let) << $for");
}
