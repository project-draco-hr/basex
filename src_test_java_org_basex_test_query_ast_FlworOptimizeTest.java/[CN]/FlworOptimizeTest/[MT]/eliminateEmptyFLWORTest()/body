{
  check("for $x in 1 to 100 where false() return $x","","exists(//Empty)");
  check("for $x in 1 to 100 return ()","","exists(//Empty)");
  check("for $x in 1 to 100 where () return $x","","exists(//Empty)");
}
