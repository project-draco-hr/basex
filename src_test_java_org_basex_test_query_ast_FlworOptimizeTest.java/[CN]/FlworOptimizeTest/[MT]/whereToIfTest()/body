{
  check("(1 to 3) ! (for $j in 1 to 5 where . eq 1 return $j)","1 2 3 4 5","exists(//If) and empty(//GFLWOR)");
  check("(1 to 3) ! (for $j at $p in 1 to 5 where . eq 1 return $j * $p)","1 4 9 16 25","exists(//If) and exists(//GFLWOR)");
  check("let $x := 0 where $x != <x>0</x> return 42 idiv $x","","exists(//If) and starts-with(//If/*[2]/@name, 'error(')");
}
