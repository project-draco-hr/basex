{
  check("for $i in 1 to 2 for $j in 1 to 2 " + "let $a := 3 * $i, $b := 2 * $i return $a - $b","1 1 2 2","exists(//For[every $let in //Let satisfies . << $let])","exists(//For[every $let in //Let satisfies . >> $let])");
}
