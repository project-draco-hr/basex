{
  check("declare function local:id($x) { $x };" + "for $i in 1 to 2 for $j in 1 to 2 " + "let $a as xs:integer := local:id(3 * $i), "+ "    $b as xs:integer := local:id(2 * $i) "+ "return $a - $b","1 1 2 2","exists(//For[every $let in //Let satisfies . << $let])","exists(//For[every $let in //Let satisfies . >> $let])","//Let[starts-with(Var/@name, '$a')] << //Let[starts-with(Var/@name, '$b')]");
}
