{
  if (args.length != 2) {
    Main.err("DeepFSImpl expects mount point" + " and DeepFS database name as arguments");
    System.exit(-1);
  }
  final String mountp=args[0];
  final String dbname=args[1];
  FUSE.main(new String[]{mountp},new DeepFSImpl(dbname,mountp));
}
