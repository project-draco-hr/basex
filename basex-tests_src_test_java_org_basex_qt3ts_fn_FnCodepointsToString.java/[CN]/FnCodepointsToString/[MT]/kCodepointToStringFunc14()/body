{
  final XQuery query=new XQuery("string-to-codepoints(codepoints-to-string(14))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("14") || error("FOCH0001")));
}
