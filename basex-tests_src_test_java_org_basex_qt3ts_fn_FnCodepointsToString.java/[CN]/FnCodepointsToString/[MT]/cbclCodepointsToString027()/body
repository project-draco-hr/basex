{
  final XQuery query=new XQuery("for $x in (13), $y in (13,9,10) return codepoints-to-string($x to $y)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"\r  "));
}
