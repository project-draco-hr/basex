{
  final XQuery query=new XQuery("count((element elem {1, 2, <a/>, 3, 4, <b/>, 5, 6})/text())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("3"));
}
