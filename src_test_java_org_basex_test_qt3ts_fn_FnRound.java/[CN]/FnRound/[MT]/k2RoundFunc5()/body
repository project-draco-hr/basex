{
  final XQuery query=new XQuery("round(xs:positiveInteger(.)) instance of xs:positiveInteger",ctx);
  query.context(node(file("fn/abs/e-1.xml")));
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(false) || assertBoolean(true)));
}
