{
  final byte[] ln=type.ln();
  final Levenshtein ls=new Levenshtein();
  for (  final Type t : Type.values()) {
    if (t.par != null && ls.similar(lc(ln),lc(token(t.name)),0))     Err.or(FUNSIMILAR,ln,t.name);
  }
  Err.or(FUNCUNKNOWN,type.atom());
}
