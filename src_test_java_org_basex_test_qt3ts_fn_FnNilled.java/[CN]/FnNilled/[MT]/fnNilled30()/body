{
  final XQuery query=new XQuery("current-date() gt current-date()+xs:dayTimeDuration('P1D') or nilled()",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPDY0002"));
}
