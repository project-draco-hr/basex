{
  dupl=false;
  if (size > 1) {
    if (force)     sort(0,size);
    int i=1;
    for (int j=1; j < size; j++) {
      if (!item[i - 1].is(item[j]))       item[i++]=item[j];
    }
    size=i;
  }
}
