{
  dupl=false;
  if (size > 1) {
    if (force)     sort(0,size);
    int i=1;
    for (int j=1; j < size; j++) {
      if (!list[i - 1].is(list[j]))       list[i++]=list[j];
    }
    size=i;
  }
}
