{
  int l=0, h=size - 1;
  while (l <= h) {
    final int m=l + h >>> 1;
    final DBNode n=(DBNode)item[m];
    final int c=n.pre - node.pre;
    if (c == 0)     return n.data == node.data;
    if (c < 0)     l=m + 1;
 else     h=m - 1;
  }
  return false;
}
