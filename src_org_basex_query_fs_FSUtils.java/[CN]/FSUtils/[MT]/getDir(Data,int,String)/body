{
  final DirIterator it=new DirIterator(data,pre);
  final String p=FSUtils.transformToRegex(path);
  while (it.more()) {
    final int n=it.next();
    if (isDir(data,n) && Pattern.matches(p,Token.string(getName(data,n))))     return n;
  }
  return -1;
}
