{
  int n=pre;
  int size=data.size(n,kind) + n;
  n+=data.attSize(n,data.kind(n));
  while (n < size) {
    if (isDir(data,n)) {
      if (Token.eq(getName(data,n),dir)) {
        return n;
      }
    }
    n+=data.size(n,kind);
  }
  return -1;
}
