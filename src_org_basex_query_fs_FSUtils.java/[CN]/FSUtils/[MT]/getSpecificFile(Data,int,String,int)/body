{
  int n=pre;
  int size=data.size(n,kind) + n;
  n+=data.attSize(n,data.kind(n));
  while (n < size) {
    if (isFile(data,n)) {
      if (Token.string(getName(data,n)).equals(dir)) {
        return n;
      }
    }
    n+=data.size(n,kind);
  }
  return -1;
}
