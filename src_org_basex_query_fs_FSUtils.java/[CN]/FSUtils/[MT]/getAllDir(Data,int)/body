{
  int kind=data.kind(pre);
  int n=pre;
  int size=data.size(n,kind) + n;
  n+=data.attSize(n,kind);
  int[] res=new int[((size - n) / 5) + 1];
  int i=0;
  while (n < size) {
    if (isDir(data,n)) {
      res[i++]=n;
    }
    n+=data.size(n,kind);
  }
  return res;
}
