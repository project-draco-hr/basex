{
  final IntList res=new IntList();
  final DirIterator it=new DirIterator(data,pre);
  while (it.more()) {
    final int n=it.next();
    if (isDir(data,n))     res.add(n);
  }
  return res.finish();
}
