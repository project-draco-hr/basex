{
  content=f.startsWith("<");
  if (content)   cont=Token.token(f);
  url=!content && f.startsWith("http://");
  path=content ? "tmp.xml" : url ? f : new File(f).getAbsolutePath().replace('\\','/');
}
