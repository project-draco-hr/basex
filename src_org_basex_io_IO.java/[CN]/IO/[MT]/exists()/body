{
  if (content)   return true;
  if (!url)   return file().exists();
  try {
    new URL(path).openConnection();
    return true;
  }
 catch (  IOException ex) {
    BaseX.debug(ex);
    return false;
  }
}
