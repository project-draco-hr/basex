{
  if (content)   return true;
  if (!url)   return file().exists();
  try {
    new URL(path).openStream();
    return true;
  }
 catch (  IOException ex) {
    BaseX.debug(ex);
    return false;
  }
}
