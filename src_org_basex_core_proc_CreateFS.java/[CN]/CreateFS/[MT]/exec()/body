{
  prop.set(Prop.CHOP,true);
  prop.set(Prop.ENTITY,true);
  return prop.is(Prop.NEWFSPARSER) ? build(new NewFSParser(fsimportpath,mount,store,prop),name) : build(new FSParser(fsimportpath,mount,store,prop),name);
}
