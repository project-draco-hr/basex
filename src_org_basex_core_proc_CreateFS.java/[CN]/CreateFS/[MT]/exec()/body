{
  prop.set(Prop.CHOP,true);
  prop.set(Prop.ENTITY,true);
  return prop.is(Prop.NEWFSPARSER) ? build(new NewFSParser(args[0],args[2],prop),args[1]) : build(new FSParser(args[0],args[2],prop),args[1]);
}
