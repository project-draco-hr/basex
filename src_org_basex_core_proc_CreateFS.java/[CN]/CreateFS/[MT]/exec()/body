{
  Prop.chop=true;
  Prop.entity=true;
  Prop.mainmem=false;
  return Prop.newfsparser ? build(new NewFSParser(fsimportpath,mountpoint,backingstore),dbname) : build(new FSParser(fsimportpath,mountpoint,backingstore),dbname);
}
