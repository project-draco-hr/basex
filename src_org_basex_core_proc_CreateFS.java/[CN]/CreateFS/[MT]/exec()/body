{
  Prop.chop=true;
  Prop.entity=true;
  return Prop.newfsparser ? build(new NewFSParser(fsimportpath,mountpoint,backingstore),name) : build(new FSParser(fsimportpath,mountpoint,backingstore),name);
}
