{
  final int a=ti.next();
  if (a < 0)   return a;
  final int b=ti.next();
  if (b < 0)   invalid();
  final int c=ti.next();
  if (c < 0)   invalid();
  final int d=ti.next();
  if (d < 0)   invalid();
  return a << 24 | b << 16 | c << 8 | d;
}
