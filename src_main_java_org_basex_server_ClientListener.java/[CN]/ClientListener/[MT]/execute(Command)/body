{
  final DecodingInput di=new DecodingInput(in);
  cmd.input(di);
  log.write(this,cmd + "[...]");
  try {
    cmd.execute(context);
    success(cmd.info());
  }
 catch (  final BaseXException ex) {
    di.flush();
    error(ex.getMessage());
  }
}
