{
  final XQuery query=new XQuery("fn:string-length(if (2 != 3) then 'foo' else 'expanded-foo')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("3"));
}
