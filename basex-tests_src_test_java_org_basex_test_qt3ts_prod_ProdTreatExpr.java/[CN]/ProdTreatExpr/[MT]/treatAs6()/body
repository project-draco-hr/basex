{
  final XQuery query=new XQuery("let $var := xs:float(100) return fn:abs($var treat as xs:float)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("100"));
}
