{
  if ((len - p) < BUFFERSIZE) {
    mbytebuffer=rwChannel.map(FileChannel.MapMode.READ_WRITE,p,len - p + 1);
  }
 else {
    mbytebuffer=rwChannel.map(FileChannel.MapMode.READ_WRITE,p,BUFFERSIZE);
  }
  off=p - 1;
  mbytebuffer.position(0);
}
