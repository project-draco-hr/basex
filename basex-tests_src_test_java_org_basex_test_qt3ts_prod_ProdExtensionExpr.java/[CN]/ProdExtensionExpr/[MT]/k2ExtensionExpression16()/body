{
  final XQuery query=new XQuery("declare namespace ex = \"http://example.com/\"; (# ex:name",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0003"));
}
