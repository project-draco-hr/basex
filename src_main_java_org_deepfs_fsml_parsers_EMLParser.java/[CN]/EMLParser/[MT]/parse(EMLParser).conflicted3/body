{
  try {
    final Date d=SDF.parse(obj.mCurrLine);
    obj.deepFile.addMeta(MetaElem.DATETIME_CREATED,ParserUtil.convertDateTime(d));
  }
 catch (  final ParseException ex) {
    obj.deepFile.debug("EMLParser: Failed to parse date",ex);
  }
  return true;
}
