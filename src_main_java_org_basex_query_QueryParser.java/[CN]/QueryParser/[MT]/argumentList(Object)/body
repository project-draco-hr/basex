{
  if (!consumeWS2(PAR1))   return null;
  Expr[] args={};
  if (!consumeWS(PAR2)) {
    do {
      args=Array.add(args,consumeWS(PLHOLDER) ? null : single());
    }
 while (consumeWS(COMMA));
    if (!consumeWS(PAR2))     error(FUNCMISS,name);
  }
  return args;
}
