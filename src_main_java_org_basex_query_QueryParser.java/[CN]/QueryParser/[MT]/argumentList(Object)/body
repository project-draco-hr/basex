{
  if (!wsConsume(PAR1))   return null;
  Expr[] args={};
  if (!wsConsume(PAR2)) {
    do {
      args=Array.add(args,wsConsume(PLHOLDER) ? null : single());
    }
 while (wsConsume(COMMA));
    if (!wsConsume(PAR2))     error(FUNCMISS,name);
  }
  return args;
}
