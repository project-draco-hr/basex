{
  final Expr e=range();
  if (!consume(CONCAT) && !consume(CONCAT2))   return e;
  Expr[] list={e};
  do   list=add(list,range());
 while (wsConsume(CONCAT) || consume(CONCAT2));
  return new Concat(input(),list);
}
