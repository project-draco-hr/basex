{
  final TokenBuilder tb=new TokenBuilder();
  while (true) {
    while (not(']')) {
      final char c=consume();
      if (c != '\r')       tb.add(c);
    }
    consume();
    if (curr(']') && next() == '>') {
      qp+=2;
      return tb.finish();
    }
    tb.add(']');
  }
}
