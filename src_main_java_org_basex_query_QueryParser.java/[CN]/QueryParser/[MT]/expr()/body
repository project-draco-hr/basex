{
  final Expr e=single();
  if (e == null) {
    if (more())     return null;
    if (alter != null)     error();
 else     error(NOEXPR);
  }
  if (!consumeWS2(COMMA))   return e;
  Expr[] list={e};
  do   list=add(list,single());
 while (consumeWS2(COMMA));
  return new List(list);
}
