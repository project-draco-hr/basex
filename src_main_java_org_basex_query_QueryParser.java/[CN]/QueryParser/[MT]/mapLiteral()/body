{
  wsCheck(BRACE1);
  Expr[] args={};
  if (!wsConsume(BRACE2)) {
    do {
      args=Array.add(args,check(single(),XPEMPTY));
      wsCheck(ASSIGN);
      args=Array.add(args,check(single(),XPEMPTY));
    }
 while (wsConsume(COMMA));
    wsCheck(BRACE2);
  }
  return new LitMap(input(),args);
}
