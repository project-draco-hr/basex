{
  final int p=qp;
  final boolean ok=consumeWS2(t);
  final int q=qp;
  final int c=curr();
  final boolean ok2=skipWS() || !XMLToken.isFirstLetter(t.charAt(0)) || !XMLToken.isXMLLetterOrDigit(c) && c != '-';
  qp=ok2 ? q : p;
  return ok && ok2;
}
