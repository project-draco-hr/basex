{
  ++ip;
  final boolean doc=next() == '~';
  if (doc) {
    xqdoc.setLength(0);
    ++ip;
  }
  while (++ip < il) {
    char curr=curr();
    if (curr == '(' && next() == ':') {
      comment();
      curr=curr();
    }
    if (curr == ':' && next() == ')') {
      ip+=2;
      return;
    }
    if (doc)     xqdoc.append(curr);
  }
  error(COMCLOSE);
}
