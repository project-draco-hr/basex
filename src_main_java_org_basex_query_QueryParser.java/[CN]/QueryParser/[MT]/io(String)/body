{
  IO fl=IO.get(fn);
  if (!fl.exists()) {
    final IO base=ctx.sc.baseIO();
    if (base != null)     fl=base.merge(fn);
  }
  if (!fl.exists() && file != null)   fl=file.merge(fn);
  return fl;
}
