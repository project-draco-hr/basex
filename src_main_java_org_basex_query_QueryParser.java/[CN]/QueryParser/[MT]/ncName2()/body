{
  char c=curr();
  if (!XMLToken.isNCStartChar(c)) {
    --qp;
    return false;
  }
  tok.add(':');
  do {
    tok.add(consume());
    c=curr();
  }
 while (XMLToken.isNCChar(c));
  return true;
}
