{
  final int p=qp;
  tok.reset();
  final QNm name=new QNm();
  if (curr('"') || curr('\'')) {
    name.uri(stringLiteral());
    if (!consume(':')) {
      qp=p;
      return null;
    }
  }
  return name;
}
