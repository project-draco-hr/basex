{
  skipWS();
  Expr name;
  if (XMLToken.isNCStartChar(curr())) {
    name=new QNm(qName(null));
  }
 else {
    if (!consumeWS2(BRACE1))     return null;
    name=check(expr(),NOTAGNAME);
    check(BRACE2);
  }
  if (!consumeWS2(BRACE1))   return null;
  final Expr e=expr();
  check(BRACE2);
  return new CElem(input(),name,new Atts(),true,e == null ? new Expr[0] : new Expr[]{e});
}
