{
  char c=curr();
  if (!XMLToken.isNCStartChar(c)) {
    if (!first)     --qp;
    return false;
  }
  if (!first)   tok.add(':');
  do {
    tok.add(consume());
    c=curr();
  }
 while (XMLToken.isNCChar(c));
  return true;
}
