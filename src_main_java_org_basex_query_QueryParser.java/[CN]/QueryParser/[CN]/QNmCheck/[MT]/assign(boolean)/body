{
  if (name.hasPrefix()) {
    name.uri(ctx.ns.uri(name.prefix()));
    if (check && !name.hasURI())     error(NOURI,name);
  }
 else   if (nsElem) {
    name.uri(ctx.nsElem);
  }
  return name.hasURI();
}
