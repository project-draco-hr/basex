{
  prepareExecution();
  int sd=0;
  for (  final UpdatePrimitive p : prim) {
    p.apply();
    if (p instanceof StructuralUpdate)     sd+=((StructuralUpdate)p).preShifts();
  }
  if (adjINT)   return sd - resolveInternalTextNodeAdjacency();
  return sd;
}
