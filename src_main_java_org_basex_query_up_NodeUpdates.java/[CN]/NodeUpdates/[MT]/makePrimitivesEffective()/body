{
  prepareExecution();
  int sd=0;
  for (int i=0; i < prim.length; i++) {
    final UpdatePrimitive p=prim[i];
    p.apply();
    if (p instanceof StructuralUpdate)     sd+=((StructuralUpdate)p).getPreShifts();
  }
  if (adjINT)   return sd - resolveInternalTextNodeAdjacency();
  return sd;
}
