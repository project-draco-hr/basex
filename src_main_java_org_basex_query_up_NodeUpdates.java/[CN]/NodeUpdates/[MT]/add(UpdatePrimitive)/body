{
  if (prim[0] == null) {
    prim[0]=p;
    return;
  }
  final PrimitiveType pt=p.type;
  for (int i=0; i < prim.length; i++) {
    if (prim[i].type == pt) {
      prim[i].merge(p);
      return;
    }
  }
  addToPrimitives(p);
}
