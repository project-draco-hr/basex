{
  if (prim[0] == null) {
    prim[0]=p;
    return;
  }
  final PrimitiveType pt=p.type;
  for (  final UpdatePrimitive up : prim) {
    if (up.type == pt) {
      up.merge(p);
      return;
    }
  }
  addToPrimitives(p);
}
