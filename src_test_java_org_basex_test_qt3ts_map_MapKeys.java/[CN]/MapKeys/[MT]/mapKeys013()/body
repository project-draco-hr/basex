{
  final XQuery query=new XQuery("map:keys(map:remove(map{\"a\":=1,\"b\":=2}, \"c\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertQuery("$result = \"a\"") && assertQuery("$result = \"b\"") && assertCount(2)));
}
