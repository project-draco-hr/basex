{
  final XQuery query=new XQuery("map:keys(map{\"a\":=1, \"b\":=2})",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertQuery("$result = \"a\"") && assertQuery("$result = \"b\"") && assertCount(2)));
}
