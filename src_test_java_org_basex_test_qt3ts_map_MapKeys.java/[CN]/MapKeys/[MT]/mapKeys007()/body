{
  final XQuery query=new XQuery("map:keys(map{\"a\":=1, \"a\":=2})",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertCount(1) && assertType("xs:string") && assertEq("\"a\"")));
}
