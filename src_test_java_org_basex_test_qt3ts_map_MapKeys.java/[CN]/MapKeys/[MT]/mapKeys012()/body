{
  final XQuery query=new XQuery("map:keys(map{number('NaN'):=1,\"b\":=2})",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertCount(1) && assertType("xs:string") && assertEq("\"b\"")));
}
