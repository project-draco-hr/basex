{
  ns=n;
  if (!visible())   return;
  try {
    final ArrayOutput ao=new ArrayOutput(gui.context.prop.num(Prop.MAXTEXT));
    if (n != null)     n.serialize(new XMLSerializer(ao));
    setText(ao,null);
    refresh=false;
  }
 catch (  final IOException ex) {
    Main.debug(ex);
  }
}
