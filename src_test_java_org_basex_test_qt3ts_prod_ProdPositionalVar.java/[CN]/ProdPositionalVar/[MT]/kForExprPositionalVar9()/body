{
  final XQuery query=new XQuery("deep-equal((1, 2, 3), for $i at $p in string-to-codepoints(\"abc\") return $p)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
