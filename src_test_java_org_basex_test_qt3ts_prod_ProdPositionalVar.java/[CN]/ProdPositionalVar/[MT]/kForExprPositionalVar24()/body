{
  final XQuery query=new XQuery("empty(for $i at $p in subsequence((1, 2, 3, current-time()), 5) return $p)",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPST0005")));
}
