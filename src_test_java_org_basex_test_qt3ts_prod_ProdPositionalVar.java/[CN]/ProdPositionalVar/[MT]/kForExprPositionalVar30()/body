{
  final XQuery query=new XQuery("deep-equal(for $i at $p in (1, 2, 3, 4) return ($i, $p), (1, 1, 2, 2, 3, 3, 4, 4))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
