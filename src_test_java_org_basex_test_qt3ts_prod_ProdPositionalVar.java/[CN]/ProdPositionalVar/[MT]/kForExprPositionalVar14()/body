{
  final XQuery query=new XQuery("deep-equal((1, 2, 3, 4), for $i at $p in insert-before((1, current-time()), 2, (current-date(), 3)) return $p)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
