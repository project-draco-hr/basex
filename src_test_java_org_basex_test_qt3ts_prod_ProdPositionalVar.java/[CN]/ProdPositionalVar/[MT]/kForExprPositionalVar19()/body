{
  final XQuery query=new XQuery("deep-equal((1, 2, 3), for $i at $p in remove((1, 2, current-time()), 10) return $p)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
