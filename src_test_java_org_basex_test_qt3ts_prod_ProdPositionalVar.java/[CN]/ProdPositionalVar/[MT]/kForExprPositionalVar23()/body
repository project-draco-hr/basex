{
  final XQuery query=new XQuery("1 eq (for $i at $p in subsequence((1, 2, 3, current-time()), 1, 1) return $p)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
