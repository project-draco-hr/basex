{
  final XQuery query=new XQuery("for $a at $p1 in (1, 2), $b at $p2 in (1, 2), $c at $p3 in (1, 2) return 1, $p3",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
