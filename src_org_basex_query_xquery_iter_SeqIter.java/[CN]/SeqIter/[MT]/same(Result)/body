{
  if (!(v instanceof SeqIter))   return false;
  final SeqIter sb=(SeqIter)v;
  if (size != sb.size)   return false;
  try {
    for (int i=0; i < size; i++) {
      if (item[i].type != sb.item[i].type || !item[i].eq(sb.item[i]))       return false;
    }
  }
 catch (  final XQException e) {
    return false;
  }
  return true;
}
