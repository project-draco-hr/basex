{
  add(o);
  if (o instanceof DBCreate) {
    if (!ctx.context.perm(Perm.CREATE,null))     BASX_PERM.thrw(o.getInfo(),Perm.CREATE);
  }
 else   if (!ctx.context.perm(Perm.WRITE,o.getData().meta))   BASX_PERM.thrw(o.getInfo(),Perm.WRITE);
}
