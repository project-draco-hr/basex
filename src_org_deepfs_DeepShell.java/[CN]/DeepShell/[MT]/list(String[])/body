{
  if (args.length != 2) {
    help(new String[]{"help","list"});
    return;
  }
  byte[][] dents=fs.readdir(args[1]);
  if (dents == null)   System.err.printf("listing failed.\n");
  for (  byte[] de : dents)   BaseX.out(">> " + string(de));
}
