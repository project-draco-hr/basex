{
  try {
    System.loadLibrary(libName);
    Main.debug("Loading library ... OK (" + libName + ").");
    if (libName.equals(SPOTEXLIBNAME)) {
      spotexLoaded=true;
      return;
    }
    if (libName.equals(JSDBFSLIBNAME)) {
      jsdbfsLoaded=true;
      return;
    }
  }
 catch (  UnsatisfiedLinkError e) {
    Main.errln("Loading library failed (" + libName + ")."+ e);
    Main.errln("-Djava.library.path is : '" + System.getProperty("java.library.path") + "'");
  }
}
