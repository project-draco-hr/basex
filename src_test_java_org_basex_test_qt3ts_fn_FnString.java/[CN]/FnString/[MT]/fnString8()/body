{
  final XQuery query=new XQuery("string(QName(\"http://www.w3.org/\", \"test:example\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("\"test:example\""));
}
