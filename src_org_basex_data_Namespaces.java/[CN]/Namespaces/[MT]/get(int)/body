{
  final Node node=root.find(p);
  final int[] ns=new int[node.key.length << 1];
  for (int n=0; n < ns.length; n+=2) {
    ns[n]=node.key[n >> 1];
    ns[n + 1]=node.val[n >> 1];
  }
  return ns;
}
