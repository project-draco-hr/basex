{
  if (eq(XML,pref))   return 0;
  final int k=id(pref);
  if (k == 0)   return 0;
  NSNode nd=node;
  while (nd != null) {
    final int i=nd.uri(k);
    if (i != 0)     return i;
    nd=nd.par;
  }
  return 0;
}
