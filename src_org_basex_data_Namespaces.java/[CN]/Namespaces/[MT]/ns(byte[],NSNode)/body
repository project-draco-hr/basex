{
  if (Token.eq(Token.XML,pr))   return 0;
  final int k=pref.id(pr);
  if (k == 0)   return 0;
  NSNode nd=node;
  while (nd != null) {
    final int i=nd.uri(k);
    if (i != 0)     return i;
    nd=nd.par;
  }
  return 0;
}
