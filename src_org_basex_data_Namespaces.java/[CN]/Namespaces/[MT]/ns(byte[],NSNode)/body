{
  if (eq(XML,p))   return 0;
  NSNode nd=node;
  final int k=id(p);
  if (k == 0)   return 0;
  while (nd != null) {
    final int i=nd.get(k);
    if (i != 0)     return i;
    nd=nd.par;
  }
  return 0;
}
