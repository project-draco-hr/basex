{
  if (Token.eq(Token.XML,pr))   return 0;
  final int i=pref.id(pr);
  if (i == 0)   return 0;
  NSNode nd=node;
  while (nd != null) {
    final int u=nd.uri(i);
    if (u != 0)     return u;
    nd=nd.par;
  }
  return 0;
}
