{
  final XQuery query=new XQuery("prefix-from-QName( QName(\"example.com/\", \"pre:lname\")) eq \"pre\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
