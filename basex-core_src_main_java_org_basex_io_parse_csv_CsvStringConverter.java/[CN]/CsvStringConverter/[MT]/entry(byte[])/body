{
  final byte[] name=headers.get(col++);
  byte[] elem=ENTRY, attr=null;
  if (atts) {
    attr=name;
  }
 else   if (name != null) {
    elem=name;
  }
  xml.open(elem,NAME,attr);
  xml.text(entry);
  xml.close();
}
