{
  final XQuery query=new XQuery("fn:hours-from-dateTime(xs:dateTime(\"2030-12-31T23:59:59\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"23"));
}
