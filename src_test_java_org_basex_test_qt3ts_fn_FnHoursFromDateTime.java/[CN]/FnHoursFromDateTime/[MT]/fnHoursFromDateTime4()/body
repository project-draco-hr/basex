{
  final XQuery query=new XQuery("fn:hours-from-dateTime(xs:dateTime(\"1999-12-31T12:00:00\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"12"));
}
