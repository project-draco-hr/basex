{
  try {
    table.flush();
    texts.flush();
    values.flush();
    meta.finish(size);
    tags.finish(meta.dbname);
    atts.finish(meta.dbname);
  }
 catch (  final IOException e) {
    e.printStackTrace();
  }
}
