{
  try {
    table.flush();
    texts.flush();
    values.flush();
    meta.write(size);
    tags.finish(meta.dbname);
    atts.finish(meta.dbname);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
