{
  final long o=textOff(pre);
  if ((o & 0x8000000000L) != 0) {
    final byte[] t=token(o);
    return Token.substring(t,Math.min(t.length,off),Math.min(t.length,off + len));
  }
  byte[] t=texts.readToken(off);
  return Token.substring(t,Math.min(t.length,off),Math.min(t.length,off + len));
}
