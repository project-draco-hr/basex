{
  if (!ok)   return;
  super.close();
  gui.context.prop.set(Prop.EXPORTER,"," + SerializerProp.S_INDENT[0] + "="+ (format.isSelected() ? YES : NO)+ ","+ SerializerProp.S_ENCODING[0]+ "="+ encoding.getSelectedItem()+ ","+ SerializerProp.S_OMIT_XML_DECLARATION[0]+ "="+ NO);
}
