{
  if (!ok)   return;
  super.close();
  gui.context.prop.set(Prop.EXPORTER,"," + S_INDENT[0] + "="+ (format.isSelected() ? YES : NO)+ ","+ S_ENCODING[0]+ "="+ encoding.getSelectedItem()+ ","+ S_OMIT_XML_DECLARATION[0]+ "="+ NO);
}
