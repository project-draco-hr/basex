{
  if (!ok)   return;
  super.close();
  gui.context.prop.set(Prop.EXPORTER,"indent=" + (format.isSelected() ? SerializeProp.YES : SerializeProp.NO) + ",encoding="+ encoding.getSelectedItem()+ ",omit-xml-declaration=no");
}
