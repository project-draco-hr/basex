{
  boolean more=false;
  while (consume('/')) {
    final Step step=step();
    if (step == null) {
      if (more)       error(NOLOCSTEP);
      break;
    }
    path.steps.add(step);
    more=true;
  }
  return path;
}
