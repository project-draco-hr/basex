{
  boolean more=false;
  while (consume('/')) {
    final char c=curr();
    if (c != 0 && c != ' ' && c != ']' && c != ')' && c != '|' && c != ',') {
      path.steps.add(step());
      more=true;
    }
 else     if (more) {
      error(NOLOCSTEP);
    }
  }
  return path;
}
