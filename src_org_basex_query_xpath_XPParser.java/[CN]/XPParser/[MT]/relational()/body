{
  Expr e=ftContains();
  consumeWS();
  while (true) {
    final Comp c=cmp(new Comp[]{Comp.LE,Comp.LT,Comp.GE,Comp.GT});
    if (c == null)     return e;
    e=new Relational(e,ftContains(),c);
  }
}
