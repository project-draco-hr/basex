{
  Expr e=relational();
  consumeWS();
  while (true) {
    final Comp c=cmp(new Comp[]{Comp.EQ,Comp.NE,Comp.APPRWORD,Comp.APPR});
    if (c == null)     return e;
    e=new Equality(e,relational(),c);
  }
}
