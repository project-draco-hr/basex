{
  tk.reset();
  if (letter(curr())) {
    while (!curr(0)) {
      tk.add(consume());
      final char c=curr();
      if (c == ':' && next() != ':')       continue;
      if (!letterOrDigit(c) && c != '-')       break;
    }
  }
  return tk.toString();
}
