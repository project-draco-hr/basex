{
  Expr e=parseRelational();
  consumeWS();
  while (true) {
    final Comp expr=cmp(new Comp[]{Comp.WORD,Comp.EQ,Comp.NE,Comp.APPRWORD,Comp.APPR});
    if (expr == null)     return e;
    e=new Equality(e,parseRelational(),expr);
  }
}
