{
  final FTArrayExpr e=ftAnd();
  if (!consume(FTOR))   return e;
  consumeWS();
  if (consume(FTNOT))   error(FTNOTEXC);
  FTArrayExpr[] list={e};
  do   list=add(list,ftAnd());
 while (consume(FTOR));
  return new FTOr(list);
}
