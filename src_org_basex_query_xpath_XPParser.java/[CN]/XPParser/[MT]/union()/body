{
  final Expr e=path();
  consumeWS();
  if (!consume('|'))   return e;
  Expr[] list={nodeCheck(e)};
  do   list=Array.add(list,nodeCheck(path()));
 while (consume('|'));
  return new Union(list);
}
