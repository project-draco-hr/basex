{
  final StringBuilder sb=new StringBuilder(new String(name()) + "(");
  for (int i=0; i != exprs.length; i++) {
    if (i != 0)     sb.append(", ");
    sb.append(exprs[i]);
  }
  sb.append(')');
  return sb.toString();
}
