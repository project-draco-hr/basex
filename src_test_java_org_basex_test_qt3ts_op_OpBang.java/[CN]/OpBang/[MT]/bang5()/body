{
  final XQuery query=new XQuery("(\"red\", \"blue\", \"green\") ! string-length() ! (.+1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("4, 5, 6"));
}
