{
  final XQuery query=new XQuery("2 + (/ works ! employee[2] ! hours[2]) ! number() ! (-.)",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("-18"));
}
