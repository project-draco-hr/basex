{
  final XQuery query=new XQuery("/ works ! employee[4] ! preceding-sibling::*[1] ! string(@name) ",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("\"Jane Doe 3\""));
}
