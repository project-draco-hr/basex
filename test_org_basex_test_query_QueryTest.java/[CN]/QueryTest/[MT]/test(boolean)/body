{
  System.out.println("Testing " + (xquery ? "XQuery" : "XPath") + " (Index "+ (Prop.ftindex ? "ON" : "OFF")+ ")");
  boolean ok=true;
  ok&=test(xquery,new FTTest());
  System.out.println(ok ? "All tests correct.\n" : "Wrong results...\n");
  return ok;
}
