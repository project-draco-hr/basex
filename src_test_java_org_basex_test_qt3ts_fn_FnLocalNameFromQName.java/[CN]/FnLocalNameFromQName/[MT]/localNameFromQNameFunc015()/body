{
  final XQuery query=new XQuery("fn:local-name-from-QName((//Folder)[1])",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
