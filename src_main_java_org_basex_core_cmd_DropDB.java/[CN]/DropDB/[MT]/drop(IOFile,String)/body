{
  boolean ok=path.exists();
  for (  final IOFile sub : path.children()) {
    ok&=sub.isDir() ? drop(sub,pat) : pat != null && !sub.name().matches(pat) || sub.delete();
  }
  return (pat != null || path.delete()) && ok;
}
