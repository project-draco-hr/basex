{
  final XQuery query=new XQuery("if (fn:true()) then <elem1/> else <elem2/>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<elem1/>",false));
}
