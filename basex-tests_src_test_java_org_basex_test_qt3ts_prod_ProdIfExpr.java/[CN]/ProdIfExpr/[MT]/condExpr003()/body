{
  final XQuery query=new XQuery("if (fn:false()) then <elem1/> else <elem2/>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<elem2/>",false));
}
