{
  final XQuery query=new XQuery("map:size(map:remove(map:remove(map{\"a\":=1,\"b\":=2},\"b\"),\"a\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("0") && assertType("xs:integer")));
}
