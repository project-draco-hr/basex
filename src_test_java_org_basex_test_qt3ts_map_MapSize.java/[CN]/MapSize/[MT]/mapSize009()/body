{
  final XQuery query=new XQuery("map:size(map:remove(map{\"a\":=1,\"b\":=2}, \"b\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("1") && assertType("xs:integer")));
}
