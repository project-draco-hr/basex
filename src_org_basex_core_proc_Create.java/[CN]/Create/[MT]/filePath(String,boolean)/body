{
  File f=new File(file.replace('\\','/'));
  boolean found=f.exists();
  if (!found && addXML) {
    f=new File(file + XMLSUFFIX);
    found=f.exists();
  }
  if (!found)   throw new FileNotFoundException(BaseX.info(FILEWHICH,f.getAbsoluteFile()));
  return f.getAbsolutePath();
}
