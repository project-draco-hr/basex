{
  try {
    while (true) {
      try (final Socket s=socket.accept()){
      }
       if (stop.exists()) {
        socket.close();
        stop.delete();
        jetty.stop();
        break;
      }
    }
  }
 catch (  final Exception ex) {
    Util.errln(ex);
  }
}
