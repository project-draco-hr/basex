{
  final XQuery query=new XQuery("<a xmlns=\"http://www.w3.org/\" xmlns=\"http://www.w3.org/\" />",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0071"));
}
