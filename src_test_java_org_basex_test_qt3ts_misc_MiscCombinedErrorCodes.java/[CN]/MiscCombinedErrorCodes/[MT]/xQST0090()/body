{
  final XQuery query=new XQuery("<bad-character-reference>&#xa999999999999999a;</bad-character-reference>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0090"));
}
