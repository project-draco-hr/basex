{
  final Query q=s.query(DATA.args(_DB_INFO.args("$p") + "/descendant::" + TIME+ "[1]"));
  q.bind("p",d);
  try {
    return InfoDB.DATE.parse(q.execute()).getTime();
  }
 catch (  final Exception ex) {
    Util.errln(ex);
    return 0;
  }
}
