{
  int l=0, h=curr.children() - 1;
  while (l <= h) {
    final int m=l + h >>> 1, c=curr.child(m).pre() - pre;
    if (c == 0)     return m;
    if (c < 0)     l=m + 1;
 else     h=m - 1;
  }
  return l;
}
