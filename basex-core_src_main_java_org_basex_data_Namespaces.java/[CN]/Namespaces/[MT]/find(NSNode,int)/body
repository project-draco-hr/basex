{
  int l=0, h=curr.sz - 1;
  while (l <= h) {
    final int m=l + h >>> 1;
    final int c=curr.children[m].pr - pre;
    if (c == 0)     return m;
    if (c < 0)     l=m + 1;
 else     h=m - 1;
  }
  return l;
}
