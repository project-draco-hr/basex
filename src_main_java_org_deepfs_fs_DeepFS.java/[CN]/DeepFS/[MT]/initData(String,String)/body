{
  final Context ctx=new Context();
  try {
    new Open(name).execute(ctx);
  }
 catch (  final BaseXException ex) {
    try {
      new CreateDB(name,"<" + S_DEEPFS + " "+ "mountpoint=\""+ mp+ "\"/>").execute(ctx);
    }
 catch (    final BaseXException exx) {
      Util.notexpected(exx);
    }
  }
  return ctx;
}
