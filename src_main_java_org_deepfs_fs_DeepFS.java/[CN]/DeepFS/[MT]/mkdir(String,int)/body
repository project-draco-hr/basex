{
  final String method="[mkdir] ";
  final int n=createNode(path,getSIFDIR() | mode);
  Main.debug(method + "path: " + path+ " mode: "+ Integer.toOctalString(mode)+ " id : ("+ n+ ")");
  return n;
}
