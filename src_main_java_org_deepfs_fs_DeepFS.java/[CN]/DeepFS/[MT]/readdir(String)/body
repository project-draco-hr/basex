{
  final int[] cld=path2preChildren(path);
  if (cld == null)   return null;
  final int len=cld.length;
  final byte[][] dents=new byte[len][];
  for (int i=0; i < len; ++i)   dents[i]=attr(cld[i],nameID);
  return dents;
}
