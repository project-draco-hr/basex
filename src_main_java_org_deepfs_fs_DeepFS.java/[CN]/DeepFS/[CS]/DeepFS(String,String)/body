{
  ctx=new Context();
  if (!new Open(dbname).exec(ctx))   new CreateDB("<" + S_DEEPFS + " "+ "mountpoint=\""+ mountpoint+ "\"/>",dbname).exec(ctx);
  data=ctx.data;
  initNames();
  initRootStat();
}
