{
  final XQuery query=new XQuery("for $var in (\"a\",\"b\",\"c\") where fn:sum(($var,1)) return $var",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
