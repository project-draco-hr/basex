{
  final XQuery query=new XQuery("for $var in (\"a\",\"b\",\"c\") where ($var * 1) = 3 return $var",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
