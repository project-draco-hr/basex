{
  final XQuery query=new XQuery(" fn:count(fn:dateTime(xs:date(\"1999-12-31+10:00\"), ()))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"0"));
}
