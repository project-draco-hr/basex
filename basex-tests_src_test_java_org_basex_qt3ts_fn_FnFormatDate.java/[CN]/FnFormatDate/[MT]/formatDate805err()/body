{
  final XQuery query=new XQuery("format-date(current-date(), '[m]', 'en', (), ())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((error("XTDE1350") || error("FOFD1350")));
}
