{
  if (enc == UTF8 || ascii(s))   return s;
  try {
    return new String(s,enc).getBytes(UTF8);
  }
 catch (  final Exception ex) {
    BaseX.debug(ex);
    BaseX.errln(ex);
    return EMPTY;
  }
}
