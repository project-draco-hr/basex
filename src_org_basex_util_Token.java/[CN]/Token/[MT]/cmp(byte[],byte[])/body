{
  final int l=(tok.length > tok2.length) ? tok2.length : tok.length;
  for (int t=0; t != l; t++) {
    if (tok2[t] > tok[t])     return 1;
 else     if (tok2[t] < tok[t])     return -1;
  }
  return 0;
}
