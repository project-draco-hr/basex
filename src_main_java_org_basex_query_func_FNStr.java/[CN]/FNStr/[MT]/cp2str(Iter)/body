{
  final TokenBuilder tb=new TokenBuilder();
  for (Item i; (i=iter.next()) != null; ) {
    final long n=checkItr(i);
    if (n < Integer.MIN_VALUE || n > Integer.MAX_VALUE || !XMLToken.valid((int)n))     INVCODE.thrw(input,i);
    tb.add((int)n);
  }
  return Str.get(tb.finish());
}
