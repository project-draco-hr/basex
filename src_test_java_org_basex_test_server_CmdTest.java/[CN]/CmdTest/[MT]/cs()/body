{
  no(new Cs("//li"));
  ok(new CreateDB(NAME,FILE));
  ok(new Cs("//  li"));
  ok(CONTEXT.current(),2);
  ok(new Cs("."));
  ok(CONTEXT.current(),2);
  ok(new Cs("/"));
  ok(CONTEXT.current(),1);
}
