{
  if (first == null)   first=new Node(pre,-count);
 else {
    Node pointer=first;
    Node b4pointer=null;
    while (pointer != null && pointer.id < pre) {
      b4pointer=pointer;
      pointer=pointer.next;
    }
    if (pointer != null && pointer.id == pre)     pointer.addend-=count;
 else {
      Node newNode=new Node(pre,-count);
      if (b4pointer == null)       first=newNode;
 else       b4pointer.next=newNode;
      newNode.next=pointer;
    }
  }
}
