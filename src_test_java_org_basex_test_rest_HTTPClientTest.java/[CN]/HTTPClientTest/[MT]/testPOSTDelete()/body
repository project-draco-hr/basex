{
  final Command delete=new XQuery("http:send-request(" + "<http:request method='delete' status-only='true'/>, " + URL + ")");
  delete.execute(context);
  checkResponse(delete,HttpURLConnection.HTTP_OK,1);
}
