{
  final QueryProcessor qp=new QueryProcessor("http:send-request(" + "<http:request method='delete' status-only='true'/>, '" + RESTURL + "')",CONTEXT);
  checkResponse(qp.execute(),HttpURLConnection.HTTP_OK,1);
  qp.close();
}
