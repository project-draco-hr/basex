{
  try {
    ctx=new Context();
    ctx.data(CreateDB.xml(ctx,IO.get(TESTFILE),dbname));
    size=ctx.data().meta.size;
  }
 catch (  final Exception ex) {
    ex.printStackTrace();
    System.exit(1);
  }
}
