{
  final XQuery query=new XQuery("let $initial := fn:substring(?, 1, 1) \n" + "            return fn:function-arity( $initial )",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"1"));
}
