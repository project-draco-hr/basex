{
  return out instanceof PrintOutput ? (PrintOutput)out : new PrintOutput(out instanceof ByteArrayOutputStream || out instanceof BufferedOutputStream || out instanceof BufferedOutput|| out instanceof ArrayOutput ? out : new BufferedOutput(out));
}
