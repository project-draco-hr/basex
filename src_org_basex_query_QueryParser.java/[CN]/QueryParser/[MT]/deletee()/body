{
  final int p=qp;
  if (!consumeWS(DELETE))   return null;
  if (!consumeWS(NODE)) {
    qp=p;
    return null;
  }
  final Expr n=check(single(),INCOMPLETE);
  error(UPIMPL);
  return new Delete(n);
}
