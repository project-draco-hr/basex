{
  try {
    for (    final byte[] sl : split(norm(fl.content()),' ')) {
      if (e)       delete(sl);
 else       if (id(sl) == 0)       add(sl);
    }
    return true;
  }
 catch (  final IOException ex) {
    return false;
  }
}
