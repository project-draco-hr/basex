{
  boolean refreshed=false;
  for (  final EditorNode node : selected()) {
    if (node.file.isDir()) {
      node.collapse();
      node.expand();
      refreshed=true;
    }
  }
  if (refreshed)   repaint();
}
