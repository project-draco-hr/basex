{
  zoomstep=ZOOM.length;
  while (--zoomstep >= 0) {
    scroll.height(tdata.rows.size * tdata.rowH(ZOOM[zoomstep]));
    repaint();
    Performance.sleep(25);
  }
  updating=false;
  findFocus();
}
