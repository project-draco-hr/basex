{
  final int calls=checkHeight(ctx);
  final VarStack cs=addArgs(ctx,args(ctx));
  try {
    final Value v=ctx.value(func);
    return v;
  }
  finally {
    ctx.vars.reset(cs);
    ctx.tailCalls=calls;
  }
}
