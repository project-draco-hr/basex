{
  final int calls=checkHeight(ctx);
  final VarStack cs=addArgs(ctx,args(ctx));
  try {
    final Item it=func.item(ctx,ii);
    return it;
  }
  finally {
    ctx.vars.reset(cs);
    ctx.tailCalls=calls;
  }
}
