{
  try {
    if (iter == null) {
      qp.parse();
      monitored=true;
      ctx.lock.before(qp.ctx.updating);
      iter=qp.iter();
    }
    item=iter.next();
    return item != null;
  }
 catch (  final QueryException ex) {
    throw new BaseXException(ex);
  }
}
