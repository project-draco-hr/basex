{
  final XQuery query=new XQuery(" ( if (3 != 2) then 16 else 0 ) + ( if (8 = 7) then 4 else 1 ) ",ctx);
  query.context(node(file("prod/IfExpr/xq311A.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("17"));
}
