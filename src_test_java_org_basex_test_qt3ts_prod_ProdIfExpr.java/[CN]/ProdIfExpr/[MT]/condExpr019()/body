{
  final XQuery query=new XQuery("if //File[1] then \"true\" else \"false\"",ctx);
  query.context(node(file("prod/ForClause/fsx.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
