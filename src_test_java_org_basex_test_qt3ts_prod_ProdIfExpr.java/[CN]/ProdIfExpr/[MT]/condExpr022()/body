{
  final XQuery query=new XQuery("if (doc-available('nodocument.xml')) then doc('nodocument.xml') else 10 cast as xs:double",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("10"));
}
