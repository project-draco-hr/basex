{
  final XQuery query=new XQuery("if (//NodeDoesNotExist) then <elem1/> else <elem2/>",ctx);
  query.context(node(file("prod/AxisStep/nw_Customers.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem2/>",false));
}
