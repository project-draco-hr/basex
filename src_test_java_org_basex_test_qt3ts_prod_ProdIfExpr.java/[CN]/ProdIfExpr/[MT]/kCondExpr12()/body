{
  final XQuery query=new XQuery("if(boolean((1, 2, 3, current-time())[1] treat as xs:integer)) then true() else 4",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
