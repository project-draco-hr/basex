{
  final XQuery query=new XQuery("(if((1, current-time())[1] treat as xs:integer) then true() else false()) eq true()",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
