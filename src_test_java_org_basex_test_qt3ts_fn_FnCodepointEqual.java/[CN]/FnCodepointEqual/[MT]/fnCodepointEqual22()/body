{
  final XQuery query=new XQuery("fn:codepoint-equal(normalize-unicode(\"gar??on\", \"NFC\"), normalize-unicode(\"gar??on\", \"NFD\"))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
