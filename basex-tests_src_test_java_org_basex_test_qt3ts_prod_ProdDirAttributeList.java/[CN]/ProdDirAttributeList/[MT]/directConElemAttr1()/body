{
  final XQuery query=new XQuery("<shoe name=\" \"\"\"\" \"/>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<shoe name=\" &#34;&#34; \"/>",false));
}
