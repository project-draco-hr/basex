{
synchronized (lock) {
    this.sink=os;
    try {
      send(cmd);
    }
 catch (    IOException e) {
      throw new BaseXException(e);
    }
    try {
      lock.wait();
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
}
