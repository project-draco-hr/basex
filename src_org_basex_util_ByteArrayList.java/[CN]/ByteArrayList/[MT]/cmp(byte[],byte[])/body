{
  int len=tok.length;
  if (tok.length > tok2.length)   len=tok2.length;
  for (int t=0; t != len; t++) {
    if (tok2[t] > tok[t])     return 1;
 else     if (tok2[t] < tok[t])     return -1;
  }
  return 0;
}
