{
  final BaseXFileChooser fc=new BaseXFileChooser(DIALOGFC,path.getText(),gui);
  final IO io=fc.select(file ? BaseXFileChooser.Mode.FOPEN : BaseXFileChooser.Mode.DOPEN);
  if (io != null)   path.setText(io.path());
}
