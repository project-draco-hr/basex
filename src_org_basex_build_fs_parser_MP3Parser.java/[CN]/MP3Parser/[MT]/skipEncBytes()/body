{
  bfc.buffer(3);
  int bytesToSkip=0;
  if ((bfc.get() & 0xFF) <= 0x04)   bytesToSkip++;
  if ((bfc.get() & 0xFF) >= 0xFE)   bytesToSkip++;
  if ((bfc.get() & 0xFF) >= 0xFE)   bytesToSkip++;
  bfc.skip(bytesToSkip - 3);
  return bytesToSkip;
}
