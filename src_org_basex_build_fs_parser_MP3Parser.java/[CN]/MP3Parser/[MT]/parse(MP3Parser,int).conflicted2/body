{
  int skipped=obj.skipEncBytes() + 1;
  byte[] lang=obj.readText(3);
  obj.bfc.get();
  if (Token.ws(lang) || lang[0] == 'X')   lang=null;
  obj.fsparser.metaEvent(Element.COMMENT,DataType.STRING,Definition.NONE,lang,obj.readText(size - 3 - skipped));
}
