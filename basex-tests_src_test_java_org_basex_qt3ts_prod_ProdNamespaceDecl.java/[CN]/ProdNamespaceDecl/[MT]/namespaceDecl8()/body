{
  final XQuery query=new XQuery("declare namespace foo:bar = \"http://www.example.com/examples\"; \"aa\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0003"));
}
