{
  final XQuery query=new XQuery("every $foo in 1 satisfies $bar + (some $bar in 2 satisfies $bar)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0008"));
}
