{
  final XQuery query=new XQuery("some $a in (1, 2), $b in (1, 2) satisfies 1, $b",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0008"));
}
