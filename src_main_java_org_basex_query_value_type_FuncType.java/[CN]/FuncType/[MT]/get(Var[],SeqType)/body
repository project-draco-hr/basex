{
  final SeqType[] at=new SeqType[args.length];
  for (int a=0; a < at.length; a++)   at[a]=args[a] == null ? SeqType.ITEM_ZM : args[a].declaredType();
  return new FuncType(at,ret == null ? SeqType.ITEM_ZM : ret);
}
