{
  final SeqType[] at=new SeqType[f.args.length];
  for (int a=0; a < at.length; a++)   at[a]=f.args[a] == null ? SeqType.ITEM_ZM : f.args[a].declaredType();
  return new FuncType(at,f.ret == null ? SeqType.ITEM_ZM : f.ret);
}
