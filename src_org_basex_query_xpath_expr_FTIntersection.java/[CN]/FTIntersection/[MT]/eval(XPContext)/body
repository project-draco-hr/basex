{
  for (  int i : pex) {
    final Bool it=(Bool)exprs[i].eval(ctx);
    if (!it.bool())     return it;
  }
  for (  int i : nex) {
    final Bool it=(Bool)exprs[i].eval(ctx);
    if (!it.bool())     return it;
  }
  return Bool.TRUE;
}
