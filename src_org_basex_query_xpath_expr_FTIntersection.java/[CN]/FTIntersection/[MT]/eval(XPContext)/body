{
  for (  int i : pex) {
    final Bln it=(Bln)exprs[i].eval(ctx);
    if (!it.bool())     return it;
  }
  for (  int i : nex) {
    final Bln it=(Bln)exprs[i].eval(ctx);
    if (!it.bool())     return it;
  }
  return Bln.TRUE;
}
