{
  final XQuery query=new XQuery("(<?alpha?>, <?beta?>, <?gamma?>)/processing-instruction(Q{}alpha)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0003"));
}
