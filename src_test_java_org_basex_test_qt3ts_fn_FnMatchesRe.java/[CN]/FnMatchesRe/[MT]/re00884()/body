{
  final XQuery query=new XQuery("matches('qwerty','((?s)^a(.))((?m)^b$)')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORX0002"));
}
