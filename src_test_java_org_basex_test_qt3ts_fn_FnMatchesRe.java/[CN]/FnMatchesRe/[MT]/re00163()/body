{
  final XQuery query=new XQuery("(every $s in tokenize('\u2028', ',') satisfies matches($s, '^(?:\\p{Zl}*)$')) and (every $s in tokenize('\u2029', ',') satisfies not(matches($s, '^(?:\\p{Zl}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
