{
  final XQuery query=new XQuery("(every $s in tokenize('\t', ',') satisfies matches($s, '^(?:\\P{So}*)$')) and (every $s in tokenize('???????', ',') satisfies not(matches($s, '^(?:\\P{So}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
