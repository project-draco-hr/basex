{
  final XQuery query=new XQuery("(every $s in tokenize('\u070f', ',') satisfies matches($s, '^(?:\\P{Cc}*)$')) and (every $s in tokenize('\t', ',') satisfies not(matches($s, '^(?:\\P{Cc}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
