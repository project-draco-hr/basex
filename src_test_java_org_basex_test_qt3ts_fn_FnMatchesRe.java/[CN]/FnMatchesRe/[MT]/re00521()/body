{
  final XQuery query=new XQuery("matches('qwerty','\\p{Nd}{4}-\\[{Nd}{2}')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORX0002"));
}
