{
  final XQuery query=new XQuery("(every $s in tokenize('??????????????????????????????????', ',') satisfies matches($s, '^(?:\\p{S}*)$')) and (every $s in tokenize('\u1680', ',') satisfies not(matches($s, '^(?:\\p{S}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
