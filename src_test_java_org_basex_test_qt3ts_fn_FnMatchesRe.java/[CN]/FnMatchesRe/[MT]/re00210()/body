{
  final XQuery query=new XQuery("(every $s in tokenize('\ue000', ',') satisfies matches($s, '^(?:\\P{Cf}*)$')) and (every $s in tokenize('\u070f\udb40\udc78', ',') satisfies not(matches($s, '^(?:\\P{Cf}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
