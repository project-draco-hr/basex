{
  final XQuery query=new XQuery("matches('qwerty','[\\u0100\\u0102\\u0104]+')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORX0002"));
}
