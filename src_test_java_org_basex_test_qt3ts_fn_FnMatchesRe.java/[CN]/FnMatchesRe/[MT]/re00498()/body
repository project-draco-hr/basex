{
  final XQuery query=new XQuery("(every $s in tokenize('_,:,a', ',') satisfies matches($s, '^(?:\\c)$')) and (every $s in tokenize(' ,\n" + ",\r,\t', ',') satisfies not(matches($s, '^(?:\\c)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
