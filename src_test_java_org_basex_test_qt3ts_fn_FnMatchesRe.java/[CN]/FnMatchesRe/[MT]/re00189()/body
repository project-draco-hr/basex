{
  final XQuery query=new XQuery("(every $s in tokenize('??', ',') satisfies matches($s, '^(?:\\P{Nl}*)$')) and (every $s in tokenize('???????', ',') satisfies not(matches($s, '^(?:\\P{Nl}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
