{
  final XQuery query=new XQuery("(every $s in tokenize('', ',') satisfies matches($s, '^(?:(\\d*){0,2}a\\1)$')) and (every $s in tokenize('33a34', ',') satisfies not(matches($s, '^(?:(\\d){0,2}a\\1)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
