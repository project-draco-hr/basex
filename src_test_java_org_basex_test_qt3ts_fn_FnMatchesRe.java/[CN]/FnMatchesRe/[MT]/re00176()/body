{
  final XQuery query=new XQuery("(every $s in tokenize('_,???', ',') satisfies matches($s, '^(?:\\P{L}*)$')) and (every $s in tokenize('aAbB,A????aa??????????????????????????', ',') satisfies not(matches($s, '^(?:\\P{L}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
