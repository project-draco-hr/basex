{
  final XQuery query=new XQuery("(every $s in tokenize('??', ',') satisfies matches($s, '^(?:\\P{Pf}*)$')) and (every $s in tokenize('??\u203a', ',') satisfies not(matches($s, '^(?:\\P{Pf}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
