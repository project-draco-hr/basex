{
  final XQuery query=new XQuery("(every $s in tokenize('??', ',') satisfies matches($s, '^(?:\\P{Lo}*)$')) and (every $s in tokenize('??????', ',') satisfies not(matches($s, '^(?:\\P{Lo}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
