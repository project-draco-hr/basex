{
  final XQuery query=new XQuery("(every $s in tokenize('\u1680\u3000\u2028\u2028\u2029\u2029', ',') satisfies matches($s, '^(?:\\p{Z}*)$')) and (every $s in tokenize('??', ',') satisfies not(matches($s, '^(?:\\p{Z}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
