{
  final XQuery query=new XQuery("matches('a&#xD;&#xA;b', '^(?:a\\r\\nb)$'), matches('ab', '^(?:a\\r\\nb)$')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("true(), false()"));
}
