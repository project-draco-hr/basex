{
  final XQuery query=new XQuery("(every $s in tokenize('??', ',') satisfies matches($s, '^(?:\\P{Ll}*)$')) and (every $s in tokenize('a????', ',') satisfies not(matches($s, '^(?:\\P{Ll}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
