{
  final XQuery query=new XQuery("(every $s in tokenize('\u1680', ',') satisfies matches($s, '^(?:\\P{Po}*)$')) and (every $s in tokenize('??\uff64', ',') satisfies not(matches($s, '^(?:\\P{Po}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
