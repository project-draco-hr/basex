{
  final XQuery query=new XQuery("matches('alpha', '^{2}alpha') and not(matches('zalpha', '^{2}alpha'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
