{
  final XQuery query=new XQuery("(every $s in tokenize('\u301e', ',') satisfies matches($s, '^(?:\\P{Ps}*)$')) and (every $s in tokenize('\u301d\uff62', ',') satisfies not(matches($s, '^(?:\\P{Ps}*)$')))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
