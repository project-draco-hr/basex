{
  session.execute(new XQuery("for $doc in collection('input/book/chapters/0/')" + " return " + "replace value of node  doc(base-uri($doc))/text/title  "+ "with (1 to 3)"),System.out);
  session.execute(new XQuery("for $doc in collection('input/book/chapters/0')" + " return $doc"),System.out);
}
