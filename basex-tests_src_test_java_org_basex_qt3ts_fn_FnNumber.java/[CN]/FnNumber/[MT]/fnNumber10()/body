{
  final XQuery query=new XQuery("number(xs:untypedAtomic(\"1000\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertType("xs:double") && assertEq("1e3")));
}
