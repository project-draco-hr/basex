{
  xquery10();
  final XQuery query=new XQuery("xquery version \"1.0\" encoding \"ISO-8859-1|\"; 1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0087"));
}
