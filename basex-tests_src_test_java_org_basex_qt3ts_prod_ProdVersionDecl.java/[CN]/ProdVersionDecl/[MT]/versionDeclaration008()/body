{
  final XQuery query=new XQuery("xquery version \"abc\"; 1,2",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0031"));
}
