{
  xquery10();
  final XQuery query=new XQuery("xquery version \"1.0\" encoding \"999-UTF-8-*\"; \"ABC\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0087"));
}
