{
  final XQuery query=new XQuery("boolean(one-or-more(month-from-date(current-date()) to 0))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0004"));
}
