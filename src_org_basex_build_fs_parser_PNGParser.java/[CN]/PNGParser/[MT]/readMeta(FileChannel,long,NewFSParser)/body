{
  if (!check(fc,limit))   return;
  byte[] width=Token.token(buf.getInt());
  byte[] height=Token.token(buf.getInt());
  fsParser.metaEvent(Element.WIDTH,DataType.INTEGER,Definition.PIXEL,null,width);
  fsParser.metaEvent(Element.HEIGHT,DataType.INTEGER,Definition.PIXEL,null,height);
}
