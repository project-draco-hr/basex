{
  if (!check(bfc))   return;
  parser.metaEvent(Element.WIDTH,DataType.INTEGER,Definition.PIXEL,null,token(bfc.getInt()));
  parser.metaEvent(Element.HEIGHT,DataType.INTEGER,Definition.PIXEL,null,token(bfc.getInt()));
}
