{
  if (!check(bfc))   return;
  fsParser.metaEvent(Element.WIDTH,DataType.INTEGER,Definition.PIXEL,null,Token.token(bfc.getInt()));
  fsParser.metaEvent(Element.HEIGHT,DataType.INTEGER,Definition.PIXEL,null,Token.token(bfc.getInt()));
}
