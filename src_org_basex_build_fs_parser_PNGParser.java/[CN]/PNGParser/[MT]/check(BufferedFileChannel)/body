{
  if (bfc.size() < HEADER_LENGTH)   return false;
  int len=HEADER.length;
  byte[] h=new byte[len];
  bfc.get(h);
  for (int i=0; i < len; i++) {
    if (h[i] != HEADER[i])     return false;
  }
  return true;
}
