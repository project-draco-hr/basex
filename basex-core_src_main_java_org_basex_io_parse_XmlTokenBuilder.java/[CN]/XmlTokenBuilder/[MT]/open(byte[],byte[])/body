{
  final TokenBuilder cch=cache;
  cch.add('<').add(name);
  final int al=atts.length;
  for (int a=0; a < al; a+=2) {
    cch.add(' ').add(atts[a]).add('=').add('"');
    attribute(atts[a + 1]);
    cch.add('"');
  }
  cch.add('>');
  open.add(name);
}
