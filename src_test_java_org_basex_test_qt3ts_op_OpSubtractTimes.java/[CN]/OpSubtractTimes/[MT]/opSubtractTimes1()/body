{
  final XQuery query=new XQuery("xs:time(\"11:12:00Z\") - xs:time(\"04:00:00Z\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"PT7H12M"));
}
