{
  final XQuery query=new XQuery("xs:time(\"11:00:00-05:00\") - xs:time(\"21:30:00+05:30\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"PT0S"));
}
