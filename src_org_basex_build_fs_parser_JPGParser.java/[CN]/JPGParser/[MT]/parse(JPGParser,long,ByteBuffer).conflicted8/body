{
  int type=buf.getShort();
  if ((type == IFD_TYPE_LONG || type == IFD_TYPE_SHORT) && buf.getInt() == 1) {
    obj.fsparser.metaEvent(Element.WIDTH,DataType.INTEGER,Definition.PIXEL,null,Token.token((long)buf.getInt() & 0xFFFFFFFF));
  }
 else   error(obj,"Image width (0x0100)");
}
