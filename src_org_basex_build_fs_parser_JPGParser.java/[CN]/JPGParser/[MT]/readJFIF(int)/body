{
  bfc.buffer(16);
  final long pos=bfc.position();
  if (!checkNextBytes(HEADER_JFIF)) {
    bfc.position(pos);
    readJFXX(size);
    return;
  }
  final int s=size - HEADER_JFIF.length;
  bfc.skip(s);
}
