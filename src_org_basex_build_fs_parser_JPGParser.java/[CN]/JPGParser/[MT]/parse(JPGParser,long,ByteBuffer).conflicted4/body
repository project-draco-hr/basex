{
  final int type=buf.getShort();
  if ((type == IFD_TYPE_LONG || type == IFD_TYPE_SHORT) && buf.getInt() == 1) {
    obj.fsparser.metaEvent(Element.HEIGHT,DataType.INTEGER,Definition.PIXEL,null,token((long)buf.getInt() & 0xFFFFFFFF));
  }
 else   error(obj,"Image length (0x0101)");
}
