{
  int size=buf.getInt();
  byte[] data=new byte[size];
  if (size <= 4) {
    buf.get(data);
  }
 else {
    bfc.position(ifdOff + buf.getInt());
    bfc.buffer(size);
    bfc.get(data);
  }
  return Token.trim(data);
}
