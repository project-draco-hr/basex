{
  final int l=op.size();
  final Integer[] t=new Integer[l];
  op.keySet().toArray(t);
  sortedPre=new int[l];
  for (int i=0; i < l; i++) {
    sortedPre[i]=t[i];
  }
  Arrays.sort(sortedPre);
  finished=true;
}
