{
  final XQuery query=new XQuery("local-name(/works/employee[1]/@name)",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"name"));
}
