{
  final XQuery query=new XQuery("fn:string(fn:local-name(element anElement {\"Some content\"}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"anElement"));
}
