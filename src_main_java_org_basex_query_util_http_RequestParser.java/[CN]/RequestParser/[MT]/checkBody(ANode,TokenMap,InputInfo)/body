{
  if (bodyAttrs.get(MEDIATYPE) == null)   REQINV.thrw(ii,"Attribute media-type of http:body is mandatory");
  if (bodyAttrs.get(SRC) != null && (bodyAttrs.size() > 2 || body.children().more()))   SRCATTR.thrw(ii);
}
