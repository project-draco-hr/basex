{
  if (bodyAttrs.get(MEDIA_TYPE) == null)   HC_REQ.thrw(info,"Attribute media-type of http:body is mandatory");
  if (bodyAttrs.get(SRC) != null && (bodyAttrs.size() > 2 || body.children().more()))   HC_ATTR.thrw(info);
}
