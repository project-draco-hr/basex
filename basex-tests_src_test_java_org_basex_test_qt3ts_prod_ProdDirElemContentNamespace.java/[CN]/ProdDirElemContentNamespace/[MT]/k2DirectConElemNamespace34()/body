{
  final XQuery query=new XQuery("<e xmlns=\"http://{exa}mple.com/\"/>/@xmlns",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0022"));
}
