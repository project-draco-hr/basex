{
  return new BXOutput(path){
    @Override String code() throws IOException {
      final ArrayOutput ao=new ArrayOutput();
      exec(cmd,ao);
      final Serializer xml=Serializer.get(out,new SerializerProp(serial(path)));
      xml.text(Token.delete(ao.toArray(),'\r'));
      xml.close();
      return session.info();
    }
  }
;
}
