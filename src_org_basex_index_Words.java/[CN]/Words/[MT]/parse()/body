{
  ts=-1;
  while (++te <= tl) {
    if (ts == -1) {
      if (te != tl && Token.ftChar(tt[te]))       ts=te;
    }
 else     if (te == tl || !Token.ftChar(tt[te])) {
      return true;
    }
  }
  tt=null;
  return false;
}
