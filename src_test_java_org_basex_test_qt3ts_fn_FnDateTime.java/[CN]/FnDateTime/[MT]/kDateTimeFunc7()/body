{
  final XQuery query=new XQuery("dateTime(xs:date(\"2004-03-04-00:01\"), xs:time(\"08:05:23Z\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0008"));
}
