{
  final XQuery query=new XQuery("typeswitch (1, 2, 3) case node() return <e/> case xs:integer return 3 default $i return 1, $i",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPST0008"));
}
