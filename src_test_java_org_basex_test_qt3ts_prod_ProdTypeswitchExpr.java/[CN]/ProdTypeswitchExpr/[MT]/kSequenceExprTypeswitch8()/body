{
  final XQuery query=new XQuery("(typeswitch(1, 2, current-time()) case element() return -1 case document-node() return -2 default return 1) eq 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
