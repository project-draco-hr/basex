{
  final XQuery query=new XQuery("typeswitch (1, 2, 3) case node() return <e/> default $i return 1 , typeswitch (1, 2, 3) case xs:integer* return $i default return 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
