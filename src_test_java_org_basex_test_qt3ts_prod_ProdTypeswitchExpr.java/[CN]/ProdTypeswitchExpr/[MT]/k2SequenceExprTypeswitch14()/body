{
  final XQuery query=new XQuery("<e/>/(typeswitch (self::node()) case $i as node() return . default return 1)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<e/>",false));
}
