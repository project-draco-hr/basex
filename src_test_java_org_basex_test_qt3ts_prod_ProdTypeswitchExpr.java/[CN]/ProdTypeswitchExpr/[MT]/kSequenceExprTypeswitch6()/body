{
  final XQuery query=new XQuery("(typeswitch(()) case xs:integer* return 1 case empty-sequence() return 1 default return -2) eq 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
