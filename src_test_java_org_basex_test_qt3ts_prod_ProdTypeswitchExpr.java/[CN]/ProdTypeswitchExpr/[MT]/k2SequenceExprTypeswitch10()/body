{
  final XQuery query=new XQuery("typeswitch (1, 2, 3) case node() return <e/> case $i as xs:integer return 3 default return 1, $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0008"));
}
