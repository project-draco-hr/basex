{
  final XQuery query=new XQuery("typeswitch (1, 2, 3) case node() return <e/> case xs:integer return 3 default $i as item() return 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPST0003"));
}
