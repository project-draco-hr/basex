{
  int s=1;
  final NodeIter at=n.attr();
  Nod fat=at.next();
  while (fat != null) {
    s++;
    fat=at.next();
  }
  if (attr)   return s;
  final NodeIter it=n.child();
  Nod i=it.next();
  while (i != null) {
    s+=fragmentSize((FNode)i,false);
    i=it.next();
  }
  return s;
}
