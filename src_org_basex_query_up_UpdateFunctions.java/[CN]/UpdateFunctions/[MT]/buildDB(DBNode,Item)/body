{
  final Data d=n.data;
  final MemData m=new MemData(20,d.tags,d.atts,d.ns,d.path,d.meta.prop);
  final Iter it=item.iter();
  Item i=it.next();
  while (i != null) {
    if (i instanceof FElem) {
    }
    i=it.next();
  }
  return m;
}
