{
  for (  final int dir : print(fs.children(pre),out)) {
    if (!fListDot) {
      if (startsWith(fs.name(dir),'.'))       continue;
    }
    out.print(NL);
    out.print(fs.path(dir));
    out.print(NL);
    lsRecursive(dir,out);
  }
}
