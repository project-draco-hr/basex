{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:date(\"2004-10-13\")), 3, 1) instance of xs:date",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
