{
  final XQuery query=new XQuery("xs:untypedAtomic(\"an arbitrary string(untypedAtomic source)\") cast as xs:QName",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("FORG0001"));
}
