{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:base64Binary(\"aaaa\")), 3, 1) instance of xs:base64Binary",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
