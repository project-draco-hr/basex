{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:hexBinary(\"0FB7\")), 3, 1) instance of xs:hexBinary",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
