{
  final XQuery query=new XQuery("not(xs:anyURI(\"http://www.example.com/an/arbitrary/URI.ext\") castable as xs:boolean)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
