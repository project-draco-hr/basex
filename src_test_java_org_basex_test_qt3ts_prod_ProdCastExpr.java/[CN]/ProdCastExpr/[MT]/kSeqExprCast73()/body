{
  final XQuery query=new XQuery("xs:untypedAtomic(\"example.com/\") cast as xs:anyURI eq xs:anyURI('example.com/')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
