{
  final XQuery query=new XQuery("xs:NMTOKEN(\" ::::...:SDA21455313 \")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"::::...:SDA21455313"));
}
