{
  final XQuery query=new XQuery("xs:base64Binary(\"aA+zZ/09\") cast as xs:hexBinary",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"680FB367FD3D"));
}
