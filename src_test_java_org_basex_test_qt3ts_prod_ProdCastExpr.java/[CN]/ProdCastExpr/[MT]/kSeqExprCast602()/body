{
  final XQuery query=new XQuery("xs:integer(\"6789\") cast as xs:integer eq xs:integer(\"6789\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
