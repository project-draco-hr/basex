{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:gMonth(\"--11\")), 3, 1) instance of xs:gMonth",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
