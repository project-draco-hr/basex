{
  final XQuery query=new XQuery("xs:date(\"2004-10-13\") cast as xs:gYear ne xs:gYear(\"1999\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
