{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:gDay(\"---03\")), 3, 1) instance of xs:gDay",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
