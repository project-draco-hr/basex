{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:time(\"03:20:00-05:00\")), 3, 1) instance of xs:time",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
