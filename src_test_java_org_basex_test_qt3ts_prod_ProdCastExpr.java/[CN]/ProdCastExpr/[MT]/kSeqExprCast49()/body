{
  final XQuery query=new XQuery("\"untyped a \" cast as xs:untypedAtomic eq xs:untypedAtomic(\"untyped a \")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
