{
  final XQuery query=new XQuery("xs:string(xs:time(\"23:59:12.432+00:00\")) eq \"23:59:12.432Z\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
