{
  final XQuery query=new XQuery("round-half-to-even(xs:float(\"-1.75e-3\") cast as xs:decimal,5)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"-0.00175"));
}
