{
  final XQuery query=new XQuery("not(xs:untypedAtomic(\"an arbitrary string(untypedAtomic source)\") castable as xs:QName)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
