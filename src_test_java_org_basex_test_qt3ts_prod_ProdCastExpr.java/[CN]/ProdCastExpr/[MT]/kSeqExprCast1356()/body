{
  final XQuery query=new XQuery("xs:hexBinary(\"0FB7\") cast as xs:base64Binary ne xs:base64Binary(\"aaaa\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
