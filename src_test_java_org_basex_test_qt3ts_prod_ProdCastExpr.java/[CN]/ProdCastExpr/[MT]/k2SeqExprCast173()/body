{
  final XQuery query=new XQuery("xs:language(\"abc-a-1-1b1efil\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"abc-a-1-1b1efil"));
}
