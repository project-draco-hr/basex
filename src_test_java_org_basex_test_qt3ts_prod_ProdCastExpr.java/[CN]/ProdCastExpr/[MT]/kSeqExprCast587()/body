{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:integer(\"6789\")), 3, 1) instance of xs:integer",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
