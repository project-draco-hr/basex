{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:string(\"an arbitrary string\")), 3, 1) instance of xs:string",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
