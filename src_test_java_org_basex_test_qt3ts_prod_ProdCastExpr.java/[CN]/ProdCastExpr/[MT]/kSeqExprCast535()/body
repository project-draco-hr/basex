{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:decimal(\"10.01\")), 3, 1) instance of xs:decimal",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
