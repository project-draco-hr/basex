{
  final XQuery query=new XQuery("xs:double(\"3.3e3\") cast as xs:string ne xs:string(\"an arbitrary string\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
