{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:QName(\"ncname\")), 3, 1) instance of xs:QName",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
