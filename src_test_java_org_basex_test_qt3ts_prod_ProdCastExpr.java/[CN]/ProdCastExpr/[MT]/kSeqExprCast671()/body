{
  final XQuery query=new XQuery("not(xs:duration(\"P1Y2M3DT10H30M\") castable as xs:gYear)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
