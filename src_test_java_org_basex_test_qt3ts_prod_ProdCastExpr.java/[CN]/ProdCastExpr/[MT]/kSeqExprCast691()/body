{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:yearMonthDuration(\"P1Y12M\")), 3, 1) instance of xs:yearMonthDuration",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
