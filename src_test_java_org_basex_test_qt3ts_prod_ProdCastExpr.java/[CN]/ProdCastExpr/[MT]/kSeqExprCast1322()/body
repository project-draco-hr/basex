{
  final XQuery query=new XQuery("xs:hexBinary(\"0FB7\") cast as xs:string ne xs:string(\"an arbitrary string\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
