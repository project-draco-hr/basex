{
  final XQuery query=new XQuery("xs:untypedAtomic(\"an arbitrary string(untypedAtomic source)\") castable as xs:untypedAtomic",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
