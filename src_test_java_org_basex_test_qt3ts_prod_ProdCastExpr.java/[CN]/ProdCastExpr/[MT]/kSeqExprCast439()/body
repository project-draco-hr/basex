{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:float(\"3.4e5\")), 3, 1) instance of xs:float",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
