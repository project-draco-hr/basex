{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:gYear(\"1999\")), 3, 1) instance of xs:gYear",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
