{
  final XQuery query=new XQuery("xs:anyURI(\"http://www.example.com/an/arbitrary/URI.ext\") cast as xs:duration",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0004"));
}
