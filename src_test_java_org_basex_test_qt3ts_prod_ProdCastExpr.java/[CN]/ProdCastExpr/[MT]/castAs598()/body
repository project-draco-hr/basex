{
  final XQuery query=new XQuery("xs:base64Binary(\"00000000\") cast as xs:hexBinary",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"D34D34D34D34"));
}
