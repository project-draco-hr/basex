{
  final XQuery query=new XQuery("xs:gMonthDay(\"--11-13\") cast as xs:untypedAtomic ne xs:untypedAtomic(\"an arbitrary string(untypedAtomic source)\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
