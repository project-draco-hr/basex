{
  final XQuery query=new XQuery("xs:anyURI(\"http://www.example.com/an/arbitrary/URI.ext\") castable as xs:string",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
