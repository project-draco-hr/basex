{
  final XQuery query=new XQuery("xs:string(xs:dayTimeDuration(\"PT9876H1M\")) eq \"P411DT12H1M\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
