{
  final XQuery query=new XQuery("xs:time(\"13:20:00-05:00\") cast as xs:time",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"13:20:00-05:00"));
}
