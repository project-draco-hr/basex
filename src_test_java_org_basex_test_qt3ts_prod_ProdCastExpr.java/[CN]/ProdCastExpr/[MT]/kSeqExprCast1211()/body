{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:boolean(\"true\")), 3, 1) instance of xs:boolean",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
