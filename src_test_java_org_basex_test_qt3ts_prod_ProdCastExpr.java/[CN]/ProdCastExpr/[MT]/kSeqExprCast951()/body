{
  final XQuery query=new XQuery("subsequence((\"dummy\", 1.1, xs:gYearMonth(\"1999-11\")), 3, 1) instance of xs:gYearMonth",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
