{
  final XQuery query=new XQuery("xs:string(xs:dayTimeDuration(\"P9876DT1M\")) eq \"P9876DT1M\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
