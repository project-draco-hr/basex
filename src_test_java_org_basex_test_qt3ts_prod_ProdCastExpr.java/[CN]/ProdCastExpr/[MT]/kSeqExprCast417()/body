{
  final XQuery query=new XQuery("boolean(xs:untypedAtomic(\"an arbitrary string(untypedAtomic source)\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
