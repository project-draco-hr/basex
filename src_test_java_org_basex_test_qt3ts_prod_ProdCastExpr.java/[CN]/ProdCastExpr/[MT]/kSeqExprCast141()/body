{
  final XQuery query=new XQuery("xs:string(xs:hexBinary(xs:base64Binary(\"aaa a\"))) eq \"69A69A\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
