{
  start=new Date();
  createLogDir();
  createLogFile(start);
  Runtime.getRuntime().addShutdownHook(new Thread(){
    @Override public void run(){
      Log.this.write("Server stopped.");
      Log.this.closeLog();
    }
  }
);
}
