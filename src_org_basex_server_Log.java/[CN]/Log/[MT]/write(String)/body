{
  Date now=new Date();
  if (!checkDay(now)) {
    try {
      fw.close();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    createLogFile(now);
  }
  SimpleDateFormat sdf=new SimpleDateFormat("HH:mm:ss.SSS");
  String tmp=sdf.format(now) + " : " + s+ NL;
  try {
    fw.write(tmp);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
