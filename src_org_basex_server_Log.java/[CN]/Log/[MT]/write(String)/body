{
  try {
    d=new Date();
    SimpleDateFormat sdf=new SimpleDateFormat("HH:mm:ss.SSS");
    String tmp=sdf.format(d) + " : " + s+ NL;
    bw.write(tmp);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
