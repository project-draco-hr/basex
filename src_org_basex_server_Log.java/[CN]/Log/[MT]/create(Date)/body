{
  new File(dir).mkdirs();
  final String file=dir + DATE.format(d) + ".log";
  start=d;
  try {
    fw=new FileWriter(file,true);
  }
 catch (  final IOException ex) {
    ex.printStackTrace();
  }
}
