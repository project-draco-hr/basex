{
  SimpleDateFormat sdf=new SimpleDateFormat("yyyy-MM-dd");
  logfile=new File(Prop.HOME + "\\BaseXLogs\\" + sdf.format(d)+ ".log");
  if (!logfile.exists()) {
    try {
      logfile.createNewFile();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
}
