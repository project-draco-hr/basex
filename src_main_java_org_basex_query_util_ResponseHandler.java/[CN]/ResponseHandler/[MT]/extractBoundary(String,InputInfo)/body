{
  int index=c.toLowerCase().lastIndexOf("boundary=");
  if (index == -1) {
    NOBOUND.thrw(info);
  }
  String b=c.substring(index + 9);
  if (b.charAt(0) == '"') {
    index=b.lastIndexOf('"');
    b=b.substring(1,index);
  }
  return token(b);
}
