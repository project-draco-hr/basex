{
  final NodeCache h=new NodeCache();
  for (  final String headerName : conn.getHeaderFields().keySet()) {
    if (headerName != null) {
      final FElem hdr=new FElem(new QNm(HEADER,HTTPURI),null,null,null,new Atts().add(HTTP,HTTPURI),null);
      hdr.atts.add(new FAttr(new QNm(HDR_NAME,EMPTY),token(headerName),hdr));
      hdr.atts.add(new FAttr(new QNm(HDR_VALUE,EMPTY),token(conn.getHeaderField(headerName)),hdr));
      h.add(hdr);
    }
  }
  return h;
}
