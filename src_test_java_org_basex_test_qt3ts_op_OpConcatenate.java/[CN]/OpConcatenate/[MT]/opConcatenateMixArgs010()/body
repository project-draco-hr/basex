{
  final XQuery query=new XQuery("xs:double(\"INF\"), xs:double(\"-INF\"), xs:float(\"-INF\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"INF -INF -INF"));
}
