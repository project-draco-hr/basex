{
  final XQuery query=new XQuery(" (xs:boolean(fn:true()), fn:false(), fn:true())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("true(), false(), true()"));
}
