{
  final boolean swap=expr[0].isValue() && !expr[1].isValue() || expr[1] instanceof Path && ((Path)expr[1]).root == null && (!(expr[0] instanceof Path) || ((Path)expr[0]).root != null);
  if (swap) {
    final Expr tmp=expr[0];
    expr[0]=expr[1];
    expr[1]=tmp;
  }
  return swap;
}
