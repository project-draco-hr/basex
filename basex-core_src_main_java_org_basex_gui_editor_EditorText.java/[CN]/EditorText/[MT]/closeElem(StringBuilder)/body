{
  int p=ps - 1;
  for (; p >= 0; p--) {
    final byte b=text[p];
    if (!XMLToken.isNCChar(b) && b != ':') {
      if (b == '<' && p < ps - 1) {
        sb.append("</");
        while (++p < ps)         sb.append((char)text[p]);
        sb.append('>');
        break;
      }
      return;
    }
  }
}
