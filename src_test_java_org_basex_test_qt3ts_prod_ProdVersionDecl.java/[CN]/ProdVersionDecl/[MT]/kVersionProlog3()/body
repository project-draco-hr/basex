{
  xquery10();
  final XQuery query=new XQuery("xquery version \"1.0\" encoding \"UTF-8 \"; 1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0087"));
}
