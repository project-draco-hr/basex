{
  if (VERBOSE) {
    StringBuilder sb=new StringBuilder();
    sb.append("{");
    boolean first=true;
    for (    byte b : ms) {
      if (first) {
        first=false;
        sb.append(b);
      }
 else {
        sb.append("," + b);
      }
    }
    sb.append("}");
    BaseX.debug("Invalid duration (%, %)",ms,sb);
  }
  return Token.EMPTY;
}
