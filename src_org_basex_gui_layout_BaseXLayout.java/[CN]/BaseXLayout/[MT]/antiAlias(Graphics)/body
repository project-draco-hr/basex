{
  try {
    ((Graphics2D)g).setRenderingHint(RenderingHints.KEY_TEXT_ANTIALIASING,RenderingHints.class.getField("VALUE_TEXT_ANTIALIAS_" + GUIConstants.FONTALIAS[GUIProp.fontalias]).get(null));
  }
 catch (  final Exception e) {
    BaseX.errln(e);
  }
}
