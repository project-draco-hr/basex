{
  try {
    ((Graphics2D)g).setRenderingHint(RenderingHints.KEY_TEXT_ANTIALIASING,RenderingHints.class.getField("VALUE_TEXT_ANTIALIAS_" + GUIConstants.FONTALIAS[pr.num(GUIProp.FONTALIAS)]).get(null));
  }
 catch (  final Exception ex) {
    BaseX.errln(ex);
  }
}
