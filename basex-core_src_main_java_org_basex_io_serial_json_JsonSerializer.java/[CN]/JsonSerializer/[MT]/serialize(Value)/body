{
  if (value.size() > 1) {
    if (adaptive != null) {
      adaptive.serialize(value);
    }
 else {
      throw SERJSONSEQ.getIO();
    }
  }
 else {
    sep=false;
    serialize(value.isEmpty() ? null : (Item)value);
  }
}
