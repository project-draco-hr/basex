{
  tok.reset();
  if (letter(curr())) {
    while (!curr(0)) {
      tok.add(next());
      final byte c=curr();
      if (c == ':' && peek(1) != ':')       continue;
      if (!letterOrDigit(c) && c != '-')       break;
    }
  }
  return tok.finish();
}
