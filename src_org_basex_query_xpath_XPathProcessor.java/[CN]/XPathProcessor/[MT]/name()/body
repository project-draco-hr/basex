{
  tok.setLength(0);
  if (letter(curr())) {
    while (!curr(0)) {
      tok.append(next());
      final char c=curr();
      if (c == ':' && peek(1) != ':')       continue;
      if (!letterOrDigit(c) && c != '-')       break;
    }
  }
  return tok.toString();
}
