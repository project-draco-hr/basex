{
  final byte[] name=name();
  if (consume(DBLCOLON)) {
    final Axis ax=Axis.find(Token.string(name));
    if (ax == null)     error(UNKNOWNAXIS,name);
    return ax;
  }
  back(name.length);
  return Axis.CHILD;
}
