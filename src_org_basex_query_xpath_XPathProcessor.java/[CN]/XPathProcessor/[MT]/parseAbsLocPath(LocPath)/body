{
  boolean more=false;
  while (consume('/')) {
    final byte c=curr();
    if (c != 0 && c != ' ' && c != ']' && c != ')' && c != '|' && c != ',') {
      path.steps.add(parseStep());
      more=true;
    }
 else     if (more) {
      error(NOLOCSTEP);
    }
  }
  return path;
}
