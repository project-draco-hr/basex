{
  final byte[] data=new byte[val.length << 1];
  for (int d=0; d < data.length; d+=2) {
    final byte v=val[d >> 1];
    data[d]=b2h((v & 0xF0) >> 4);
    data[d + 1]=b2h(v & 0x0F);
  }
  return data;
}
