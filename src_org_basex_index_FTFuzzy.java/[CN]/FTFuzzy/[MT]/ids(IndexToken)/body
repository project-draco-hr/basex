{
  final Tokenizer ft=(Tokenizer)ind;
  final byte[] tok=ft.get();
  if (ft.fz) {
    int k=Prop.lserr;
    if (k == 0)     k=tok.length >> 2;
    return fuzzy(tok,k);
  }
  final int id=cache.id(tok);
  return id == 0 ? get(tok) : iter(cache.getPointer(id),cache.getSize(id),dat);
}
