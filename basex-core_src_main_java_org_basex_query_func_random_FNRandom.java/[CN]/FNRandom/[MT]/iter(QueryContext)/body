{
switch (func) {
case _RANDOM_SEEDED_DOUBLE:
    return randomSeededDouble(qc);
case _RANDOM_SEEDED_INTEGER:
  return randomSeededInt(qc);
case _RANDOM_GAUSSIAN:
return randomGaussian(qc);
default :
return super.iter(qc);
}
}
