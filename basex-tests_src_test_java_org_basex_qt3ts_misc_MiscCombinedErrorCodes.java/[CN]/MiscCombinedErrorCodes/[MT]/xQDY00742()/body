{
  final XQuery query=new XQuery("<a> { attribute { 'prefix:localName' } { } } </a>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQDY0074"));
}
