{
  final XQuery query=new XQuery("dateTime(xs:date('2001-01-01-14:00'), xs:time('01:01:01+14:00'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0008"));
}
