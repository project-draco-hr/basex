{
  final XQuery query=new XQuery("starts-with('a', 'b', 'http://www.cbcl.co.u,/collation')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOCH0002"));
}
