{
  final XQuery query=new XQuery("xs:yearMonthDuration('P768614336404564650Y0M') * 2",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FODT0002"));
}
