{
  final XQuery query=new XQuery("declare base-uri \"http://www.w3.org/\"; resolve-uri(\"%gg\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0002"));
}
