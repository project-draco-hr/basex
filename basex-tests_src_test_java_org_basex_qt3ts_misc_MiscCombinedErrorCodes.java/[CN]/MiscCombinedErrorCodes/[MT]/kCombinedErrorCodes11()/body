{
  final XQuery query=new XQuery("validate lax { 1 }",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((error("XQTY0030") || error("XQST0075")));
}
