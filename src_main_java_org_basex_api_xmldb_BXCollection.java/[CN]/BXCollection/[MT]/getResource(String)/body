{
  check();
  if (id == null)   return null;
  final Data data=ctx.data;
  final byte[] idd=Token.token(id);
  for (  final int pre : data.doc()) {
    if (Token.eq(data.text(pre,true),idd))     return new BXXMLResource(data,pre,id,this);
  }
  return null;
}
