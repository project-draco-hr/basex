{
  final Class<?> c=Class.forName(PKG + ".ext." + lang.toString().toLowerCase()+ "Stemmer");
  CLASSES.put(lang,new StemmerClass(c,findMethod(c,"setCurrent",String.class),findMethod(c,"stem"),findMethod(c,"getCurrent")));
}
