{
  if (pe >= size)   return false;
  ps=pe;
  int ch=Token.cp(text,ps);
  pe+=Token.cl(text[ps]);
  if (sep(ch))   return true;
  while (pe < size) {
    ch=Token.cp(text,pe);
    if (sep(ch))     break;
    pe+=Token.cl(text[pe]);
  }
  ;
  return true;
}
