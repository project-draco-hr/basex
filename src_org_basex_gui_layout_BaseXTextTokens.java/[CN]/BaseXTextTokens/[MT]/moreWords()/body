{
  col=Color.black;
  if (pe >= size)   return false;
  ps=pe;
  final int tl=Token.cl(text[ps]);
  if (sep()) {
    pe+=tl;
    return true;
  }
  pe+=tl;
  while (pe < size) {
    if (sep())     break;
    pe+=Token.cl(text[pe]);
  }
  return true;
}
