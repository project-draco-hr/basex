{
  try {
    return optPre(has(Flag.CTX) || has(Flag.NDT) || has(Flag.HOF)|| has(Flag.UPD)|| !allAreValues() ? opt(qc,scp) : sig.ret.zeroOrOne() ? item(qc,info) : value(qc),qc);
  }
 catch (  final QueryException ex) {
    return FnError.get(ex,seqType);
  }
}
