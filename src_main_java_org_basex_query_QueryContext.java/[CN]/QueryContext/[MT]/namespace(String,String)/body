{
  if (prefix.isEmpty()) {
    nsElem=token(uri);
  }
 else {
    final QNm name=new QNm(token(prefix),token(uri));
    if (!uri.isEmpty()) {
      ns.add(name,null);
    }
 else {
      ns.delete(name);
    }
  }
}
