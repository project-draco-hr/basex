{
  final XQuery query=new XQuery("parse-json(\"[12345]\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertType("map(*)") && assertCount(1) && assertQuery("map:contains($result, 1)")&& assertQuery("map:get($result, 1) eq 12345")));
}
