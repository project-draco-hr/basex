{
  final XQuery query=new XQuery("parse-json(\"[true]\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertType("map(*)") && assertCount(1) && assertQuery("map:contains($result, 1)")&& assertQuery("$result(1) eq true()")));
}
