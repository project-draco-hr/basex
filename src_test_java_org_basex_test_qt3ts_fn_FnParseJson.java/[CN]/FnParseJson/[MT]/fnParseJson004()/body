{
  final XQuery query=new XQuery("parse-json('{\"abc\":-1.2e0}')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("map{\"abc\":=-1.2e0}"));
}
