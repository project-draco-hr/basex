{
  final XQuery query=new XQuery("parse-json('{\"abc\":12}')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("map{\"abc\":=12e0}"));
}
