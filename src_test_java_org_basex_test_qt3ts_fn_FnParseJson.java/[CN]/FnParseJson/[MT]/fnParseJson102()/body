{
  final XQuery query=new XQuery("parse-json(unparsed-text('parse-json/data002.json'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertCount(1));
}
