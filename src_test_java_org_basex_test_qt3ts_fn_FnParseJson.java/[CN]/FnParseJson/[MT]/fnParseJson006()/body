{
  final XQuery query=new XQuery("parse-json('{\"abc\":false}')",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertDeepEq("map{\"abc\":=false()}"));
}
