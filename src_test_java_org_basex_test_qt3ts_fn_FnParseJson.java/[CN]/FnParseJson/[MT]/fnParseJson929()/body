{
  final XQuery query=new XQuery("parse-json('\"abcd\\n\"', map{'spec':='RFC4627','unescape':=false()})",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOJS0001"));
}
