{
  while (super.more()) {
    final byte[] b=super.get();
    Integer c=token.get(b);
    if (c != null) {
      System.out.println(new String(b) + " " + (c + 1));
      token.setValue(b,++c);
      if (c > max)       max=c;
    }
 else     token.add(b,1);
  }
}
