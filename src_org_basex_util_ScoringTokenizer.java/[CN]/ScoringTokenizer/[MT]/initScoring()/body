{
  token=new Map<Integer>();
  while (super.more()) {
    final byte[] b=super.get();
    Integer c=token.get(b);
    if (c != null) {
      token.set(b,++c);
      if (c > max)       max=c;
    }
 else {
      token.add(b,1);
    }
  }
}
