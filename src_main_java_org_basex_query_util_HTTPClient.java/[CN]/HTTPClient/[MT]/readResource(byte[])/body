{
  final BufferedInputStream bis=new BufferedInputStream(new URL(string(src)).openStream());
  try {
    final ByteList bl=new ByteList();
    int i=0;
    while ((i=bis.read()) != -1)     bl.add(i);
    return bl.toArray();
  }
  finally {
    bis.close();
  }
}
