{
  if (context.data() == null)   return Prop.info ? info(DBEMPTY) : true;
  try {
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return context.close() ? Prop.info ? info(DBCLOSED) : true : error(DBCLOSEERR);
}
