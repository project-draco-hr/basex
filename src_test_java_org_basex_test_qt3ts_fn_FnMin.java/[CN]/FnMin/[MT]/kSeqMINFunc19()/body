{
  final XQuery query=new XQuery("min((xs:float(\"NaN\"), 1, 1, 2, xs:double(\"NaN\"))) instance of xs:double",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
