{
  final XQuery query=new XQuery("string(min((xs:float(\"NaN\"), xs:untypedAtomic(\"3\"), xs:float(2)))) eq \"NaN\"",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
