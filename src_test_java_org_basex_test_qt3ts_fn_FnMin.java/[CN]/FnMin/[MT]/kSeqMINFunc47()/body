{
  final XQuery query=new XQuery("min((xs:date(\"2005-01-01\"), xs:date(\"2001-01-01\"))) eq xs:date(\"2001-01-01\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
