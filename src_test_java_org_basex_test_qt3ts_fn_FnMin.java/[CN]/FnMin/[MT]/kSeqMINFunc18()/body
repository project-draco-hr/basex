{
  final XQuery query=new XQuery("min((xs:float(\"NaN\"), xs:untypedAtomic(\"3\"), xs:double(2))) instance of xs:double",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
