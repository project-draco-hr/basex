{
  final String msg="messagemessagemessagemessagemessagemessagemessage";
  query("let $e := crypto:encrypt('" + msg + "','symmetric','abababababababab',"+ "'AES') return crypto:decrypt($e,'symmetric','abababababababab','AES')",msg);
}
