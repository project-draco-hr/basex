def login(self):
    ts = self.readInput()
    pwmd5 = hashlib.md5(self.pw).hexdigest()
    m = hashlib.md5()
    m.update(pwmd5)
    m.update(ts)
    complete = m.hexdigest()
    self.send(self.user)
    self.send(complete)
    data = self.read1byte()
    return ('\x00' == data)
