def execute(self, com, out):
    s.send(com)
    s.send('\x00')
    count = 0
    while True:
        data = s.recv(1)
        if (data != '\x00'):
            out.write(data)
        elif (count != 0):
            break
        else:
            count += 1
    return s.recv(1)
