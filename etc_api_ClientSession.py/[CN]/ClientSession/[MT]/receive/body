def receive(self):
    part1 = self.readInput()
    part2 = self.readInput()
    part3 = self.read1byte()
    recv = ''
    if (part1 != '\x00'):
        recv += (part1 + part2)
    else:
        recv = part2
    return recv
