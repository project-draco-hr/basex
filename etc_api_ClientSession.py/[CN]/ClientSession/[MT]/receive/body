def receive(self):
    part1 = self.getIt()
    part2 = self.getIt()
    part3 = self.s.recv(1)
    recv = ''
    if (part1 != '\x00'):
        recv += (part1 + part2)
    else:
        recv = part2
    return recv
