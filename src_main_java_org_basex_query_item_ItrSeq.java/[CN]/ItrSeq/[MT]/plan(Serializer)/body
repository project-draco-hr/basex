{
  ser.openElement(Token.token(Util.name(this)),SIZE,Token.token(size));
  for (int v=0; v != Math.min(size,5); ++v)   ser.emptyElement(ITM,VAL,Token.token(vals[v]),TYP,type.nam());
  ser.closeElement();
}
