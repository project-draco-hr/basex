{
  final StringBuilder sb=new StringBuilder(name);
  if (value != null) {
    sb.append('=').append(value instanceof String[] ? Arrays.asList((String[])value) : value instanceof int[] ? Arrays.asList((int[])value) : value);
  }
  return sb.toString();
}
