{
  final XQuery query=new XQuery("every $x in (\"true\", \"false\"), $y in (\"false\",\"true\") satisfies xs:boolean($x) = xs:boolean($y)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
