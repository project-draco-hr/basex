{
  final XQuery query=new XQuery("some $x as xs:integer in (1, 2, 3), $y as xs:float in (xs:float(2), xs:float(3)) satisfies $x + $y = 5",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
