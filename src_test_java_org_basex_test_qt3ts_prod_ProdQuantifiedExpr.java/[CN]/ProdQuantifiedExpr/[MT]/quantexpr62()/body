{
  final XQuery query=new XQuery("some $x as xs:string in (\"cat\",\"dog\",\"rat\") satisfies fn:string-length($x) = 3",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
