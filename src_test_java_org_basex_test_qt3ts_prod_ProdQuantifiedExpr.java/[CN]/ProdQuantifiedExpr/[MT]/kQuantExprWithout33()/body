{
  final XQuery query=new XQuery("some $var in (true(), true(), xs:hexBinary(\"FF\")) satisfies $var",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("FORG0006")));
}
