{
  final XQuery query=new XQuery("every $i in subsequence((1, 2, 3, current-time()), 1, 3) satisfies boolean($i treat as xs:integer)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
