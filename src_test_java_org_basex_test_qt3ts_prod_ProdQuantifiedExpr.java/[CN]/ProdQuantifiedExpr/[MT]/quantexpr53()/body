{
  final XQuery query=new XQuery("every $x in (1,2,3), $y in (4,5,6) satisfies fn:string($x) = fn:string($y)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
