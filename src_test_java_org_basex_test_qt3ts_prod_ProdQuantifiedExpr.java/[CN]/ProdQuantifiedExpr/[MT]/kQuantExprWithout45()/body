{
  final XQuery query=new XQuery("\n" + "        declare variable $i := false(); \n" + "        every $i in (true(), true()) satisfies $i",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
