{
  final XQuery query=new XQuery("some $x as xs:integer in (1, 2, 3) , $y as xs:integer in (2, 3, 4) satisfies $x + $y = 4",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
