{
  final XQuery query=new XQuery("some $x in (1,2,3), $y in (4,5,6) satisfies xs:integer($x) = xs:integer($y)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
