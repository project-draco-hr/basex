{
  final XQuery query=new XQuery("every $x as xs:string in (\"cat\",\"dog\",\"rat\"), $y as xs:integer in (3, 3, 3) satisfies fn:string-length($x) = $y",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
