{
  final NodePrimitives up=op.get(n);
  if (up != null)   for (  final UpdatePrimitive pr : up)   if (pr.type() == REPLACENODE || pr.type() == DELETE)   return true;
  final int p=d.parent(n,d.kind(n));
  if (p == -1)   return false;
  return parentDeleted(p);
}
