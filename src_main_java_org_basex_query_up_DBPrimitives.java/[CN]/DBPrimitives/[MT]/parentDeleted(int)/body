{
  final NodePrimitives up=op.get(n);
  if (up != null)   for (  final Primitive pr : up)   if (pr.type() == REPLACENODE || pr.type() == DELETENODE)   return true;
  final int p=d.parent(n,d.kind(n));
  if (p == -1)   return false;
  return parentDeleted(p);
}
