def __console(self):
    session.execute('SET INFO ON')
    while True:
        com = str(raw_input('> ')).strip()
        if (com == 'exit'):
            break
        if com:
            session.execute(com)
            print session.info()
