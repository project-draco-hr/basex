def __console(self):
    out = sys.stdout
    session.execute('SET INFO ON', out)
    while True:
        com = str(raw_input('> ')).strip()
        if (com == 'exit'):
            break
        if com:
            session.execute(com, out)
            print session.info()
