def session(self):
    user = raw_input('Username: ')
    pw = getpass.getpass('Password: ')
    try:
        global session
        session = Session(self.__host, self.__port, user, pw)
        self.__console()
        session.close()
        print 'See you.'
    except IOError as e:
        print e
