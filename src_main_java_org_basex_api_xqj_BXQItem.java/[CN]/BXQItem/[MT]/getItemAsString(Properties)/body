{
  try {
    return it.isNode() && it.type != NodeType.TXT ? serialize() : Token.string(it.string(null));
  }
 catch (  final QueryException e) {
    throw new XQException(e.getMessage(),e.code());
  }
}
