{
  try {
    final Type ip=it.type;
    return ip.isNode() && ip != NodeType.TXT ? serialize() : Token.string(it.string(null));
  }
 catch (  final QueryException e) {
    throw new XQException(e.getMessage(),e.code());
  }
}
