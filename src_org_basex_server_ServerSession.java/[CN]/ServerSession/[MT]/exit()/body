{
  stopProcess();
  if (timeout != null)   timeout.interrupt();
  context.delete(this);
  new Close().execute(context);
  try {
    socket.close();
  }
 catch (  final IOException ex) {
    Main.error(ex,false);
  }
}
