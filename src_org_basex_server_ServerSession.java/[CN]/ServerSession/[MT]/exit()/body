{
  if (core != null)   core.stop();
  stopTimer();
  new Close().execute(context);
  context.delete(this);
  try {
    socket.close();
  }
 catch (  final IOException ex) {
    Main.error(ex,false);
  }
}
