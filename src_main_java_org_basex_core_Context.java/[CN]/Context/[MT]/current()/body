{
  if (current == null && data != null) {
    final Resources res=data.resources;
    current=new Nodes((docpath == null ? res.docs() : res.docs(docpath)).toArray(),data);
    current.root=docpath == null;
  }
  return current;
}
