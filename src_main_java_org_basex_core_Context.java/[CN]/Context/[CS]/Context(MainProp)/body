{
  mprop=mp;
  datas=new Datas();
  events=new Events();
  sessions=new Sessions();
  lock=new Lock(this);
  users=new Users(true);
  repo=new Repo(this);
  databases=new Databases(this);
  user=users.get(ADMIN);
  listener=null;
}
