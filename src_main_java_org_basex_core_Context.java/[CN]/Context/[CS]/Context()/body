{
  prop=new Prop(true);
  datas=new DataPool();
  triggers=new TriggerPool();
  sessions=new Sessions();
  lock=new Lock(this);
  users=new Users(true);
  user=users.get(ADMIN);
  repo=new Repo(prop);
}
