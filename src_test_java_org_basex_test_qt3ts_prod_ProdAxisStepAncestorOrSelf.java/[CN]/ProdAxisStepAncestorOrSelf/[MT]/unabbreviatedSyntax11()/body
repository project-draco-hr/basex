{
  final XQuery query=new XQuery("(for $h in (/works/employee[1]/hours) return $h/ancestor-or-self::employee)/@name",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"Jane Doe 1"));
}
