{
  final XQuery query=new XQuery("1, <!-- content -->/ancestor-or-self::node(), 1",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("1<!-- content -->1",false));
}
