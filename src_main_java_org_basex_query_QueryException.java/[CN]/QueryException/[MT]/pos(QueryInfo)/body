{
  line=1;
  col=1;
  for (int i=0; i < qi.pos && i < qi.query.length(); i++) {
    final char ch=qi.query.charAt(i);
    if (ch == 0x0A) {
      line++;
      col=1;
    }
 else     if (ch != 0x0D) {
      col++;
    }
  }
}
