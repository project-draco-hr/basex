{
  final int ns=qnames.size();
  for (int n=0; n < ns; n++) {
    final byte[] name=qnames.name(n);
    if (name == null || (idref ? Token.contains(name,IDREF) : Token.contains(name,ID) && !Token.contains(name,IDREF)))     return true;
  }
  return isEmpty();
}
