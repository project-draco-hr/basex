{
  return new FTNodeIter(){
    @Override public FTNodeItem next() throws XQException {
      if (FTUnion.this.more(ctx))       return FTUnion.this.next();
 else       return FTUnion.this.next();
    }
  }
;
}
