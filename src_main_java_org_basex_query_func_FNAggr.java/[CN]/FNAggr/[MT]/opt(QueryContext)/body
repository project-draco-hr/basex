{
  final Expr e=expr[0];
  if (e.has(Flag.NDT) || e instanceof VarRef)   return this;
  final long c=e.size();
switch (sig) {
case COUNT:
    return c >= 0 ? Int.get(c) : this;
case SUM:
  return c == 0 ? expr.length == 2 ? expr[1] : Int.get(0) : this;
default :
return this;
}
}
