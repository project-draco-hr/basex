{
switch (func) {
case COUNT:
    final long c=expr[0].size(ctx);
  return c >= 0 && !ctx.grouping ? Itr.get(c) : this;
case MIN:
case MAX:
case AVG:
return expr[0].e() ? Seq.EMPTY : this;
default :
return this;
}
}
