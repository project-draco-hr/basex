{
  final Expr e=expr[0];
  final long c=e.size();
  if (c < 0 || e.uses(Use.CTX))   return this;
switch (def) {
case COUNT:
    return Itr.get(c);
case SUM:
  return c == 0 ? expr.length == 2 ? expr[1] : Itr.get(0) : this;
default :
return this;
}
}
