{
  final Expr e=expr[0];
  final long c=e.size();
  if (c < 0 || e.uses(Use.NDT))   return this;
switch (def) {
case COUNT:
    return Int.get(c);
case SUM:
  return c == 0 ? expr.length == 2 ? expr[1] : Int.get(0) : this;
default :
return this;
}
}
