{
  if (IO.isSymlink(d)) {
    for (    final FSVisitor v : visitors)     v.symlinkEvent(d);
    return;
  }
  for (  final FSVisitor v : visitors)   v.preEvent(d);
  visitOrDescend(d);
  for (  final FSVisitor v : visitors)   v.postEvent();
}
