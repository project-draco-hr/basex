{
  if (attribute == null)   return false;
  byte[] b=Token.token(attribute);
  final boolean tmp=!Token.contains(b,AT);
  b=Token.delete(b,AT);
  isTag=tmp;
  attr=b;
  refreshAxis();
  return true;
}
