{
  final XQuery query=new XQuery("fn:deep-equal((xs:positiveInteger(\"1\")),(xs:positiveInteger(\"1\")))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
