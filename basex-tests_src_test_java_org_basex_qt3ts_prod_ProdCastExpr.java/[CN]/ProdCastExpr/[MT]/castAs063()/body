{
  final XQuery query=new XQuery("xs:untypedAtomic(\"---03-05:00\") cast as xs:gDay",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"---03-05:00"));
}
