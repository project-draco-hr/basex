{
  final XQuery query=new XQuery("not(xs:integer(\"6789\") castable as xs:yearMonthDuration)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
