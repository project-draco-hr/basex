{
  final XQuery query=new XQuery("not(xs:yearMonthDuration(\"P1Y12M\") castable as xs:anyURI)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
