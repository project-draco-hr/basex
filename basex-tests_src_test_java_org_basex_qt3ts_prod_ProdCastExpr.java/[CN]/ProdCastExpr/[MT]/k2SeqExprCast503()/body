{
  final XQuery query=new XQuery("xs:language(xs:yearMonthDuration(\"P4Y5M\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0001"));
}
