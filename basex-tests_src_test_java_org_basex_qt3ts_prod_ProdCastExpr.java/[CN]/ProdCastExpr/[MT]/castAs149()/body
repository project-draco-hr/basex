{
  final XQuery query=new XQuery("xs:string(\"--12-05:00\") cast as xs:gMonth",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"--12-05:00"));
}
