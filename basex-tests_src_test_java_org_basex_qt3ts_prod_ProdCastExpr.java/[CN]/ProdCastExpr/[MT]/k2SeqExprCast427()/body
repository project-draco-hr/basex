{
  final XQuery query=new XQuery("xs:anyURI(\"//server/share/dir1\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"//server/share/dir1"));
}
