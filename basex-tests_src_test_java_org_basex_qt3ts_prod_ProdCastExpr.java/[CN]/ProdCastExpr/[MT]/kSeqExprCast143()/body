{
  final XQuery query=new XQuery("xs:hexBinary(remove((\"3B\", 1.1), 2) treat as xs:string)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"3B"));
}
