{
  final XQuery query=new XQuery("translate(\"--aaa--\",\"bbb++\",\"\") eq \"--aaa--\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
