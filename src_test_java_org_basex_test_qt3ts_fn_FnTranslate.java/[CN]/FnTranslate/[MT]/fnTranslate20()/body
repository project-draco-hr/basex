{
  final XQuery query=new XQuery("translate(\"abcd????e\", \"a????e\", \"????YZ\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("\"????bcdYZ\""));
}
