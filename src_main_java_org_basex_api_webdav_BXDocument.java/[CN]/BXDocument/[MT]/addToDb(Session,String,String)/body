{
  final Query q=s.query("declare variable $db as xs:string external; " + "declare variable $doc as xs:string external; " + "declare variable $name as xs:string external; "+ "db:add($db, collection($doc), $name)");
  q.bind("$db",dbName);
  q.bind("$doc",db + SEP + path);
  q.bind("$name",name);
  q.execute();
}
