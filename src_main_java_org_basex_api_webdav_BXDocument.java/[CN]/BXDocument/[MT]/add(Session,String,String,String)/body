{
  final Query q=s.query(DBADD.args("$db",DBOPEN.args("$sdb","$spath"),"$name","$path"));
  q.bind("db",trgdb);
  q.bind("sdb",db);
  q.bind("spath",path);
  q.bind("name",name);
  q.bind("path",trgdir);
  q.execute();
}
