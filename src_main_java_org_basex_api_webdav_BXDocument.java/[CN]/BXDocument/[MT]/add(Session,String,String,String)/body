{
  final Query q=s.query("db:add($db, db:open($sdb, $spath), $name, $path)");
  q.bind("$db",trgdb);
  q.bind("$sdb",db);
  q.bind("$spath",path);
  q.bind("$name",name);
  q.bind("$path",trgdir);
  q.execute();
}
