{
  new BXCode<Object>(this){
    @Override public void run() throws BaseXException {
      s.setOutputStream(out);
      final Query q=s.query("collection($path)");
      q.bind("$path",db + SEP + path);
      q.execute();
    }
  }
.eval();
}
