{
  new BXCode<Object>(this){
    @Override public void run() throws IOException {
      s.setOutputStream(out);
      final Query q=s.query(raw ? "declare option output:method 'raw'; " + DBRETRIEVE.args("$db","$path") : DBOPEN.args("$db","$path"));
      q.bind("db",db);
      q.bind("path",path);
      q.execute();
    }
  }
.eval();
}
