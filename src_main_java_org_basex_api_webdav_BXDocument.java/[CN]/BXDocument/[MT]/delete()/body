{
  try {
    final Session s=BXResourceFactory.login(user,pass);
    try {
      s.execute(new Open(db));
      s.execute(new Delete(path));
    }
  finally {
      s.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
