{
  return new BXCode<LockToken>(this){
    @Override public LockToken get() throws IOException {
      try {
        return getCurrentActiveLock();
      }
 catch (      SAXException e) {
        throw new IOException(e);
      }
    }
  }
.evalNoEx();
}
