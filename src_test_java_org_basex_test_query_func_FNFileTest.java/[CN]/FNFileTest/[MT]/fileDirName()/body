{
  check(_FILE_DIR_NAME);
  assertEquals(norm(Prop.TMP),norm(query(_FILE_DIR_NAME.args(PATH1))).toLowerCase(Locale.ENGLISH));
  query(_FILE_DIR_NAME.args(""),".");
  query(_FILE_DIR_NAME.args(NAME),".");
}
