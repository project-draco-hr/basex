{
  check(FLPATHNATIVE);
  assertEquals(norm(new File(PATH1).getCanonicalPath()),norm(query(FLPATHNATIVE.args(PATH1))));
  query(FLPATHNATIVE.args(Prop.TMP + ".." + "/test.xml"),new File(Prop.TMP + ".." + "/test.xml").getCanonicalPath());
}
