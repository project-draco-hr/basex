{
  check(_FILE_PATH_TO_NATIVE);
  assertEquals(norm(new File(PATH1).getCanonicalPath()),norm(query(_FILE_PATH_TO_NATIVE.args(PATH1))));
  query(_FILE_PATH_TO_NATIVE.args(Prop.TMP + ".." + "/test.xml"),new File(Prop.TMP + ".." + "/test.xml").getCanonicalPath());
}
