{
  final XQuery query=new XQuery("avg((xs:string(1), xs:integer(3), xs:untypedAtomic(3)))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
