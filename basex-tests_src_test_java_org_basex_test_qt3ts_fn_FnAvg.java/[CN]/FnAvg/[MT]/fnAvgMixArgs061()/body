{
  final XQuery query=new XQuery("fn:avg(( (xs:boolean(\"1\"), xs:double(\"-INF\"), \"s\")))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
