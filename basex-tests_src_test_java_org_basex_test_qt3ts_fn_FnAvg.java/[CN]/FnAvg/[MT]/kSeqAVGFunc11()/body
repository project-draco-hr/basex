{
  final XQuery query=new XQuery("avg((xs:float(1), xs:integer(0), xs:untypedAtomic(-4))) eq -1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
