{
  final XQuery query=new XQuery("fn:avg(( (xs:integer(\"100\"), xs:string(\"abc\"))))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
