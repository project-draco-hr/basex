{
  while (in.more()) {
    if (in.read() != '\n')     continue;
    final int n=in.read();
    if (n == '0' && in.read() == '0' && in.read() == '1') {
      off=in.pos() - 3;
      return text(in);
    }
  }
  return null;
}
