{
  final XQuery query=new XQuery("declare namespace p = \"http://example.com/\"; count(in-scope-prefixes(<p:e/>)), count(in-scope-prefixes(element p:e {()}))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"2 2"));
}
