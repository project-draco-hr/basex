{
  final XQuery query=new XQuery("in-scope-prefixes(/*)",ctx);
  query.context(node(file("fn/in-scope-prefixes/NamespaceSuppliedInternally.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertPermutation("\"\", \"xml\", \"xlink\""));
}
