{
  final XQuery query=new XQuery("fn:contains(fn:in-scope-prefixes(<anElement>Some content</anElement>)[1],\"l\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
