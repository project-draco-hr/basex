{
  final XQuery query=new XQuery("declare default element namespace \"http://www.example.com\"; count(fn:in-scope-prefixes(<e/>))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("2"));
}
