{
switch (type) {
case TAG:
    return elemNames;
case ATTNAME:
  return attrNames;
case TEXT:
return textIndex;
case ATTRIBUTE:
return attrIndex;
case FULLTEXT:
return ftxtIndex;
case PATH:
return paths;
default :
throw Util.notExpected();
}
}
