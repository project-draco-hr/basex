{
  final XQuery query=new XQuery("xs:string((1 to 10)[. mod 2 = 0] instance of xs:integer+)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"true"));
}
