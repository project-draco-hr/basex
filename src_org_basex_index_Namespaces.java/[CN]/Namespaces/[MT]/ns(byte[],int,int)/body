{
  if (s == -1)   return 0;
  final byte[] pr=substring(n,0,s);
  if (eq(XML,pr))   return 0;
  for (int i=p; i >= 0; i--)   if (eq(pref[i],pr))   return vals[i];
  BaseX.notexpected();
  return 0;
}
