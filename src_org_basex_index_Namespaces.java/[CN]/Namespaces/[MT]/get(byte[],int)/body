{
  if (sz == 0)   return 0;
  int i=sz;
  while (--i > 0 && p < pre[i])   ;
  i=Math.min(sz - 1,i + 1);
  int s=indexOf(n,':');
  if (s == -1) {
    for (; i >= 0; i--)     if (pref[i].length == 0)     return vals[i];
    return -1;
  }
  return ns(n,s,i);
}
