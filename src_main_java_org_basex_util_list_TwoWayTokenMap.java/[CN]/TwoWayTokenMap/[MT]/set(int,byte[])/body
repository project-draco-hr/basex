{
  if (-1 == map.value(e) || Arrays.equals(get(i),e)) {
    map.delete(get(i));
    super.set(i,e);
    map.add(e,i + 1);
  }
 else {
    throw new IllegalArgumentException("Cannot insert value twice!");
  }
}
