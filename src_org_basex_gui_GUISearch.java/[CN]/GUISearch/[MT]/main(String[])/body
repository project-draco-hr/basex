{
  Prop.read();
  GUIProp.read();
  GUIConstants.init();
  Link.delete();
  final Proc proc=Proc.get(CONTEXT,Commands.OPEN,FSDATABASE);
  if (!proc.execute()) {
    BaseX.errln(proc.info());
  }
 else {
    BaseXWin.init();
    new GUISearch();
  }
}
