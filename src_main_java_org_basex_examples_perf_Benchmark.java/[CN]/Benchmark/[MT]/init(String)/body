{
  out.println("=== " + Main.name(this) + " Test ===\n");
  if (!parseArguments(args))   return false;
  final Context ctx=new Context();
  session=local ? new LocalSession(ctx) : new ClientSession(ctx,"admin","admin");
  session.execute(new Set("info","all"));
  drop();
  return true;
}
