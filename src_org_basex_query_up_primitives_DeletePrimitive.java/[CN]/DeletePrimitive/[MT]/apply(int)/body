{
  if (!(node instanceof DBNode))   return;
  final DBNode n=(DBNode)node;
  final Data d=n.data;
  final int p=n.pre + add;
  d.delete(p);
  if (p >= d.meta.size)   return;
  final int l=p - 1;
  if (l > 1 && d.parent(l,d.kind(l)) == d.parent(p,d.kind(p)))   mergeTextNodes(d,l,p);
}
