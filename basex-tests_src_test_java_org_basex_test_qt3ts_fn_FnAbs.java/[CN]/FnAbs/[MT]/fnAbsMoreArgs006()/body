{
  final XQuery query=new XQuery("fn:abs(xs:decimal(\"-0\"))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("0") && assertType("xs:decimal")));
}
