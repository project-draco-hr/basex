{
  final XQuery query=new XQuery("declare variable $myVar := unordered(fn:reverse((<a/>, <b/>))); deep-equal($myVar, (<a/>, <b/>)) or deep-equal($myVar, (<b/>, <a/>))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
