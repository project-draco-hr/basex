{
switch (func) {
case _HOF_SORT_WITH:
    return sortWith(qc).iter();
case _HOF_ID:
case _HOF_CONST:
  return qc.iter(exprs[0]);
case _HOF_FOLD_LEFT1:
return foldLeft1(qc).iter();
case _HOF_UNTIL:
return until(qc).iter();
case _HOF_TOP_K_BY:
return topKBy(qc).iter();
case _HOF_TOP_K_WITH:
return topKWith(qc).iter();
default :
return super.iter(qc);
}
}
