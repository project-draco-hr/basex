{
  final XQuery query=new XQuery("max(for $x in 1 to 10 return xs:yearMonthDuration(concat(\"P\",$x,\"M\")))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"P10M"));
}
