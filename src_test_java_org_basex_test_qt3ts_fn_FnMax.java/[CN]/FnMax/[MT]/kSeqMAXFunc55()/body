{
  final XQuery query=new XQuery("max((current-date(), xs:date(\"1999-01-01\"))) eq current-date()",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
