{
  final XQuery query=new XQuery("max(for $x in 1 to 10 return xs:dayTimeDuration(concat(\"PT\",$x,\"H\")))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"PT10H"));
}
