{
  final XQuery query=new XQuery("xs:dateTime(\"1999-12-04T12:12:23\") eq xs:date(\"1999-12-04\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
