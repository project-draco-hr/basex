{
  assertEquals("2",get("?query=switch(1)+case+1+return+2+default+return+3&" + Options.XQUERY3[0] + "=true"));
  try {
    get("?query=switch(1)+case+1+return+2+default+return+3&" + Options.XQUERY3[0] + "=false");
    fail("Error expected.");
  }
 catch (  final IOException ex) {
    assertContains(ex.getMessage(),"[XPST0003]");
  }
}
