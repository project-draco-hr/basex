{
  final XQuery query=new XQuery("remove((1 to 10)[. mod 2 = 0],4)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"2 4 6 10"));
}
