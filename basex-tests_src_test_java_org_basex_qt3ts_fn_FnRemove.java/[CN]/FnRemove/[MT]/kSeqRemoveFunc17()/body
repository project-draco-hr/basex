{
  final XQuery query=new XQuery("remove((4, xs:untypedAtomic(\"4\")), 1) eq 4",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
