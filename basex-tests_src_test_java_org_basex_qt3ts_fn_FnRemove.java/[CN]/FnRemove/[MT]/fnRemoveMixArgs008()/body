{
  final XQuery query=new XQuery("fn:remove( (xs:integer(\"100\"), xs:string(\"abc\")), 1)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("\"abc\""));
}
