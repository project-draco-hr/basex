{
  final IntList docs=data.docs();
  if (list.length != docs.size()) {
    root=false;
  }
 else {
    int c=-1;
    while (++c < list.length && list[c] == docs.get(c))     ;
    root=c == list.length;
  }
  return this;
}
