{
  final IntList docs=data.resources.docs();
  if (pres.length != docs.size()) {
    root=false;
  }
 else {
    int c=-1;
    while (++c < pres.length && pres[c] == docs.get(c))     ;
    root=c == pres.length;
  }
  return this;
}
