{
  final int[] docs=data.doc();
  if (list == docs) {
    root=true;
  }
 else   if (list.length != docs.length) {
    root=false;
  }
 else {
    int c=-1;
    while (++c < list.length && list[c] == docs[c])     ;
    root=c == list.length;
    if (root)     list=docs;
  }
  return this;
}
