{
  final TokenBuilder tb=new TokenBuilder(Util.name(this) + '[');
  for (int i=0; i < list.length; ++i) {
    if (i > 0)     tb.add(',');
    tb.addNum(list[i]);
  }
  tb.add(']');
  return tb.toString();
}
