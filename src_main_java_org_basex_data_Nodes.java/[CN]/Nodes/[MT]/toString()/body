{
  final TokenBuilder tb=new TokenBuilder(Util.name(this) + '[');
  for (int i=0; i < nodes.length; ++i) {
    if (i > 0)     tb.add(',');
    tb.addNum(nodes[i]);
  }
  tb.add(']');
  return tb.toString();
}
