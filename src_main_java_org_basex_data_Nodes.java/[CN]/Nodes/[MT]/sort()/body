{
  if (sorted != null)   return;
  int i=Integer.MIN_VALUE;
  for (  final int n : nodes) {
    if (i > n) {
      sorted=Arrays.copyOf(nodes,size);
      Arrays.sort(sorted);
      return;
    }
    i=n;
  }
  sorted=nodes;
}
