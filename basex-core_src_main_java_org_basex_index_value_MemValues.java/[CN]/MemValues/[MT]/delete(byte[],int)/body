{
  if (index()) {
    final int i=id(key);
    if (i == 0 || len[i] == 0)     return;
    int p=-1;
    while (++p < len[i])     if (ids[i][p] == id)     break;
    if (p < len[i] - 1)     Array.move(ids[i],p + 1,-1,len[i] - (p + 1));
    len[i]--;
  }
}
