{
  final XQuery query=new XQuery("fn:error(fn:QName('http://www.w3.org/2005/xqt-errors', 'err:FOTY0012'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FOTY0012"));
}
