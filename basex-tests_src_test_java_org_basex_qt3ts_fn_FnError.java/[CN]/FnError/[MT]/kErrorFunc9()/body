{
  final XQuery query=new XQuery("error(QName(\"\", \"XPDY6666\"), \"description\", \"error object\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("*"));
}
