{
  final XQuery query=new XQuery("<elem><a/>{//west/@mark}</elem>",ctx);
  query.context(node(file("prod/AxisStep/TopMany.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XQTY0024"));
}
