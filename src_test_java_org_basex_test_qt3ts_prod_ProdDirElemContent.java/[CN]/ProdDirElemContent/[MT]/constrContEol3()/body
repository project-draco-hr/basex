{
  final XQuery query=new XQuery("<codepoints>{string-to-codepoints(<elem>&#xD;&#xA;</elem>)}</codepoints>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<codepoints>13 10</codepoints>",false));
}
