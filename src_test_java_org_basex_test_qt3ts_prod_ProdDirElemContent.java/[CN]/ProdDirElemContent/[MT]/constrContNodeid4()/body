{
  final XQuery query=new XQuery("for $x in <?pi content?>, $y in <elem>{$x}</elem> return exactly-one($y/processing-instruction()) is $x",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
