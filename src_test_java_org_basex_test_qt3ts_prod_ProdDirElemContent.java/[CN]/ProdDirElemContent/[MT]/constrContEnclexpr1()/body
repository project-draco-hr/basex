{
  final XQuery query=new XQuery("count((<elem>{1,'a',3.5,4e2}</elem>)/text())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("1"));
}
