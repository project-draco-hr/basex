{
  final XQuery query=new XQuery("<elem>{()}{//west/@mark}</elem>",ctx);
  query.context(node(file("prod/AxisStep/TopMany.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem mark=\"w0\"/>",false));
}
