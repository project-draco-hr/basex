{
  final XQuery query=new XQuery("<elem>{1, '', 2}</elem>",ctx);
  query.context(node(file("prod/CompAttrConstructor/DupNode.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem>1  2</elem>",false));
}
