{
  final XQuery query=new XQuery("<elem>{//west/@mark, //center/@mark}</elem>",ctx);
  query.context(node(file("prod/AxisStep/TopMany.xml")));
  final QT3Result res=result(query);
  result=res;
  test(error("XQDY0025"));
}
