{
  final XQuery query=new XQuery("count( document {'abc', 'def', document {'ghi', <anode/>, 'jkl'}, 'mno' } /node() )",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertEq("3"));
}
