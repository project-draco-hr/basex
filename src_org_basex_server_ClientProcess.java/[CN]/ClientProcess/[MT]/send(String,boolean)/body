{
  socket=new Socket(host,port);
  if (first && get) {
    new DataOutputStream(socket.getOutputStream()).writeUTF(command + "$");
  }
 else {
    new DataOutputStream(socket.getOutputStream()).writeUTF(command);
  }
}
