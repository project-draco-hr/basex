{
  final XQuery query=new XQuery("declare base-uri \"\"; ends-with(fn:static-base-uri(), \"prod/BaseURIDecl.xml\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPST0001")));
}
