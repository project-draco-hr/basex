{
  final XQuery query=new XQuery("declare base-uri \"abc\"; fn:ends-with(fn:string(fn:static-base-uri()),\"abc\")",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
