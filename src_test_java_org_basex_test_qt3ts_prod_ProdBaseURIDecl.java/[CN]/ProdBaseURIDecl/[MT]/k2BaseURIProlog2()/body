{
  final XQuery query=new XQuery("declare base-uri \"c:\\windows\"; 1",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("1") || error("XQST0046")));
}
