{
  try {
    final String key=((String)o).toUpperCase();
    obj.metaTypeEvent(SpotlightMediaType.valueOf(key));
  }
 catch (  final IllegalArgumentException ex) {
    BaseX.debug("SpotlightExtractor: unsupported MediaType found (%)",(String)o);
  }
}
