{
  final ByteList tb=new ByteList();
  tb.add('"');
  for (  final byte v : value) {
    if (v == '&')     tb.add(E_AMP);
 else     tb.add(v);
    if (v == '"')     tb.add('"');
  }
  return tb.add('"').toString();
}
