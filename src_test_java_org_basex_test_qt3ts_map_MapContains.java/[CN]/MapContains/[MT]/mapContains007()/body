{
  final XQuery query=new XQuery("map:contains(map:entry(xs:untypedAtomic(\"foo\"), \"bar\"), \"foo\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
