{
  final XQuery query=new XQuery("map:contains((map{}, map{\"a\":=\"b\"}), \"a\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
