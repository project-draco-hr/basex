{
  final XQuery query=new XQuery("fn:count(//center/node())",ctx);
  query.context(node(file("prod/AxisStep/Tree1Text.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
