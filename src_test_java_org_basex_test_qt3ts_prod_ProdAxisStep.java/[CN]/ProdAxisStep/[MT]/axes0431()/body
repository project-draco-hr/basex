{
  final XQuery query=new XQuery("fn:count(//center/text()/descendant-or-self::node())",ctx);
  query.context(node(file("prod/AxisStep/TreeTrunc.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
