{
  final XQuery query=new XQuery("let $attribute as attribute(foo) := attribute foo { } return count($attribute/self::bar)",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("0") || error("XPST0005")));
}
