{
  final XQuery query=new XQuery("xquery, version, encoding, default, declare, function, option, collation, schema, import",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPDY0002"));
}
