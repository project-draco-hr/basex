{
  final XQuery query=new XQuery("fn:count(//center/parent::nowhere)",ctx);
  query.context(node(file("prod/AxisStep/TreeCompass.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
