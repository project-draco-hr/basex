{
  final XQuery query=new XQuery("declare variable $var := <e><f>f's value</f></e>; $var/(/)/(/)//f",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPDY0050"));
}
