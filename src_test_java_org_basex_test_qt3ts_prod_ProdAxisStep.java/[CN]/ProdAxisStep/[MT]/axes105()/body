{
  final XQuery query=new XQuery("let $comment as comment() := <!--comment--> return count($comment/self::*:foo)",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("0") || error("XPST0005")));
}
