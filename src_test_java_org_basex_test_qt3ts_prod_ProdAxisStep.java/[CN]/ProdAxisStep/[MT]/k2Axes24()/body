{
  final XQuery query=new XQuery("<e>some text</e>/text()/..",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<e>some text</e>",false));
}
