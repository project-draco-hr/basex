{
  final XQuery query=new XQuery("<e><?name data?></e>/processing-instruction()/..",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<e><?name data?></e>",false));
}
