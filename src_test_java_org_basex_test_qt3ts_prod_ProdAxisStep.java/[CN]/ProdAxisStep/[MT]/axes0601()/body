{
  final XQuery query=new XQuery("fn:count(/descendant-or-self::south)",ctx);
  query.context(node(file("prod/AxisStep/TreeTrunc.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
