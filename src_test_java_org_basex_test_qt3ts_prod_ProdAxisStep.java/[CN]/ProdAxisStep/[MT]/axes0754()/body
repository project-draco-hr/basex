{
  final XQuery query=new XQuery("fn:count(//center//child::south)",ctx);
  query.context(node(file("prod/AxisStep/TreeStack.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("8"));
}
