{
  final XQuery query=new XQuery("fn:count(//west/attribute::west-attr-2)",ctx);
  query.context(node(file("prod/AxisStep/TreeTrunc.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
