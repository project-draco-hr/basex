{
  final XQuery query=new XQuery("<a> <b c=\"\"/> <d/> </a>/descendant-or-self::node()[last()]/ancestor::*",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a><b c=\"\"/><d/></a>",false));
}
