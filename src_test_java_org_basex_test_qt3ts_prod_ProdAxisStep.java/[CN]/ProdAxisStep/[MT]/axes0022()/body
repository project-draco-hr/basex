{
  final XQuery query=new XQuery("fn:count(//center/child::south-east)",ctx);
  query.context(node(file("prod/AxisStep/Tree1Child.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("0"));
}
