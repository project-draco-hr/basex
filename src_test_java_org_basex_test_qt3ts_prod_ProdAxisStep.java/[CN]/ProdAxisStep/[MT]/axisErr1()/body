{
  final XQuery query=new XQuery("let $var := <anElement>Some content</anElement> return $var/20[child::text()]",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("XPTY0020"));
}
