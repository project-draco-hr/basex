{
  final XQuery query=new XQuery("\n" + "        declare variable $myVar := <e/>; \n" + "        empty($myVar/preceding-sibling::comment()/a/b/c/1/@*)",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertBoolean(true) || error("XPST0005") || error("XPTY0019")|| error("XPTY0020")));
}
