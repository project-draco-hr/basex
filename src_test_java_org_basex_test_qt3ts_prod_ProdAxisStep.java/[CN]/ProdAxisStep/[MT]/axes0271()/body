{
  final XQuery query=new XQuery("fn:count(//center/@center-attr-3/self::node())",ctx);
  query.context(node(file("prod/AxisStep/TreeCompass.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
