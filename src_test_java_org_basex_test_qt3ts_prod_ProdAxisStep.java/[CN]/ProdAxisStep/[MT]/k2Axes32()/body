{
  final XQuery query=new XQuery("declare variable $myVar := <e/>; empty(fn:root($myVar/(/)/..))",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertBoolean(true) || error("XPST0005") || error("XPDY0050")));
}
