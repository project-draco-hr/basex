{
  final XQuery query=new XQuery("map-pairs(function($a as xs:integer, $b as xs:integer) as xs:integer{$a + $b}, 1 to 5, 1 to 5)",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("2, 4, 6, 8, 10"));
}
