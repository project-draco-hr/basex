{
  final XQuery query=new XQuery(" let $in := 1 to 5 return map-pairs(function($a, $b){$a+$b}, $in, tail($in)) ",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertDeepEq("3, 5, 7, 9"));
}
