{
  final Var v=scp.newCopyOf(ctx,var);
  vs.add(var.id,v);
  try {
    return new Window(info,sliding,v,expr.copy(ctx,scp,vs),start.copy(ctx,scp,vs),only,end != null ? end.copy(ctx,scp,vs) : null);
  }
 catch (  QueryException e) {
    throw Util.notexpected(e);
  }
}
