{
  if (level != 0)   return false;
  if (!super.doctype(type) && html5) {
    if (sep)     indent();
    out.print(DOCTYPE);
    if (type == null)     out.print(HTML);
 else     out.print(type);
    out.print(ELEM_C);
    newline();
  }
  return true;
}
