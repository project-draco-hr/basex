{
  if (level != 0)   return false;
  if (!super.doctype(dt) && html5) {
    if (sep)     indent();
    print(DOCTYPE);
    if (dt == null)     print(HTML);
 else     print(dt);
    print(ELEM_C);
    print(nl);
  }
  return true;
}
