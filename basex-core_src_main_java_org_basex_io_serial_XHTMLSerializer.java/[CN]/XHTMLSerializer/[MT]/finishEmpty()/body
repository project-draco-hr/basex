{
  if (ct(true,false))   return;
  if ((html5 ? HTMLSerializer.EMPTIES5 : HTMLSerializer.EMPTIES).contains(lc(tag))) {
    print(' ');
    print(ELEM_SC);
  }
 else {
    print(ELEM_C);
    sep=false;
    finishClose();
  }
}
