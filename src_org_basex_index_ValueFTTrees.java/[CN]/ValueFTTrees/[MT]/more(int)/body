{
  if (nsize == -1) {
    nsize=getNextMin();
    if (nsize == -1)     return false;
  }
  if (trees[nsize].more(cf))   return true;
  sizes.set(-sizes.get(nsize),nsize);
  nsize=-1;
  return more(cf);
}
