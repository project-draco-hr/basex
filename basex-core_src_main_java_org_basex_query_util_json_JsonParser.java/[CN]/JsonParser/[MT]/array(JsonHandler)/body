{
  consumeWs('[',true);
  h.openArray();
  if (!consumeWs(']',false)) {
    do {
      h.openArrayEntry();
      value(h);
      h.closeArrayEntry();
    }
 while (consumeWs(',',false) && !(spec == Spec.LIBERAL && curr() == ']'));
    consumeWs(']',true);
  }
  h.closeArray();
}
