{
  if (size != ft.size)   return false;
  for (int i=0; i < size; i++) {
    if (pos[i].pre != ft.pos[i].pre || !Array.eq(pos[i].pos,ft.pos[i].pos) || !Array.eq(pos[i].poi,ft.pos[i].poi))     return false;
  }
  return true;
}
