{
  if (size != ft.size)   return false;
  for (int i=0; i < size; i++) {
    if (pre[i] != ft.pre[i] || !Array.eq(pos[i],ft.pos[i]) || !Array.eq(poi[i],ft.poi[i]))     return false;
  }
  return true;
}
