{
  if (size != ft.size)   return false;
  for (int i=0; i < size; i++) {
    if (pos[i].pre != ft.pos[i].pre || !Arrays.equals(pos[i].pos,ft.pos[i].pos) || !Arrays.equals(pos[i].poi,ft.pos[i].poi))     return false;
  }
  return true;
}
