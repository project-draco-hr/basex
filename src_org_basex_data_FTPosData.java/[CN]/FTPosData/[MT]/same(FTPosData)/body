{
  if (size != ft.size)   return false;
  for (int i=0; i < size; i++) {
    if (ftp[i].pre != ft.ftp[i].pre || !Array.eq(ftp[i].pos,ft.ftp[i].pos) || !Array.eq(ftp[i].poi,ft.ftp[i].poi))     return false;
  }
  return true;
}
