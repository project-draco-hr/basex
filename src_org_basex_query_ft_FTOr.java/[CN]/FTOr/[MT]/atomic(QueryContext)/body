{
  FTItem it=null;
  for (  final FTExpr e : expr) {
    final FTItem i=e.atomic(ctx);
    if (it != null) {
      it.all.or(i.all);
      it.score(ctx.score.or(it.score(),i.score()));
    }
 else {
      it=i;
    }
  }
  return it;
}
