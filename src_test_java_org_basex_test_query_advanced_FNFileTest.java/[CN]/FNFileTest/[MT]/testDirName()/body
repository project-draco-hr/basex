{
  final String fun=check(Function.DIRNAME);
  assertEquals(norm(query(fun + "('" + PATH1+ "')")).toLowerCase(),norm(Prop.TMP));
  assertEquals(query(fun + "('')"),".");
  assertEquals(query(fun + "('" + NAME+ "')"),".");
}
