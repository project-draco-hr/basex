{
  final String fun=check(FunDef.PATHNATIVE);
  final String path1=query(fun + "('" + PATH1+ "')");
  assertEquals(path1,PATH1);
  final String path2=query(fun + "('" + PATH1+ DIRSEP+ ".."+ DIRSEP+ "test.xml"+ "')");
  assertEquals(path2,Prop.TMP + "test.xml");
}
