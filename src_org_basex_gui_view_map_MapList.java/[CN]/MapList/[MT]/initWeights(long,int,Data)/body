{
  weights=new double[list.length];
  for (int i=0; i < size - 1; i++) {
    if (GUIProp.sizep != 100) {
      weights[i]=MapLayout.calcWeight(sizes[i],nrchilds[i],parsize,parchilds,data);
    }
 else {
      weights[i]=0d;
    }
  }
}
