{
  weights=new double[list.length];
  for (int i=0; i < list.length; i++) {
    long s=data.fs != null ? Token.toLong(data.attValue(data.sizeID,list[i])) : 0;
    weights[i]=MapLayout.calcWeight(s,list[i + 1] - list[i],parsize,parchilds,data);
  }
  System.out.println(weights.toString());
}
