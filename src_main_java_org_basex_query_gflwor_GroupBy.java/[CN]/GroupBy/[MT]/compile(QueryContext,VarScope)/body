{
  for (int i=0; i < by.length; i++)   by[i].compile(cx,sc);
  for (int i=0; i < pre.length; i++) {
    final SeqType it=pre[i].type();
    post[i].refineType(SeqType.get(it.type,it.mayBeZero() ? Occ.ZERO_MORE : Occ.ONE_MORE),info);
  }
  return this;
}
