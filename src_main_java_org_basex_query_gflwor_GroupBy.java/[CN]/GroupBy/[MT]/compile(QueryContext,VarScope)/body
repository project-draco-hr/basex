{
  for (int i=0; i < by.length; i++)   by[i].compile(cx,sc);
  for (int i=0; i < pre.length; i++) {
    final SeqType it=pre[i].declaredType();
    post[i].refineType(it.withOcc(it.mayBeZero() ? Occ.ZERO_MORE : Occ.ONE_MORE),cx,info);
  }
  return this;
}
