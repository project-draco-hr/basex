{
  try {
    final ClientSession cs=login(user,pass);
    try {
      cs.execute("drop database " + dbname);
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
