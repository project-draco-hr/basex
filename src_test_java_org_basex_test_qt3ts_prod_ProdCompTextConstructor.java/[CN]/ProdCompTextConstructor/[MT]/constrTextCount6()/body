{
  final XQuery query=new XQuery("count(document {text {''},<b/>,text {''},<b/>,text {''}}/text())",ctx);
  final QT3Result res=result(query);
  result=res;
  test((assertEq("0") || error("XPST0005")));
}
