{
  final XQuery query=new XQuery("<elem>{text {'one'}}<a>{text {'two'}}</a>{text {'three'}}</elem>",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<elem>one<a>two</a>three</elem>",false));
}
