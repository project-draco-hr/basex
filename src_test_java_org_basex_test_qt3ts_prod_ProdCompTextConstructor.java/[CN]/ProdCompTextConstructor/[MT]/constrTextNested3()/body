{
  final XQuery query=new XQuery("document {text {'one'}, <a/>, text {'two'}, <b/>, text {'three'}}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("one<a/>two<b/>three",false));
}
