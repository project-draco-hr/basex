{
  final XQuery query=new XQuery("text {text {'one', text {'two'}}, 'three', text {'four'}}",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"one two three four"));
}
