{
  final XQuery query=new XQuery("text {., .}",ctx);
  query.context(node(file("prod/CompAttrConstructor/DupNode.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"texttext texttext"));
}
