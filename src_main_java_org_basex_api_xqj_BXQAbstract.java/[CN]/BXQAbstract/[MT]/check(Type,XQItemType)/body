{
  opened();
  if (tar == null)   return e;
  final Type t=((BXQItemType)tar).getType();
  if (e != t && e != Type.ATM && (e.node() || t.node()))   throw new BXQException(WRONG,tar,e);
  return t;
}
