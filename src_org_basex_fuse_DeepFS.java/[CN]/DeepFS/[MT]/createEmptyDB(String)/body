{
  try {
    final Parser p=new Parser(IO.get(n)){
      @Override public void parse(      final Builder build){
      }
    }
;
    final Data d=CreateDB.xml(p,n);
    d.fs=this;
    return d;
  }
 catch (  final IOException e) {
    e.printStackTrace();
  }
catch (  final Exception e) {
    e.printStackTrace();
  }
  return null;
}
