{
  try {
    final Data d=CreateDB.xml(ctx,Parser.emptyParser(IO.get(name),ctx.prop),name);
    ctx.openDB(d);
    d.fs=this;
    return d;
  }
 catch (  final IOException ex) {
    ex.printStackTrace();
  }
catch (  final Exception ex) {
    ex.printStackTrace();
  }
  return null;
}
