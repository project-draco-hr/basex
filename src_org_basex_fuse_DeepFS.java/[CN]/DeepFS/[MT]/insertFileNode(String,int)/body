{
  int ppre=parentPre(path);
  if (ppre == -1) {
    BaseX.errln("no parent");
    return -1;
  }
  if (isRegFile(mode))   return insert(ppre,buildFileData(path,mode));
 else   return insert(ppre,buildData(path,mode));
}
