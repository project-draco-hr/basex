{
  final String method="[-basex_mkdir] ";
  final int n=createNode(path,S_IFDIR | mode);
  BaseX.errln(method + "path: " + path+ " mode: "+ Integer.toOctalString(mode)+ " id : ("+ n+ ")");
  refresh();
  return n;
}
