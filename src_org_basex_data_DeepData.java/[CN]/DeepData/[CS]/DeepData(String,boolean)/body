{
  if (libError != null)   throw new IOException(libError);
  jni_init(index);
  meta=new MetaData(db);
  size=jni_get_file_table_size();
  tags=null;
  atts=null;
  skel=null;
  ns=new Namespaces();
  table=null;
  texts=null;
  values=null;
}
