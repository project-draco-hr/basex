{
  if (libError != null)   throw new IOException(libError);
  jniInit(index);
  meta=new MetaData(db);
  size=jniGetFileTableSize();
  tags=null;
  atts=null;
  skel=null;
  ns=new Namespaces();
  table=null;
  texts=null;
  values=null;
}
