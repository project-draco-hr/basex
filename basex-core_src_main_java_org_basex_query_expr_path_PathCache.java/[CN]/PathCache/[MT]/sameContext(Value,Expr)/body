{
  if (value == initial && (value == null || value.sameAs(initial)))   return true;
  return root instanceof Root && value instanceof ANode && initial instanceof ANode&& ((ANode)initial).root().sameAs(((ANode)value).root());
}
