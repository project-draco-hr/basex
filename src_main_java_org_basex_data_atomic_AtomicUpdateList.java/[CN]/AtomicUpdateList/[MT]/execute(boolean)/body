{
  check();
  optimize();
  applyValueUpdates();
  if (cacheDistanceUpdates)   data.cache=true;
  applyStructuralUpdates();
  updateDistances();
  if (mergeTexts)   resolveTextAdjacency();
  data.cache=false;
}
