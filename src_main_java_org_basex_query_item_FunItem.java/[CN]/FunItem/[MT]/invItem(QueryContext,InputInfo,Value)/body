{
  final ValueIter ir=invIter(ctx,ii,args);
  final Item it=ir.next();
  if (it == null || ir.size() == 1)   return it;
  final Item n=ir.next();
  if (n != null)   XPSEQ.thrw(ii,PAR1 + it + SEP+ n+ (ir.next() != null ? SEP + DOTS : "")+ PAR2);
  return it;
}
