{
  final XQuery query=new XQuery("fn:number(xs:integer(\"830993497117024304\")) eq 830993497117024304",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertBoolean(true));
}
