{
  final XQuery query=new XQuery("number(true())",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertType("xs:double") && assertEq("1.0e0")));
}
