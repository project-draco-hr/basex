{
  final XQuery query=new XQuery("declare namespace xml = \"http://www.w3.org/XML/1998/namespace\"; \"a\"",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XQST0070"));
}
