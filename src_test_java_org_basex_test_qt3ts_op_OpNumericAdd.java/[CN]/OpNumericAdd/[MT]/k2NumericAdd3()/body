{
  final XQuery query=new XQuery("<a foo=\"1\"/>/@foo + <b foo=\"2\"/>/@foo",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("3"));
}
