{
  for (  final ForLet f : fl) {
    if (f.uses(v))     return true;
    if (f.shadows(v))     return false;
  }
  return where != null && where.uses(v) || order != null && order.uses(v) || group != null && group.uses(v) || ret.uses(v);
}
