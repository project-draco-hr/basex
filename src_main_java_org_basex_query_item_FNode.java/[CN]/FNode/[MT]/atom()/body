{
  if (val == null) {
    final TokenBuilder tb=new TokenBuilder();
    for (int c=0; c < children.size(); ++c) {
      final ANode nc=children.get(c);
      if (nc.type == NodeType.ELM || nc.type == NodeType.TXT)       tb.add(nc.atom());
    }
    val=tb.finish();
  }
  return val;
}
