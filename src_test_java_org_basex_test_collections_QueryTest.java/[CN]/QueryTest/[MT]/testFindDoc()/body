{
  final String find="for $x in ." + " where $x[ends-with(document-uri(.), '" + FILES[1] + "')]"+ " and $x//religions/text() contains text 'Catholic' "+ " return base-uri($x)";
  QueryProcessor qp=new QueryProcessor(find,CTX);
  assertEquals(1,qp.query().size());
}
