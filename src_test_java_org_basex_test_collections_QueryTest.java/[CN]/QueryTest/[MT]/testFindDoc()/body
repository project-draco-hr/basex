{
  final String find="for $x in ." + " where $x[ends-with(document-uri(.), '" + FILES[1] + "')]"+ " and $x//location contains text 'uzbekistan' "+ " return base-uri($x)";
  final QueryProcessor qp=new QueryProcessor(find,CTX);
  assertEquals(1,qp.query().size());
  qp.close();
}
