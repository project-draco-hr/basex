{
  new CreateColl(NAME).execute(CTX);
  for (  String file : FILES)   new Add(file,"etc/xml").execute(CTX);
  new Add(ZIP,"test/zipped").execute(CTX);
  new CreateIndex(Commands.CmdIndex.FULLTEXT);
}
