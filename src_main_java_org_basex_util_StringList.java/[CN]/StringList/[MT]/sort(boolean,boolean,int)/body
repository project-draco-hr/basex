{
  Arrays.sort(list,pos,size,new Comparator<String>(){
    @Override public int compare(    final String s1,    final String s2){
      final int c=cs ? s1.compareTo(s2) : s1.toLowerCase().compareTo(s2.toLowerCase());
      return asc ? c : -c;
    }
  }
);
}
