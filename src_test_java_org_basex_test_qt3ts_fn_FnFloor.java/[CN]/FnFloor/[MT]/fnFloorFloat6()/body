{
  final XQuery query=new XQuery("floor(xs:float(-12345678.567890e0))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("-12345679e0") && assertType("xs:float")));
}
