{
  final XQuery query=new XQuery("floor(xs:unsignedShort(.)) instance of xs:integer",ctx);
  query.context(node(file("fn/abs/e1.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
