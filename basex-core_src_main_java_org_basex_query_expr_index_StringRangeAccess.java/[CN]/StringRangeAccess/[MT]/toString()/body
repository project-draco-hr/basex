{
  final boolean text=index.type() == IndexType.TEXT;
  return (text ? Function._DB_TEXT_RANGE : Function._DB_ATTRIBUTE_RANGE).get(null,info,Str.get(ictx.data.meta.name),Str.get(index.min),Str.get(index.max)).toString();
}
