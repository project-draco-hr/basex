{
  final XQuery query=new XQuery("fn:doc-available(document-uri(/))",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
