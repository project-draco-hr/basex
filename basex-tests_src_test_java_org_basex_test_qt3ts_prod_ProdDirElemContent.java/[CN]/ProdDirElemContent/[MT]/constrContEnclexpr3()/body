{
  final XQuery query=new XQuery("<elem>{1,'a',3.5,4e2}</elem>",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertSerialization("<elem>1 a 3.5 400</elem>",false));
}
