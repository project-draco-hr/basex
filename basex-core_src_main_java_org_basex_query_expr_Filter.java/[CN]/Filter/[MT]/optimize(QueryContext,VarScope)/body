{
  final Value cv=qc.value;
  try {
    if (root.isEmpty())     return optPre(qc);
    if (root instanceof AxisPath && !super.has(Flag.FCS))     return ((Path)root.copy(qc,scp)).addPreds(qc,scp,preds);
    return preds.length == 0 ? root : opt(qc);
  }
  finally {
    qc.value=cv;
  }
}
