{
switch (func) {
case _MAP_GET:
    return get(qc).iter();
case _MAP_KEYS:
  return toMap(exprs[0],qc).keys().iter();
case _MAP_FOR_EACH_ENTRY:
return forEachEntry(qc);
default :
return super.iter(qc);
}
}
