{
  final XQuery query=new XQuery("10000 < (/works/employee[1]/hours[1])",ctx);
  query.context(node(file("docs/works.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
