{
switch (func) {
case INDEXOF:
    return indexOf(arg);
case DISTINCT:
  return distinct(arg);
case INSBEF:
return insbef(arg);
case REVERSE:
return reverse(arg);
case REMOVE:
return remove(arg);
case SUBSEQ:
return subseq(arg);
case DEEPEQ:
return Bln.get(deep(arg)).iter();
default :
throw new RuntimeException("Not defined: " + func);
}
}
