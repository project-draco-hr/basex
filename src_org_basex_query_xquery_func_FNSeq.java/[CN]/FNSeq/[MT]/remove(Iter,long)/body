{
  return new Iter(){
    long c=0;
    @Override public Item next() throws XQException {
      Item i;
      while ((i=iter.next()) != null)       if (++c != pos)       return i;
      return null;
    }
  }
;
}
