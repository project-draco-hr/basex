{
  final QueryException qe=new QueryException(err,arg);
  mark();
  if (qe.simple().startsWith("Unexpected") || qe.simple().equals("Expecting attribute name."))   filter(false);
  qe.complete(this,complete());
  throw qe;
}
