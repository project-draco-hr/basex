{
  final Type ta=a.type;
  final Type tb=b.type;
  if (ta != tb && (!a.isUntyped() && !b.isUntyped() && !(a.isString() && b.isString())&& !(a.isNumber() && b.isNumber())&& !a.isFunction()&& !b.isFunction() || ta == AtomType.QNM || tb == AtomType.QNM))   XPTYPECMP.thrw(input,ta,tb);
  return op.op.e(input,a,b);
}
