{
  ctx.wrapping=DataText.YES.equals(val);
  if (!ctx.wrapping && !DataText.NO.equals(val)) {
    throw new RESTException(SC_BAD_REQUEST,SerializerProp.error(WRAP,val,DataText.YES,DataText.NO).getMessage());
  }
}
