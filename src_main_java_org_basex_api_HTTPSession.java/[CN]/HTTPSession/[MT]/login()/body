{
  if (user == null || pass == null)   throw new LoginException(NOPASSWD);
  return CLIENT.equals(System.getProperty(DBMODE)) ? new ClientSession(CONTEXT,user,pass) : new LocalSession(CONTEXT,user,pass);
}
