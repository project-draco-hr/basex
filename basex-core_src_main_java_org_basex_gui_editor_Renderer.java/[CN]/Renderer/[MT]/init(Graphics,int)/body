{
  font=defaultFont;
  color=Color.black;
  syntax.init();
  text.init();
  link=false;
  offset=OFFSET;
  if (lineNumbers)   offset+=fontWidth(g,Integer.toString(text.lines())) + OFFSET * 2;
  x=offset;
  y=fontHeight - pos - 2;
  line=1;
  if (g != null)   g.setFont(font);
}
