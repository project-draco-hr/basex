{
  final XQuery query=new XQuery("floor(xs:float(0.000000001e0))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test((assertEq("0.0e0") && assertType("xs:float")));
}
