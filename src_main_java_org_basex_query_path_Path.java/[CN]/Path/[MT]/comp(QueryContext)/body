{
  if (root != null) {
    root=checkUp(root,ctx).comp(ctx);
    if (root instanceof Context)     root=null;
  }
  final Value v=ctx.value;
  try {
    ctx.value=root(ctx);
    return compPath(ctx);
  }
  finally {
    ctx.value=v;
  }
}
