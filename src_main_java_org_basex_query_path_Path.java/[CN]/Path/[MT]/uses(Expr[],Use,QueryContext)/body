{
  if (use == Use.CTX || use == Use.ELM)   return root == null || root.uses(use,ctx);
  for (  final Expr s : step)   if (s.uses(use,ctx))   return true;
  return root != null && root.uses(use,ctx);
}
