{
  final XQuery query=new XQuery("let $ordDoc := (/) return (max( (2, 1, 5, 4, 3) ))",ctx);
  query.context(node(file("app/FunctxFn/functx_order.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("5"));
}
