{
  final XQuery query=new XQuery("(reverse( (1, 2, 3, 4, 5) ), reverse( (6, 2, 4) ), reverse( () ))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"5 4 3 2 1 4 2 6"));
}
