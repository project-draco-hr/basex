{
  final XQuery query=new XQuery("let $in-xml := <in-xml> <a></a> <b/> <c>xyz</c> </in-xml> return (empty($in-xml/a))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
