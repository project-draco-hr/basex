{
  final XQuery query=new XQuery("let $in-xml := <in-xml> <a>3</a> <b>5</b> <b>3</b> </in-xml> return (distinct-values( ('a', 2, 3) ))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"a 2 3"));
}
