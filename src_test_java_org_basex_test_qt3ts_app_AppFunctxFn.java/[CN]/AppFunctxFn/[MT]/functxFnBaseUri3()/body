{
  final XQuery query=new XQuery("let $cats := (/) return (base-uri($cats//catalog[2]/product/@href))",ctx);
  query.context(node(file("app/FunctxFn/functx_cats.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"http://example.org/WMN/"));
}
