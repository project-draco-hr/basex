{
  final XQuery query=new XQuery("(resolve-uri( 'http://example.org','http://datypic.com'))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"http://example.org"));
}
