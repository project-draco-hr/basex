{
  final XQuery query=new XQuery("(remove( ('a', 'b', 'c'), 2) , remove( ('a', 'b', 'c'), 10), remove( ('a', 'b', 'c'), 0))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"a c a b c a b c"));
}
