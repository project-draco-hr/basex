{
  final XQuery query=new XQuery("(replace('25', '(\\d+)', '\\$$1.00'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"$25.00"));
}
