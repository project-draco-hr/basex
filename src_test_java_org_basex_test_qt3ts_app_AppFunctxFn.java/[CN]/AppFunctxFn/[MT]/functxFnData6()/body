{
  final XQuery query=new XQuery("let $cat := (/) return (data($cat//product[4]/desc))",ctx);
  query.context(node(file("app/FunctxFn/functx_catalog.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"Our favorite shirt!"));
}
