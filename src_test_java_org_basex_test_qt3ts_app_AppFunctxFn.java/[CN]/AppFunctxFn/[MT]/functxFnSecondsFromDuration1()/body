{
  final XQuery query=new XQuery("(seconds-from-duration( xs:dayTimeDuration('PT30.5S')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"30.5"));
}
