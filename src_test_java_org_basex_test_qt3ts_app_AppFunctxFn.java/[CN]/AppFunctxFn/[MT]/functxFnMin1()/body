{
  final XQuery query=new XQuery("let $ordDoc := (/) return (min( (2.0, 1, 3.5, 4) ))",ctx);
  query.context(node(file("app/FunctxFn/functx_order.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("1"));
}
