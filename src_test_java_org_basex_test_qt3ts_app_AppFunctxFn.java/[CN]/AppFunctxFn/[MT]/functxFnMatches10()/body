{
  final XQuery query=new XQuery("let $address := '123 Main Street Traverse City, MI 49684' return (matches($address, 'Street.*City', 's'))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
