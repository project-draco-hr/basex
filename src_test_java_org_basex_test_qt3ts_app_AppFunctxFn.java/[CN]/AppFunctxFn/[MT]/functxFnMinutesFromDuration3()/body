{
  final XQuery query=new XQuery("(minutes-from-duration( xs:dayTimeDuration('PT1M90S')))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertEq("2"));
}
