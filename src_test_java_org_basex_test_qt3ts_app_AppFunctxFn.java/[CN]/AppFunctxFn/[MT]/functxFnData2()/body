{
  final XQuery query=new XQuery("let $cat := (/) return (data($cat//number))",ctx);
  query.context(node(file("app/FunctxFn/functx_catalog.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"557 563 443 784"));
}
