{
  final XQuery query=new XQuery("(replace('query', '[ry]+', 'l'))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"quel"));
}
