{
  final XQuery query=new XQuery("let $ordDoc := (/) return (boolean('false'))",ctx);
  query.context(node(file("app/FunctxFn/functx_order.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
