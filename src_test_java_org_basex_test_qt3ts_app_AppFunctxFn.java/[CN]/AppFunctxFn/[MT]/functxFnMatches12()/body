{
  final XQuery query=new XQuery("let $address := '123 Main Street\n" + "Traverse City, MI 49684' return (matches($address, 'Street$', 'm'))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
