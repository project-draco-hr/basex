{
  final XQuery query=new XQuery("(exists( ('a', 'b', 'c') ), exists( '' ), exists( () ), exists( false() ))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"true true false true"));
}
