{
  final XQuery query=new XQuery("let $ordDoc := (/) return (sum( (1, 2, 3) ))",ctx);
  query.context(node(file("app/FunctxFn/functx_order.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertEq("6"));
}
