{
  final XQuery query=new XQuery("(index-of( (<a>1</a>, <b>1</b>), <c>1</c> ))",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(assertStringValue(false,"1 2"));
}
