{
  final XQuery query=new XQuery("let $priceDoc := (/) return (number('29.99'))",ctx);
  query.context(node(file("app/FunctxFn/functx_prices.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertStringValue(false,"29.99"));
}
