{
  final XQuery query=new XQuery("let $ordDoc := (/) return (boolean( () ))",ctx);
  query.context(node(file("app/FunctxFn/functx_order.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(false));
}
