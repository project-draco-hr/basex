{
  final XQuery query=new XQuery("let $in-xml := <a><x>123</x></a> return (root($in-xml/x))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertSerialization("<a><x>123</x></a>",false));
}
