{
  final XQuery query=new XQuery("fn:error(fn:QName('http://www.example.com/HR', 'myerr:toohighsal'), 'Does not apply because salary is too high')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("*"));
}
