{
  final XQuery query=new XQuery("fn:error((), 'err:FOER0000','error raised by this test by setting first argument to empty sequence')",ctx);
  final QT3Result res=result(query);
  result=res;
  test(error("FOER0000"));
}
