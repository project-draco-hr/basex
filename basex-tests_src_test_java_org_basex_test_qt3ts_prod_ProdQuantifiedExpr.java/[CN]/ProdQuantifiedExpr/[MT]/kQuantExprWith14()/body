{
  final XQuery query=new XQuery("some $a as xs:integer+ in (1, 2), $b as xs:string* in $a satisfies $b",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
