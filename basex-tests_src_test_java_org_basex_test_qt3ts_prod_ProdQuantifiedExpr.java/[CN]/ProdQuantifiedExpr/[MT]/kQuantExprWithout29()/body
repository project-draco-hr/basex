{
  final XQuery query=new XQuery("every $var in (true(), xs:hexBinary(\"FF\"), true()) satisfies $var",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
