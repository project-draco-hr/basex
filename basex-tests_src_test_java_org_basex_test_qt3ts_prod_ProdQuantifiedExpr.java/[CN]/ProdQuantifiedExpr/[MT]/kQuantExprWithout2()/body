{
  final XQuery query=new XQuery("every $foo in 1 satisfies QName(\"example.com/\", \"ncname\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("FORG0006"));
}
