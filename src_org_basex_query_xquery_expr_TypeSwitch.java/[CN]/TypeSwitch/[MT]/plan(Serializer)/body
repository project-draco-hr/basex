{
  ser.startElement(this);
  if (var.name != null)   ser.attribute(VAR,var.name.str());
  ser.finishElement();
  for (  Case c : cs)   c.plan(ser);
  ts.plan(ser);
  expr.plan(ser);
  ser.closeElement();
}
