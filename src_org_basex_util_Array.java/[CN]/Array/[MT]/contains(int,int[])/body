{
  if (a == null || a.length == 0)   return false;
  int f=0;
  int l=a.length - 1;
  while (f <= l) {
    final int m=f + ((l - f) >> 1);
    if (a[m] < i) {
      f=m + 1;
    }
 else     if (a[m] > i) {
      l=m - 1;
    }
 else {
      return true;
    }
  }
  return false;
}
