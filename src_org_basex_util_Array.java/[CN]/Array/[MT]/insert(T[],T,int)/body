{
  final T[] tmp=(T[])java.lang.reflect.Array.newInstance(ar.getClass().getComponentType(),ar.length + 1);
  System.arraycopy(ar,0,tmp,0,p);
  System.arraycopy(ar,p,tmp,p + 1,ar.length - p);
  tmp[p]=a;
  return tmp;
}
