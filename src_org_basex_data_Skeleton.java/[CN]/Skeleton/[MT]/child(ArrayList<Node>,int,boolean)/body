{
  final ArrayList<Node> ch=new ArrayList<Node>();
  for (  final Node r : n) {
    if (t != 0 && r.name != t)     continue;
    for (    final Node c : r.ch) {
      if (desc)       c.desc(ch);
 else       ch.add(c);
    }
  }
  return ch;
}
