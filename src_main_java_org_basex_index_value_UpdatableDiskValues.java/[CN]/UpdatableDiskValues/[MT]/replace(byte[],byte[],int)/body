{
  final int p=get(o);
  if (p >= 0) {
    final int[] tmp=new int[]{id};
    if (deleteIds(p,o,tmp) == 0) {
      cache.delete(o);
      tmp[0]=p;
      deleteKeys(tmp);
    }
  }
  insertId(n,id);
}
