{
  final byte[] nm=lc(name.ln());
  FNIndex.get().error(nm);
  final Levenshtein ls=new Levenshtein();
  for (int n=0; n < size; n++) {
    if (ls.similar(nm,lc(func[n].var.name.ln())))     Err.or(FUNSIMILAR,name.str(),func[n].var.name.str());
  }
}
