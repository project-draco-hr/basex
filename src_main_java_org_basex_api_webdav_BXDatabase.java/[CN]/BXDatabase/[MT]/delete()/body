{
  try {
    final ClientSession cs=login(user,pass);
    try {
      cs.execute(new DropDB(db));
    }
  finally {
      cs.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
