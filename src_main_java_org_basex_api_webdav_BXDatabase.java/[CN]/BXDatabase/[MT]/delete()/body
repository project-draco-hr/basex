{
  try {
    final Session s=BXResourceFactory.login(user,pass);
    try {
      s.execute(new DropDB(db));
    }
  finally {
      s.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
