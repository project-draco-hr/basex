{
  try {
    final Session s=factory.login(user,pass);
    try {
      s.execute(new DropDB(db));
    }
  finally {
      s.close();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
