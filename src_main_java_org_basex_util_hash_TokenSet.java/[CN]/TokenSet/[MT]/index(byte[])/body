{
  checkSize();
  final int b=Token.hash(key) & bucket.length - 1;
  for (int r=bucket[b]; r != 0; r=next[r])   if (eq(key,keys[r]))   return -r;
  next[size]=bucket[b];
  keys[size]=key;
  bucket[b]=size;
  return size++;
}
