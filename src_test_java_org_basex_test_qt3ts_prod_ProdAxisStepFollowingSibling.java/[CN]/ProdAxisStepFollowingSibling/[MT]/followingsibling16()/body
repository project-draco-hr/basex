{
  final XQuery query=new XQuery("(/works[1]/employee[12]/following-sibling::employee) and fn:true()",ctx);
  query.context(node(file("docs/works-mod.xml")));
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
