{
  try {
    if (first) {
      session.event(EVENT_NAME,"1",value);
    }
 else {
      session.event(EVENT_NAME + 1,"1",value);
    }
    session.close();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
