{
  data=new MemData(tags,atts,ns,path,prop);
  meta=data.meta;
  meta.name=name;
  meta.textindex=true;
  meta.attrindex=true;
  meta.ftindex=false;
  meta.path=parser.file;
  parse(name);
  data.init();
  return data;
}
