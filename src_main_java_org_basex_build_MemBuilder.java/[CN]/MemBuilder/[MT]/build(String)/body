{
  data=new MemData(tags,atts,ns,path,prop);
  meta=data.meta;
  meta.name=name;
  meta.txtindex=true;
  meta.atvindex=true;
  meta.ftxindex=false;
  meta.file=parser.file;
  parse(name);
  data.init();
  return data;
}
