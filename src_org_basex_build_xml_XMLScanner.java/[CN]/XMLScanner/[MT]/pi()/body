{
  final byte[] tok=name(true);
  if (eq(lc(tok),XML))   error(PIRES);
  token.add(tok);
  int ch=nextChar();
  if (ch != '?' && !ws(ch))   error(PITEXT);
  do {
    while (ch != '?') {
      token.addUTF(ch);
      ch=nextChar();
    }
    if ((ch=consume()) == '>')     return;
    token.add('?');
  }
 while (true);
}
