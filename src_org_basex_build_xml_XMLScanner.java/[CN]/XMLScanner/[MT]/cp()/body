{
  s();
  byte[] name=name(false);
  if (name == null) {
    check('(');
    s();
    cp();
  }
  boolean s=s();
  if (consume('|') || consume(',')) {
    cp();
    s();
  }
 else   if (s) {
  }
  occ();
  if (name == null) {
    check(')');
    occ();
  }
}
