{
  final TokenBuilder name=new TokenBuilder();
  byte c=consume();
  if (!isFirstLetter(c)) {
    if (f)     error(INVNAME);
    prev(1);
    return null;
  }
  do   name.add(c);
 while (isLetter(c=nextChar()));
  prev(1);
  return name.finish();
}
