{
  final TokenBuilder name=new TokenBuilder();
  int c=consume();
  if (!isFirstLetter(c)) {
    if (f)     error(INVNAME);
    prev(1);
    return null;
  }
  do   name.addUTF(c);
 while (isLetterOrDigit(c=nextChar()));
  prev(1);
  return name.finish();
}
