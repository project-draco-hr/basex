{
  token.reset();
  final int ch=consume();
  if (ch == 0) {
    type=Type.EOF;
    return false;
  }
switch (state) {
case CONTENT:
    scanCONTENT(ch);
  break;
case TAG:
case ATT:
scanTAG(ch);
break;
case QUOTE:
scanATTVALUE(ch);
}
return true;
}
