{
  byte ch=0;
  chars=true;
  while (true) {
    while ((ch=nextChar()) != ']')     token.add(ch);
    if ((nextChar()) == ']') {
      if ((nextChar()) == '>')       return;
      prevChar(1);
    }
    prevChar(1);
    token.add(ch);
  }
}
