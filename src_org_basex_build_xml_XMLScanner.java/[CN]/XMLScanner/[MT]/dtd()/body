{
  if (!prolog)   error(TYPEAFTER);
  if (!s())   error(ERRDT);
  name(true);
  s();
  externalID(true,true);
  s();
  while (consume('[')) {
    s();
    while (markupDecl())     ;
    s();
    check(']');
    s();
  }
  check('>');
}
