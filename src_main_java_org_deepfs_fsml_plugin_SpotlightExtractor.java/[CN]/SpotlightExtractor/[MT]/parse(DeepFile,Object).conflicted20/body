{
  if (!check(o,String.class))   return;
  final String str=(String)o;
  if (EMLParser.isEmailAddress(str))   deepFile.addMeta(MetaElem.CREATOR_EMAIL,str);
 else   deepFile.addMeta(MetaElem.CREATOR_NAME,str);
}
