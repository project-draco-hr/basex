{
  final XQuery query=new XQuery("concat(\"1\", \"2\", \"3\", (\"a\", \"b\"), \"c\")",ctx);
  try {
    result=new QT3Result(query.value());
  }
 catch (  final Throwable trw) {
    result=new QT3Result(trw);
  }
 finally {
    query.close();
  }
  test(error("XPTY0004"));
}
