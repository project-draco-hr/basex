{
  final XQuery query=new XQuery("deep-equal( (1, 2, 3, remove((current-time(), 4), 1)) [position() >= 1], (1, 2, 3, 4))",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
