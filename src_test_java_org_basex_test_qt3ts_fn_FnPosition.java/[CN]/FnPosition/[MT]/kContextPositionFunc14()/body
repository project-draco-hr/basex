{
  final XQuery query=new XQuery("empty((1, 2, 3, current-time(), current-date(), 6, 7, 8) [position() <= 0])",ctx);
  final QT3Result res=result(query);
  result=res;
  test(assertBoolean(true));
}
