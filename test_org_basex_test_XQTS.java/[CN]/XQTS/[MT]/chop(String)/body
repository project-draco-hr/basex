{
  if (string == null)   return "";
  final String str=CHOP.matcher(string).replaceAll(" / ");
  final int sl=str.length();
  return sl < maxout ? str : new StringBuilder(str.substring(0,maxout)).append("...").toString();
}
