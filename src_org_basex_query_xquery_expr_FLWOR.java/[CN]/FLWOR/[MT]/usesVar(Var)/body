{
  if (v == null)   return true;
  for (  final ForLet f : fl) {
    if (f.usesVar(v))     return true;
    if (f.shadows(v))     return false;
  }
  return where != null && where.usesVar(v) || order != null && order.usesVar(v) || super.usesVar(v);
}
