{
  final Args arg=new Args(args);
  final boolean ok=arg.more();
  if (ok) {
    quindex=arg.num();
  }
 else {
    Main.outln("Usage: " + Main.name(this) + " query"+ NL+ "  query  perform specified query (1-#queries)");
  }
  return ok;
}
