{
  final Value cv=qc.value;
  if (cv == cvalue && (cv == null || cv.sameAs(cvalue)))   return true;
  return root instanceof Root && cv instanceof ANode && cvalue instanceof ANode&& ((ANode)cv).root().sameAs(((ANode)cv).root());
}
