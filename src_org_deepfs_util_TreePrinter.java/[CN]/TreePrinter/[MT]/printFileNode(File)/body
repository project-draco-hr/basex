{
  printIndent(level);
  File[] ch=f.getParentFile().listFiles();
  if (ch.length == 1 || ch[ch.length - 1].equals(f)) {
    indentStrings[level]="    ";
    System.out.println("`-- " + f.getName());
  }
 else {
    indentStrings[level]="|   ";
    System.out.println("|-- " + f.getName());
  }
}
