{
  final byte[] text=t.length > 60 ? concat(substring(t,0,60),token("...")) : t;
  out.println(Main.info(NODE,count,text,col));
  nodes.set(count,level);
  if (level > 0)   children[level - 1].add(count);
  count++;
}
